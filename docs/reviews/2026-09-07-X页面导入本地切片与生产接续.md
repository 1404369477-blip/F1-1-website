# X 页面导入本地切片与生产接续

状态：**本地合成导入切片通过独立审查；真实 X 捕获、生产部署及网站自动发布均未完成。** 本文件是交接记录，运行真值统一见 [当前生产状态与执行待办](../当前生产状态与执行待办.md)，开发范围以 Spec 为准。

## 已确认范围与实际阻断

用户于 2026-09-06 确认沿 `TASK-20260829-0ED611` 的 **M1 已登录页面读取**路线继续，并明确要求自动抓取、自动审批、发布到 F1 网站。选择集为原有 27 个 X 账号，Sky Sports 使用官方 RSS。无需重问页面路线或网站自动发布授权，不要求另购 X API，也不把 RSS feed URL 冒充 X 来源。

当前会话可以通过 SSH 访问 M1，但受支持的浏览器工具只连接本地 M5；未获得 M1 Chrome 的本轮 DOM/完整正文/时间/作者/status 证据。该结果不能推断成 M1 的 X 登录失效。当前真实捕获数为 **0**，真实 X 模型处理、审批及网站发布数均为 **0**。交互浏览器能读到页面后，仍须实现可持续调度与可信捕获入口。

## 本地已完成的边界

- `x-page/normalize.ts` 验证固定选择集、作者/status、绝对时间、完整正文与关系；同一帖子编辑产生新 sourceVersionHash，观察时间和媒体尺寸差异不制造内容版本。图片只保留来源引用，不下载或公开热链。
- `0013_x_page_import.sql` 与 `applyXPageCloneMigration` 仅允许 `:memory:` 合成 clone，输入精确钉 0012 schema，输出实验 schema11。保留既有 RSS、59 个 manual source、旧策略与历史，来源权限重新 closed。生产文件数据库会被明确拒绝。
- `importCapturedXPost` 经 gateway/permit/事务写入候选和不可变完整 capture，支持创建、重复与编辑；已见旧版本重放不回退最新内容，同 status 的作者冲突拒绝。21,000 字符全文独立存储，preview 不替代模型输入。
- X source enable/disable/requeue 使用专用 Admin policy；当前不支持 retire，提前报 `X_PAGE_RETIRE_SUCCESSOR_REQUIRED`。导入身份没有审批、发布、投影权限；生产签名 capability 未开启。
- 既有 RSS refiner 限定五个 RSS source，避免将新 X excerpt 错当完整正文送入模型。

独立审查共 **125 个不重复测试通过**；最终 importer 定向 17 项复跑属于重复核验，不叠加为测试总数。全 schema SQL 对象 325→342，无旧对象丢失，仅六表与四个旧 trigger 有预期变化，完整性/外键通过。非空 RSS 候选、bundle、decision、publication、outbox 历史逐项保留。fixture 中 manual submission、backup、anchor 业务行仍为空，因此尚未证明真实完整生产历史迁移。

Admin 固定发布闭包已补入 gateway 新依赖 `x-page/schema-identity.ts` 与 source registry 新依赖 `x-page/normalize.ts`，显式清单165文件覆盖实际派生158文件；pathset SHA为 `ea75ad37f82ba55e34281c9a9f84569fdaaa85ba020949e43ddc3280bb92f810`。13项独立集成检查全部通过，包括使用准确Node24.18.0/npm11.16.0隔离工具链完成发布manifest clean-build，以及新备份包762文件中全部31个本地TypeScript模块（含4 CLI）实际导入和前后校验。新本地备份包根SHA为 `e98bb1576f080c13845d791c3e143febef43185857caba8ee090903bf1225f42`。生产备份包 `ad574960…06a17` 不随工作区变化，不能将重新生成的本地包称作已部署。

## 按依赖接续

| 顺序 | 必需工作 | 完成出口 |
|---:|---|---|
| 1 | 接通 M1 已登录 Chrome 的受支持工具，钉 browser/host，取得完整实时 DOM 捕获 | 至少三源样本具备真实工具回执、完整正文、作者/status、时间和关系；登录墙、截断或失去上下文应失败 |
| 2 | 将合成实验补为可信 producer/importer | 固定运行身份验证回执、来源 config/epoch/授权、capture hash；普通调用方无法用布尔 verifier 伪造证据；成功 operation 的重放有稳定结果收据 |
| 3 | 正式 source/schema/authority 与备份兼容后继 | 修复旧 `verifyAuthorityActivationReceipt` schema 钉、live 准入流程；真实非空恢复 clone 迁移；新备份 snapshot/register/restore/application-drill 接受同一 schema，失败可回退 |
| 4 | 通用完整输入 refiner 与确定性审核 | 精确 candidate revision/hash + 完整 capture，合法 model owner/预算/失败重试/CAS；人工拒绝保护、文本完整性及来源/内容安全事实绑定，不从 normalize 成功推导所有审查事实 |
| 5 | X signed Public DTO、发布及 receiver 兼容 | 保持 RSS 历史签名包可读；合法 automatic reviewer/publisher、outbox 幂等、来源版本及审核记录一致；媒体权利未知时不公开媒体 |
| 6 | 接替实际旧自动 sidecar，受控生产灰度 | 同窗口停旧 manual 身份自动路径再启合法自动调度；三源真实新帖至少一条走到公开站，重读不重复发布，失败停止/恢复与下一自然周期通过后再扩 27 源 |

M1 实际旧入口为 `.ac5cc2/runtime/scheduled-refine.ts` 与 `scheduled-review-publish.ts`，需要同时钉 wrapper、调度、release 和源码 hash。仅扩展工作区未被调用的 `automatic-pipeline.ts` 不能证明接入。后续生产迁移先停相关 writer 与备份周期，确保新备份协议兼容后再恢复；不得直接修改现役 sealed 文件或手工写 control/fence。

当前 `synthetic_clone`、调用方 verifier、实验 policy/config 不能简单改名为 `real` 后部署。0013 尚未生产 apply，可按实际样本修订候选；正式 apply 后只追加迁移，不重写历史。

## 证据与任务衔接

实现详细交接、文件 SHA、前置脏文件身份及增量 patch 位于 `scratch/x-successor-20260907/`；独立报告为其 `independent-review/independent-review.json`。最终可携带的小证据已镜像到 [证据索引](evidence/2026-09-07-x-successor/evidence-index.json)，包括 [实现身份](evidence/2026-09-07-x-successor/implementation-manifest.json)、[独立业务审查](evidence/2026-09-07-x-successor/independent-review/independent-review.json) 和 [独立闭包审查](evidence/2026-09-07-x-successor/independent-review/release-closure-review.json)。索引同时保留原始及路径脱敏副本SHA；主控已逐项回算16个实现文件SHA一致。证据保留本地合成 provenance，任何测试通过都不计入真实网站发布。

正式 `0ED611` JSON 仍是旧 `blocked/user_required`，其原“等业务授权”的描述已被本次明确确认覆盖；当前缺口是访问条件和生产后继实现。注册统筹应通过正式任务工具修订后继合同及状态，保留历史审计。本轮未冒领其他会话、手改 TASK、虚报 complete 或 ACK。
