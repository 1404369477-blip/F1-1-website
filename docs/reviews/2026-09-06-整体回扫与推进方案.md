# 2026-09-06 整体回扫与推进方案

> 性质：当前用户授权的项目审计与局部缺陷修复记录；不代替正式 TASK 完成、产品合同或生产部署授权。现场核验窗口：2026-09-06 08:13–08:25（Asia/Shanghai）。生产只读，代码修复仅在 M5 工作区。

## 当前判断

项目已具备三源 RSS 入库、中文稿、审核发布切片和签名公开读模型，处于可访问的 Public Beta 阶段。持续发布、可靠备份、发布可复现性和完整双端运营验收仍未闭合。当前应优先恢复可恢复性与持续运行，再扩展信源。

本轮发现生产备份事故，覆盖 9 月 5 日交接中“备份在线、恢复点续登记”的健康判断；保留历史上线记录，不倒写历史结果。没有足够证据计算一个客观的“项目完成百分比”。旧功能矩阵 complete 计数包含 synthetic、backend 与历史候选，不能折算为生产完成率。

## 现场事实与证据

通过已配置 M1 SSH 仅执行服务状态、日志尾部、只读 SQLite SELECT、公开 loopback GET；未读取密钥值，未执行生产 SQL 写入、服务切换、API mutation 或外部模型调用。

| 检查 | 当前结果 | 含义 |
| --- | --- | --- |
| Public / projection | 服务 running，工作目录 `candidate-v10-20260905-1408/app` | 部署版本与最近交接一致 |
| Public health / feed | HTTP 成功；`accepted-public-real-snapshot`；V1 feed 12 条 | 公开读模型可用，不证明生产全链健康 |
| 公开 pointer | `activatedAt=2026-09-05T12:56:43.160Z`，manifest `7290f16e72c37d4bc06dbfdd4c64ee16e35f1c5cbcba32bd1a00bd64532086a6`；outbox `succeeded=201 / terminal_failed=6` | 尚无证据说明此后有新公开世代 |
| 控制面 | `live / clear / ready`，writer epoch 2，version 22 | 仅为控制状态，不等于健康心跳 |
| Collector / refiner | 服务已加载，WD `0145`，最近退出码 0 | 周期实例的 running/not running 只能结合收据解释 |
| 最近采集 | 23:30、23:45、00:00 UTC 三个槽三源均 succeeded/OK；23:15 Motorsport 有 1 new / 3 updated | 采集确实仍在持续；是否满足正式自然周期核收仍需合同证据 |
| cutoff 后待审核 | `first_seen_at >= 2026-09-05T02:30:00.000Z` 且 pending_review：Motorsport 21 / Sky 9 / The Race 10 | 合计 40，较旧交接的 9 条增长；不要误作全部历史 backlog |
| SNAP | LaunchAgent 最近 exit=1；连续 `STALE_LOCK` | 当前备份不健康 |
| 遗留锁 | `run.lock` PID 89374，始于 `2026-09-05T06:52:40.873Z`；只读 ps 查无该进程 | 孤立锁阻断后续周期；最初进程退出原因未确定 |
| 最新本地快照 | latest 与 drill report 均为 `2026-09-05T06:37:01.077Z` | 核验时已约 17 小时 40 分钟，无当前 RPO≤15 分钟证据 |
| 已登记恢复点 | view 共 95 行，max `2026-09-01T00:13:13.164Z`；fence 时间相同 | “存在有效格式行”不能代替恢复点新鲜度 |
| 登记日志 | 9 月 5 日 04:46–06:37 UTC 连续 `REGISTER_FAILED no-receipt`；stderr 有 Node ESM 读文件 `Unknown system error -11` | 清孤立锁不能单独修好登记链；具体失败文件和全部历史原因尚未定位 |
| 磁盘 | 可用约 188 GiB | 本次只读时没有磁盘空间耗尽证据 |
| 工程基线 | HEAD `3a8fe96`（8 月 31 日）；修复前统计 67 个 tracked 修改、31 个 untracked 文件 | 多个后续发布内容尚未形成可移交的 Git 切片；保留所有已有改动 |
| 任务机械检查 | 独立审计 `doctor`：`TASK_DOCTOR_OK tasks=484` | 只代表结构与索引一致，不证明任务语义及生产完成 |

## 优先级与处理清单

优先级含义：P1 为当前可靠性、正确性或恢复阻断；P2 为应在下一稳定版本补齐的工程与体验问题；P3 为稳定后优化。这里不把尚无证据的数据损坏或未授权发布写成已发生事件。

| 编号 | 级别 | 问题、依据与影响 | 处理与验收出口 |
| --- | --- | --- | --- |
| A01 | P1 | `backup-snapshot/core.ts` 的失效锁按设计拒绝继续；`backup-snapshot-cycle.sh` 立即退出，现场每轮失败 | 受控隔离孤立锁，修复登记依赖，完成一次完整周期和后续自然周期。保留锁/失败证据，禁止无条件自动删锁 |
| A02 | P1 | 登记恢复点已陈旧；登记脚本仍从 `Documents/F1+1/app` 加载，与已迁出同步目录的采集链不同 | 把登记 CLI 及完整依赖纳入固定可校验运行产物；定位 ESM 读取失败。失败必须有明确 reason；不能只修改时间戳或手工置 fence=true |
| A03 | P1 | cycle 脚本 rsync 到本机 iCloud 同步目录后即传 `--off-host-verified`；周期 drill 读取本地 BACKUP_ROOT | 增加可核验异机/远端确认及从该副本恢复的证据。当前不能据本机 rsync 成功宣称云端已持久化；历史人工恢复收据保留 |
| A04 | P1 | getReceipt 只见 generation 文件即认定已提交；文件写入与 active pointer 切换存在崩溃窗口 | 本轮修复候选：恢复 active 链提交证明。回归覆盖 orphan 不成功、当前代及已提交旧代可核对；性能另行优化 |
| A05 | P1 | 新候选缺 fence 时 refiner 跳过；发布仍靠 sidecar 人工窗口；40 条 cutoff 后稿件待审核 | 独立受控“采集→中文稿→人工审核→发布→投影”新稿任务；同候选验证过期 fence、source_updated、outbox unknown 和恢复，保持自动权限关闭 |
| A06 | P1 | collector/refiner `0145`、opener/Public/Admin `1408`、运行根辅助脚本三套身份；切 WD 会失配 | 先做运行依赖清单与精确 SHA，再构建统一自包含候选/同 schema 回退。验收重启、文件缺失、身份漂移与完整自然周期 |
| A07 | P1 | AC5CC2 正文要求两源/141 不变/零发布，授权记录允许关闭141，交接又挂三源发布运维 | 经正式任务工具形成后继或修订授权与验收，保存旧任务历史；现任务禁止直接 complete。文档刷新不替代任务流转 |
| A08 | P2 | GLM 标记不支持落库，但此前先读凭据、请求 API 再返回 blocked | 本轮本地修复：候选存在且模型不能落库时，读 key/外联前返回 blocked，调用和 token 为 0，空队列仍 idle |
| A09 | P2 | Admin 定时轮询未处理拒绝，断网/401 后仍保留旧成功数据；历史柱根据单个最后发布时间推断多日 | 本轮本地修复：错误有显式状态与恢复、旧成功不冒充当前、历史未知明确显示；控制门文案不得宣称全链健康 |
| A10 | P2 | `findCommitted` 顺链读取验签，随公开世代累积；当前 10s HTTP timeout 已迫使 sidecar 进程内投递 | 基于提交真实性实现有验证的索引/checkpoint或缓存；以 200/1000/10000 世代测 GET/receive 延迟、损坏与崩溃恢复。单纯抬 timeout 不能解决增长复杂度 |
| A11 | P2 | health 表达 Public reader，ops backups 仍 unavailable；缺集中暴露恢复点年龄/发布滞后/周期失败 | 明确 Public 可读、采集新鲜度、待处理年龄、公开滞后、有效异机恢复点年龄五项状态；缺 producer 必须 unknown，告警不能仅写日志 |
| A12 | P2 | Spec 多层历史覆盖、roadmap 的备份完成态已漂移；两套 Function 矩阵多为旧时间点 | 以当前运行入口给最近核验事实；Spec 保留产品合同并将历史时态整理到归档，accepted ADR 不原地改；逐 Function 标记实现/生产/证据/阻断 |
| A13 | P2 | Public V1 可用，V2 双语、Admin Mac/iPhone 完整状态链及六格视觉证据未整体验收 | 以 E59ACA 和双语矩阵为边界，优先审核/发布/撤回/恢复真实出口；本次局部 UI 修复不升级 Function 状态 |
| A14 | P2 | 工作区多日未提交；无 `.github` CI 目录；`npm run check` 含迁移和 seed，不能盲跑到生产环境 | 保留既有变更，按可独立验收切片封存；建议加隔离无凭据的 typecheck/lint/聚焦测试，再做候选构建。既有外部 CI 是否存在未核实 |
| A15 | P3 | Quick Tunnel 地址可变；X 27 账号只有历史单次可读证明；流量/成本 producer、完整英文尚未闭合 | 核心新稿与恢复稳定后再做固定公开域名、X 3 源灰度、运营数据；每项沿用既有授权边界和明确预算 |
| A16 | P1 | sender 的 externalReconcile 拒绝后曾裸 GET；attempt-only 配置也可能绕过 | 本轮本地修复：拒绝或缺执行器都保持 reconcile_wait，零 GET / 零新 POST；合法授权恢复后只查同 delivery。真实进程重启的持久化 handle 恢复另验 |
| A17 | P2 | Admin requestJson 无超时；请求长期挂起时 overviewLoading 持续 true | 后继补请求取消与有限超时，验证挂起/超时/恢复/晚返回；本轮已修立即失败恢复，未覆盖无限挂起 |
| A18 | P2 | 390px 实际 DOM：工序为 44px + 1fr 两列，第三个子项 p 自动排入下一行首列，文本宽度仅 44px | 后继按既有两列意图显式放置描述列，补窄屏文本/缩放视觉；本轮只记录问题，不宣称手机视觉通过 |

## 下一步执行顺序

1. **恢复可恢复性**：先解决 A01/A02，重新获取新鲜完整快照、隔离恢复与合法登记，并核查 A03 异机证据。退出标准包含失败告警和下一自然周期成功，不能只看一次命令 exit=0。
2. **收敛任务与运行产物**：把目前三源事实、sidecar、新稿范围、发布授权与验收出口放进语义自洽的正式任务；把脚本依赖固定进产物，保留原 `0145/1408` 回退锚和脏工作区。
3. **恢复持续新稿链路**：使用最少现有组件补齐合法 fence 生命周期与 outbox 恢复；以新稿贯穿采集、中文稿、审核、公开详情，观察自然周期及 source_updated 路径。先清明确 cutoff allowlist，禁止把约 1600 条历史 queued 倾倒上线。
4. **接入健康事实与完成局部候选复验**：接入备份年龄/流水线滞后，发布本轮已审查修复需新签名候选与部署确认；不热改 `1408` 密封文件。
5. **完成双语和双端 Admin 验收**：同候选、同数据、真实入口，覆盖 offline/partial/fresh-expired/reconcile，补 1440/1024/390 深浅主题与 iPhone 实机证据。
6. **扩展**：上述闭合后再单独推进 X、固定域名、流量成本；不引入 Redis、消息中间件或新数据库来解决目前局部链路缺口。

无需新增架构实体即可先完成前四步。任务拆分采用互不写入同一模块的边界，本轮代码链路/UI/任务运维分别审计，独立审核不替执行人修代码。

## 生产备份事故的具体处置边界（待专项授权）

1. 执行前重新读备份 LaunchAgent、锁内容/文件身份、PID 和进程命令、最新快照/恢复点/日志；任何与本报告不符即重新评估。
2. 只暂停 `com.f1plus1.backup-snapshot`，等待实例和其子进程退出；复核没有快照/登记在进行。保留 collector、Public、Admin 的当前状态，不把清锁扩展为重启全站。
3. 将已证实孤立的 `backups/snap/run.lock` **改名保留**到同目录事故文件；若 PID 活着、锁已替换或写入者不明则不操作。保留 `.staging` 和已有备份，不先做 prune/删除。
4. 登记 ESM 读失败必须先定位具体依赖，准备固定运行闭包与回退副本，校验 manifest/代码/schema 身份；不从 iCloud 按需下载目录继续跑登记，不放宽 gateway 或伪造 off-host 证据。
5. 使用既有一致性快照流程完成快照→异机确认→隔离演练→合法登记→fence 原子刷新。任一步失败停止成功宣称，保留 reason 和旧副本；不 raw UPDATE 控制面。
6. 恢复原调度，观测自然周期，并按实际可恢复点时间验证 RPO。旧脚本/配置保留回退；回退后若依旧失败，继续显示故障。

这是可审查的操作范围；登记依赖的具体失败文件尚未定位，因此尚无可声称通过演练的生产修复包。不得把本地代码测试通过当作生产恢复完成。

## 验证与限制

- 已用项目内 Node 24.18.0 做全量 TypeScript 检查（`--noEmit --incremental false`），通过；未使用系统 Node 26 代替合同版本。
- 本地修复 A04/A08/A09/A16 已完成；Node24 聚焦 Vitest 三文件 13/13、全量 typecheck、相关文件 ESLint 均通过。没有执行带迁移/seed 的全量 `npm run check`。A04 是撤回审计前脏树中的不安全快速查询，恢复 Git HEAD 已有的完整链验证语义；不属于性能优化。
- 本轮未执行生产写入/部署、真实模型请求、公开发布、X canary、备份恢复写入；未 commit/push。
- 未重新核验公网 Quick Tunnel、私网 Passkey、iPhone 实机、完整视觉/辅助技术，未将单元测试记为这些门禁通过。
- 本轮以入口全扫、关键模块定向代码审计及生产只读核验为范围；没有对每一行代码、每个历史证据和每个 Function 做形式化证明。上述清单为已证实问题与有依据的待验工作，不能保证不存在其他缺陷。

## 本轮交接收口

- **独立对抗性审查**：运维/任务审计 Agent 独立复核本轮代码，并独立跑最新三文件 13/13 测试；首次发现 sender attempt-only 缺失保护，主线程补齐后复核通过。通过范围限定本地四组修复，真实 gateway 重启、生产、手机与完整视觉门均未放行。执行 Agent 曾因额度中断，主线程核对落盘、补验证后续接，没有把中断当作完成证据。
- **修复文件**：`rss/refinement.ts`、`review-real/projection.ts`、`review-real/sender.ts`、`admin-ui/ops.js`、`admin-ui/ops.html`，及三个对应测试文件。已有文件改动保留，`app.css` 本轮只读。
- **可视化**：[本地预览画册](../../scratch/audit-20260906/index.html)，六张 1440/1024/390 × 深浅主题的真实浏览器视口截图；使用模拟 DTO，同份工程 HTML/CSS/JS。此处是实现验证，无新增设计方向，因此未调用 OpenDesign 生成设计；不对 OpenDesign 的可用性作判断。手机描述窄列问题明确登记 A18，未作视觉完成核收。
- **候选身份**：[文件及截图 SHA-256 清单](../../scratch/audit-20260906/manifest.json)。scratch 被 Git 忽略，换机器交接时须显式携带此目录；只复制文档会丢失截图和清单。
- **已同步入口**：当前生产状态、当前分工、handoff、progress、Spec/roadmap 顶部、统筹/自动化交接均增加带时间的故障覆盖；synthetic 部署 README 标明历史用途。历史记录不删除、TASK JSON 与收件箱未手改、未建新部门会话。
- **下一位接手者的首件事**：按 A01–A03 重新只读核验现场，定位登记依赖失败，准备隔离验证过的恢复候选与精确回退，再执行获得确认的生产动作；不要先从旧交接继续 sidecar 发布。原 AC5CC2 仍未 complete，任务语义整改使用正式工具。
- **尚未执行**：生产恢复/部署/服务暂停/清锁/云端确认/备份重跑、真实模型调用、X、commit/push。原因分别是需要具体生产处置确认、依赖尚未闭合或不在本轮局部修复范围。此次审计和交接已交付，整体优化与生产恢复仍有未完成项。

## 18:23 续接进展

详见 [备份事故诊断与受控恢复方案](2026-09-06-备份事故诊断与受控恢复方案.md)。A01–A03 新增失效锁人工处置、固定 CLI 运行包及登记前置检查候选。已复现 live 阶段登记策略冲突，并完成旧包 M5 独立密文读取与 M1 隔离恢复；生产事故仍未关闭。上文“未执行备份恢复写入/云端确认”仅描述早间审计阶段，本次仅新增隔离恢复与一份旧包异机可读证据，未改生产服务/库/fence，未形成逐周期异机验证。

## 22:09 收尾覆盖

- **A01 已处置该孤立锁及一次生产救援**：新快照与隔离恢复通过，原scheduler已恢复；严格周期健康仍需A02/A03闭合。
- **A03 本包异机密文读回已补齐**：M5独立读取330,417,035-byte对象与manifest/latest，SHA与M1一致；自动逐包确认与M5解密未做。
- **A17/A18 本地缺陷已修复并验收**：超时与body挂起/恢复/晚到隔离10/10独立测试；390/1024/1440深浅六格真实浏览器视口通过，窄屏描述240px且无横向溢出。无新设计方向，未部署，不升级完整手机Function状态。
- **A02 仍阻断**：固定28文件包已准备并在M1独立目录验证，生产cycle仍读源码同步目录，恢复后再现ESM -11；live新锚点登记亦被既有策略拒绝。新包不等于合法恢复点已登记。

当前结果、证据和下一步统一见 [备份事故报告末节](2026-09-06-备份事故诊断与受控恢复方案.md)，上述早期未执行描述保留为历史阶段事实。
