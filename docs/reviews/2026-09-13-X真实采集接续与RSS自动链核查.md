# X真实采集接续与RSS自动链核查

2026-09-13。用户明确要求继续推进X，并追问RSS是否可以进入自动审批发布。本记录承接9月6日已确认的27源M1已登录页面读取、自动处理及网站发布范围；不重复要求业务授权。

## 本轮范围与依赖

1. 主控核当前受支持M1浏览器连接及真实页面证据。仅已确认keep27，Sky走RSS，不恢复59源、不采购API、不读取cookie/会话存储、不调用未公开GraphQL、不进行账号社交写操作；登录墙、挑战、限流和正文截断失败关闭。
2. 执行Agent核E7 native输出→producer/adapter/verifier→artifact→正式import接口，优先复用已有operator。当前先形成有边界方案与真实输入需求，不把synthetic fixture声明为真实捕获。
3. 真实页面入口闭合后，更新全量构建输入，保留近期RSS v6、流式备份及Pages后继；同版本完整候选/非空迁移保留/X附件和应用恢复通过后，三源灰度再扩27源。旧9月10日451输入清单只作历史，不能直接执行。
4. 独审Agent只读核RSS自动审批/发布真实路径、当前unknown和未决outbox；保持gen407旧合同待答，不重放POST、不原始修改DB/attempt/幂等身份。任何新恢复动作需先将具体方案与既有授权对应。

## 当前边界

- 主控负责浏览器操作、任务与范围、生产执行和最终核收；x_integration_audit_0909先只读接口审计，qos_diag_review_0912先只读RSS核查，后续独审X候选。
- 本任务沿用既有公开站/Admin设计，不增加或修改UI。若后续真实X入口需要UI变更，必须先绑定既有Function ID及冻结视觉，不以后台局部PASS替代双端体验。
- 9月13日22:10:39固定f1采样：M1公开200，Pages本次M1探测HTTP_TIMEOUT；live/recoveryready；备份点22:04:44、清单/异机/演练回执有效；RSS待审核0，待发布55/20/21，unknown65、未决投递1。此为该次采样，不承诺后续持续健康；未决投递准确身份由只读核查确认。
- 用户已明确选择保留M1采集。本次浏览器发现仅M5 Chrome及当前Codex内置浏览器，没有M1受支持连接；Chrome通信当前还出现超时。没有使用SSH脚本或另建调试通道绕过浏览器工具；主机选择已闭合，待M1本机会话取得受支持工具的真实证据。

## M1自定义模型与执行环境

本轮通过SSH仅检查程序版本、配置中非敏感字段和文件存在性：M1默认 `model_provider=custom`、`model=glm-5.3-flash`、`wire_api=responses`、`model_reasoning_effort=max`；Codex CLI版本为 `0.149.1`。这是默认配置，未证明当前交互会话没有profile或命令行覆盖。

Chrome插件已启用，缓存目录 `26.908.40834` 存在；已配置node_repl MCP，程序 `/Applications/ChatGPT.app/Contents/Resources/cua_node/bin/node_repl` 存在。M1没有独立Codex.app不等于没有Codex CLI或工具。非交互SSH默认PATH找不到node，给该次版本检查临时补 `/opt/homebrew/bin` 后CLI正常输出版本；未改用户配置或调度。

以上支持保留GLM进行小样本验收，不能据此宣称模型调用、MCP握手、Chrome连接、正文抽取或无人值守调度已通过。本轮没有发起模型请求，也没有实际采集X帖子。验收需由M1本机会话先证明实际工具可用，再读取F1/McLarenF1/ChrisMedlandF1各最多3帖，完整保留原生返回和来源身份；遇挑战、限流、登录墙或截断应明确停止，不编造字段。实际输出到位后才能冻结native adapter。

官方参考：[Codex自定义provider](https://learn.chatgpt.com/docs/config-file/config-advanced)、[Codex CLI的MCP接入](https://learn.chatgpt.com/docs/extend/mcp?surface=cli)。官方支持配置自定义provider和MCP，不构成GLM具体兼容性的验收。

## RSS核查结果

22:45:51样本待审核0、已批准待发布95（Motorsport54/The Race20/Sky21），缺当前中文稿8。现役v6每60秒运行自动链，最近20周期均 `PUBLICATION_RECONCILE_WAIT`、refined/approved/published均0；不能把现存95条全部归因于本轮自动批准。

唯一最新outbox确认为原generation407、attempt1、`reconcile_wait/DELIVERY_RESPONSE_UNKNOWN`，现役代码阻止创建下一代。71条unknown并非全局总开关，须按owner分别核查；Pages本次探测超时也没有与该阻点建立因果关系。[RSS只读报告](../../scratch/rss-auto-audit-20260913/report.md)记录精确身份、代码位置及日志证据。gen407的一次性恢复合同仍待原选择，不执行重放或清状态。

## 正式接续

新统筹任务 `TASK-20260913-D620C2` 经claimed后，以等待M1真实工具/native证据正式block；已正式supersede旧 `TASK-20260829-0ED611`，保留旧合同及授权历史。业务授权已确认；真实M1工具入口及整链技术验收仍未通过。旧451输入有17项漂移，Admin/Public声明均缺3个Pages共享依赖，须按[接口与构建审计](../../scratch/x-e7-resumption-20260913/interface-audit.md)准备后继，禁止直接复用旧PASS发布。

## 验收与回退

真实三源页面需完整正文、作者、canonical status、绝对时间及关系，具有真实工具来源和主机身份；producer/artifact/导入的闭合验证、版本/去重/失败恢复，固定同版本构建和恢复演练，灰度到公开网站的实际内容证据、自然周期与27源调度均须分别核实。无真实入口时不得完成整链或启用调度；失败保持原业务与已有站点，保留原包、证据、未知请求及审计历史。
