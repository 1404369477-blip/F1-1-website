# X采集与RSS受控恢复实施合同

2026-09-13。用户在获知M1默认GLM与工具尚需实际验证、RSS被generation407未知投递阻断后，明确要求“把这两个事项都推进下去”。本合同将该指令落实为两项执行范围；不重复要求已给出的业务授权。授权不等同于实现、技术门或生产验收通过。

## 1. M1真实采集

保留M1已登录Chrome与默认custom/glm-5.3-flash。允许通过现有M1 Codex CLI作一次有界执行，以其受支持Chrome工具取得F1、McLarenF1、ChrisMedlandF1每源最多3帖。SSH只启动/观察该本地Codex执行器并收取非敏感产物；浏览器控制必须由M1会话的受支持工具完成。不得通过SSH脚本、CDP、AppleScript、隐藏接口或cookies替代。前轮的“不另起付费API测试”不禁止本次用户要求的实际CLI采集执行；不另加供应商或换模型。

真实原生输出须保留主机/实例/调用、页面、绝对时间、作者、status链接、完整正文和关系；失败按实际原因停止，不编造。接口方案及交接见[上一轮审计](../../scratch/x-e7-resumption-20260913/interface-audit.md)。真实输入取得后才能冻结adapter/producer/verifier。首次准入、同版本构建及恢复、三源到27源灰度继续按既有技术门，不把本次样本当作已完成生产链。

## 2. generation407限定恢复

本次执行承接[原限定提案](../../scratch/admin-recovery-20260911/permanent-build-20260912/gen407-controlled-recovery-analysis/proposal-for-user.md)。默认未知投递继续只查凭据；仅对当前generation407新增有证据的一次受控负向恢复。单纯404、超时、客户端断开或等待较久仍不得触发重发。

1. 先实现并独立审查正式operator及必要的状态/预算/幂等恢复协议。旧sender和receiver的固定身份、退出及阻止继任、旧请求不能晚到必须有可核验证据。实际停止点只在候选通过、备份新鲜和回退方案明确后执行；sender位于Admin进程，可能短暂停用Admin及投递服务。
2. 停止边界内完整验签公开链，确认原delivery未提交；若已提交仅走既有成功凭据结算。核对原attempt、generation407、publication、snapshot manifest、canonical envelope、前驱、逻辑idempotency/reconcile key和同包孤儿文件。失配继续unknown。
3. 正式入口将退出/absence证据绑定原attempt，如实终结原未提交结果和预算，最多创建一次具有独立有限预算的恢复尝试。保留原逻辑delivery/key及全部历史，明确解决external key唯一性，不删除旧attempt或历史0007、不伪造capability、不把GET回应当原POST回应、不直接改DB状态。
4. 当前来源/权利/stop/recovery/writer/有效备份/release重新通过后执行一次。精确receiver凭据、本地outbox成功、原预算及新预算各一次结算、崩溃后的本地重放须可验证。新尝试结果未知则停止，不自动第三次请求。

最低反例包括旧POST仍在读body而GET已404、原PID退出但继任存在、链损坏或身份错配、不同包孤儿、过期/篡改证据、重复operator、预算重复结算、全局key唯一性、停机/撤销/备份变化，以及负向结算到外发、远端提交、本地完成之间各崩溃点。

## 3. 构建、用户出口与验收

RSS恢复候选以现役v6为基础，保留流式备份和Pages运行包，不夹带尚未验收X代码。候选、完整输入、部署reader及rollback身份必须固定；不能直接把混合脏树复制生产。

本任务影响矩阵中的 `AUTO-PUBLISH-005`（第122行）、`ADMIN-PUBLISH-007`（第106行）、`RECOVER-ADMIN-004`（第132行）、`RECOVER-EXTERNAL-005`（第133行）的后台投递恢复行为。沿用既有公开站和Mac/iPhone Admin视觉及操作语义，不新增UI或删减端能力；矩阵旧历史状态不自动提升。正式operator为安装态受控恢复入口，真实用户出口为原RSS候选自动链→同投递成功→公开投影→Pages自动同步→首页/详情可读。若实现需要新UI，先绑定冻结设计精确身份和六格视觉，再进入该UI实施。

RSS完成至少需gen407真实终态、后一自然周期整理/审核/发布推进、新一代成功及公开可读；不能用服务在跑、历史批准队列或Pages旧200代替。X完成需真实三源样本及独审、完整候选/同版恢复、真实导入公开、去重与自然周期后再扩27；M1工具不可用时交原始错误和所需连接动作，继续独立RSS工作。

## 4. 分工与回退

主控负责范围、任务、事实核收和生产执行；x_integration_audit_0909独占M1临时CLI采集与对应scratch；pipeline_audit在隔离目录设计和实现RSS恢复；qos_diag_review_0912不参与实现，独立检查设计、候选和实际安装证据。共享app源码写入必须由主控统一指定；先保持隔离候选。

恢复失败保留原release、backup、投递/预算历史和全部证据；恢复已知兼容的服务运行方式，保留unknown，不回退数据库代次或公开链。主控仅在已验证兼容且进程身份明确时恢复旧运行包。新尝试一旦可能外发，不得用回退重新获得外发机会。

执行证据分别归档 `scratch/x-m1-canary-20260913/` 和 `scratch/rss-gen407-recovery-20260913/`，生产状态唯一入口仍为 `docs/当前生产状态与执行待办.md`。

## 5. 当前执行记录（不等同完成）

- M1真实Codex执行已完成两轮，未换模型；实际模型响应和工具调用通过。第二轮官方Chrome selector返回 `Browser is not available: chrome`，随后官方列表为 `[]`，三源0帖。主控复核原始JSONL第8/14行，与报告一致；已请用户在M1完成实际Chrome扩展连接，RSS工作继续。[真实执行报告](../../scratch/x-m1-canary-20260913/report.md)。worker生成摘要中的部分主机命令/Chunk定位没有原生事件，已明确排除，不作为采集或主机证明。
- 23:07:21巡检：M1公开与Pages200/12条；备份最新合法点22:38:13.507，已超龄，Admin请求超时。只读日志显示22:43后快照仍成功，恢复演练多轮在ADMIN_READY 45秒期限失败且清理完成，未见遗锁。
- 23:15:41–23:16:23用M1实际Node24只读连接逐项测得foreign_key_check为15.843秒、integrity_check为26.811秒，均通过；DB约968MB。数据完整性通过不等同应用恢复成功；正在分析完整启动中的重复扫描与预算，不跳过检查。[原始计时](../../scratch/backup-drill-recurrence-20260913/readonly-integrity-timing.stdout)。
- RSS设计候选采用原wire逻辑key保持、新物理尝试显式映射，优先不迁移schema；跨release可信安装桥和未started崩溃的合法清理需正式实现并独审。现役v6源包387件已按原manifest逐件验真解包至隔离候选；生产服务、DB、outbox及预算尚未改动。

### 备份依赖的最小修复决策

主控选择仅更新独立backup tools的有界预算：Admin ready由45秒改120秒，application drill外层由180秒改300秒。实际安装态源码已确认同一Admin启动执行两对完整FK/integrity；一对实测42.654秒，两对约85.308秒仅为估算，不代替实际完整演练。当前已观察到ADMIN_READY前约80–85秒，因此须实测新300秒整链预算。

保持两轮完整检查、恢复包校验、Admin/Public业务点、无外联与子进程退出、签名回执和注册拒绝超龄等全部条件；不改Admin/runtime/schema/保留策略/当前模型，RPO900秒与RTO4小时不变。先对固定候选独审及完整隔离drill，固定安装后验证连续自然周期和恢复点间隔。备份自身修复可在恢复点已过期时做必要的隔离准备和经审查的工具替换；这不授予RSS预算结算或重发权限，gen407生产恢复仍必须等新的合法恢复点。

23:42–23:43主控完成已冻结9件input及inactive target stage，原plist/调度未改；独审实际readback通过。23:45:54原自然周期已结束后，正式helper取得自身guard、复验精确idle job与无backup子进程、bootout仅backup label并释放guard，stop receipt `008ff009432e481ca3cc6c22007ea371f2e6673740e84d483357aac3b2a0d7b1`。23:46:16同target隔离完整drill开始，尚待结果；此时backup job有意unloaded。必须完成[固定命令](../../scratch/backup-drill-recurrence-20260913/install-r1/COMMANDS.md)中的真实演练核收及新候选安装，或失败后恢复原调度，不能将无调度维护态留作交付。

23:48:15隔离演练成功，完整118.092秒、Admin ready阶段62.603秒；Admin200/未授权401/Public200/12条、childrenExited/bootRootCleaned均通过，6件原始证据在`install-r1/isolated-evidence`、manifest SHA `28b6ee9d902a3b36b38930bf814a280efcba7faf1684f7d8ba6f128ac0f89b7d`。独审使用现役公钥SHA `a2e944019ad16e229392391116a259b4bddb9b60037527c10adde7d99589e09a`完成Ed25519验签，给出[安装放行](../../scratch/backup-drill-recurrence-20260913/review/isolated-drill-r1-install-review.md)，Passkey恢复仍未验，不扩称Admin完整恢复。

23:54:16正式apply成功，receipt `88a6383a523a80a4124d28f8ebb9619b5c96b68a2577e221eb0d4954578bed92`；23:54:17 bootstrap成功，receipt `85ca4f41ec2f2a4e864943ed9cae8fcad43f9dd761e7666d081158fcb5ac742a`，job实际loaded idle、argv精确指向新tools及原runtime f008，未kickstart。23:55:13首自然周期已运行；隔离演练不登记生产，当前等待至少3个新完整合法恢复点和连续900秒RPO验证。自然观察入口`observation-r1/sample-new-cycle.py`继承旧只读采样器，生成输入绑定实际bootstrap、生成器SHA `b65f010c5822ec7897b9024d8cd30bc6bb681f59d8319a599a007596bbafb796`。

00:08完成三个完整自然周期核收，独立结论`PASS_INSTALLED_AND_POSTINSTALL_RECURRENCE_ACCEPTED`；严格前点到后点登记447.025/479.069秒小于900秒，旧4791.549秒缺口保留。00:56只读刷新已累计13个合法点，最新点00:49:16.978、第14轮运行，调度和验证未再次失败；各次采样均只证明对应窗口。[备份最终独审](../../scratch/backup-drill-recurrence-20260913/review/postinstall-three-cycle-review.md)。

### RSS恢复的崩溃与安装边界补充

- 原base plan、recoveryId和逻辑包永久保持；维护授权通过固定序号的原子追加记录续期，仅准许inspect/GET对账/本地完成/放弃未发请求/服务恢复。维护授权存在即永久禁止新POST，不能靠续期取得第二次外发机会。
- 服务日志按原子追加cycle保留；尚无恢复operation、mapping和physical attempt且outbox仍为原attempt_count=1/reconcile_wait时，可重新验证并建立下一停机周期。一旦新尝试已经准入，不能再开启POST周期。各失败路径必须执行兼容服务恢复，已恢复实例仅签发GET核验能力；进程运行与HTTP就绪分开验收。
- 权威文件先完整临时写入、fsync，再RENAME_EXCL发布和目录fsync。任意隔离目录的低层安装验证不得签发生产操作权威，Darwin、gateway和正式CLI只能接受固定安装根的可信pins。
- 每个supervisor handoff在单独固定子进程签发，核实父operator的真实内核/锁身份，使用原安全DB opener和正式一次性授权。原因是实际Node24.18.0在同进程第二次开库时会触发SQLite缓存描述符的既有身份门；不放宽该门。M1真实parent-child及有效期是POST前必须通过的实际门。

01:01左右，R1的197件source已stage到M1独立目录，未读私钥/开业务库/改服务。实际构建被`DEPENDENCY_INPUT_CHANGED`正确阻断：早期v6源码tar的lock为09af0413…，实际v6已发布lock为cb7640a7…；package.json一致，原185件仅该lock为非预期漂移，另3件为本次已审变更。实际lock已按现役manifest的SHA只读收回，R1现场及清单保留；后继R2将同步实际lock及隔离品牌修正后重新封版。此时尚未安装RSS pins、结算预算或发出gen407恢复请求。

01:28–01:31，R3的197件已在M1重新stage、验证实际v6完整基础并构建通过，canonical构建收据SHA `6d8baffd044d9e0aaf4741f279f0c29b420e8b1bef92c5cc109ad5fd0ab5b98e`。R2隔离失败来自Admin发送端与receiver使用不同路径的同一公钥；R3分别绑定两份原有公钥路径/身份/SHA，没有修改生产配置。R3真实隔离安装、三种隔离品牌拒绝、维护两代/期限/不可恢复POST及同字节inode反例均通过，收据SHA `ae9a600ee15448584cf112ee6881aa752622af198d900f0a812f5286822e166d`，没有写生产pins。

M1完整406代历史验签实测23.596秒，后续对同一私有冻结成员集合的逐文件原字节SHA复核为203/198毫秒；完整目录身份、active指针、公钥、目标缺失与进程退出前后检查保持。执行时序为准备fences后完整观察P1、事务消费P1、全字节复核后生成唯一P2、receiver启动边界消费P2；P2不能再刷新，60秒期限不变。真实子进程及消费时序仍需实际operator证明，纯只读计时不签发absence权威。

01:37正式launcher独审发现外部pins SHA预读与CLI自行加载间缺少同一输入绑定，R3尚未执行生产安装或恢复。最小R4补充为CLI必填外部pins SHA、在loader首次稳定读取后且签发品牌前核对；wrapper传入同一SHA并保留实际进程与输出收据。R1/R2/R3原始冻结材料及失败/通过证据全部保留，R4需重新构建与隔离核验，不据此增加外发次数或延长原POST时窗。01:34正式只读巡检仍见公开/Pages200、99条待发布、gen407未决、Admin请求超时；有效备份点01:26:54.836在15分钟内。待审查项与实现文件见 `scratch/rss-gen407-recovery-20260913/installation/r4/`。

01:48–01:49，R4在M1真实构建、隔离新反例及独审后已正式安装，生产pins SHA `3bc7f30208075e5b19c6de2f1c0832da1d8c59765d692dc3a9b62647e15750c0`。固定带SHA的CLI inspect通过，17.077秒，原gen407仍未知、新尝试不存在。此时没有停止服务或改业务账本。后续只使用R4 launcher，不能重跑初次发布/安装或沿旧无SHA命令执行。

02:00登记故障已被原始日志和独审定位到独立REGISTER外层45秒预算：01:35/01:45/01:55的应用演练分别成功后，登记在45.360/45.525/45.887秒被TIMEOUT终止，不能把0字节last-register输出当登记成功。23个成功证明阶段样本中位30.182秒、P90 31.897秒、最大40.469秒，未见失败轮新backup_worker handoff或登记operation；不归因为已证实数据库锁争用。主控选择最小backup tools后继只改REGISTER 45→60秒，保留120秒Admin ready、300秒application drill、runtime f008、完整复验、内层重试及900秒RPO/登记拒绝规则；固定安装和自然新合法点通过后再执行RSS恢复。若60秒仍失败，应先取得阶段耗时证据再决定，不盲目延长。[独立诊断与证据](../../scratch/backup-drill-recurrence-20260913/review/register-deadline-review.md)。原失败包、回执及当时RPO缺口继续保留。

02:08–02:12，REGISTER60候选经限定差异、16/16协议独审后，在M1完成私有input/inactive target stage；02:11:41真实idle后仅bootout backup并释放guard，02:12:02.770 apply成功，收据`67d6a460af9a7537410a330cdbe004d9414f84247fcd97e4e6e1ce5724517b3b`；02:12:03.152 bootstrap成功，收据`ac678fd7d6ee7c02f6943e547871dad21df98daa878de6b1a3bc9c1253bf4708`。新tools为20260914-register-budget-r1，实际argv/五件工具/runtime/受保护配置独审通过，0kick、0业务动作；旧工具完整保留，固定入口见[COMMANDS](../../scratch/backup-drill-recurrence-20260913/install-register-r1/COMMANDS.md)。等待三个安装后自然完整合法点及严格900秒连续性，不以安装PASS代替周期核收。

02:12:44，新只读业务验收器真实生产基线全部检查通过，原gen407仍1POST/unknown，新attempt与mapping均不存在，1601次GET单列。旧验收器误将同delivery GET计为物理POST，v3真实请求method导出证实是假阳性，旧件留档。修正版SHA`661e51331a56236d85c66af2a3de843a2ccad96edbd86a1d1e038045e6ebcaca`，实际回件SHA`e12029087d60a04633d05973fbdfcbafd34481162cca47056182391e5239a3ef`；独审包含实际GET样本、第三POST改body/key/错误endpoint与无关delivery反例。[独审](../../scratch/rss-gen407-recovery-20260913/review/post-run-business-verifier-review-r1.md)。此时completed=false，0recover/新POST，当前生产结论不变。


### 03:26 R5 技术修复接续（原业务授权保持）

REGISTER60已取得安装后三个自然合法点的独立核收；03:18只读刷新为13个连续合法点，严格前恢复点到下一登记完成的最大间隔592.679秒，全部小于900秒。[连续性复算](../../scratch/backup-drill-recurrence-20260913/install-register-r1/observation/root-thirteen-cycle-time-check.json)。02:29首次R4恢复真实执行在prepared前失败，原服务未停止、owner已释放、零新POST/attempt/mapping；只有cycle.json。真实只读重现确认合法backup wrapper/application因携带同一Admin配置被计成额外服务。原失败和诊断保留，不能按成功计数。

暂停backup方案经严格预算核算放弃。R5保持自然backup调度，内部验证当前REGISTER60五工具与f008完整运行字节、launchd/lock/内核父链后，仅从服务数量检查中剔除已验备份角色，全部原始进程数据仍入证；旧服务PID/PGID、未知/手工进程、3102外来连接门不放宽。M1自然周期raw4→services2实证489.633ms；40项分类、14项真实Darwin/锁与控制器、原子记录/维护/CLI/业务反例均已独审。受影响正式源码均在独立199件R5包，R4原源码及pins/plan/deployment字节、inode保持。

新技术安装以`maintenance-grants/000001.json`下严格repair record单次EXCL发布为唯一生效点。该记录绑定原base pins、R5 repair plan与历史owner/cycle完整前像，关闭旧R4入口，但不是local_only维护授权，不重开业务次数或时窗。原core/RID/gen407原文包、逻辑key、最多一次新增POST及17:29–21:29 UTC窗口保持。真正local_only维护从序号2接续并永久禁止POST。R5 CLI必须同时给外部base pins SHA和repair record SHA；服务历史只可追加R5 cycle，不在原R4空cycle补写动作。

03:21 M1 R5 inactive stage/build通过：冻结manifest`656099f5cf43c4cfded03e4a07383a23fff5f48144553821723552f69c6e9b44`、199件pathroot`cdc71cdaf886b828d708009da0ef47233e7274c05ae9e2f5c5d132a41e029167`、真实build receipt`7a2969c03f2ce5185b1ce26294461b11a7b465bd1461fc15136a9a125cb728cb`。03:23准备的repair plan SHA为`d73144c552bf0d10a57157ad2014136eb83b201e88675d8d0a017b2ac86ed53c`，完整R4/R5生产路径材料只读核验16.5秒通过，此时record尚未提交、无服务/账本/POST动作。独立核收后才执行专用维修installer的共享锁及单record提交，随后双hash inspect，再由一次recover内部先验证真实父子handoff/TTL、静默服务、P1/P2，满足后才准许POST；不能另造手工handoff充当前置证据。[R5独审](../../scratch/rss-gen407-recovery-20260913/review/r5-repair-and-backup-review-r1.md)、[实施协议](../../scratch/rss-gen407-recovery-20260913/installation/r5/pin-successor-design.md)。
