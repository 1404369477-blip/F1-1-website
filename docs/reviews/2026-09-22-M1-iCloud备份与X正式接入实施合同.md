# M1 iCloud 备份与 X 正式接入

用户于 2026-09-22 明确 M5 是随身笔记本，要求继续推进 M1 的 iCloud 备份及 X 信源链路；随后确认维护入口“采用当前预览，继续实现”。本合同记录本次已授权范围，不表示生产切换或验收已经完成。

## 运行目标

- M1 独立承担日常采集、处理、发布和备份调度。M5 可合盖、离线、换网；其在线状态不作为新的生产成功前提。
- 复用现有 iCloud Drive 保存应用加密备份，不采购新服务，不改变 Apple 账户或密钥权限。
- 一致快照、加密、完整性、恢复演练、RPO≤15分钟和现有停用/恢复保护继续成立。不得把本机目录存在、复制成功、单个 latest.json 已上传或 M1 自签的旧 M5 回执当作异机可恢复证明。
- X 沿已确认的 M1 已登录 Chrome 和 Playwright 路线；先原三账号，来源准入与合格全文才可入库，继而通过既有审核发布与公开投影。不得用 M5 代采、补签旧 unsigned 样本、绕过登录挑战或向 X 发帖。

## 备份后继与回退

新后继先在隔离候选实现：M1 云外一致加密包保全；同包密文与 manifest 写入 iCloud；完整上传及错误状态校验；实际从云端重新取回并核对字节身份；隔离解密与数据库/投影/应用恢复；以明确的新存储证明类型登记。旧 M5 证明的来源、签名、历史及校验含义保留，不冒签、不重解释历史。

必须证明迟到、断网、未上传、账户错误、成员缺失、包身份变化、过期、进程中断后不会伪报成功或丢失原包，并可继续处理原包。正式安装前冻结实际候选和回退材料，经独立审查及真实 canary 通过后，在不破坏在途业务的维护窗口切换。新路径未通过前保留现役可用路径；不以关闭旧 observer 或放宽新鲜度来制造恢复。

## X 维护入口及后继

视觉采用 `scratch/x-resume-20260922/preparation/maintenance-design-r2/manifest.json`，SHA-256 `29bf427f201c6ddc40dc1918541c804f2f0effc3ed0fb4d5c89bb26fc9ca491f`。现有预览确认覆盖布局、文案与交互方向；真实认证和服务操作仍须验证。

正式 successor 复用已审事务原型，完成运行时私有连接、真实 WebAuthn fresh binding、会话轮换后的 CSRF、状态查询与维护执行路由、人类操作者审计、Mac/iPhone 等价操作、未知结果只读查询及完整失败恢复。绑定既有 `ADMIN-VISUAL`、`ADMIN-SEC-002`，以及原实施合同的自动发布/恢复 Function ID，不新增占位出口。

数据库 0017 的正式准入仍要求真实维护关闭、无 intent_committed/started 请求、投递均为 succeeded/terminal_failed、原生进程状态可判定及新鲜闭合恢复证明；构建、迁移、恢复及回退必须绑定同一正式版本。两条已核历史 GET 若经精确前像及真实原生退出证明确认旧执行者已退出，可通过正式 gateway 原子转入 reconcile_required/unknown，保留原请求、一次外部调用、空响应、预算及审计，不伪造终态、不退款、不重发；此项修复遵循既有 0017 的请求谓词，不修改门限。前驱保留，不用 schema16 程序打开已经安装的 schema17 库。部署后用新的真实捕获完成来源准入、入库、自动审核、投递和公网详情，再验证自然调度与断连恢复。

实施核查补充（22:50）：正式备份登记会在现役数据库追加合法操作、审计及恢复点记录。因此，已签应用恢复的旧 snapshot 仅作为真实闭合恢复 canary 和恢复能力证明，不直接作为生产替换前像。生产迁移必须在真实维护关闭、所有 writer 原生退出及无数据库占用后，从当前完整 schema16 库按 held-FD 完整身份/hash 复制到新隔离文件；调用同一 sealed APP 的既有0017事务迁移，核全历史保留、schema、integrity/FK及重开后，再按原库与配套配置精确CAS安装。实际当前源SHA和已签恢复snapshotSHA分栏记录，不能合并或补签。原库inode/配置、clone和失败回执均保留；新业务提交后不得简单回滚丢弃历史。这是已授权升级中的数据保全修复，不改变来源准入或0017闭合门。

## 验收与执行边界

root 独占 M1、生产与云端动作；备份及 X 在各自新隔离目录开发，审查者不参与实现。先验证受影响正常路径、权限与重放反例、崩溃/断网接续，再独立审查。旧 407/408 恢复及已完成安装不可重放。

整链完成必须具有 M5 离线条件下的真实备份/恢复与自然 RSS 发布证据，及 X 正式来源的小范围完整运行证据。云端状态错误、真实通行密钥或主机解锁未完成时，继续独立代码准备并如实保留未验收项。

本轮盘点原始证据：`scratch/backup-icloud-audit-20260922/inventory-r1/` 与 `icloud-status-r2/`。21:23–21:26 的数据只代表该次采样：最新本地包593成员、约2.065GB；iCloud 15个密文对象中14个标为已上传，最新对象与 manifest 报4355。不得把这份盘点当作新云端恢复点验收。
