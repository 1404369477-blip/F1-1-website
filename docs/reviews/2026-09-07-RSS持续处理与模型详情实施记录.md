# RSS 持续处理与模型详情实施记录

更新时间：2026-09-07 15:55（Asia/Shanghai）。RSS v5、0016/schema ae757 与模型详情已部署。运行事实与接续统一见 [当前生产状态](../当前生产状态与执行待办.md)；本记录保存证据和历史。X真实生产链仍未完成。

## 15:37 Sky等待原因补充

只读使用实际v5 worker选择谓词和持久cursor检查：待处理总41，MS34/Race6/Sky1。Sky唯一候选ref为a3c3f456f0ce18a0，与已保存的旧未知模型请求一致，当前来源准入允许，但没有模型成功结果；该候选已被cursor越过，不阻断其他来源。没有证据支持Sky被全源停用或另有新稿被队首饿死。原始证据 `scratch/rss-source-epochs-20260907/v5/postcutover-readonly/sky-cursor-0737.json`。R4首自然REGISTER失败与新RPO超龄、R5诊断/有界重试候选统一见备份报告，不能继续沿用首轮成功声明周期完成。

## 15:23持续处理与备份边界增补

真实只读快照 `scratch/backup-projection-boundary-20260907/r4/business-before-install.json` 显示control仍live/clear/ready v30。自13:52:49恢复以来，MS新中文稿36（当前版本35）、自动审核59、成功投递44；Race新中文稿16、自动审核17、成功投递16；Sky新增处理0。公开已确认到generation279，280当时pending。阶段计数包含既有稿和版本更新，不能当作文章一一对应的漏斗；Sky等待原因、完整积压变化与三源持续吞吐尚待核验。

旧Sky未知模型请求在14:30的六表逐行hash保持不变，同版本新gateway POST=0，证据在 `scratch/rss-source-epoch-independent-review-20260907/v5-business-followup-0630.json`。本窗口另有MS模型请求14:48:42创建，14:49:19成为EXTERNAL_UNKNOWN；继续保留待对账，不能重发或写成功。

14:04与15:12旧备份登记失败，连续RPO有超龄区间；14:00及凌晨成功证据只代表对应窗口。严格确认投影边界的R4已于15:24切入，首轮登记后双签及自然周期仍在核收。实现、原始失败、独立审查、安装和回退统一见 [备份边界修复报告](2026-09-07-备份投影边界修复与周期复验.md)。业务v5发布包、数据库schema和三常驻PID不因该backup-only切换改变。

## 14:00 v5 实际生产迁移、安装和恢复

最终 v5 在真实非空副本上重新完成 0016 迁移、固定候选构建、同版本备份、Admin/Public 恢复启动、M5 密文实际读回与签名、合法恢复点登记，并通过独立审查。v4 的首次未知请求保留条件误将历史 `unit_kind=requests` 写成单数，且额外假设注册 route identity 等于端点 URL 的 hash，因此真实副本拒绝；未修改真实业务记录绕过门禁。v5 使用实际注册的 opaque route identity 与请求内精确绑定，同时接受既有请求计量名称；最终谓词直接在两个真实只读副本通过，并验证 gateway 重启后同一未知请求为零 POST。旧失败与 v3/v4 候选保留，均未安装生产。

生产切换前的已登记恢复点为 13:37:46.042，完成于 13:39:48.331。先卸载处于 idle 的采集/备份任务，再向旧 Admin 仅发送一次 SIGTERM，等待 PID 自然退出后卸载；原 outbox 为零，无需执行备用 drain 工具。通过正式 gateway 将 control 26→27→28（paused/stopped），停止 Public 后核实所有服务卸载、生产 DB/WAL/SHM 无其他句柄。13:46 执行 0016：原 67 表全部行与计数保持不变，仅增加空的来源绑定表；数据库身份、control、未知模型 operation/attempt/预算及其绑定保持不变。安装按 staging receipt 校验并逐文件原子替换，保留完整 journal 与旧文件。

| 身份 | 当前精确值 |
|---|---|
| 正式候选 | `candidate-v10-20260907-rss-v5/app` |
| 官方 Admin manifest 原始字节 SHA | `97aa22124a1e77c1a9193b011a9400e6fd62e47791f6f2b3b567ef88e09d9074` |
| full release manifest 原始字节 SHA | `d410d5ee85c23460c8d39af5c4025b50d901409ff7453ab3e580864112328f78` |
| 生产 Admin deployment 原始字节 SHA | `2f3306cc32374b5831620666ebc2fe70c85a1de066e8c681d46bc23a2d14681e` |
| 生产 Public deployment 原始字节 SHA | `d21bf3c5aafd42b9edca322e161af3fcbcb3b4670704ce33abd62563c9235119` |
| 当前物理 schema SHA；user_version | `ae757027a0d6ce2ed4437989dc98634b0e320ff70cd76b852fe5e753dcb2383e`；`10` |
| 固定备份运行包根 SHA；文件数 | `75dd5d29e88e4538c718bc7d6b5436a16bba03377bff5c8229ad7258a44c0dca`；`761` |

安装后先执行新版本的真实生产备份：包 `1788760099782_60adf53686c635a8` 的恢复点为 13:48:19.782，13:50:18.686 完成登记；精确绑定上述 full/deployment/schema，M5 独立实读 395,557,708 字节密文，SHA `6896b3e4f899ca406c515bb4834d6627445de72d4c54cd3b86de41899934f410`。实际应用恢复耗时 25.888 秒，Admin 200、未授权 401、Public health/feed 200、12 条内容，子进程退出与演练目录清理通过；两份 Ed25519 签名由独立审查复核。相邻恢复点间隔 633.740 秒，本次新登记完成时旧点年龄 752.644 秒；仅代表该切换窗口，不据此承诺全部未来周期。附加 iCloud 归档仍失败，Passkey 恢复未实测。

13:51:40.719 control 合法恢复至 live/clear v30，writer/recovery epoch 均为 2。13:52:49 启动 Public/receiver/Admin，等三个端口监听与 HTTP 检查通过后于 13:56:12 启动正式三源 collector 和 Standard 备份调度。Public 为 `127.0.0.1:3000`，Admin 为 `3101`，receiver 为 `3102`；早期准备备注中的 Public `3100` 不准确。旧独立 refiner 不再加载，自动整理/审核/发布与 sender 由新 Admin runtime 调度。

生产模型设置页与 assets 均由 M5 通过实际私网入口取得 HTTP 200；served settings.js SHA `5071d2e3b311ff680ed058ce2936b15bd1e714c3dc9a88f258bd05db020f2806` 与已验证实现相同。模型详情可通过设置页卡片进入，保留仅写入 Key、先连接验证再原子更换、fresh Passkey 与失败保留原配置；没有使用真实新 Key 或用户 Passkey 完成一次凭证更换，实际 iPhone 键盘也未实测。

13:58:37 的真实业务窗口快照已见 Motorsport 当前 revision 中文稿 4、自动审核成功 4、自动发布成功 2、outbox 成功 2，公开 generation 221；The Race 与 Sky 本窗口新增暂为 0，不能据此宣布三源持续验收完成。新 schema 本窗口带 reason_code 的操作为 0。公开接口曾有一次 10 秒超时，原探针不能区分 health/feed；保留该失败，后续未改服务重读 health 200/1.808 秒、feed 200/1.088 秒。性能长期稳定性与后续业务吞吐继续核验。

执行证据集中于 `scratch/rss-source-epochs-20260907/v5/production-cutover/`，业务只读材料在同级 `postcutover-readonly/`。独立收据集中于 `scratch/rss-source-epoch-independent-review-20260907/`：`v5-production-install-addendum.json` SHA `823d23110164b8d4032eee3e209ee44cbd373bc2d860c0444c6b61a9ba845ee6`、`v5-production-backup-addendum.json` SHA `e0ca65039d60010a7a7e99ff9d9a98a009e3d862ad31dfc01d05c5754f01efa5`、`v5-running-addendum.json` SHA `b44b4ecc8872e61dc0eed8a32f5a6adb649e879e416b25672b3dad6d2b56e144`。

当前回退材料为 `[M1-HOME]/F1-1-website/.rss-automatic-install-rollback-20260907-v5/` 的 journal/before 与 `.rss-source-epoch-preflight-20260907-v5/production-pre0016.sqlite`。生产已提交 0016，旧 v2/schema81a 不能直接重启；应用回退须使用同 schema 的 v5 fallback 并保持兼容备份，恢复旧数据库须另走完整恢复流程，禁止覆盖运行中的库。旧 `phase-control` 与钉 version 26 的备用 drain 均不能用于现态。未将 RSS/X/完整 Admin TASK 标记 complete 或 ACK，也未混合提交共享脏工作树。

## 13:05 非空后继验证及未知模型请求

0016 的 SQL 与物理 schema 已冻结为 `ae757027a0d6ce2ed4437989dc98634b0e320ff70cd76b852fe5e753dcb2383e`。v3 在真实非空副本上保留全部 67 张既有表的计数和逐行 hash，仅增加空的 `rss_automatic_source_binding_v1`；重复迁移无新增写入。后续加密快照、恢复启动、M5 实际密文读回及签名、合法恢复点登记均通过独立核验。恢复启动返回 Admin 200、未授权 401、Public health/feed 200 和 12 项，未验证真实 Passkey 恢复。证据位于 `scratch/rss-source-epochs-20260907/{migration-evidence.json,migration-history-evidence.json,clone-cycle/}`，独审增补为 `scratch/rss-source-epoch-independent-review-20260907/{r2-clone-addendum.json,v3-cycle-addendum.json}`；此处均为隔离克隆证据，不代表 v3 已部署。

生产继续运行期间，`2026-09-07T04:49:45.895Z` 的 Sky 模型请求在约 30 秒后变为 `EXTERNAL_UNKNOWN`，operation 与 attempt 均为 `reconcile_required`，没有响应 hash。不能把它写成成功或已结算，也不能再次发送同一版本、内容和模型的请求。原 0016 门禁止任何旧自动请求未结算，因此阻断此次切换。已另取包含该未知请求的真实一致性副本（SHA-256 `75eaad3791794c83bf12a2def377fc8510b6f5b36a52c7901e8f80c109afb805`，294,117,376 字节，完整性通过、外键 0、未完 outbox 0）。后继只考虑允许证据完整且已持久冻结的未知请求原样保留，预算、attempt、审计和历史不得改写；必须验证新 schema、新 gateway 重启后同一请求仍为零 POST，其他未完成操作继续拒绝。v4 构建和新包恢复验证尚未完成，不得复用 v3 证据宣称通过。

v2 首轮重启曾因备份 plist 的 ProcessType 回退为 Background，在并行启动压力下导致快照超时和陈旧锁。已先停空闲备份调度，核实原锁进程不存在，使用既有锁恢复工具隔离原锁，再将该 plist 恢复为 Standard。之后真实新点及自然周期重新完成；原事件与修复收据保存在 `scratch/rss-source-epochs-20260907/production-v2-repair/`。M1 附加 iCloud 归档仍失败，M5 独立密文备份链通过，二者状态不得混同。

X 可信模块 v2 的 45 项测试与独立失败探针通过。同一 status 正文更新时合法时间精度变化的 P2 已修复；独审结论限定 `PASS_MODULE_CONTRACT_ONLY`，收据在 `scratch/x-trusted-independent-review-20260907/independent-review.json`。真实 M1 DOM、适配当前 RSS 后继的生产 schema/gateway、来源启用及网站自动链仍未验收。

## 12:20 生产 v2 切换与真实运行缺口

已通过合法暂停、global stop、卸载原写入进程并核对 DB/WAL/SHM 无句柄，执行生产 0014 迁移。原 67 表逐行历史 hash 保持，只有 9 条预期策略增量；数据库 inode 和控制版本在迁移期间不变。随后安装 `candidate-v10-20260907-rss-v2`、同版备份运行包与新调度，退役旧 refiner。新 schema 的实际备份点为 `2026-09-07T04:00:52.474Z`，`04:03:23.110Z` 完成登记，绑定 full 文件 hash `8b83b5903266ebc77a2c6ba2dd5ff04ca3091e0c9f8590d2513fa5c2401caee6`、deployment hash `a29cb14f8bb5b8e08cd9957e03e6ea419bcad49035fa99830f615149d084a39b` 与 schema `81a13c75401a12e32777cc641e0a4db3a0658a9d6195172d3387ae34fa603c20`。合法清停/进 live 后控制为 live/clear/ready、version 26。

Public feed 真实 HTTP 200、12 条；现有 generation 201 的内部 receipt GET 200，状态 active，连续两次约 2874 ms / 217 ms。M5 通过真实 Tailnet 域名访问模型设置页面和 settings.js 均 200，JS hash 与本次代码完全一致（`5071d2e3b311ff680ed058ce2936b15bd1e714c3dc9a88f258bd05db020f2806`）。真实设备 Passkey 换 Key 尚未验收。首次同时启动多个后台进程时文件校验约耗数分钟，不能把隔离预演耗时当作实际停机时长。

真实自动周期返回 `RSS_AUTO_SOURCE_EPOCH_STALE`。只读核实：MS 来源 config/safety 为 4/4、recovery 1，Race 为 1/1、recovery 1，Sky 为 1/1、recovery 2；全局为 1/1、recovery 2。0010 来源登记与既有 authority 合同明确区分来源局部计数和全局计数，新 TS 及 0014 current view 错误地要求跨作用域相等。当前拦截属于实现缺陷，不能据此要求修改来源历史、伪造新授权或宣称自动链已完成验收。

0014 SQL 与已签发布包保持不可变；修复以新增 0016 迁移及新 schema hash 进行，来源快照随新 fence 留下不可变绑定，消费者比较来源当前状态与该快照，全局 operation 仍校验当前全局状态。旧 fence 不回填，必须重新合法签发；未终结的旧自动投递须先收尾。生产过程收据在 `scratch/rss-automatic-release-20260907/production-v2/`，后继实现与复现进入 `scratch/rss-source-epochs-20260907/`。

## 已确认范围

用户要求查明待处理内容持续积压与 X 无采集的原因，并让后台模型卡片打开详情、在页面内更换 Key。用户已明确回复“按这版界面实现”；模型详情以 `design/ui/F1+1-admin-model-details-v0.1/` 冻结预览为依据。原 RSS 自动处理与 X 页面采集、审核发布上站授权继续有效，无需重新确认业务方向。

本次 RSS 保留三源 Motorsport / The Race / Sky Sports，以及固定边界 `2026-09-05T02:30:00.000Z`。人工当前版本的修订、拒绝和安全决定必须受保护；原文版本变化须重新准入和提炼。新发布沿既有一代一投递状态机，经唯一 sender 送达公开投影。回退采用与后继 schema 匹配的手工 fallback；不能把旧 schema 包当作迁移后的兼容回退。

## 08:21 生产只读诊断

同一只读事务确认：边界后三源当前待处理 95 条（Motorsport 55 / The Race 22 / Sky 18），均缺当前 revision/hash 的机器草稿和候选准入 fence。已有 451 张 fence 尚未到期；缺口是新条目没有持续准入。refiner 的选择条件过滤掉这些条目，所以运行返回 idle，未调用模型。

数据库 `queued=1611` 中，1573 行为旧版本或历史 bundle；38 行虽是当前 bundle，但都早于已确认边界。不能把 1611 当作当前待发布新稿。已有边界后 published 61 条中，25 条原文后来变化；旧审核发布选择器没有纳入这些更新。

collector 为 900 秒、refiner 为 120 秒；没有持续自动审核发布入口，现活签名 capability 两项为 false。Admin 实际已有 60 秒 sender，不能因缺少独立 sender plist 就断言没有发送调度；当时 outbox 为 201 succeeded、6 terminal_failed、0 pending/leased/reconcile。

X 选中 27 个账号已在旧 59 行 registry 内，但全部是 disabled 的 manual 配置，identity/adapter 未验证。生产尚无 `x_page_*` 结构、capture 或自动采集 worker。真实 M1 浏览器接入只影响真实捕获与可信 producer 验收；结构、后继接口和备份兼容可以继续本地实现。原 0013 仍仅为合成内存实验，不能改标记后当作生产能力。

只读材料：`scratch/pipeline-diagnosis-20260907/production/` 的 report、summary、queues、eligibility、inventory、files；X 诊断在同级 `x/`。生产 DB、模型 Key 和私有身份未导出到报告。

## 本地实现与审查状态

- 模型详情：真实 HTML/CSS/JS 已实现桌面右侧详情与手机全屏详情，Key 只写不回显；新 Key 验证成功后才替换，fresh Passkey 再认证、CSRF、幂等操作、响应丢失核对与失败恢复均已接线。后端明确区分已配置与连接已验证；不支持落库的 GLM 不可设为当前模型。六档视口/主题和 390×560 小视口已验，无实际 iPhone 键盘或真实 Passkey 验收。独立本地审查通过；最后的 16 项 UI 测试通过，后端和安全复现材料见 `scratch/model-credentials-20260907/` 与诊断目录 `model-settings/`。
- RSS 后继：新增 0014 精确 schema、当前版本准入、正式 automatic reviewer/publisher、模型结果落库及仅 run 入口启动的调度。SQL 已冻结为 `81a13c75401a12e32777cc641e0a4db3a0658a9d6195172d3387ae34fa603c20`，运行代码仍在收口投递恢复边界，尚未冻结正式发布包。`createReviewAdminRuntime` 的恢复演练路径必须保持无定时任务、无 handoff、无外联；实际 run 退出前须等待模型与 sender 完成。
- 独立审查已复验修复：队首模型失败不再饿死后续候选、当前人工修订保持不变、已知模型错误可以在新预算操作下恢复、candidate/media 半写会整体回滚。provider 已分别绑定备份的 full manifest hash 和原始 deployment 文件 hash，sender 使用独立 owner，RSS 启动不依赖人工 handoff。已补原文在入队后更新的固定发布版本证明，并以当前合法授权续期同一投递所需 fence。响应丢失后的持久恢复已通过下述独立复验；异常细分原因的审计读模型仍在收尾，最终整体审查尚未签结。
- 正式 sender 已补 `acct-projection`。投影读取新增实例内有界验证缓存，每次仍读取和 hash 全部祖先的当前原始字节、重验文件边界并走到 generation 1；缓存只减少重复解析和验签，冷验证移到 receiver 监听前。7 项投影测试及独立复核通过；201 代×100 条、约 91.7 MB 合成链在 M5 的热读取从约 2527 ms 降至约 42 ms，随后以最终代码在 M1 隔离合成目录、同 Node 24.18.0 复测冷 3830 ms / 热 70 ms，原生产目录、端口和服务未修改；当前真实 HTTP 交付仍待部署核收。缓存上限 4096 代，不能据此声称超长历史增长问题已关闭。
- 原 collector wrapper 只接受旧 schema 和两项自动能力关闭的旧清单，无法直接随新迁移继续运行。正式三源 collector 已完成真实 runtime/本地 HTTP 集成，包括 candidate/media 单事务与同 slot 零重复请求；替换时须退役旧 refiner 调度，保留唯一采集/发送执行者。

## 09:49 真实历史副本预核对

通过 SQLite 在线一致性备份，从只读生产连接生成 M1 私有副本（270,344,192 字节；SHA-256 `cc987bff38d48a03478644ba1a5c925e9f2682b019219656869f95194621ea56`）。完整性检查通过、外键零异常，生产服务与控制面未修改。副本保留 579 候选、2890 草稿、2635 bundle、2626 审查与发布行、207 outbox、13148 internal operation 和 157 个有效恢复点。

按 parser 的实际字段顺序复算固定边界后三源的当前原文与图片摘要：156/156 匹配、0 异常。Motorsport 78 条与 The Race 38 条均有匹配图片；Sky 40 条原 hash 使用 media=null。该结果证明此副本中的存量一致性，不证明未来旧采集器不存在半写窗口；新版本正在将单 item 的 candidate/media 写入合并为一个事务，并在自动准入时复算完整 payload。收据位于 `scratch/rss-automatic-release-20260907/production-clone-{evidence,media-integrity}.json`。最终生产切换前需重新核对最新点。

## 10:00 非空迁移与发布包验证

M1 独占克隆 `migration-working-r3.sqlite` 已通过 0014 迁移与重复执行检查。先在克隆上通过原 gateway 合法暂停并开启 global stop，再对迁移前后的 67 张表逐行计算 canonical hash 与计数；排除严格限定的 9 条新增策略记录后，全部原有记录一致。新增行分布为 internal_operation_policy 4、internal_control_action_policy 2、internal_required_fence_policy 2、gateway_entity_policy 1；未增加业务行。原 `before.sqlite` 和生产 deployment 文件 hash 保持不变，生产写入与服务动作均为 0。此证明覆盖迁移本身，克隆中的两次控制转换仍作为明确审计增量保留。

证据为 `scratch/rss-automatic-release-20260907/production-clone-migration.json`、`production-clone-migration-history.json`；独立审核已复算表计数、SQL hash 和 replay=false，收据在 `scratch/pipeline-diagnosis-20260907/rss-independent-review/production-clone-evidence-review.json`。前两次克隆实验仅因验证脚本未处理 WITHOUT ROWID、未列全策略表而失败，保留原失败现场；最终验证已修正这些证据脚本问题。

180 文件闭包的完整发布验证 4/4 通过。投递恢复新增 source-state 与 delivery-authority 后，最终 Admin 闭包更新为 182 文件、路径集合 hash `a5fe9f2bbbd6010edb80939137ba0ae9354cc68b25006d27d2f4402b06ea3d26`；需待运行代码冻结后验证这一最终版本。尚未生成正式 M1 RSS full/fallback 候选、执行新包恢复启动演练或切换生产。

## 11:15 同一投递持久恢复与安装准备

独立审核使用真实 runtime、真实本地 HTTP receiver 和文件数据库，分别验证了关闭所有连接后重启、真实子进程在 receiver 接受 POST 后直接退出、原授权过期 16 分钟，以及 fallback 查询确认后在 outbox 收尾前中断、再回 full 仅本地重放。均完成原 delivery 与 generation，远端调用总计 POST 1 / GET 1，无重复 POST；原 attempt 如实为远端成功，过期的本地旧 operation 保留明确失败原因。预算 consumed_units=2、reserved_units=0，三个无外联的数据库操作 reservation 均释放。证据在 `scratch/pipeline-diagnosis-20260907/rss-independent-review/runtime-reconcile-{same_full,expired_fallback,crossrole-replay}-final.json` 和 `runtime-post-crash-final.json`。

404、5xx、凭据错配以及仅提交 intent 但无法证明已发送的情况继续保留待处理与零重 POST。旧 outbox SQL 禁止只修改 reconcile_wait 的原因字段，因此细分原因采用既有不可变审计并在 Admin 读模型显示；0014 SQL 与已验迁移指纹保持不变。此项仍待最后负例回归，不把上述正例验证扩大为所有异常均可自动恢复。

安装工具已准备并完成限定范围审查：Admin/collector/backup 三份配置与 Public 两份配置均只生成新 staging；所有 plist 的 RunAtLoad=false，Public Beta 的既有 KeepAlive=true 仍可能在 bootstrap 时启动，因此 bootstrap 安排在新身份恢复点与合法 clear/live 后。控制工具对精确旧 1408 与新 RSS candidate 提供合法暂停/停止，新版清停/进 live 则同时要求精确 full/deployment/schema 的新鲜备份，在事务内部重新读当前时间核验。

生产迁移工具尚未运行。它固定 DB、旧部署和新 candidate，检查 official manifest、原始部署 hash、inode、paused/stopped 与 control version、无其他 DB/WAL/SHM 句柄，然后在线生成 pre0014 保留副本，执行已在真实历史克隆验证的迁移，逐行比对原 67 表仅新增 9 条策略记录，并检查 control/inode 不变。正式 M1 candidate、新备份包、非空应用恢复演练及真实生产切换仍未执行。工具位于 `scratch/rss-automatic-release-20260907/m1-production-migration.mts`，安装准备在 `scratch/rss-runtime-20260907/INSTALL-PLAN.md`。

## 11:15 时的部署前待办（后续结果见顶部）

1. 处理独立审查发现，冻结 worker、collector、runtime、迁移、UI 与发布闭包，完成相应集成验证。
2. 新签 full/fallback，生成同版本固定备份运行包，在非空真实恢复 clone 上演练迁移、备份恢复、Admin/Public 启动与登记身份，确认无自动副作用。
3. 生产重新核对新鲜可恢复点与在跑写入者，通过既有合法控制入口进入迁移窗口；保留旧库恢复材料、服务清单和包。迁移后启用同 schema 的新清单与兼容备份包，再接通 collector 和自动管道。
4. 以实际新草稿、自动审查、outbox 成功、公开 generation/内容和自然周期核收；本地测试、手工注入或一次性脚本不代替持续运行验收。

主控负责模型前端、投影性能、发布闭包与最终整合；两位实现 Agent 分别负责 RSS 内核与 runtime/collector；未参与实现的 Agent 独立审查。本轮未混合提交共享脏工作树，未将任何 TASK 标记 complete/ACK。精确候选与生产执行收据将在通过后补入本文及当前运行入口。
