# 备份投影边界修复与周期复验

更新时间：2026-09-07 15:55（Asia/Shanghai）。**R4 安装通过，首轮及两次自然周期的整体验收失败。** 首轮与第二自然周期实际验证通过，第一自然周期 REGISTER_FAILED 导致新 RPO 超龄。R5 仅诊断及写锁争用重试候选已完成22项本地验证，独审中，尚未部署；现场失败根因仍未确定。

## 15:47三轮独立核收及R5本地候选

| 周期 | 恢复点（北京时间） | 登记 | 独立结论 |
|---|---|---|---|
| R4单次kickstart | 15:25:22.542 | 15:28:36.456成功 | M5实物528,948,542字节、两类Ed25519、manifest/DB/checkpoint/实际应用通过 |
| 第一自然 | 15:34:11.843 | 15:37:38.583 REGISTER_FAILED | M5实物538,112,484字节、双签及应用37.667秒通过；无新登记点 |
| 第二自然 | 15:43:07.108 | 15:46:40.442成功 | M5实物546,068,499字节、双签/绑定/应用41.710秒通过 |

有效点间隔1064.566秒，新点登记完成前旧点年龄1277.900秒，均超过900秒。15:47观察时最新点年龄251.541秒，只说明当时重新新鲜。三个业务PID、control v30和双锁退出保持正常。独立报告为 `scratch/backup-projection-boundary-independent-review-20260907/boundary-install-and-three-cycle-review-final.md`，SHA `f84d3eb4563647c9529c652500e06a8e5adbd65758fefbff69fcd2481bd74f28`。

第一自然失败只有CLI的REGISTER_FAILED，没有原始错误码；该轮在包验证后、backup handoff持久化之前或其写入处失败，未出现新handoff/operation。双签、时效和异机密文问题已排除，仍不能定因SQLite争用。真实双连接隔离复现已证明：持写锁时现有BEGIN IMMEDIATE的三次短等待最终抛ConfigError LOCK_CONTENTION，CLI原实现因message带解释文字而丢掉code，和其他原生错误混同。

R5保持原REGISTER/包验证/签名/恢复门及250ms连接策略，仅在CLI为LOCK_CONTENTION或Node SQLite主码5做至多三次尝试，250/500ms退避，30秒后不再开始新尝试，外部45秒命令总界保持。每次重新安全打开数据库并完整验证同包，成功回放保持同一个operation及预算。闭集诊断仅含stage、attempt、reason、允许的native code及数值SQLite码，不打印message、stack、路径或密钥。正式22项通过，包括真实CLI子进程锁释放后单次登记/幂等、永久锁3次失败、缺Key/坏签名不重试、两次之间篡改签名时重新校验拒绝；tsc/lint通过。候选清单 `scratch/backup-projection-boundary-20260907/r5/implementation-manifest.json` SHA `776d6aede5fbf6e97c2f519800935b9e3ad900d27484e1cf8583dbd45d239751`，待独审与生产验证。

## 已复现的问题与修复

14:04:38.833 的包 `1788761078833_01757aa996f3a9a4` 快照成功，14:06:59.615 登记失败 `BACKUP_PROJECTION_DELIVERY_MISSING`。receiver 于 14:04:25.783 激活 generation 227，Admin ACK 于 14:04:54.185 才落库；原实现先 VACUUM 数据库，再读取 live pointer，混合了两个时间边界。相邻成功恢复点间隔 946.650 秒，新点完成登记时旧点年龄 1079.960 秒，均超过 900 秒。

15:12:36.486 的旧包 `1788765156486_93f2976bc9405f59` 于 15:15:49 再次登记失败。下一成功点为 15:21:15.102，完成于 15:23:56.699；前一成功点仍为 15:03:33.536。该区间也超龄，失败原因及实际签名复核由后续独立报告补充，不能仅凭同阶段失败认定原因相同。

R4 官方备份 CLI 强制使用 `confirmed-delivery-v1` 边界：从一致性数据库快照选择最新已成功 ACK 的 outbox，与真实 live pointer、严格规范字节的 receipt、签名 generation 和信封身份交叉校验。数据库确认点在 live 之前才允许使用其对应的投影；数据库确认点反而领先 live、最新 receipt 不闭合或文件不安全时直接失败，不退回更旧记录掩盖问题。已验证的 generation 字节与规范 active pointer 冻结后一起归档，后续 live 切换不会改变该包。

REGISTER、应用恢复验证及签名要求没有放宽。原失败生产包在 M1 解密到隔离目录后，R4 重备份与恢复保持 69 张表、187,922 行的逐行集合不变，原数据库和原 Public 文件未变；历史快照中 ACK=226/live=227，恢复后的 Public 使用确认过的 226。实际 v5 Admin/Public 进程隔离启动通过：Admin 200、未授权 401、Public 200、12 项，31.410 秒，子进程退出及启动目录清理通过。该材料是历史数据库重备份测试，不能作为新的生产 RPO 点或 M5 实读证据。

## 验证与冻结身份

| 项 | 结果或身份 |
|---|---|
| 正式聚焦测试 | 40 项通过（17 项边界、7 项快照、16 项恢复门）；全量 tsc、定向 ESLint 通过 |
| R4 独立审查 | 10 项独立反例通过；规范字节、实际 Public reader、历史 generation 替换竞争、DB 领先 live 均覆盖 |
| 隔离真实失败包演练 | M1 实际应用恢复通过，独立签名及 69 表对照通过 |
| core.ts SHA | `fa35cf0607998f90a582df8df3700c2a65f9b0c8b6f29d3b6cf710d2f4589037` |
| 官方 CLI SHA | `8be600bfa63e74de3a8411b8ebcb79817e9166ab8dc341b64b448a569800cb3a` |
| 新固定运行根 SHA | `ea30638ffb2d194d90fa46f18f0d40dadf11bbd8ed47881ee1ca16a01c9269d5`，761 文件 |
| 前驱固定运行根 SHA | `75dd5d29e88e4538c718bc7d6b5436a16bba03377bff5c8229ad7258a44c0dca` |
| 最终安装 helper SHA | `768e010bec4eee31c1d3c6e44c7316f8ea3a680f4d10d69d96f06dab1b59b1f4` |
| 新 plist SHA | `5aade300c882c782bd21f055a7ae9fe49014f2a75256fb6718566487206a18ed` |

原安装 helper 的两个 P1 已用本地模拟复现：bootout 已产生卸载副作用但命令超时会漏回退；SIGTERM 可能在取得 guard 后留下锁。最终版以延迟信号检查和真实 loaded/PID/plist 状态对账修复，独立 8 场景、31 断言通过，包含启动返回不确定、替换后中断、回退失败留证、活动备份 PID 与 guard 所有权变化。

## R4实际切换（历史步骤）

15:24:13 准备收据确认仅修改 plist 的 `ProgramArguments`；七个配套工具与旧版字节相同，WorkingDirectory 仍是 RSS v5。15:24:33 在真实备份 idle 窗口取得共用 cycle guard、卸载备份任务、原子替换 plist，再以 `RunAtLoad=false` 加载新任务。before/after：Admin PID 40599、Public PID 40596、receiver PID 40594，control 均为 `live/clear/ready`、version 30、writer/recovery epoch 2。安装零生产数据库写，guard 已释放。随后只执行一次 kickstart 开始新运行包首轮。

新路径为 `[M1-HOME]/F1-1-website/backup-runtime-releases/20260907-projection-boundary-r4/`；保持 `StartInterval=300`、`ProcessType=Standard`、`RunAtLoad=false`、`KeepAlive=false`。本轮不重启业务服务。

当时计划核收（结果见顶部）：首轮加密包、M5 同包真实密文读回与签名、Admin/Public 应用恢复、合法登记，以及至少两个未 kickstart 的自然周期；分别计算恢复点间隔和新登记完成前旧点实际年龄。附加 iCloud 归档失败与 Passkey 恢复未实测继续单列。

回退材料在 `[M1-HOME]/F1-1-website/.backup-projection-boundary-20260907/installation-r4/`：`previous.plist`、`candidate.plist`、`prepared.json`、`install-result.json`，旧兼容运行包保留。回退仍需真实 idle/互斥检查，不终止活动快照，不覆盖运行数据库；旧包仍有已知投影时间边界缺陷，不能用回退代表验收通过。R1/R3 临时包及失败的合成预检均不具备生产身份。

## 证据位置

- 实现、R4 冻结与 M1 隔离演练：`scratch/backup-projection-boundary-20260907/r4/`；实际切换原始文件为 `installation-r4/`。
- 独立源码/演练/安装脚本审查：`scratch/backup-projection-boundary-independent-review-20260907/`，包括 `review-r4-final.md`、`m1-preflight-review-final.md`、`switch-script-final-review.md`。
- 第一次失败和旧自然周期：`scratch/rss-source-epoch-independent-review-20260907/v5-natural-followup-gap-addendum.json`、`v5-natural-backup-addendum.json`。
- 15:22 原始只读生产回扫：`scratch/backup-projection-boundary-20260907/r4/live-before-install-0734.json`，文件实际 `at=2026-09-07T07:22:31.349Z`；文件名尾数不代表采集时间。
