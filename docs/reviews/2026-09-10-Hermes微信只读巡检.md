# Hermes 微信只读巡检

## 已确认合同

**2026-09-11 21:39 入口确认补充**：用户在上一轮候选、独审结果及入口前提说明后回复“可以，继续推进吧”。正式验收采用微信 `/f1` → 原生固定命令 → 单次只读采样与标准报告 → 手机实际收件；此前自然问句入口的准确性失败保留为历史，不据此声称其已修复。已明确的空闲、无更新待答、前置hook未接管条件继续适用。允许按既有冻结候选完成必要安装、配置接入、加载和实收验证；业务维护、定时任务及主动消息不在本切片内。开发95D001已正式补录授权并resume；下方“待选择”段为本次确认前的准备记录，最新安装/激活事实按后续收据更新。

用户在 2026-09-10 核查既有微信链路后明确要求“推进下一步吧”，承接同日 [链路核查与下一步](../../scratch/hermes-weixin-link-audit-20260910/report.md)。本切片实施、独立验证并接入 M1 既有 Hermes；真实出口为微信私聊询问 F1 网站状态 → Hermes 的 `get_site_status()` → 带采样时间的简短结果。

新增固定参数为空的只读 MCP 工具。部署配置由操作者固定，模型不能提供 SQL、路径、URL、shell 或维护动作。Node 24 原生 SQLite 以 `readOnly`、`query_only`、短事务读取；文件、进程和 HTTP 均采用固定白名单、有界读取和超时。Python MCP 使用 M1 已安装 SDK，不新增依赖。代码单独部署，保留现役网站封存包。

输出合同为 `healthy / degraded / failed / unknown`、`observedAt`、`expiresAt`、各分项采样时间、闭集原因和可核验数值；读取失败、身份不符、旧观测不能表现为零队列或健康。DB 事务与 HTTP/公开 pointer 分别采样，不声称跨对象原子一致。生产健康结论只对采样时刻有效。

- 网站：本机公开 health、公网 HTTPS health、Admin 进程和无鉴权边界拒绝、隧道进程分别报告。公网来自当前受控部署/隧道记录，严格限制允许主机，不跟随跳转。Admin 的正常 loopback 请求缺少真实私网代理身份，因此只验证 `404/ADMIN_REQUEST_INVALID` 的边界拒绝，不能据此宣称私网登录或完整后台可用；不伪造身份头。公网探针明确来自 M1，不能把单一观察点推广为所有用户网络状态。
- RSS：固定 Motorsport / The Race / Sky，最近尝试、成功、实际采集间隔；按现役 allowlist、cutoff、当前 revision/hash 精确计算缺少当前草稿的队列和最老等待年龄。审批、发布队列与未决投递另外计数，避免混为同一队列。
- 未知请求：只读统计当前与既有未知，已有基线须绑定操作身份；无可靠基线时新增数量为 unknown。任何未知都不触发重发。
- 备份：有效恢复点必须匹配当前 deployment/schema/epoch/writer 身份与合法登记，检查年龄、对应异机与实际应用演练签名。文件存在或本地最新快照不等于有效备份。完整离机对象重读与灾难恢复不属于每次按需采样，准确标示本次可验证范围。
- 存储：当前可用/总字节；增长率需至少两个可靠样本，首样本为 unknown，不凭单点声称增长正常。

只读边界仅约束新增工具：现有 Hermes 与生产文件同用户，且仍有通用终端能力；本切片不宣称整个通用助手隔离。后续自主维护、定时巡检和主动通知另行实施。本轮不触碰采集控制、发布、schema、writer/fence、凭证或备份执行。

## 实施与验收

源代码置于 `app/src/server/site-status/`、命令置于 `app/scripts/`，测试位于 `app/src/tests/`。冻结候选后由未参与实现的 Agent 独立审查权限边界、误报反例和真实工具入口，再安装独立目录并原子更新 Hermes 配置；配置前保留字节和 SHA，失败仅回退本次项。正式 `/f1` 入口在安装后由用户于既有微信私聊发送 `/restart`，待重启返回并确认会话空闲后发送 `/f1`，核对新 Gateway、原生执行及手机实际报告。

已安装 Hermes 的 SIGUSR1 计划重启还会向 home channel 发通知并尝试恢复未完成任务，因此此前 r3/r4 接入采用 slash 热加载，未执行 Gateway 重启。新增工具集改变缓存签名与同名工具重载是不同路径：现役同名刷新函数可能保留旧描述，不能保证描述已更新。独立 CLI MCP 探针不算 Gateway 已加载。已确认的 `/f1` 新增 `quick_commands`，必须重启 Gateway 才会加载；用户发起的 `/restart` 用于本次激活。

必测：正常、RSS/备份过期、身份不符、数据库不可读/锁超时、HTTP 非预期/重定向/超时、错误或缺失签名、未知请求、观测过期及参数拒绝。模型可见结果不包含凭据、账号、原始文章、私有绝对路径、任意错误内容。

最终验收分为本地独立验证、M1 固定工具实际调用、Gateway 新工具可用、用户真实微信询问并收到回复。未取得手机确认时保留该缺口，不把本地 MCP 调用或 assistant 存档当作送达。

本切片为无界面 operator，不改变冻结 UI 或已确认 Function ID：设计身份、六格视觉、Mac/iPhone Admin 功能验证不适用；不提升任何既有 Function ID 状态。微信输出仅本合同时间与状态的文字表达。

## 当前状态

**2026-09-11 22:12：真实微信 `/f1` 已通过独立最终验收；实施95D001和审查3B70C6均已正式completed/acknowledged。** 用户按要求操作后于22:01回传正文，采样21:59:11.107、有效至22:00:11.107，共20行、1050字符/2252字节，正文SHA `a9acd145f4f5f0cf32064ba90e59be241da0f8e538eefdc8dcd7b6a4d7088524`。Gateway已于21:58:19重启为5281/runs2，配置与分发源码身份保持。独立限定日志显示21:59:10.496入站、21:59:13.523准备发送1050字符，采样位于同一3.027秒窗口且发送尝试在有效期内；同窗口没有对应持久化模型或工具turn。用户全文、固定配置与分发源码、重启身份及请求/发送窗口交叉支持本次原生入口和手机实收。

主控与独审分别复算60秒TTL及三源/备份四个年龄；正文与安装后实际报告仅相差两个采样/过期时间及四个派生年龄，其余逐字一致，Admin登录/容量变化率未知、任务存活未采样及根因未查都完整保留。新证据见[用户完整回传](../../scratch/hermes-site-status-activation-20260911/user-weixin-reply-215911.json)、[独立请求窗口](../../scratch/hermes-site-status-activation-review-20260911/wechat-215911-window.json)、[独立准确性核验](../../scratch/hermes-site-status-activation-review-20260911/wechat-215911-report-validation.json)。没有直接捕获本次子进程父链、服务器stdout/原始DTO、模型API网络跟踪或精确手机送达时间；零history与发送尝试各自都不能单独证明入口或收件，不把用户粘贴时间当手机送达时间。上述交叉证据已覆盖已确认的本次入口验收，不要求用户重复查询。

[实收验收附录](../collaboration/部门/测试部/报告/2026-09-11-TASK-20260911-3B70C6-Hermes微信f1实收验收附录.md) SHA `c75466d3e8af45f22fa5570e25262630c6e75436d5f6020685931e252f8c8275`，[新增12件证据清单](../../scratch/hermes-site-status-activation-review-20260911/wechat-215911-evidence-manifest.json) SHA `52a3dc074b30a53c7b2657f1b4c61e25eb6195a85ee27ee174359d486b9e89ef`；主控已阅读完整报告，并对原安装26件及新增12件清单逐项复算，零不一致，见[最终核收记录](../../scratch/hermes-site-status-activation-20260911/root-weixin-review-verification.json)。旧报告/历史失败不覆盖；当前使用空闲会话中的 `/f1`，无须再次安装或重启。业务故障恢复另按生产待办推进，未以本次接入完成替代。

### 安装与激活过程记录

**2026-09-11 21:49：正式 `/f1` 已获用户确认并安装；实际 Gateway 激活和微信实收待验。** 21:42 对新鲜原配置完成固定 dry-run 后执行已审 helper，只追加 `quick_commands.f1`。新 release 共13文件，十件payload和原r4九件逐字/权限回读通过；原MCP和其余配置保持，旧配置完整备份。配置由 `03941a9b10e3f967c13c005bbe710c9744589b185076fafd52941e982d990fee` 变为 `007fd2cc87885a4d625292d8f55089237228e3a656987e25b5e7ccf5649006a5`，M1 release内安装收据SHA `014690ddc86b2797a3ca7274214f3b33c49b3d850cd414e41fe4ee9999af9618`（本地格式化安装回报另有文件hash）。主控及独审分别完成安装读回；本轮未重启Gateway，21:44仍为PID1080。

主控于 **21:44:03.726** 执行配置中的精确固定命令，3.094秒返回1051字符/2253字节，exit0、stderr0。报告包含Admin登录/存储增长未知、未采样任务存活和未查根因；本机公开200、公网M1失败、三源成功仍为9月10日10:20、合法恢复点仍为9月10日10:08且清单/两签通过。该样本已过期，只证明对应时刻的命令与报告。见[安装收据](../../scratch/hermes-site-status-activation-20260911/installation-receipt.json)、[安装后检查](../../scratch/hermes-site-status-activation-20260911/installed-check.json)、[实际报告](../../scratch/hermes-site-status-activation-20260911/installed-report.txt)。开发95D001为claimed；后继独审3B70C6检查安装、加载及真实出口，未整体complete。

21:49独审在只读守卫下完整调用现役 `load_gateway_config()`，新进程得到唯一 `f1/exec`，command SHA `f8d03ac1bdc4825404907ded3e7ec108064ffd45fa9cf9847868c40823f709a8`，无managed scope覆盖，配置保持。只读守卫模拟既存目录的重复mkdir与相同chmod，并允许urllib3的一次IPv6本机临时端口能力探测；未创建文件、访问外部网络、调用模型或发送消息。该[加载检查](../../scratch/hermes-site-status-activation-review-20260911/loader-check.json)不等于正在运行的Gateway已重载。21:52已请求用户微信 `/restart` 返回后再发 `/f1` 并回传完整正文；仅有入站/发送尝试日志不能代替手机实收。

**21:56验收边界更新**：21:52:09–21:55:09的[180秒只读观察](../../scratch/hermes-site-status-activation-20260911/native-dispatch-observation.jsonl)自然结束，Gateway前后仍为1080/runs1，配置保持，未捕获固定reporter/sampler父链；0.4秒轮询不能用于断言窗口外或极短执行不存在。尚无用户回传完整报告，95D001已正式waiting_input，authorization_state继续user_confirmed；等待实际微信操作与实收证据。没有常驻观察器或新增定时任务；不重复apply。

### 入口确认前的技术准备记录

**2026-09-11 00:54：r4真实微信回复准确性仍未通过；`/f1` 候选技术准备已独审通过，等待用户选择正式入口。** 用户贴回00:00:44报告；Gateway已在00:00:04加载r4，普通查询只调用一次空参数巡检工具，标准正文和严格DTO完整到达，时间/数字正确，手机收件已有用户确认。最终模型摘要仍将记录未更新外推为“无采集/任务卡住”，并省略Admin私网登录和磁盘变化率unknown。旧技术PASS保留，真实出口FAIL见[后继准确性附录](../collaboration/部门/测试部/报告/2026-09-11-TASK-20260911-FAB9DA-Hermes微信回复r4准确性附录.md)，14件证据已由主控复算。

原r4报告中“同名MCP重载会刷新工具描述”的判断已撤回：现役函数在工具名集合相同时提前返回，可能保留旧描述；只更新正文已核实，新描述实际进入当前模型请求未取证。完整更正与反例均在上述附录，不修改冻结旧报告。

已向用户询问正式入口偏好：推荐新增原生 `/f1` exec 直接输出标准巡检报告，或继续自然问句并适配输出。用户尚未选择，不改变原自然问句验收合同。开发95D001已通过正式工具转为waiting_input；后继审查FAB9DA按技术准备分层出口completed/acknowledged，E37D5D保持ACK。原自然问句验收尚未完成。

### 固定命令候选与此前未激活的验证

`candidate-site-status-20260911-direct-r1` 已冻结：10 个 payload、107769 字节，manifest SHA `481246328bc9d88826e8d3e755ea782e3afb60b9b95aaef3761b94eb9c1569f4`。仅新增 [无参数报告 CLI](../../app/scripts/f1-status-report.py)，SHA `68ee75878989b35f431e4117d878c12d13d88c1623644d4390b5f0ef5cc21408`；原 r4 九文件与运行配置完全不变。CLI 验证并执行同一份已读的冻结 MCP 字节，调用一次采样，再用原严格校验与标准报告函数；不调用模型。每次为新进程，容量变化率保持首样本 unknown。

开发验证覆盖原 MCP 23 项、新 CLI 14 项和安装 helper 18 项。CLI 首轮一项测试的 macOS 路径预期修正后做了定向复验，不能把分次证据说成完整命令一次通过。真实 24 秒采样超时和外层截止均有采样器及派生进程停止证据。独审已核验原12项与最终正式fixture的2项清理用例；后两项在原CommonJS临时目录中通过，用时25.303秒。最终测试仅把producer.ts改为producer.mts，消除临时目录的模块语义依赖，发布payload未改。

主控 M1 暂存实际调用于 **00:35:04.733** 采样，2.181 秒返回 1051 字符／2253 字节，stderr 为零；[完整报告样例](../../scratch/hermes-site-status-direct-20260911/m1-report-preview.txt) 与 [调用证据](../../scratch/hermes-site-status-direct-20260911/m1-inactive-cli-probe.json) 保留。报告仍为 failed：本机公开200、公网M1探测失败、RSS最近成功10:20、备份点10:08；同时完整报告Admin登录未知、容量变化率未知、任务存活未采样。样例已经过期，只用于检查格式与准确性。

[安装与回退说明](../../scratch/hermes-site-status-direct-20260911/INSTALL.md) 和 [M1 dry-run](../../scratch/hermes-site-status-direct-20260911/m1-direct-dry-run.json) 已准备；helper SHA `acf7f8ce5b8d538c38b1e3e33d51948bf803f06a42959d209aa0bb75bb043ff0`。预期只追加 `quick_commands.f1`，保留原 YAML 每个字节和 MCP r4；原配置 SHA仍为 `03941a9b10e3f967c13c005bbe710c9744589b185076fafd52941e982d990fee`，计划 post 为 `007fd2cc87885a4d625292d8f55089237228e3a656987e25b5e7ccf5649006a5`。现役配置没有写入，新 release 与备份没有创建，Gateway没有因本轮重启。

**入口限制已实证，不能省略：** 原生 `/f1` 只有在会话空闲、无 `/update` 等待回答、前置hook没有接管时才会到达固定执行分支。忙碌 steer 可将消息交给现有模型，等待更新回答时可将其作为更新输入；[两项反例](../../scratch/hermes-site-status-review-20260910/native-f1-preemption-probe.json) 保留。本候选不提供所有状态下全局绕过模型的保证；CLI无法约束未执行到自己的前置分流。正常分支的实际原函数、参数忽略、鉴权、正文保留与30秒超时行为见[独立原生路径验证](../../scratch/hermes-site-status-review-20260910/native-f1-path-probe.json)。

固定 shell 命令清空继承环境并抑制 stderr；解释器启动失败时可能返回 Hermes 自带 `Command returned no output.`，表示没有取得报告。CLI 内部异常返回固定中文unknown，保持24秒采样及27秒总截止。安装后要由用户在既有微信私聊发 `/restart` 并待其返回，再在空闲状态发 `/f1`；实际新 Gateway、该查询准确报告和手机收件均另验，不把暂存调用当成闭环。

[正式独立报告](../collaboration/部门/测试部/报告/2026-09-11-TASK-20260911-FAB9DA-Hermes固定f1直出候选独立审查.md) SHA `8a134b402f3242cc69514a091616275c6a773a50166a87edd7d2e278a4648afd`；[41件冻结证据清单](../../scratch/hermes-site-status-review-20260910/evidence-manifest-direct-r1.json) SHA `9416aac96c13e2c22495d64f1d8882b7d26cc3a733fe547b0498e4c94671a1f2`。主控已读完整报告并逐件复算，零不一致，见[核收记录](../../scratch/hermes-site-status-direct-20260911/root-verification-direct-r1.json)。该PASS仅覆盖技术准备，入口选择、正式安装与Gateway激活、实际/f1准确报告和手机收件均未完成。后继真实证据须新增附录，保持本报告及原r4失败证据不变。

### r4 历史安装记录

以下23:44—23:56的安装/等待记录保留为历史，实际微信加载和收件边界以上述00:00查询及新附录为准。

**2026-09-10 23:44，r4 已安装，安装后 M1 实际 MCP 调用通过；新版真实微信回复仍待验。** r4 把时间统一转换为完整日期的北京时间（UTC+8），向 Hermes 返回确定性的中文报告，同时保留原严格 DTO。报告显示控制状态，明确 Admin 私网登录、未采样任务状态和未查根因，避免把旧成功记录外推为任务未运行或人为原因。

独立审查放行后部署固定 `candidate-site-status-20260910-r4`：9 个 payload、103,473 字节。Node 采样器与固定运行配置保持 r3 字节身份，只更新 MCP Python 展示。安装仅把 Hermes 配置中的两个 release 路径由 r3 指向 r4，全文只有两处 `3`→`4` 字节变化；原配置逐字备份，r3/r4 各 12 个文件的身份和权限均读回验证。Gateway/Public/Admin/隧道 PID 保持，网站 deployment 身份未变。

此前 r3 的微信链路已真实通过，但回复准确性失败：23:14:40.690 入站、23:15:10.473 调用唯一巡检工具、23:15:12.888 返回正确 DTO、23:15:35.942 生成回复；用户粘贴的手机报告与存档对应。模型把 UTC 15:15 错标为 UTC+8，并无依据推断备份任务未运行、排除人为操作、判断同时停摆。失败证据与旧候选报告均保留，不能用 r4 工具层通过覆盖这次历史失败。

已请用户在同一微信私聊发送 `/reload-mcp`，如提示审批则 `/approve`，然后普通询问“查一下 F1 网站状态”并粘贴报告开头。此时尚未获得新版热加载、普通查询、对应最终回复和手机收件的联合证据；不把独立 SDK 调用当成 Gateway 或手机验收。无需 `/always`，没有代用户发微信或重启 Gateway。

开发任务 `TASK-20260910-95D001` 已经正式工具转为 `waiting_input`，等待新版微信出口；独审任务 `TASK-20260910-E37D5D` 已按分层出口 completed/acknowledged，范围仅为冻结候选、升级和 M1 工具层；实施任务尚未整体 complete。实际主执行会话 `01a0895d-8147-75f0-8f8c-fad73e46be80`；沿用已登记统筹 actor 仅作项目任务审计，不更换部门会话。共享脏树保留，未混合 commit/push。业务生产事实统一从 [当前生产状态与执行待办](../当前生产状态与执行待办.md) 进入。

## 固定身份与验证证据

| 项目 | 当前 r4 结果 |
|---|---|
| payload manifest SHA-256 | `5970667e5bcb5d5c7210d0a09cd02470e8b6f04c72ec9ff21bbda3c56a7c7ad3` |
| 固定运行配置 SHA-256 | `ed93f3acf8fb468cec5c4c575ec36b00ffed3703c5e3c06fe62095992eb8be7f` |
| MCP Python SHA-256 | `9521cce27a50e4c5632c5f3b3769c0fb5bcd17d000ec987814705852322faaba` |
| 升级 helper / 冻结依赖 SHA-256 | `2945eaf5fc5299d889d9e78e5b0a7d4cbc59e41f91f9446531bf18616fa776b1` / `670d2268e1fbe5ffe9b3a8c554fe38092c8af92b0f28368a70dcc10769cee61d` |
| Hermes 升级前当前配置 SHA-256 | `a01adaea5172e2a72972c53377cbbafec7775da63bcc985e65dae64c866c8a1d` |
| Hermes 升级后配置 SHA-256 | `03941a9b10e3f967c13c005bbe710c9744589b185076fafd52941e982d990fee` |
| 本地验证 | 23/23 Python/MCP、13/13 升级 helper；未改的 Node 69/69、原 helper 23/23、lint/全量 TypeScript 结果按冻结字节身份继承 |
| 新增独立反例 | 10 项时间/控制/未知/DTO 不变、7 项实际 SDK 边界、5 项升级反例通过；旧 Node 登记/签名/成员/双摘要反例保持 |
| M1 实际入口 | 现有 Node 24.18.0 / MCP SDK 1.26.0；唯一 `get_site_status`；23:40 暂存及 23:44 安装后实际调用均收到中文文本和严格 DTO，`failed` 仍为有效巡检（`isError=false`） |

本轮主控证据在 [`scratch/hermes-site-status-20260910/`](../../scratch/hermes-site-status-20260910/)：`candidate-freeze-r4.json`、`r4-staging-and-dryrun.json`、`r4-install-receipt.json`、`r4-installed-readonly-check.json`、`r4-installed-mcp-call.json`。独立证据在 [`scratch/hermes-site-status-review-20260910/`](../../scratch/hermes-site-status-review-20260910/)；所有 PASS 只覆盖绑定候选和对应范围。 [r4 正式独审报告](../collaboration/部门/测试部/报告/2026-09-10-TASK-20260910-E37D5D-Hermes只读巡检r4独立审查.md) SHA `e5ae585bc72c179b13cea01fb5231ea0ac9ece9ed306051dac753cd5963ca204`，30 件独立证据 manifest SHA `f5f12a1eba548df1d0cf220863ca8ab565e73b8bc7cb0103c227bc2dd4d970b1`；主控已逐件复算，零不一致。

安装前发现 Hermes YAML 从早前 r3 post `1af5a2cc…` 的 4,434 字节变成 `a01adaea…` 的 2,539 字节。主控从原备份重建旧 post，独审另行复核，严格 YAML 值及类型全相等，完整 `f1_status` 未变；写入者未确定。本轮保留当前新字节并只重绑升级 helper 的固定前态 SHA，没有接受任意语义等价输入。旧 helper 及 7 件证据保存在 `upgrade-r4-pre-drift/`，漂移证据见 `r4-config-drift.json` 和独立对应记录。

回退使用 [固定 r3→r4 升级说明](../../scratch/hermes-site-status-20260910/UPGRADE-R4.md) 的 `rollback`，同时匹配上表当前 post 与本次备份 SHA，仅恢复本次升级前 r3 配置；不覆盖并发变化。保留 r3/r4 release 及备份，回退后仍需热加载。原 r3 的首次安装回退是另一历史操作，不应用本次 r4 的前后身份。实际命令和私有路径均在本地安装收据中。

后继部署或隧道重启若改变部署、数据库或隧道 PID/启动时间/URL 身份，当前工具会返回身份未知并拒绝请求旧目标；操作者须先现场核实新身份、冻结新配置并沿相同独审/升级路径接续。不能原地改封存 release，也不能跳过身份门恢复绿色。

## 23:44:27 实际采样

观测时间 `2026-09-10T15:44:27.438Z`（北京时间 23:44:27），60 秒后过期；下表只保留这次现场事实，后续微信查询须重新采样。

| 分项 | 实际结果 |
|---|---|
| 本机公开服务 | 200，真实公开投影检查通过 |
| 公网 | M1 观察点请求失败 `HTTP_NETWORK_ERROR`；22:14 的 M5 预检为 530，不能沿用上午公网恢复结论 |
| Admin | 进程运行，loopback 无私网身份请求按预期 404；真实私网登录仍 unknown |
| 控制状态 | live / clear / ready；只说明采样时的值，不推断操作来源或根因 |
| RSS | 三源最近成功均在 10:20，已超过 13 小时；最近两次成功实际开始间隔约 18 分 15 秒，时效失败 |
| 当前版本队列 | backlog 总 36：缺当前草稿 15、等待发布 21；等待人工审核 0，当前版本结果未知的 refiner 操作 2 |
| 其他历史状态 | 19 条历史结果未知操作；自 22:19:59 基线新增 0。未决操作 59，未决 outbox 1，历史终态失败 outbox 6，不能将历史计数等同当前新增失败 |
| 备份 | 当前身份匹配的登记恢复点为 10:08:43.925，年龄约 13 小时 35 分；清单、异机签名、应用演练签名均验证 true，仅报 `BACKUP_POINT_STALE`，时效上限 15 分钟 |
| 存储 | 可用约 198.30 GiB，容量状态 healthy；首样本变化率 unknown，须同一 MCP 进程获得相隔 60 秒至 15 分钟的可靠第二样本才能计算 |

这些是巡检发现的现存故障，业务恢复不属于本次工具接入。没有执行新备份、离机密文全量复读、灾难恢复或未知投递重发；本工具也未采样 collector/backup 任务存活状态。

## 候选历史与已关闭缺陷

r1/r2 inactive stage、dry-run 和旧报告保留。独审关闭了 resources/prompts 配置层级、未来 RSS 时间误绿和 pointer 成员未绑定登记字段三个问题。随后 r2 首次 M1 有效采样暴露真实备份格式兼容缺口：现役 manifest 含 363 个成员，且 producer 的全部 `sqlite_master` 分隔符摘要与 deployment 的过滤 canonical JSON 摘要采用不同口径。

r3 在同一短只读事务中分别验证两种摘要；允许有界且路径唯一的历史 generation，继续对全体成员核算内容 hash、文件集合 hash 与总字节，保留数据库/pointer/当前 generation 和双签名登记绑定。363 成员 fixture 同时通过现有 producer 解析器；真实 M1 清单与双签验证闭合。r2 的 `BACKUP_MANIFEST_MISMATCH` 属于巡检器兼容缺陷，不据此认定真实备份损坏。

r3 真实微信回复的准确性失败另见 [微信 r3 准确性附录](../collaboration/部门/测试部/报告/2026-09-10-TASK-20260910-E37D5D-Hermes微信回复r3准确性附录.md)。r4 的独立反例又发现控制停止为唯一失败原因时正文漏报，已补完整控制状态行并通过真实 Node fixture 复核；Node 与 DTO 未改。新版真实微信闭环仍单独验收。
