# 2026-09-09 重启身份与备份死锁恢复

本报告截至北京时间20:54；当前运行态唯一入口为[当前生产状态与执行待办](../当前生产状态与执行待办.md)。恢复在进行，未升级正式任务完成状态。

## 已核验的恢复结果

M1公开站保持generation342。投递接收器20:12 kickstart后running PID19687；Admin部署中的DB设备号在重启后与实际不符，真实startup诊断停在read-manifest并报`ADMIN_REVIEW_DATABASE_RECEIPT_MISMATCH`。原dev16777233→当前16777230，其余inode30338295、uid501、单链接、0600、真实路径和8月28日birthtime保持；DB quick_check通过、FK0、schema ae757/user_version10/control v30不变。当前卷UUID已取hash固定，但没有历史卷UUID前证，不能将跨重启同卷推断当作完整历史证明。

20:27按独立审查的固定helper，仅将已死pid54451持有的`run.lock`隔离到`backups/snap/lock-incident-jtAQAX/`。原dev/inode/SHA完整保留，fresh停机证据与收据保留；backup和collector确认idle后卸载，原plist不变，双guard释放，独立回读通过。没有删除备份、运行库或旧恢复点。

20:40最小CAS重绑只改变`reviewDatabaseIdentity.dev`，原manifest与candidate均保留；当前deployment SHA为`909be492189fdc7a7872d8577a2c73e4dd3907820ccf4d886e87f59fbc1a94da`，原SHA为`2f3306cc32374b5831620666ebc2fe70c85a1de066e8c681d46bc23a2d14681e`。独立本地反例验证写入前的目录替换只产生零私有字节、精确dev变换和缺失sidecar新增拒绝；实际fresh preflight绑定DB/WAL完整字节与main/wal/shm存在集，实际apply前后比较。

原apply收据为`committed:true / ok:false / REBIND_PROTECTED_FILE_CHANGED`，没有改写或重跑。独立后续取样确认四保护文件仅ctime变化，路径及dev/inode/uid/nlink/mode/size/mtime/birthtime均一致。生产sealed reader恰对这四项调用`assertPrivateFile`，其无条件`chmod(0600)`解释该元数据变化；helper postcheck遗漏了此副作用。原preflight未取保护文件内容hash，不把stat证据夸大为逐字节前后hash。附加`deployment-rebind-reconciliation.json`保留这一限制。

20:43新manifest通过真实Admin factory，尚未启动background/listener的诊断为PASS。随后原Admin job kickstart，PID20563。20:52从M5实际canonical私网HTTPS访问`/admin/settings`返回200/222ms；无session访问`/api/admin/reviews`返回401/14ms。没有伪造身份头、发cookie或保存响应内容，证据仅状态/耗时/bodyhash。M1自访HTTPS ECONNRESET保留为失败取样，不覆盖M5真实入口结论；loopback缺头404/14ms亦保留。此验证不代表真实Passkey换Key或iPhone键盘操作已完成。

## 备份候选和未完成验收

生产仍使用R5-r3的761文件runtime，根SHA`1e33c59de6020c5452296ef35c34aa8f36ce4376dbf0f0ec18ffe33e77e8acc9`；尚无新鲜登记恢复点。最后有效点仍为9月7日18:17:42，当前RPO失败。两机旧有效密文674447692字节、双签名/成员/DB/checkpoint已本次独核；更晚677469221字节包缺应用回执与登记，不能替代。

新tools候选只修改两文件：snapshot外层120→180秒；Admin ready内部25→45秒，加入闭集阶段/耗时诊断。其余五工具、成功输出、签名payload、其他超时与所有验收门保持；原runtime不复制或变更。候选manifest SHA`03a60a7687c4d34a9c8e9f5885d1330478553fbdf5f52cc4544774ca28da2ec7`，tar SHA`68b96be76822a726c9f90c83cd3c261d70bd962793c4606da430ac4944fc292e`，plist SHA`4ba94666c094751de34f2ccbd724e3b289d6686b1f7ba8c783eb04f2930072d3`。候选7项本地验证、语法与root独立7成员/字节/模式/精确diff/plist审查通过，待实际安装和周期。

验收必须包括新快照、M5原生实际读取及签名、实际Admin/Public隔离启动、合法REGISTER绑定新deployment，以及首轮和两个自然周期在新点完成前的旧点年龄≤900秒。完整阶段最坏超时预算不自动保证900秒RPO，仍以实际连续证据核收。20:54时collector仍停止，RSS自动worker持续`RSS_AUTO_BACKUP_STALE`；不得改fence时间或旧证明解除门禁。重启后RunAtLoad=false及设备号持久绑定的长期启动连续性仍需后续处理，不将本次手动恢复外推到未来重启。

## 证据入口

- `scratch/production-backup-audit-20260909/report.md`及`evidence-manifest.json`：现场旧有效点、未登记包、锁和磁盘独审。
- `scratch/resumption-20260909/`：死锁执行、fresh preflight、原apply失败、reconciliation、真实startup、服务取样与独立反例。
- `scratch/production-backup-audit-20260909/admin-m5-private-https-observation.json`：真实M5私网GET结果。
- `scratch/production-backup-audit-20260909/snapshot180-ready45-candidate/`：新tools固定候选；较早snapshot-only候选已废止，勿安装。

旧生产和审计历史保留。没有混合提交脏工作树，未把恢复进行中或X局部软件PASS标成生产完成。

## 21:04 新tools切换与首轮实际失败

固定候选 `snapshot180-ready45-candidate/manifest.json` SHA `03a60a7687c4d34a9c8e9f5885d1330478553fbdf5f52cc4544774ca28da2ec7`；安装helper SHA `b4b81927ef0b00e2ff177a56527ce9359f9068da7715e1d06ea6225bcc31edf7`。主控独立核对七归档成员、两个变更、plist唯一参数变更、签名成功payload，以及服务查询不确定/活动writer/快照锁拒绝和父目录竞态零字节。记录 `scratch/resumption-20260909/tools-install-independent-review.json`。

M1 fresh preflight于21:03:58通过，原字节SHA `c900f87bbf554d78ad3f127257fda80fee7bb4ad2a2de12855a6a50f1f1057a1`；21:04:28安装返回 `ok/committed/loaded/guardReleased=true`。新tools目录 `backup-runtime-releases/20260909-snapshot180-adminready45-tools`，tools identity SHA `02763e187e283e074cd19499831a0c42bb1dddcdf9568979067302c784b61684`；新plist SHA `4ba94666c094751de34f2ccbd724e3b289d6686b1f7ba8c783eb04f2930072d3`。旧plist及阶段收据保留于M1 `.resumption-20260909/tools-install-cf9e3b7f-e600-4dc1-ba6d-11a2d722e79b/`。761文件runtime保持R5-r3根SHA不变。

21:05:23.157单次kickstart成功。独立21:05:40核七文件、身份、原运行服务PID不变。后续cycle guard owner20918和mtime21:05:23.470对应本轮进程，不是安装残锁。21:06:29 snapshot成功，用时65.707秒，恢复边界21:06:02.248，新包 `1788959162248_f18b58e2f450c47a`，retainedCount24，快照锁正常释放。

该轮21:10:30在 `OFFHOST_INDEPENDENT_READ` 等待240秒后失败；没有执行新合法登记，旧有效点仍过期。M5曾于21:07:05创建该轮transfer临时目录，正在分别核实际字节/observer状态与代码时序。collector继续停用；不得把snapshot成功等同于有效恢复点，不盲调等待时间或重发。失败和后续自然轮由独立Agent保留 `scratch/production-backup-audit-20260909/cycle-observations/`。

本次Chrome受支持连接两次返回连接失败，IAB请求实际后台settings得到ERR_CONNECTION_CLOSED；M5先前原生HTTPS 200/401是独立时间与路径的观察。真实浏览器UI未验，未据浏览器失败改变生产认证、配置或凭证。

## 21:26 异机失败定位与后台代理例外

第二自然周期point21:16:11.304、包`1788959771304_f18b58e2f450c47a`，snapshot21:16:36成功（65.570秒），21:20:37再次OFFHOST_INDEPENDENT_READ失败。M5内层SCP对象命令限120秒、随后多层170/175/179/185秒限时，M1等待240秒；失败统一TRANSFER_COMMAND_FAILED，不能仅从该代码精确确定底层网络原因。代码独审14既有测试与7小对象探针通过，源码未变：`scratch/offhost-code-audit-20260909/report.md`，manifest SHA `dd91145b18e05bd1bc673a999cb4ee9b798035aa873da1c1840ca03059731b8a`。

M5已有同对象677469221B完整缓存，package时间戳变化仍冷下载，旧回执不能直接复用。后台SCP pri4；前台SSH pri31用30秒仍只收到950272B（31.7KB/s），这些字节与本地缓存同长度前缀精确相等。初次错误地比较不足1MiB与完整8MiB的hash已另存reconciliation，不作为内容不匹配证据。后台同inode双时点40秒推进4700160B（117495B/s）。Background优先级与低速同时出现，未证明因果，未改observer配置。

M5/M1均Tailscale1.102.3，分别macOS26.6.2/26.5.1。Tailscale直连IPv6；1200字节disco/ICMP三次均可达，延迟有抖动。两机都配置192.168.1.0/24不证明同一广播域；一次严格沿既有M1 SSH host key的192.168.1.15:22连接8秒超时、0字节，没有绕过host key或更换生产目标。原生故障样本和原始错误保留在备份审查目录。

后台浏览器网络另有可复现代理问题：显式使用当前127.0.0.1:12000代理，CONNECT200后TLS关闭；直连同一HTTPS地址HTTP2 200。21:23:51使用原列表CAS，仅向Wi-Fi代理例外追加精确后台域名，网络代理地址/端口/启用状态未改。准备/前后字节/完整回退argv位于 `scratch/resumption-20260909/admin-proxy-route/`；APPLIED且readback精确相等。IAB原错误页reload触及data URL限制，改回实际HTTPS导航又30秒超时，浏览器连接随后重置，真实UI仍NOT_VERIFIED。不能把这项代理修正当作备份SSH慢速已解决。
