# 设计部报告 · 2026-08-07 · v0.2 草案证据收尾

> 任务：`TASK-20260807-CA04A8`（收尾 v0.2 草案证据并提交确认门）
> 部门：设计部（执行层）· 提交人：设计部/统筹部并行子代理
> 前置：`TASK-20260804-795FB0`（v0.2 全站设计草案）已收敛，方向已获用户确认（`user_confirmed`）。

## 1. 结论速览

- v0.2 全站设计草案证据已覆盖 **1440 / 1024 / 390 × 深浅** 六档组合，证据齐备。
- 新增 2 张 PNG（1024 宽深浅）；未改动既有 4 张已确认证据。
- `evidence.html` 已同步为「6 张 PNG + 交互态 iframe 实况」结构。
- `design-system.md` 与 `brand-spec.md` 方向与最终收敛一致；记录 brand-spec 的 token 级数值漂移（见 §5）。
- 停在用户体验确认门，未进入正式合同或应用实现。

## 2. 重导 / 新增的证据 PNG

| 文件 | 尺寸 | 字节 | 捕获视图（URL 参数） |
| --- | --- | --- | --- |
| `evidence/v0.2-dark-1440.png` | 1440×2000 | 700,802 | 既有，未改动：深色默认时间线 |
| `evidence/v0.2-light-1440.png` | 1440×2000 | 707,691 | 既有，未改动：浅色默认时间线 |
| `evidence/v0.2-dark-1024.png` | **1024×2000** | **511,957** | **新增**：深色 + 就地展开（`#theme=dark&width=1024&open=r1`，r1 汉密尔顿 4 图：主图+缩略图+中文提炼+证据行） |
| `evidence/v0.2-light-1024.png` | **1024×1660** | **540,192** | **新增**：浅色默认时间线（`#theme=light&width=1024`） |
| `evidence/v0.2-dark-390.png` | 390×844 | 155,816 | 既有，未改动：深色默认时间线 |
| `evidence/v0.2-light-390.png` | 390×844 | 157,169 | 既有，未改动：浅色默认时间线 |

- 命名与既有 4 张一致（`v0.2-{dark,light}-{width}.png`），统一放在 `design/ui/F1+1-v0.2-全站设计/evidence/`。
- 既有 1440/390 未重写（已被评审确认）；本次以相同工具做了重现性抽查，确认其视图为「默认时间线」，1440 重现文件字节与原证据几乎一致（差异 <20B）。

## 3. 渲染工具与收据

- 主渲染：**Playwright 1.58.2**（`npx playwright --version` 验证），Chromium headless（playwright chromium v1208，Chrome for Testing 145.0.7632.6），Node v26.5.1。
- 加载方式：`file://` 直接加载 `design/ui/F1+1-v0.2-全站设计/index.html`，由页面自带 hash 路由驱动（`#theme / #width / #state / #open / #search / #reveal`）。
- 截图方式：viewport 截图（非 fullPage），deviceScaleFactor=1，viewport 尺寸即导出 PNG 尺寸（1024×2000 / 1024×1660）。
- 图片：Motorsport.com CDN 真实图片可联网加载（抽查 CDN 返回 200/248KB）；渲染后 DOM 校验主图 10/10、缩略图 16/16 均 `naturalWidth>0`。
- 渲染后校验（每张均通过）：
  - `documentElement.dataset.theme` 与 `body.dataset.width` 正确（dark/light × 1024）；
  - 时间线 10 条渲染、无状态框误显；
  - dark-1024 已就地展开 r1（`.tl-item.is-open` 命中「菲蒂帕尔迪力挺汉密尔顿…」）。
- 兜底未触发（Playwright 渲染全程可用，无需 Chrome/Edge headless 或可打开截图兜底）。

## 4. evidence.html 同步情况

- 已重写 `design/ui/F1+1-v0.2-全站设计/evidence.html`，纳入 **6 张证据 PNG**（`evidence/v0.2-{dark,light}-{1440,1024,390}.png`），保持原有 head / panel / grid / foot 结构与风格。
- 结构：「已导出 PNG 证据」区（6 张 PNG 全宽展示，panel 头注明主题/宽度/捕获参数）＋「交互态 / 状态补充预览」区（保留 3 个同源 iframe：悬停浮现态 reveal、搜索过滤、错误状态），不丢失既有交互态证据。
- 已验证：6 张 PNG `<img>` 在 evidence.html 中全部加载（`complete && naturalWidth>0` 为 6/6）。
- 修正一处既有死参数：原 iframe 用 `#open=c1`（数据 id 为 r1–r10，c1 无法展开），PNG 捕获与面板标注统一改用有效的 `#open=r1`。
- 顶层历史 `timeline-*.png`（1440/1024/390 × reveal/search/state/detail）未改动，仍在原位置供回溯。

## 5. 一致性复核（design-system.md × brand-spec.md）

**方向一致性：一致。** 两份文档均与最终收敛方向一致（内容优先 / 极简克制 / Apple OS 式克制玻璃 / BMW M 影响仅限标题字重·紧凑节奏·速度感细线·克制红蓝温差 / 默认深色+完整浅色 / 可访问与低性能降级为硬约束）。未发现方向性漂移。

**记录的 token 级漂移（brand-spec.md 相对规范，未自行修改）：**

1. `--blur`：brand-spec.md 写 `18px`；`index.html`（`--blur:16px`）、`F1+1-v0.2-token-map.json`、`design-system.md` 均为 `16px（低性能 8px）`。
2. 玻璃透明度：brand-spec.md 写 `--glass-bg 约 50%`、`--glass-bg-strong 约 84%`；`index.html`/`design-system.md` 为深 55%/浅 50%、深 85%/浅 85%。
3. Display 字重：brand-spec.md 写 `600–800`；`index.html` 引入 `Barlow Condensed 500;600;700;800`，design-system.md 写 `500–800`。

以上属实现级 token 数值表述差异，不影响方向；按任务约束记录到本报告，交统筹转正式合同阶段核对，本任务不改动。

## 6. 已验证 / 未验证项

**已验证：**
- 深浅 × 1440/1024/390 证据 PNG 齐备（6 张），命名一致，均在 `evidence/`。
- 新增 1024 PNG 主题/宽度/就地展开状态经 DOM 校验正确；真实图片加载完整。
- `evidence.html` 已同步且 6 张 PNG 正常展示；既有 4 张证据未被动过。
- 方向一致性与上述 token 漂移已记录。

**未验证（交统筹/测试部/用户）：**
- 真实设备 1024 宽视觉与交互细节（Playwright 无头近似，未在真机/实网核对；悬停浮现、lightbox、触控等需人工/测试复核）。
- 正式合同（实现级文档）与应用实现尚未启动，须待用户体验确认门通过。

## 7. 错题自检

- 已检查错题集，无命中（渲染、路由参数、命名、evidence.html 结构与既有证据一致性均已核对）。
- 未改动 `app/`、v0.1 合同、`docs/spec.md`、accepted ADR、`data/`；未触发任何真实外网发布/部署。
