---
task_id: TASK-20260814-5F3AEF
department: 设计部
status: completed
decision: ready_for_user_confirmation
date: 2026-08-14
---

# TASK-20260814-5F3AEF · 当前真实站发布视频重基线与开工包完成报告

## 1. 结论

当前真实站发布视频开工包已收口为 **21 秒、16:9、首版静音** 的单一推荐方向。画面只引用生产身份 `52e654 / 5d99dc / ae009` 下的首页和公开详情证据；2026-08-08 旧画面只保留为历史比较，没有进入候选画面。

交付包含 21 秒动态预览、5 镜头分镜、3 张 1920×1080 风格帧、动效 tokens、来源/媒体权利清单、shot QA、HyperFrames 同镜头 source/preflight、Remotion frame-driven source/pipeline preflight、renderer 决策和一个合并用户确认问题。当前工作只完成方向与成片开工包，不代表正式 MP4、renderer 实跑、Open Design 候选、音频或媒体发布权已通过。

## 2. 固定生产身份

- commit：`52e654915d19ab31802c97a406402896b6a68978`
- tree：`7a0e9efd59954e5b9c8494b27ed851d5596c0c14`
- release root：`5d99dc951ea3312f83f4ab097d185e37eaf820e76f8d817e7e764b7d34f8eaed`
- production evidence manifest：`ae009b2a1ef3acbcb2a36bab4a9bb61b2bf4ba2a7f74d8b8d44d251e0787baa4`
- production deployment report：`84ba9dd6773b6918c342b4aaa460bc93dbd5a1e9a7c3d38ef748e0a63ce6ec21`

生产 manifest 的 12 项首页/详情、深浅主题、1440/1024/390 图像 hash 已逐项校验。开工包复制的 6 张实际引用资产与 production evidence 源 hash 成对一致。

## 3. 视觉方向与三张风格帧

推荐结构为 `Brand → Home → Responsive → Detail → Close`，总长 21 秒。没有引入指标、筛选、管理能力、breadcrumb、状态标签或 `PUBLIC API` 字样。真实产品画面不叠营销标题。

| 时刻 | 风格帧 | 用途 | SHA-256 |
|---:|---|---|---|
| 1.8s | `styleframes/01-brand-1920x1080.png` | F1+1 文字身份、信息+时间+时间线、深色编辑基调 | `f8437b026f47cae4e851eb38d5d0346ae9f5cb62d202028dcbfef43ffd0eb21b` |
| 9.2s | `styleframes/02-responsive-1920x1080.png` | 浅色桌面与深色 390 同镜头，证明当前真实响应式画面 | `ba84f9de2fa27be93dceba37ab8f35a5d9ce1d8259f9796952cc0d91a0a0dd97` |
| 14.0s | `styleframes/03-detail-1920x1080.png` | 深色桌面详情与浅色移动详情 | `61809ac194a7b0297149e57d6622ef011380e5f070143a3b765d374f88eaca3b` |

三图均为 1920×1080、8-bit RGB、non-interlaced PNG。最终批次临时把开发者设备指标固定为 1920×1080 / DPR=1，页面收据为 `innerWidth=scrollWidth=1920`、`innerHeight=scrollHeight=1080`，再从同一 viewport 原生输出 PNG；捕获后已清除覆盖。内置浏览器默认截图曾返回 1791×1080 JPEG，早期 DPR=2 clip 也曾造成一张错误放大帧，两者均已拒绝并覆盖，没有进入交付。

## 4. 动态预览与 renderer 路径

`index.html` 是零依赖、可循环、可 seek 的 21 秒方向预览。无查询参数时，运行收据显示时钟从 `00:00.0` 前进到 `00:00.7` 且 `freeze=false`；`?t=<0..21>` 才固定取帧。Space 暂停/继续，R 重播；reduced-motion 环境直接停在品牌落定态。候选自身没有外部 URL、远程资产、网络字体、fetch/XHR/WebSocket、音频、随机数或墙钟副作用。

三条路径职责保持为：

1. Open Design：视觉/动效方向候选。用户已确认推广、15–30 秒、16:9 的 Brief；统筹已在本机 Local Codex 的唯一项目运行生成。当前结果尚未回传到本任务，因此不登记为 artifact 或 verified，也不等待 Cloud 登录。
2. HyperFrames：同镜头候选与工作流对照。`hyperframes-candidate/` 已提供设计合同、结构化 source 和 preflight；CLI/package 不存在，lint/inspect/preview/capture 保持 `NOT_RUN`。
3. Remotion：正式成片的可复现管线。`remotion-preflight/` 已提供 frame-driven TSX、630 帧主片范围和 144 帧验证镜头；package 不存在，typecheck/Studio/render/ffprobe 保持 `NOT_RUN`。

插件规则入口可发现，只能证明作者规则可用。没有把它外推成 renderer、CLI 或依赖已经运行。Grok CLI 只读研究因连接失败没有产生可引用结论，继续保持 Unknown。

## 5. 权利、CTA 与正式制作门

- 首版静音；未采购 BGM、旁白、TTS 或音效。
- Quick Tunnel 临时地址不进入结尾；当前 CTA 只保留 `F1+1` 与“信息 + 时间 + 时间线”。
- `F1+1` 是站内文字处理，未宣称存在正式矢量 logo。
- Motorsport 图片、人物肖像和来源内容只用于内部方向确认；正式对外成片前必须取得权利、替换资产或采用经确认的规避方案。
- 下一步只有在用户确认本方向后，才提交精确的隔离依赖授权：固定单一 HyperFrames/Remotion 版本，只运行 4.8 秒同镜头；不包含正式 MP4、音频、云渲染、付费 API 或第三方媒体公开使用。

## 6. 已验证

- 生产 commit/tree/release/manifest/部署报告身份与 12 项证据清单一致。
- 6 张复制资产与源逐项同 hash；当前预览没有旧 2026-08-08 UI。
- 21 秒、5 镜头区间、0.6 秒交叉淡化和取帧时刻与分镜一致。
- 主预览和 HyperFrames source 的 HTML/JS/CSS 静态完整性、Remotion frame/time 规则、零随机/零远程候选路径。
- 3 张风格帧格式、尺寸、内容、页面宽高收据和 hash；品牌安全区已恢复，桌面保持 1440:900、移动保持 390:844 完整源比例。
- 本地静态服务只绑定 `127.0.0.1`，完成后已关闭；浏览器临时视口已复位、临时 tab 已清理。
- 没有修改 `app/`、Spec、accepted ADR、生产证据或历史冻结基线；设计产物位于隔离目录。

## 7. 未验证

- Open Design Local Codex 候选尚未回传，不能登记结论。
- HyperFrames CLI/runtime、lint/inspect/preview/seek/capture；Remotion package/typecheck/Studio/逐帧渲染/编码/ffprobe。
- 正式 1920×1080/30fps MP4、9:16、海报帧、跨浏览器和低性能播放。
- 音频策略、正式域名/CTA、正式矢量 logo、Motorsport 对外营销权利。
- 浏览器宿主出现自身遥测网络报错；候选代码为零外联，无法据此声称整台工作站绝对零外呼。
- 用户尚未确认本视觉方向进入 renderer 实跑；正式成片占位任务没有领取。

## 8. 错题自检

- 未用旧冻结 UI 或手绘假 UI 冒充当前生产站。
- 未把早期 Chrome exit 0、默认 JPEG 截图或错误尺寸写成合格 PNG。
- 未把 Open Design 项目运行中写成候选已返回，也未重复创建项目/run。
- 未把 HyperFrames/Remotion 插件规则、source 文件或 Node/FFmpeg 前置写成 runtime 通过。
- 未安装 npm 依赖、调用付费/生成 API、写入网络服务、渲染正式 MP4或引入音频。
- 未把内部生产截图使用外推成 Motorsport 媒体对外发布授权。
- 本任务 complete 只代表开工包交付，不解除用户视觉确认、renderer 依赖、媒体权利或正式成片门。

## 9. 合并确认入口

确认问题见 `decision-card.md`：是否接受当前深色编辑时间线主线、浅色/移动端真实产品对照的 21 秒方向，并进入 4.8 秒 renderer 实跑验证。确认后仍需对依赖安装做精确授权。

任务事务已于 2026-08-14T23:47+08:00 持久化完成；本任务停在合并用户视觉确认门，不领取或启动正式成片任务。

TASK_STATE_OK
