---
task_id: TASK-20260811-EE3F90
department: 设计部
status: waiting_input
decision: rescreenshot_authorization_required
frozen_sha256: 5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1
candidate: design/ui/F1+1-v0.2-integrity-successor-20260811/F1+1-v0.2-integrity-successor-candidate-20260811.html
---

# TASK-20260811-EE3F90 · v0.2 integrity successor 具体优化方案报告

## 1. 结论

已完成对 Kimi/frontend-design 诊断的逐条独立复核，并形成一套从冻结 SHA `5a84bfb2…8cb1` 直接派生的隔离候选。对抗审查随后在 390 证据中发现一个可见 P1：移动证据行的 `width:100%` 与后续 `flex-wrap:nowrap` 冲突，来源和日期被压成近竖排。当前候选不可进入完成态或用户视觉确认。

唯一修复路径是让元数据与动作区各占完整一行，再重新形成与修后候选 SHA 绑定的证据。用户上一条明确禁止重新截图，本会话据此未修改候选、未重采证据，并把任务转为 `waiting_input`。冻结基线不变，不授权正式 App、发布视频或生产发布。

## 2. 输入、产物与保护范围

独立复算的固定输入：

- 诊断报告：`a4022a97f139654e1f536754816c70b993384515f901f6269b233cbfb48ae487`
- 冻结 HTML：`5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1`
- 冻结 `design-system.md`：`6d7a966044ebadff614f60a7d72c0d59e8cf8d805f2883384872c1d2ce4e69e6`
- 冻结 `brand-spec.md`：`508717c7e2699a153af3a46064177e764c5b04bc3c21a02f7d735936632d2986`

交付目录：`design/ui/F1+1-v0.2-integrity-successor-20260811/`，包含候选 HTML、设计系统补充、token map、决策增量、开发实施清单、运行收据、8 张证据及 SHA-256 清单。未修改冻结目录、`app/`、Spec、accepted ADR 或正式发布视频。

## 3. 逐项裁定矩阵

证据状态只表达本轮可证明范围：`verified` 为代码/运行或持久证据成立；`refuted` 为原诊断的具体表述被证据推翻；`Unknown` 为条件未形成。

| ID | 证据状态与独立裁定 | 处置 / 优先级 | 精确动作与影响文件 | 开发边界、验收与回退 |
|---|---|---|---|---|
| A1 | `verified`：冻结 `.tl-title` 未声明 display 字体；候选声明已补。实际 Barlow 文件仍 `Unknown` | 修改 / P1 | HTML/CSS：标题 `font-family:var(--font-display)`；生产影响 `globals.css`/feed 标题规则 | 必须自托管字体后验 computed font；失败回退至 CJK 系统栈，不撤销可读性 |
| A2 | `verified`：冻结渲染为 shell 880、内容约 800；旧文档 920 漂移 | 保留渲染并同步 / P1 | token map、设计补充；未来 `globals.css`/shell | 六档量测版心与无横溢；回退到固定 App SHA，禁止让开发猜 920 |
| A3 | `verified`：冻结为桌面 360、移动 280；旧文档 255 漂移 | 保留渲染并同步 / P1 | token map、设计补充；未来媒体 CSS | 三宽验证原比例不裁切；回退只撤 successor 实现 |
| A4 | `verified`：冻结 390 缩略图约 32×22，文档 32/40 不一致 | 修改 / P1 | 视觉图 32/28，外层原生按钮 44×44；候选 HTML、token、未来 feed/CSS | 六档按钮 44×44；视觉图保持紧凑。回退为旧媒体导航组件 |
| A5 | `verified`：冻结摘要已完整显示，旧文档仍写两行钳制 | 保留完整并同步 / P1 | 文档/token；未来 feed 摘要 CSS | 长摘要在 390 无裁断；若回退，必须由用户明确接受信息损失 |
| B1 | `verified`：冻结缩略图低于 44px；候选已改为原生 44×44 按钮 | 修改 / P1 | 候选媒体 DOM/CSS；未来 `feed-experience.tsx`、`globals.css` | 鼠标、Enter/Space、pressed/current 与 coarse 真机；回退旧控件仅限候选未获确认时 |
| B2 | `verified + partial refutation`：22px 元数据文字本身不交互；真正缺口是冻结原文入口 32px。候选已升至 44px，动作区可分行 | 修改交互项 / P1 | `.tl-original-link min-height:44px`；移动 `.ev-right` 可换行 | 展开态量测原文入口 44px、390 无横溢；不把非交互元数据强制成按钮 |
| C1 | `verified`：冻结硬写 7，实际数据 10 | 修改 / P1 | kicker 使用 `window.FEED.length`；未来 shell/feed | 任意条目数显示一致；回退到旧静态文案仅作为历史基线 |
| C2 | `verified`：冻结页脚“原创占位影像”与真实热链不符 | 修改 / P1 | 候选改成设计样板/来源/摘要说明；正式法律文案 `user_required` | 用户确认版权/免责声明后实施；候选文案可整体回退 |
| C3 | `verified`：部分 alt 为截断英文文件名 | 修改 / P1 | 主图按中文标题/主题；缩略图按钮持有名称，内部图 `alt=""` | 读屏无重复播报、切图后 alt 同步；回退旧模板仅保留历史比较 |
| D1 | 代码风险 `verified`；真实 back-swipe 冲突 `Unknown` | 保留候选行为 / P1 实机门 | 本候选不改 wheel；Mac Chrome/Safari 真实触控板补证 | 冲突成立后由产品/用户选“仅 lightbox 捕获”或删除 inline wheel；不得由开发先删 |
| D2 | `verified`：手势组合复杂；现有 thumbs/键盘/touch/lightbox 均有功能价值 | 保留，条件化简 / P2 | 若 D1 失败，只移除桌面 inline wheel，保留显式入口与触屏 swipe | 对照所有输入路径回归；回退到冻结行为需保留问题收据 |
| E1 | `verified`：同日日期重复；属于 v0.3 IA | 不改 / Product | 形成日分组合同提案，禁止本候选改 DOM/数据 | 需产品定义分组、锚点与分页后另验；本候选天然回退为未分组 |
| E2 | `partial refutation`：折叠态 `.tl-lead` 已是中文摘要主体；展开态 `.tl-zh` 才是第二层且较弱 | 不改 / Product | 先定义“摘要/提炼/正文证据”命名与优先级，再做层级候选 | 用真实长短内容与读屏顺序验收；无合同不改现状 |
| E3 | `verified`：`aggregatedAt` 存在且同日语义不一致，页面未显示 | 不改 / Product | 若展示相对时间，应从唯一已发布时间派生；禁止信任示例 `aggregatedAt` 文案 | 时区、跨日、未来时间和刷新边界测试；未确认时不渲染 |
| F1 | `verified`：meta 泄露内部修订史 | 修改 / P1 | 候选 meta 改成简短页面描述；未来 `app/src/app/layout.tsx` | 查看生成 head 不含 TASK/修订日志；回退为上一合法产品描述 |
| F2 | `verified`：冻结使用第三方热链；候选证据的真实媒体质量/权利为 `Unknown` | 风险隔离 / P1 发布门 | 候选代码保留身份；`capture=local` 仅用内联 SVG 形成零外联布局证据 | 正式 App/视频使用前固定本地权利资产并验 CSP/失败态；不得把占位图带进生产 |

### 3.1 独立证据与影响文件索引

| ID | 冻结/诊断证据 | successor / 运行证据 | 当前影响文件 | 后继影响文件 |
|---|---|---|---|---|
| A1 | 冻结 `.tl-title`；诊断 §2 A1 | 候选 `.tl-title` | 候选 HTML、token map、design-system-successor | `app/src/app/globals.css`；组件归属由开发任务确认 |
| A2 | 冻结 `.app max-width:880px`；诊断 §2 A2 | 候选 `--shell-max/--content-max`；receipt 六档宽度 | 候选 HTML、token map、design-system-successor | `f1-page-shell.tsx`、`globals.css` |
| A3 | 冻结/诊断 `.ph-main 360px/280px` | 候选媒体 tokens；六格 | 候选 HTML、token map、design-system-successor | `feed-experience.tsx`、`globals.css` |
| A4 | 诊断 §6.1 `.ph-thumb 32×22` | 候选 `.ph-thumb-img 32/28` | 候选 HTML、token map | `feed-experience.tsx`、`globals.css` |
| A5 | 冻结 `.tl-lead` 无 clamp；诊断 §2 A5 | 候选 `.tl-lead` 与 390 六格 | design-system-successor、token map；候选保持渲染 | `feed-experience.tsx`、`globals.css` |
| B1 | 诊断 §2 B1 | receipt `folded[*].thumbMinWidth/Height=44` | 候选 HTML、token map | `feed-experience.tsx`、`globals.css` |
| B2 | 冻结 `.tl-original-link`；诊断 §2 B2 | receipt `expanded[*].originalLink.height=44`；390 图同时暴露 P1 | 候选 HTML、token map；修复待授权 | `feed-experience.tsx`、`globals.css` |
| C1 | 冻结 `.timeline-kicker` 与 `FEED` 10 项；诊断 §2 C1 | receipt `content.kicker`、`itemCount=10` | 候选 HTML | shell/feed 实际持有组件，由后继任务定位 |
| C2 | 冻结 `footer` 与热链数据；诊断 §2 C2 | 候选 footer | 候选 HTML、decision-delta | `f1-page-shell.tsx` 或实际 footer 组件；法律文案需用户确认 |
| C3 | 冻结 `imgs[].alt` 截断串；诊断 §2 C3 | 候选 `itemHTML()`/标题 alt/按钮 accessible name | 候选 HTML、token map | `feed-experience.tsx` |
| D1 | 冻结 `bindMainSwipe()` 的 `wheel/deltaX`；诊断 §2 D1 | 实机触控板 `Unknown`；候选保持冻结行为 | 无行为改动；只影响报告/decision-delta | 产品合同与 `feed-experience.tsx`，需后继任务定位 |
| D2 | 冻结 thumbs/swipe/wheel/lightbox 输入路径；诊断 §2 D2 | 候选保留；完整手势组合运行 `Unknown` | 无行为改动；只影响报告/decision-delta | 产品合同与 `feed-experience.tsx` |
| E1 | 冻结 `FEED[*].date` 全为 `08-06`；诊断 §2 E1 | 本候选不改，证据六格仍可见重复 | 当前候选无；报告/decision-delta 记录 | v0.3 产品合同；实现文件由后继任务定位，开发不得猜测 |
| E2 | 冻结 `.tl-lead` 与 `.tl-zh`；诊断 §2 E2 | 本候选不改；390 展开图显示两层关系 | 当前候选无；报告/decision-delta 记录 | v0.3 产品合同与 feed 组件，开发不得猜测 |
| E3 | 冻结 `aggregatedAt` 数据字段；诊断 §2 E3 | 页面无相对时间 UI，本轮运行不覆盖 | 当前候选无；报告/decision-delta 记录 | v0.3 时间语义合同与 feed 组件，开发不得猜测 |
| F1 | 冻结 `<meta name=description>`；诊断 §2 F1 | 候选 head 描述 | 候选 HTML | `app/src/app/layout.tsx` |
| F2 | 冻结 `imgs[].src` Motorsport CDN；诊断 §2 F2 | receipt `remoteImageElements=0`、`inlineSvgDataImages=26`；真实媒体 `Unknown` | 候选 HTML、runtime receipt、报告 | 资产/权利/CSP 合同与实际媒体加载层，由后继任务定位 |

## 4. 六格与展开态证据

| 主题 | 1440×900 | 1024×768 | 390×844 |
|---|---|---|---|
| 深色 | `evidence/integrity-successor-dark-1440.png` | `evidence/integrity-successor-dark-1024.png` | `evidence/integrity-successor-dark-390.png` |
| 浅色 | `evidence/integrity-successor-light-1440.png` | `evidence/integrity-successor-light-1024.png` | `evidence/integrity-successor-light-390.png` |

关键展开态：`evidence/integrity-successor-open-dark-1440.png`、`evidence/integrity-successor-open-dark-390.png`。

运行收据确认六档均有 `innerWidth == scrollWidth`，10 条数据、44×44 原生缩略图按钮和正确 kicker。390 展开态在切至第二张媒体后仍为 `scrollWidth=innerWidth=390`，原文入口 `77.3359×44`，动作行 `356×44`。无横向溢出只证明页面边界，不能证明内部信息可读；现有 390 深色折叠态与展开态清楚显示来源/日期被动作组压成近竖排，这一 P1 阻断任务完成。

证据生成边界必须保留：内置浏览器 `file:` 路径不可用，用户随后授权只绑定 `127.0.0.1` 的临时静态 HTTP。候选通过 `capture=local` 使用内联 SVG 占位，服务已关闭。截图接口交付 JPEG 字节；1440 逻辑视口被宿主归一为 1258×900。本轮只进行一次机械格式/尺寸归一，得到真实 PNG；1440 图恢复为 1440×900。六格可用于构图、层级和响应式确认，1440 图不承担像素级横向几何或字形栅格证明。

## 5. 与 F1358A taste successor 的关系

两套候选都从相同冻结 SHA 派生，目录和身份独立。本轮只复用隔离、hash 清单和清除无效字体 fallback 的证据纪律。没有自动合并 F1358A 的日期 18px、trace `.68`、390 时间轴恢复、Dock 渐变模糊或 coarse 缩略图直接放大方案。

推荐顺序：用户先确认 integrity successor 的客观勘误；taste 变化继续作为单独视觉选择。如果用户需要合并，应创建新的 merge successor、重新生成六格并独立确认，不能用两个已存在候选的证据拼接出第三个基线。

## 6. 开发出口

有序清单见 `implementation-checklist.md`。当前出口分为四类：

1. 用户确认后可立即实施：A1–A5、B1/B2、C1–C3、F1。
2. 用户视觉确认：移动证据区密度、唯一数值、页脚口径、taste 合并关系。
3. 产品合同决策：D1、E1–E3、F2。
4. 实机补证：Mac 触控板、iPhone coarse/DPR/安全区、读屏、200%、forced-colors、字体自托管与真实媒体。

## 7. 已验证、未验证与错题自检

已验证：固定输入 hash 未漂移；候选 HTML/HTMLParser/内联 JS/CSS 结构可解析；A–F 全项完成独立裁定；深浅三宽六格和两张展开态均为可解码 RGB PNG，尺寸符合交付；运行收据覆盖无页面级横溢、10 条计数、44px 缩略图与原文入口、第二张媒体活动态；`capture=local` 页面无远程图片元素、无外部字体链接，最终页面控制台为空；临时服务已关闭；冻结稿、App、Spec、accepted ADR 未由本任务修改。对抗审查同时确认 390 元数据可读性 P1，当前不具备 complete 条件。

未验证：真实 Barlow/生产自托管与跨平台字形；真实照片的颜色、裁切、加载、权利与 CSP；Mac 触控板 D1；iPhone 真机 coarse/DPR/实体安全区；完整键盘/焦点返回、读屏、200%、forced-colors 和低性能设备；E1–E3、页脚与媒体策略；用户视觉确认。浏览器宿主曾出现一次 Statsig 遥测超时，候选级零外联已由页面结构和请求模式确认，工作站级绝对零网络保持 Unknown。

错题自检：没有把 Kimi 诊断直接当事实；B2 纠正为“交互入口未达 44px”，未把非交互元数据强制按钮化；E2 纠正了折叠态摘要层级的过宽判断；未自动合并 taste 审美；未复用历史截图；已披露 Browser JPEG 与 1440 宿主归一及机械转换；没有把内联 SVG 占位外推为真实媒体通过；没有用 `scrollWidth==innerWidth` 掩盖内部元数据被压缩的问题；当前不写 TASK_STATE_OK，不替换冻结基线。

## 8. 用户确认入口

当前只需确认一个恢复动作：**是否允许设计部最小修正 390 证据行为“元数据一行 + 动作一行”，并重新采集与修后候选 SHA 绑定的六格及 390 展开态证据？**
