---
type: work_report
department: 设计部
task_id: TASK-20260809-F1358A
status: completed
date: 2026-08-09
target: F1+1 v0.2 frozen baseline SHA-256 5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1
candidate: design/ui/F1+1-v0.2-taste-successor-20260809/F1+1-v0.2-taste-successor-candidate-20260809.html
candidate_sha256: 371d42e54870bce60d5a4d394d15f008e55ce35947c5e3891392914b343535bb
decision: 交付隔离 successor 供用户视觉确认，不替换冻结基线
---

# TASK-20260809-F1358A · taste 标准像素复核与 v0.2 打磨候选报告

## 一、结果

已对 SHA-256 为 `5a84bfb2…6168cb1` 的冻结 HTML 与原六格证据完成同一基线复核，并从该 HTML 的字节副本派生隔离 successor。冻结稿、正式 App、Spec、accepted ADR、公开页功能开发候选均未修改。

本轮建议保留整体视觉方向，并在候选中落地五项小范围打磨：

1. 390 恢复发丝时间轴与节点；
2. 1024/390 Dock 与规范重新对齐为渐变模糊背板；
3. 日期 20px 降到 18px；
4. trace 默认透明度 .50 提到 .68；
5. 删除未加载的 `Inter Tight` fallback，并把 Barlow 自托管列入实现出口。

## 二、五项原未确认内容

| 项目 | 最终状态 | 摘要 |
|---|---|---|
| 浅色 Dock 实感 | verified | 冻结图显示硬实色底边；候选渐变模糊更轻，且 reduced-transparency 保留实色降级 |
| 深色底层次 | verified | 底色、内容面、轴线和媒体边界均可区分；保留现有深色 token |
| Barlow Condensed 实际落屏 | verified：当前 Open Design 预览未加载 | 字体集合已完成加载，但 Barlow check=false；冻结图不能证明具体字体文件，生产自托管仍未验证 |
| 390 22px 缩略图 | verified：视觉；Unknown：真机 coarse | 22px 偏小；候选 fine-pointer 28px、coarse CSS 44×44；真实手机命中待补 |
| 真实图最差裁切区文字对比 | verified：当前构图不适用 | 当前没有图上文字；未来引入叠字需重新验证 |

详细证据、P1/P2 保留/修改裁决与影响见 `pixel-review-and-design-delta.md`。

## 三、六格 successor 证据

| 主题 | 1440×900 | 1024×768 | 390×844 |
|---|---|---|---|
| 深色 | `evidence/F1+1-v0.2-taste-successor-dark-1440.png` | `evidence/F1+1-v0.2-taste-successor-dark-1024.png` | `evidence/F1+1-v0.2-taste-successor-dark-390.png` |
| 浅色 | `evidence/F1+1-v0.2-taste-successor-light-1440.png` | `evidence/F1+1-v0.2-taste-successor-light-1024.png` | `evidence/F1+1-v0.2-taste-successor-light-390.png` |

六图均由 Open Design MCP 独立项目的本机预览，经内置浏览器同一页面导出；格式为 RGB、non-interlaced PNG，尺寸逐项精确。三宽运行读取均满足 `scrollWidth === innerWidth`。

## 四、Open Design 与视觉协同边界

- 已通过安装在本机的 Open Design MCP 创建独立项目并导入候选；没有复用历史 `Source+1` 项目。
- Open Design Cloud 当前未登录。遵守不等待外部登录/审批的边界，本轮未启动登录，因此云端视觉代理结论为 `Unknown`。
- Kimi 本轮未读取 successor 六图，结论为 `Unknown`；没有把历史代码级 DeepSeek 结论或其他页面截图冒充本候选视觉通过。
- Open Design 和 Kimi 均不能替代用户视觉确认、像素 diff、真机触控或无障碍测试。

## 五、已验证

- 冻结输入 SHA 未漂移，仍为 `5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1`。
- 候选只包含标题标识、字体 fallback 清理与隔离 override；未删除冻结 token、布局、媒体、Dock、主题或交互主体。
- 六张 PNG 文件格式、尺寸、深浅主题和内容可读性已逐张检查。
- 390 发丝轴/节点可见；1024/390 Dock 渐变与模糊 computed style 生效；三宽无横向溢出。
- 未修改冻结 HTML/index、`app/`、Spec、accepted ADR 或公开页开发候选。

## 六、未验证

- Open Design Cloud 视觉代理、Kimi 视觉复核均为 `Unknown`。
- Barlow 在当前本机预览明确未加载；生产自托管、跨平台字形和离线字体策略未验证。
- 真实 coarse-pointer 44px 命中、实体安全区、读屏、200% 缩放、forced-colors 实机、低性能设备未验证。
- 用户尚未确认 successor；候选不能替换冻结基线。

## 七、错题自检

- 已从用户确认的最新冻结稿直接派生，没有另起视觉外壳。
- 没有把 Open Design 项目存在写成云端代理审查通过。
- 没有把 computed `font-family` 声明误写成 Barlow 实际加载；运行 check=false 已明确落账。
- 没有把 fine-pointer 390 模拟外推为真机触控通过。
- 没有把六格候选写成用户已确认或正式 App 已实现。

TASK_STATE_OK
