---
type: audit_report
status: final
date: 2026-08-02
department: 测试部
task_id: TASK-20260802-C093C7
domain_stage: M4-A-v0.3-cross-contract-revalidation
execution_mode: local_read_only
decision: fail
target: docs/spec.md; docs/progress.md; docs/decisions/system/2026-08-01-F1+1-M4本地Kickoff系统路线-proposed.md; data/mvp-contract-v0/; data/m3-base-shadow-import-v0/; docs/collaboration/tasks/TASK-20260802-D80846.json
related_task: TASK-20260802-C093C7
tags: [M4, A-axis, v0.3, cross-contract, read-only, mechanical-revalidation]
summary: A轴v0.3的P0-01至P0-09机械复算全部通过，但D80846当前ACK收据与Spec、progress、M4 proposed ADR中的进行中/未ACK状态冲突，P1=1，按出口给FAIL；B/C运行证据保持未覆盖。
---

# TASK-20260802-C093C7：M4 A 轴 v0.3 跨合同定版复验报告

## 1. 结论

`decision=fail`。

本轮已正式 claim `TASK-20260802-C093C7`（当前任务 JSON revision 2），只读重跑 M4 A 轴 v0.3 机械验收。A 轴产物的 P0-01 至 P0-09 均通过，发现 1 个唯一 P1：`TASK-20260802-D80846` 已在当前任务收据中为 `acknowledged`，但 `docs/spec.md`、`docs/progress.md` 和 M4 proposed ADR 的当前叙述仍写成“进行中/尚未交付或 ACK”。这会使当前合同基线和定版状态产生跨文件时态冲突；任务出口要求 `P0=0 且 P1=0`，因此本轮不能给 PASS。

计数结论：

| 层级 | 结果 | 说明 |
|---|---|---|
| P0-01–P0-09 | `0` 个失败 | 9 项 A 轴机械出口均只读复算通过；P0-06 的 Node24/app 证据按任务口径留在 B/C，不升级为 A 轴失败。 |
| P1 | `1` 个失败 | 当前 ACK 收据与 Spec/progress/ADR 的 v0.3 任务状态漂移，见第 5 节。 |
| P2 | `2` 个未覆盖项 | 原生 Draft 2020-12 JSON Schema 引擎、Node24/app/SQLite/worker/UI/API/安全运行收据均未执行；这些属于验证边界，不改写为 A 轴 P0。 |
| 唯一判定 | `FAIL` | PASS 前提 `P0=0/P1=0` 未满足。 |

未修改被审的 Spec、progress、ADR、data 产物、app 或任何外部资源。没有运行生成器、安装依赖、联网、调用 provider/Collector、访问或写入真实 Base/飞书、提交表单或触发发布。

## 2. 领取、范围与证据方法

### 2.1 领取与范围

- 已执行任务协议 claim：`TASK-20260802-C093C7` → `测试部/019fb379-9212-7e80-a5df-587d339936c4`。
- 只读范围：`docs/spec.md`、`docs/progress.md`、`docs/decisions/system/2026-08-01-F1+1-M4本地Kickoff系统路线-proposed.md`、`data/mvp-contract-v0/` 全部 v0.3 产物、M3 离线载荷及当前数据任务收据。
- 任务明确的出口是 A 轴静态合同；Node24、lockfile、app、SQLite、真实 worker/provider/UI/安全行为属于 B/C 后置证据。

### 2.2 实际检查

- 对 `data/mvp-contract-v0/` 全部 JSON 做 JSON 解析；未发现解析错误。
- 以内存只读脚本按项目 `canonical-json-v1`（UTF-8、Unicode code-point key order、紧凑分隔符、显式 null、`ensure_ascii=false`、SHA-256）重算 manifest 中 11 个 artifact SHA-256、Content/Summary/ReleaseBundle/ReviewDecision hash、Publication/key、internal schema SHA 和 seed subset hash。
- 以内存只读 JSON Schema 解释器检查 `schema.json`、`internal-contract.schema.json`、`security-fixtures.schema.json` 的 `required/type/const/enum/pattern/minItems/uniqueItems/additionalProperties/$ref`，并逐个检查 11 个 TaskEnvelope 的 runtime schema；结果均为 0 error。环境中没有安装原生 Draft 2020-12 校验引擎，故该项保留 P2 未覆盖。
- 图遍历检查 6 组 state machine 的初始状态可达性，逐项核对 fixture 枚举覆盖、状态恢复出口和 stale/snapshot 独立性。
- 重新读取 M3 CSV、33 字段、9 字段和 59×33 batch，核对输入 SHA、payload SHA、字段顺序、行顺序、每行宽度、唯一性及 59 行默认值。

## 3. 机械复算总览

| 出口 | 复算结果 | 证据指针 |
|---|---:|---|
| v0.3 版本与 canonical JSON | `mvp-local-v0.3`；规则为 `canonical-json-v1` | `data/mvp-contract-v0/manifest.json:2-17`、`data/mvp-contract-v0/state-machine.json:1-13` |
| manifest artifact hashes | `11/11` 匹配当前落盘字节 | `data/mvp-contract-v0/manifest.json:19-43`；逐文件 SHA-256 无 mismatch |
| `6/5/9/3` | 6 state machines / 5 idempotency keys / 9 invariants / 3 published projections | `data/mvp-contract-v0/manifest.json:57-81`、`data/mvp-contract-v0/state-machine.json:20-21,500-617` |
| `16/4` | 16 security fixtures / 4 reconcile outcome cases | `data/mvp-contract-v0/manifest.json:57-64`；主 fixture `:3244-3312` |
| M3 `33/9/59/59` | 33 主信源字段 / 9 手机捕获字段 / 59 rows / 59 `enabled=false` | `data/m3-base-shadow-import-v0/manifest.json:6-11,18-48`；`data/mvp-contract-v0/manifest.json:49-55` |
| no-egress | manifest、主 fixture、安全 fixture、seed、internal fixture 均 `external_calls=0`；M3 `external_write_performed=false` | `data/mvp-contract-v0/manifest.json:93-97`；`data/mvp-contract-v0/fixtures.synthetic.json:16-17`；`data/mvp-contract-v0/security-fixtures.synthetic.json:16-18`；`data/mvp-contract-v0/seed-layers.json:1-5`；`data/mvp-contract-v0/internal-fixtures.synthetic.json:1-5`；`data/m3-base-shadow-import-v0/manifest.json:4-5` |
| 旧名检查 | data 现行 artifact 仅保留允许的 `upgrade_from: mvp-local-v0.2`；未发现 `jcs-v1`、`source_config_version`、`publish_unknown`、`summary_draft_version` 或 `source_lifecycle` | `data/mvp-contract-v0/manifest.json:4-5`；当前 data JSON/Python 只读 grep |

### 3.1 Hash 链

- 7 个 Content 的 `content_version_hash` 均由落盘 `content_hash_input` 重算通过。
- 7 个 Summary 的 `summary_version_hash` 均由落盘 `summary_hash_input` 重算通过。
- 5 个 ReleaseBundle 的 `payload_hash=SHA-256(canonical-json-v1(canonical_payload))`、`bundle_hash=SHA-256(canonical-json-v1(bundle_hash_input))` 全部通过；每个 `canonical_payload` 都实际包含 `content_snapshot`、`summary_snapshot`、source snapshot、original URL、rights、media、policy、schema 和五 fence，并显式冻结 content/summary version hash。
- 5 个 ReviewDecision 的 `decision_hash` 重算通过，`approved_bundle_hash` 与对应 Bundle hash 一致。
- `bundle_hash_input` 仅使用 `release_bundle_id`、`bundle_version`、`payload_hash`、规则版本和 `immutable`；未把已写入 hash 当作 payload hash 的输入。

### 3.2 Task / lease / operation / Publication

- 11 个 OutboxJob 的 `job_id`、TaskEnvelope `task_id`、`operation_id`、`lease_token`、`payload_hash`、`idempotency_key` 均全局唯一；所有 job 顶层字段与嵌套 envelope 逐字一致。
- 11 个合成 lease 全部匹配 `synthetic:lease:<32 lowercase hex>`；32 个十六进制字符对应 128 bit 合成形状。`lease_expiry`/`deadline` 均满足以 fixture `created_at` 为锚的有限 900 秒窗口，所有有效 envelope 五 epoch 从 1 开始；`runtime-envelope.schema.json` 对五 epoch `minimum=1`，`epoch=0` 为拒绝输入。
- `src-queued` 的 `Source.onboarding_operation_id`、activation receipt、Outbox 和 TaskEnvelope 的 `operation_id` 均为 `op-activation-queued`；启用从 `false` 到 `true` 与 `queued` 在同一 synthetic transaction receipt 中绑定。
- 3 个 publish OutboxJob（成功、retryable_failed、reconcile_wait）都按 `aggregate_id` 找到唯一 Publication，并与该 Publication 的 `idempotency_key`、`reconcile_key` 及 TaskEnvelope 两类 key 逐字相等；非 publish job 的 `reconcile_key` 全部为 null。已发布的另外两个 Publication 没有强行制造无意义的 Outbox fixture，未计为缺失。
- 5 个 Publication 的 `(release_bundle_id, approved_bundle_hash)`、`public_id` 均无重复；3 个 PublishedProjection 按 bundle、public_id、content/summary、generation 和 published hash 逐字段匹配对应 Publication。

## 4. P0-01 至 P0-09 唯一判定

| P0 | 判定 | 独立只读证据与核对结果 |
|---|---|---|
| P0-01 信源门禁、原子 enable+queued、operation 复用 | `PASS` | `validating → activation_pending` 要求 canonical URL valid、normalization valid、dedup unique；三门阻断顺序为 platform > authorization > adapter；`activation_pending → queued` 明确要求三门、stop、五 fence 和 `enabled=false`，并声明同一事务写 `enabled=true`/queued/outbox。16 个 onboarding 状态均有 Source fixture，activation receipt 逐字段闭合。证据：`data/mvp-contract-v0/state-machine.json:20-113`、`:131-178`；`data/mvp-contract-v0/fixtures.synthetic.json:2894-2908`。 |
| P0-02 `reconcile_wait`、失败/恢复出口、snapshot/stale 隔离 | `PASS` | Publication 的 retry、reconcile、terminal、blocked、急停出边完整；Outbox 的 stale、cancelled、dead-letter、reconcile 出边完整。4 个 reconcile outcome case 独立覆盖 confirmed published / confirmed not submitted / terminal failed / emergency stopped；snapshot failure 使用 `job-snapshot-partial`，stale epoch 使用 `job-content-stale`，二者 operation/key/job 独立。证据：`data/mvp-contract-v0/state-machine.json:343-421,423-496`；`data/mvp-contract-v0/fixtures.synthetic.json:2910-2917,3031-3048,3244-3312`。 |
| P0-03 TaskEnvelope 五 fence、epoch0、lease、task/lease/operation 唯一 | `PASS`（A 静态） | required fields 14 项完整；五 epoch、attempt、lease、deadline、两类 key 均在 runtime schema；epoch 最小值为 1、synthetic lease 为 32 hex、900 秒窗口可从 11 个 envelope 逐个重算；task/lease/operation/payload/key 全部无重复；stale fixture 明确为 envelope epoch 1 对 current epoch 2，状态 `stale_epoch`，且 expected 为 provider call 前拒绝。真实 CAS、worker lease 和 CSPRNG 行为留 B/C。证据：`data/mvp-contract-v0/state-machine.json:547-569`；`data/mvp-contract-v0/runtime-envelope.schema.json:38-113`；`data/mvp-contract-v0/fixtures.synthetic.json:3031-3048`。 |
| P0-04 单一领域映射、internal-only 记录、无第二实体 | `PASS` | domain schema `$defs` 只有 13 个实体，未注册 `SourceObservation`、`SummaryDraft` 或 `AuditEvent`；Base mapping 只把 Source/CapturedItem 作为 M3 映射实体，SummaryDraft 映射到 `Summary(summary_status=draft)`，internal-only 仅列 SourceObservation/AuditEvent。internal schema/fixture `additionalProperties=false`、`domain_refs=[]`、`base_mapping_refs=[]`，3 条 AuditEvent 的实际 `schema_hash` 均等于当前 internal schema SHA。证据：`data/mvp-contract-v0/schema.json:252-2846` `$defs`；`data/mvp-contract-v0/base-mapping.json:354-384,405-428`；`data/mvp-contract-v0/internal-contract.schema.json:1-7,42-67,60-181`；`data/mvp-contract-v0/internal-fixtures.synthetic.json:46-127,170-171`。 |
| P0-05 三层 seed、subset hash、M3 边界 | `PASS` | 顶层 seed 恰为 `m3-shadow-seed`、`synthetic-case-seed`、`security-error-seed`；published projection/snapshot/internal 为 synthetic-case 子集，不构成第四/第五层。3 个 subset 的实际数量为 `3/1/5`，来源文件 SHA 和 canonical subset hash 均逐个匹配；M3 输入 SHA、payload SHA、字段顺序、59 rows、33 values/row、source_id/canonical_url 唯一性和 59 条默认值全部通过，全部 `enabled=false`。证据：`data/mvp-contract-v0/seed-layers.json:6-84`；`data/mvp-contract-v0/manifest.json:49-69`；`data/m3-base-shadow-import-v0/manifest.json:6-53`。 |
| P0-06 Node24 / lockfile / app 命令边界 | `PASS`（A 轴不阻断） | ADR P0 矩阵明确 Node24 exact candidate、lockfile、spike 和 app 运行收据属于 B/C；本任务未把尚未初始化 app 或 Node25 环境升级成 A 轴合同失败。没有创建 app、安装依赖或执行命令。B/C 保留未覆盖。证据：`docs/decisions/system/2026-08-01-F1+1-M4本地Kickoff系统路线-proposed.md:338-348`；`docs/spec.md:82-98`。 |
| P0-07 状态枚举、可达性、恢复/停止/取消/死信 | `PASS` | 6 组机器图遍历全部可达：source 16/16、capture 4/4、content 9/9、review 5/5、publication 8/8、outbox 9/9；source 的 16 个 onboarding enum 与 16 个 fixture 集合完全相等，`paused` 只出现在 `lifecycle_status`；blocked、normalization/dedup recovery、queue/collection retry、stop/cancel、dead-letter 和 publication blocked 出口均有 transition。证据：`data/mvp-contract-v0/state-machine.json:20-179,343-496`；`data/mvp-contract-v0/fixtures.synthetic.json:2894-3312`。 |
| P0-08 `published` happy path、唯一公开身份、projection | `PASS` | 5 个 Publication 中 3 个 `published` 记录与 3 个 projection 逐字段绑定；5 个 public_id 无重复，单一 bundle/hash 无第二 Publication/generation，publish/retry/reconcile job 的 key 复用通过。3 条 published happy-path case 和 retry/reconcile case 均为 synthetic、zero-egress。证据：`data/mvp-contract-v0/fixtures.synthetic.json:2226-2386,2841-2892,3011-3029,3068-3121`；`data/mvp-contract-v0/state-machine.json:343-421`。 |
| P0-09 R1–R13 A 层 predicate、strict hash、安全/错误样例、no-egress | `PASS`（A 静态） | canonical hash、strict release snapshots、internal audit/schema SHA、五 fence、security fixture categories 和 fail-closed expected assertions 均可读复算；安全 fixture 恰 16 类，`synthetic_only=true`、`external_calls=0`，包含 XSS/SSRF/open redirect/prompt injection/CSRF/secret/stale/reconcile 等类别。原生运行时 deny-all、URL/媒体处理、CSRF/并发/恢复和 UI 安全收据留 B/C。证据：`data/mvp-contract-v0/security-fixtures.synthetic.json:1-60`、`:37-423`；`data/mvp-contract-v0/state-machine.json:571-616`；`data/mvp-contract-v0/fixtures.synthetic.json:16-17,2894-2917`。 |

**P0 汇总：** `P0=0`。以上 PASS 只覆盖 A 轴可由现有落盘合同和合成数据机械验证的部分，未向 B/C 运行时行为外推。

## 5. P1/P2

### P1-01：当前 ACK 状态与 Spec/progress/ADR 时态漂移（FAIL）

当前数据任务收据明确为 `execution_state=acknowledged`，并带最终报告、verified 和 unverified 列表：`docs/collaboration/tasks/TASK-20260802-D80846.json:39-64`。当前 `docs/spec.md:12,86,167`、`docs/progress.md:17` 以及 M4 proposed ADR `:32,40,48,184,259,304,466,483` 仍写成 `TASK-20260802-D80846` “仍在进行中/尚未交付或 ACK/尚未复验”。这些句子处在当前状态、领域合同候选、验收和未验证项叙述中，属于可影响定版判断的跨合同时态冲突。

历史 `mvp-local-v0.2` 在数据 manifest 的 `upgrade_from` 字段中属于允许的迁移来源；该历史字段不构成 P1。P1 指向的是同一当前任务在收据与现行产品文档中的状态不一致。测试部没有替换或修正文档，避免越过只读审核边界。

**要求：** 由拥有 Spec/progress/ADR 的部门依据当前 D80846 ACK 收据同步状态和“当前 v0.3 可作为复验基线”的表述，再重新执行本任务；不应把 B/C 未验证项改写为 A 轴已验证。

### P2-01：原生 JSON Schema 引擎未覆盖（UNVERIFIED）

当前环境没有可用的 Draft 2020-12 原生校验引擎。本轮使用只读解释器覆盖 required/type/const/enum/pattern/minItems/uniqueItems/additionalProperties/$ref，并对主 schema、internal schema、安全 schema 和 11 个 TaskEnvelope 得到 0 error。该结果不能替代原生引擎的完整规范兼容性证据，保留为 P2/验证边界。

### P2-02：B/C 实现与真实运行收据未覆盖（UNVERIFIED）

Node24 exact runtime、`package.json`/lockfile、app 路由与四页出口、SQLite/WAL/事务/CAS/crash recovery、worker lease、真实 UI/AT、HTTP/Origin/CSRF、构建/测试/安全 deny-all、真实 provider/Collector/Base/飞书/平台网络和发布服务均未运行。`P0-06` 按任务口径不阻断 A 轴；这些项目在 B/C 仍为门禁，不能写成实现 PASS。

## 6. A / B / C 分层 Go/No-Go

| 层 | 当前判定 | 理由 |
|---|---|---|
| A 合同定版 Go | `NO-GO` | P0 已清零，但 P1-01 的当前任务状态漂移仍未闭合；PASS 需要 P1=0。 |
| B 定版后 app 初始化许可 | `PENDING` | 本轮不创建或修改 app；待 A 重新达到 PASS 后，按 Node24/lockfile/scaffold 合同办理。 |
| C 实现验收 Go | `NO-GO / PENDING` | 没有 Node24、lockfile、UI 实现、运行端口、SQLite/worker、构建/测试、安全和 no-egress 运行收据；真实外部能力继续关闭。 |

## 7. 已验证 / 未验证

### 7.1 已验证

- 正式 claim 和任务 JSON 当前所有权、revision、验收出口已核对。
- v0.3 全部现行 JSON 可解析；manifest 11 个 artifact SHA-256 全部匹配。
- `6/5/9/3`、`16/4`、14 个 TaskEnvelope required fields、900 秒窗口、epoch0 schema 拒绝、32 hex synthetic lease、11 项 task/lease/operation/payload/key 唯一性通过。
- source onboarding 三门、blocked 优先级、原子 enable+queued、operation 复用、16 状态 fixture 覆盖和 6 组状态机图可达性通过。
- Content、Summary、strict ReleaseBundle payload/bundle、ReviewDecision approval hash 全链路重算通过。
- Publication 与 publish Outbox/TaskEnvelope key、单一 bundle/hash/public_id/generation、3 条 PublishedProjection 绑定通过。
- internal-only schema/fixture、`SourceObservation`/`AuditEvent` 边界、真实 internal schema SHA、SummaryDraft→Summary(draft) 映射通过。
- 三层 seed、3/1/5 subset count/hash/source file SHA、snapshot failure 与 stale epoch 独立、M3 33/9/59/59 和默认值通过。
- 16 类安全/错误 fixture、合成标记、`external_calls=0`、M3 `external_write_performed=false` 和本轮无外部 I/O 通过。

### 7.2 未验证

- 原生 Draft 2020-12 JSON Schema 引擎的规范兼容性。
- Node24/lockfile/app 初始化、Next/React/UI 四页、真实路由、键盘/AT、HTTP、Origin/CSRF、SQLite/WAL/CAS/crash recovery、worker lease、构建、lint、typecheck、test、check。
- live CSPRNG lease、CAS/fence 在 provider/adapter/outbox/publication mutation 前的真实执行顺序。
- 真实 Base/飞书、provider、Collector、平台授权、网络、媒体权利、AI、部署、真实表单提交、自动发布或外部发布服务。
- Spec/progress/ADR 中 D80846 当前状态漂移的上游修正与复验。

## 8. 错题自检与失败路径复核

1. 首次 M3 探针误用了不存在的 `m3-base-shadow-import.json` 文件名，收到只读 `FileNotFoundError`；随后按实际目录改读 `manifest.json`、`main-source-fields.json`、`mobile-capture-fields.json`、`main-source-record-batch.json`，SHA/计数/字段/行值全部通过。没有修改文件。
2. 首次通用读取脚本把 `state_machines` 当作数组、把 reconcile case 的 outcome 当作顶层字段，收到只读类型/字段错误；按当前 JSON 结构改为 dict 和 `expected.outcome` 后，6 组图遍历与 4 case 检查通过。
3. 首次 Publication 绑定脚本把 OutboxJob 当作带顶层 `publication_id` 的结构，导致已发布的 002/003被误报为缺 job；按唯一合同改用 `aggregate_type=publication` + `aggregate_id`，对实际 3 个 publish jobs 逐字核对 Publication/Envelope key 后通过。没有把误报写入结论。
4. `agent_team_task.py show` 不是当前 CLI 支持的命令；该命令仅返回 usage，没有写入任务状态。随后直接读取任务 JSON，并以 claim 后 revision 2 为准。
5. `job-content-stale` 的 `current_source_config_epoch=2` 与 envelope epoch 1 是专门的 stale fence failure fixture，且 expected 明确要求 provider call 前拒绝；已将它识别为预期失败路径，不将其误判为普通 epoch mismatch。
6. 本轮没有把“原生校验引擎缺失”“Node24/app 未初始化”“真实运行时未执行”升级为 A 轴 P0；这些均按任务 failure path 留在 P2/B/C 未覆盖。唯一进入最终 FAIL 的原因是当前 ACK 收据与现行文档的 P1 时态冲突。

## 9. 任务收口

- 测试报告路径：`docs/collaboration/部门/测试部/报告/2026-08-02-M4-A轴v0.3跨合同定版复验报告.md`
- 只读审核结论：`decision=fail`；P0=0，P1=1，P2=2。
- 未修改任何被审上游产物；后续文档同步和重新复验由统筹部安排。

TASK_STATE_OK
