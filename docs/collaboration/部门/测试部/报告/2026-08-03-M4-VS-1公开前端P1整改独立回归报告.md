---
type: audit_report
department: 测试部
target: TASK-20260803-CD4228 公开前端 P1 整改快照
status: final
date: 2026-08-03
related_task: TASK-20260803-5991EE
decision: pass
tags: [M4, VS-1, public-ui, P1-remediation, Node24, responsive, accessibility, read-only]
summary: 独立复验确认被测10文件SHA零漂移；固定Node24下51/51测试、lint、typecheck、build与完整check全部通过；390/1440深浅主题、原4项P1、0/1/3/12组合、详情44px、共享Shell、主题持久化、锚点、标题层级、HTTP 200/200/404、控制台和零外部资源均达到出口。P0=0、P1=0、P2=2，唯一结论PASS。
---

# TASK-20260803-5991EE：公开前端 P1 整改独立回归报告

## 1. 唯一结论

`decision=PASS`。

本轮严格只读审核 `TASK-20260803-CD4228` 整改后快照。固定 Node 24.18.0 的命令链、真实本地页面、390/1440 双视口、深浅主题、四项原 P1 和直接回归面均已亲自复验。未修改 `app/`、Spec、ADR、`data/`、`design/`、package/lock 或其他上游产物。

| 级别 | 数量 | 门禁结果 |
|---|---:|---|
| P0 | 0 | 未发现伪造真实资讯、外部请求、路由/共享壳层严重回归或越界修改 |
| P1 | 0 | 四项原 P1 均闭合，快照、构建、响应式、主题、语义、HTTP、console 和资源边界无新 P1 |
| P2 | 2 | 实机辅助技术与完整 forced-colors/200% zoom 未运行；依赖 SQLite/API 的动态状态属后续切片 |
| 唯一判定 | PASS | 任务规定的 `P0=0 且 P1=0` 已满足 |

本结论只放行当前本地合成数据的公开前端整改快照；不扩大到 SQLite 公开读模型/API、admin、真实 provider/Base/Collector、平台采集、AI、媒体、发布、部署、R5/R12 或另案 Next 后置启动错误脱敏。

## 2. 快照与独立性

### 2.1 被测快照 SHA-256

领取任务后先复算一次，全部验证和服务停止后再复算一次。两次都与开发报告逐字一致：

| 文件 | SHA-256 |
|---|---|
| `app/src/app/globals.css` | `306327f161907e3a5144341ddf4bb44b903864291adfcb2e0f84dd31f90ea16a` |
| `app/src/app/layout.tsx` | `205dba10a35638c888fd4cc67af9668b041356764b937cc8629a0af23907099f` |
| `app/src/app/page.tsx` | `853cff32988d0344e6e5ec0b566be327148121c4fc934ff8cebdc10b5316104e` |
| `app/src/app/stories/[publicId]/page.tsx` | `0c842483182c1e306aa1d4572064ab06682a95b42fd1ed122fed7359b0386e32` |
| `app/src/app/stories/[publicId]/not-found.tsx` | `57aafe35999e2f4553c8b2193aa7d68192547e09a2e5e7611615ffde3eee82a1` |
| `app/src/components/f1/f1-page-shell.tsx` | `341879012e71603d371e0f548ece9767cd8869cbc4e6eb21ed5f9eddcccb4726` |
| `app/src/components/f1/story-parts.tsx` | `6df4d3b44d2dcd0ef205330be9dcf16360bd50ae46bff4749608470a4e7e3602` |
| `app/src/components/f1/theme-preference.ts` | `35b13341643e9a120db900eccd66c5dee369d5133f2b37a1c856a5ff8e0c24d6` |
| `app/src/features/stories/feed-experience.tsx` | `bb86c1279aa2f0f802d1b4e1b5cf71a07d4f1e9478b555dccfb1d76642f0c862` |
| `app/src/tests/public-ui.test.ts` | `77d7dae86ded7a34ae377d5bbe2ec8d47882fa350eae4d5a016ca63323b21853` |

验证期间没有出现并发修改或快照漂移。

### 2.2 执行边界

- 使用项目固定工具链 `app/.local/toolchains/node-v24.18.0-darwin-arm64/bin`；现场为 Node `v24.18.0`、npm `11.16.0`。
- npm 命令使用 `env -i` 和最小 `PATH`，避免宿主 Node 污染子脚本。
- 独立启动生产构建在 `127.0.0.1:3000`；未使用开发报告中的浏览器结论代替本轮证据。
- 浏览器验证后恢复了临时视口覆盖，关闭验收标签页，停止服务并复核 3000/3101 无监听。

## 3. 固定 Node24 命令收据

工作目录均为 `app/`，下列脚本均由本轮独立执行：

| 命令 | 结果 | 关键收据 |
|---|---:|---|
| `npm --silent run test` | exit 0 | Vitest 3 files，51/51 PASS；`public-ui.test.ts` 9/9 |
| `npm --silent run lint` | exit 0 | ESLint 无错误 |
| `npm --silent run typecheck` | exit 0 | `tsc --noEmit` 无错误 |
| `npm --silent run build` | exit 0 | Next 16.2.11 构建通过；路由为 `/`、`/_not-found`、`/api/health`、`/stories/[publicId]` |
| `APP_BIND_HOST=127.0.0.1 APP_PORT=3000 npm --silent run check` | exit 0 | env/migration/59×39 seed/runtime readiness、51/51、lint、typecheck、build、`test:p1` 全通过；`externalCalls=0` |

完整 `check` 中的 SQLite 收据为 3.53.1、`userVersion=2`、WAL、foreign keys 开启；59×39 Source 投影保持 `rowCount=59`、`enabledFalseCount=59`、`projectionHash=e7a8312c70a9a49922aedb3cfbeaa190db8f5dce8d4ab45db1570748fc329f17`。这些只证明既有本地检查未回归，不把本任务扩大为后续公开读模型/API 放行。

## 4. 四项原 P1 复验

### 4.1 媒体标签双主题对比度

- 1440 深色和浅色主题中，所有 `.media-demo-mark` 读得前景 `rgb(255, 255, 255)`、背景 `rgb(20, 20, 23)`。
- 按 WCAG 相对亮度公式独立复算为 `18.3859:1`，高于普通文本 `4.5:1` 门槛。
- 主题切换后 computed style 保持同一实色保护层，未回退到半透明浅色背景。

判定：`PASS`。

### 4.2 分类选中态非纯颜色

- 默认“全部”和交互后“赛事新闻”均只有一个 `aria-pressed=true` 选中按钮。
- 选中按钮同时存在可见 `✓`、computed `font-weight=750` 和颜色变化；未选项不显示勾选标记。
- 390 实际筛选交互中，选中态与结果数量一同变更。

判定：`PASS`。

### 4.3 `no-more` 与分类/关键词的 0/1/3/12 组合

390 真实筛选面板逐一操作得到：

| 可见数 | 条件 | DOM article | 页面文案 |
|---:|---|---:|---|
| 12 | 全部 + 无关键词 + `no-more` | 12 | `当前显示 12 条…`、`查看 12 条内容`、`12 条本地合成演示内容已全部显示…` |
| 3 | 赛事新闻 + `no-more` | 3 | 三处计数均为 3 |
| 1 | 全部 + 关键词“车库里的赛前仪式” + `no-more` | 1 | 三处计数均为 1 |
| 0 | 全部 + 关键词“不存在的本地词” + `no-more` | 0 | `当前显示 0 条…`、`查看 0 条内容`；空状态明写“当前筛选后显示 0 条…” |

0 条时进入明确空状态，没有继续渲染虚假的 no-more 卡片。

判定：`PASS`。

### 4.4 详情返回链接 44×44

- 1440×900 浅色详情：`58.2421875 × 44px`。
- 390×844 浅色详情：`58.2421875 × 44px`。
- 两视口的 root/body 均 `clientWidth=scrollWidth`，没有页面级横向溢出。

判定：`PASS`。

## 5. 共享壳层、主题、锚点与语义回归

### 5.1 唯一共享 Shell 与导航 current

| 页面 | HTTP | `.f1-shell` | `.top-bar` | H1 | 全局导航 current |
|---|---:|---:|---:|---:|---|
| `/` | 200 | 1 | 1 | 1 | `资讯流` 链接 `aria-current=page` |
| `/stories/demo-qualifying-window` | 200 | 1 | 1 | 1 | 无误标；面包屑当前项独立标记 |
| `/stories/not-a-local-demo` | 404 | 1 | 1 | 1 | 无误标；H1 为“这条演示内容不存在或已下架” |
| `/api/health` | 200 | 不适用 | 不适用 | 不适用 | 健康接口仅用于状态复核 |

判定：`PASS`。

### 5.2 主题跨路由与刷新保持

- 首页从深色切至浅色后，`html[data-theme]=light`，按钮可读名为“切换至深色主题”，`aria-pressed=true`。
- 通过流内链接进入有效详情，主题仍为 light；刷新详情后仍为 light。
- 从 1440 切至 390 并刷新详情，主题与按钮名保持一致。
- 全过程未观察到水合错误、主题闪回后定格为错误值或共享 Shell 重复挂载。

判定：`PASS`。

### 5.3 sticky 锚点

390×844 直达 `/#demo-boundary` 时：

- `#demo-boundary` top = `80.0625px`
- sticky `.top-bar` bottom = `57px`
- 锚点首部位于导航之下，未被遮挡。

判定：`PASS`。

### 5.4 标题层级

- 首页：页面 H1 后依次为筛选/Featured/Feed/no-more H2；精选和信息流卡片标题为 H3。
- 详情：内容标题 H1，“中文摘要/关键点/来源与原文状态/相关内容”为 H2，相关卡片为 H3。
- 首页、详情、404 均只有一个 H1。

判定：`PASS`。

### 5.5 键盘焦点回归

- 390 手机菜单打开后，焦点进入首个“资讯流”链接；Escape 关闭后返回“菜单”按钮。
- 390 筛选 dialog 打开后，焦点进入首个“全部”分类按钮；Escape 关闭后返回“筛选本地演示内容”触发按钮。
- dialog 与菜单的 `aria-expanded` 在开/关后分别为 `true/false`，关闭后对应 DOM 结构移除。

判定：`PASS`。

## 6. 控制台、资源与外部边界

- 浏览器完成首页、筛选交互、主题切换、详情、刷新、390 键盘路径和 404 后，console `warn/warning/error` 为 `0`。
- 首页、有效详情和 404 的 DOM `src/href/srcset` 中外部 HTTP(S) 值均为 `0`。
- 浏览器已观测资源清单中，首页、详情和 404 的 HTTP(S) origin 集合均只有 `http://127.0.0.1:3000`，外部资源数为 `0`。
- 页面不含真实赛车摄影，原文入口保持 disabled，页面明写本地合成演示与零外部请求边界。

判定：`PASS`。

## 7. P2 与未验证边界

### P2-01：实机辅助技术、200% zoom 和完整 forced-colors

本轮已验证 DOM 语义、ARIA、键盘焦点、390/1440 布局与双主题 computed style；没有使用 VoiceOver/NVDA/TalkBack 实机逐句走查，也没有执行完整 200% 原生缩放与 forced-colors 视觉回归。保留为后续完整可访问性终验项，不改写为已通过。

### P2-02：依赖 SQLite/API 的动态状态

`loading/error/partial/live-refresh/withdrawn/correction` 仍等待公开读模型和 API 切片。当前页面没有伪造这些后端状态，因此本报告既不把它们记为整改失败，也不推导为已实现。

## 8. 清理、零漂移与错题自检

### 8.1 清理收据

- 浏览器临时视口已 reset，验收标签页已 finalize，未保留给用户。
- 本轮启动的 Next 服务已停止。
- 停止后 `lsof` 复核：`3000 CLEAR`、`3101 CLEAR`。
- 结束时的 10 个 UI SHA 与开始及开发报告一致；`git diff --check` exit 0；任务诊断为 `TASK_DOCTOR_OK | tasks=108 | full_history_validated=true`。

### 8.2 错题自检

- 工作树在审核前已含大量项目既有 dirty/untracked 文件；本轮用明确 10 文件 SHA 的前后双重复算隔离该风险，没有将整个工作树误称为 clean。
- 不继承开发报告的浏览器数值；对比度、44px、计数、主题、导航、锚点、HTTP、console 和资源均取得本轮独立收据。
- 最初尝试等待 `networkidle` 时，当前浏览器接口明确不支持该 wait state；随后按接口支持的 `domcontentloaded` 和目标 DOM/computed state 继续，没有把该工具差异隐藏或冒充页面失败。
- 清空关键词时，一次空字符串 `fill` 未改变受控输入值；改用筛选 dialog 内唯一“清除筛选”控件恢复 12 条，再重新选择 no-more 独立取证，没有将失效交互记为 12 条通过。
- 报告仅给出测试判定，未 ack 开发任务，未替代设计部独立核验或统筹部最终核收。

## 9. 结论

当前被测快照的 `P0=0、P1=0`，四项原 P1 及其直接回归均已闭合，本测试部任务唯一结论为 `PASS`。统筹部仍需合并设计部的独立核验结论，再决定是否 ACK `TASK-20260803-CD4228` 及前序公开前端任务。
