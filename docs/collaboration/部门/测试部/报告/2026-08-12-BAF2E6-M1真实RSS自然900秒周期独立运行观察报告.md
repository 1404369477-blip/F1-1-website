---
type: audit_report
department: 测试部
target: "TASK-20260812-BAF2E6 / M1 RSS StartInterval=900 natural cycle"
status: final
date: 2026-08-12
related_task: TASK-20260812-BAF2E6
decision: pass
severity_count: { P0: 0, P1: 0, P2: 1 }
tags: [RSS-REAL-001, M1, LaunchAgent, StartInterval-900, natural-run, pending-review, public-zero-drift]
summary: "PASS。测试部在0手动触发、0修复、0DB/文件写入条件下观察到LaunchAgent自然runs由1增至2，唯一新slot 1984984已终态succeeded/OK、last exit0、externalCalls1、HTTP200、50/20/new0/updated0/duplicate20；source保持enabled/epoch4，job保持loaded且周期进程已退出，private integrity ok、candidate仍20条pending_review且人工字段非空计数均0；公开DB、四页面及三项服务零漂移。当前第一版持续运行通过一个自然900秒周期。P2仅为既有非空人工字段遇真实更新仍缺动态样本；本次重复20条未形成更新样本。"
---

# M1 真实 RSS 自然 900 秒周期独立运行观察报告

## 1. 结论

**PASS；P0=0，P1=0，P2=1。**

在无人操作条件下，`com.f1plus1.rss-collector`按`StartInterval=900`自然触发一次：LaunchAgent `runs`从`1`增至`2`，唯一新增DB slot从`1984983`推进到`1984984`。该进程已安全结束，`last exit code=0`；新run为`succeeded/OK`、`externalCalls=1`、HTTP `200`，候选与公开面均无漂移。

因此，**当前第一版持续运行通过一个自然900秒周期验收**。结论只覆盖本次自然周期，不外推未来每个周期、未来网络可用性或尚无样本的真实内容更新路径。测试结论回统筹部，由统筹部决定是否继续常驻；本任务没有执行停采、修复或发布动作。

## 2. 范围与禁止项履行

本轮只执行以下只读动作：

- SSH读取LaunchAgent状态；
- SQLite只读回读private DB；
- 读取collector stdout/stderr尾部最多5行；
- 读取公开DB SHA/stat；
- GET既有local/public home/detail；
- 读取三项既有公开LaunchAgent状态。

未执行wrapper、collector、resume、stop、bootstrap、kickstart或load；未修改DB、文件、日志、plist、release、source或LaunchAgent；未重试RSS请求；未执行代码、测试脚本、清理、Admin或发布操作。

## 3. 基线

基线时间：`2026-08-11T21:59:47Z`至`22:00:30Z`。

### 3.1 LaunchAgent

- service可读取，说明job已loaded；
- `state=not running`、`active count=0`，符合间隔任务的周期空闲态；
- `runs=1`；
- `last exit code=0`；
- `run interval=900 seconds`；
- `properties=runatload | inferred program`。

### 3.2 private DB

- run总数：`4`；
- max slot：`1984983`；
- running run：`0`；
- candidate总数：`20`；
- source：`enabled=1 / stop_epoch=4 / last_reason_code=OK`；
- 最新RunAtLoad：slot `1984983`，`succeeded/OK/HTTP200`，`50/20/new0/updated0/duplicate20`。

### 3.3 公开DB

- SHA-256：`949c78d505e4c032d2495174deaf62d24f9d99b76284ad7ba6fb29a5ac83bb50`；
- size `737280`、inode `24546198`、mode `0600`、UID `501`、单硬链接。

## 4. 自然触发时间线

测试使用50秒间隔，只读轮询；每次等待均小于任务上限60秒，总观察时长约6分钟，小于18分钟上限。

| 轮询 | UTC时间 | LaunchAgent | private DB |
|---:|---|---|---|
| 1 | `22:00:30Z` | `runs=1 / active=0 / lastExit=0` | `total=4 / maxSlot=1984983 / running=0` |
| 2 | `22:01:20Z` | 同上 | 同上 |
| 3 | `22:02:11Z` | 同上 | 同上 |
| 4 | `22:03:01Z` | 同上 | 同上 |
| 5 | `22:03:51Z` | 同上 | 同上 |
| 6 | `22:04:42Z` | 同上 | 同上 |
| 7 | `22:05:32Z` | `runs=2 / active=0 / lastExit=0` | 自然进程已结束，进入终态回读 |

新run的DB时间为：

- started：`2026-08-11T22:05:13.589Z`；
- finished：`2026-08-11T22:05:14.349Z`。

该时间位于两次只读轮询之间，LaunchAgent的runs计数和DB slot同时单调增加，没有测试侧命令触发采集。

## 5. 唯一新run终态

终态回读结果：

| 字段 | 实值 |
|---|---|
| run ID | `rss-run-1984984` |
| slot | `1984984` |
| status / reason / next action | `succeeded / OK / next_slot` |
| LaunchAgent | loaded、周期进程已结束、`runs=2`、`last exit=0` |
| externalCalls | `1` |
| HTTP | `200` |
| validator | `modified / unknown` |
| response SHA-256 | `09754466c27b4a1cdac5af9f6b5817fc716c0db08814d3a6f0dde3e5d3017d7f` |
| response bytes | `67818` |
| item / selected | `50 / 20` |
| new / updated / duplicate | `0 / 0 / 20` |
| finished | 是 |

collector stdout尾部恰有RunAtLoad slot `1984983`和自然slot `1984984`两条脱敏JSON收据；自然slot收据与DB逐字段一致。stderr尾部为空。没有第二条新slot或第二次真实请求。

## 6. source、candidate与人工字段

自然周期完成后的private DB：

- `PRAGMA integrity_check=ok`；
- run总数从`4`增至`5`，max slot从`1984983`增至`1984984`，running run为`0`；
- source保持`enabled=1 / stop_epoch=4 / last_reason_code=OK`；
- `last_attempt_at`与`last_success_at`均更新为`2026-08-11T22:05:14.349Z`；
- candidate总数仍为`20`，全部`pending_review`；
- 20条候选revision仍为`1..1`；
- `editor_title`、`editor_excerpt`、`editor_notes`、`editor_based_on_source_revision`非空计数均为`0`。

本次20条全部判为duplicate，未创建、未更新候选，人工字段与review状态保持不变。

## 7. 公开面零漂移

### 7.1 公开DB

终态SHA/stat与基线逐字段一致：

- SHA-256：`949c78d505e4c032d2495174deaf62d24f9d99b76284ad7ba6fb29a5ac83bb50`；
- size `737280`、inode `24546198`、mode `0600`、UID `501`、单硬链接。

### 7.2 页面

- local home：HTTP `200`；
- local固定detail：HTTP `200`；
- public home：HTTP `200`；
- public固定detail：HTTP `200`；
- 脱敏public origin SHA-256：`a472476b3947308e89b613c9c82b0f91c3c72cb9c4cbc9d82301e6573e075b9f`，与基线一致。

### 7.3 三项公开服务

- `com.f1plus1.public-beta`：running，PID `52089`；
- `com.f1plus1.quick-tunnel`：running，PID `53446`，last exit维持既有`1`；
- `com.f1plus1.receipt-refresh`：loaded、周期空闲、last exit `0`。

未发现公开DB、页面或服务漂移。

## 8. 只读回读工具观察

自然进程结束边界后，系统`sqlite3 -readonly <private-db>`两次返回`unable to open database file (14)`；同时`stat`与`shasum`仍可读取该regular file，文件为mode `0600`、UID `501`、单链接、size `86016`，且没有`-wal/-shm`文件。

为保持零写入，本轮没有用普通读写连接重试或调用产品status入口。终态改用SQLite URI `mode=ro&immutable=1`读取已结束且无sidecar的主文件，成功得到`integrity_check=ok`及上述完整终态。该现象没有影响自然collector运行、LaunchAgent退出或DB内容完整性；普通`-readonly`失败的底层原因本任务未诊断，记录为观察工具边界，不据此判定产品缺陷。

## 9. Findings与Unknown

### P0

无。

### P1

无。任务列明的未触发、不可判定run、source停用、job卸载、候选/人工字段漂移和公开面漂移均未发生。

### P2-01：既有非空人工字段的真实更新保护仍无动态样本

当前20条候选的四个人工字段均为空；本自然周期全部为duplicate，`updated_count=0`。因此，本轮证明了重复采集不改变当前候选和人工字段，却无法动态证明已有非空人工字段在`updated_count>0`的真实更新中保持。状态：**Unknown / P2，当前自然周期PASS不受阻断**。

## 10. 未验证与错题自检

### 未验证

- 第二个及后续900秒自然周期的持续稳定性；
- 上游未来网络、HTTP状态及内容变化；
- `updated_count>0`且候选已有非空人工字段时的动态保持；
- 普通`sqlite3 -readonly`在本次自然进程后返回error 14的底层成因。

### 错题自检

- 没有运行wrapper、collector或任何手动触发命令；
- 没有执行resume、stop、bootstrap、kickstart、load或修复；
- 没有修改DB、文件、服务、日志或收据；
- 每次等待为50秒，总观察时间未超过18分钟；
- 网络结果成功后没有重试；
- 没有把单个自然周期扩大为长期稳定性已全面证明；
- 没有把duplicate路径扩大为真实updated路径的人工字段保护已证明。

TASK_STATE_OK
