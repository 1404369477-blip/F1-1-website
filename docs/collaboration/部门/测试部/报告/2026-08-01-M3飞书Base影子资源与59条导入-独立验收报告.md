---
type: audit_report
department: 测试部
target: F1+1 信源库｜M3影子（真实 Base；只读验收）
status: final
date: 2026-08-01
related_task: TASK-20260801-57E98D
decision: fail
tags: [M3, Feishu-CLI, shadow-base, read-only-acceptance]
summary: 本地离线载荷和统筹部脱敏鉴权收据通过静态复核；当前受限环境以用户身份执行非提权 lark-cli Base 读取时因 macOS Keychain 未初始化而失败。按本任务失败路径及最新指令，不再请求或执行提权命令，也不以鉴权收据推断目标 Base 的资源状态，因此未能独立现场验证两表、视图、字段、表单和59条真实记录，结论为 FAIL。
---

# M3 飞书 Base 影子资源与 59 条导入独立验收

## 审核范围与只读边界

- 任务：`TASK-20260801-57E98D`；已从测试部收件箱领取。
- 目标：标题为 `F1+1 信源库｜M3影子` 的真实 Base，核对两张表、三张业务视图、一张手机表单、33/9 字段、表单可见性/分享状态及 59 条影子记录。
- 参照：任务 JSON、产品部 M3 执行包、`data/m3-base-shadow-import-v0/` 离线载荷、统筹部脱敏 CLI 鉴权收据。
- 操作边界：没有使用浏览器，没有执行任何飞书写入、删除、分享或修复；没有修改被审产品文档；没有把 token、open_id、appId 或原始 auth JSON 写入仓库。
- 当前环境限制：非提权 `/opt/homebrew/bin/lark-cli` 的用户身份 Base 读取返回 `keychain Get failed: keychain not initialized`。收到“不要等待新的工具/系统审批、不得执行需要提升权限的操作”的指令后，未再发起提权命令。

## 结论

`decision=fail`。

任务验收出口要求对真实 Base 做现场结构和记录级证据闭环。现有脱敏收据只证明某一时间点的 CLI 用户身份、token 有效和租户级 Base scope；收据明确没有访问任何具体 Base。静态载荷通过不能替代真实资源读回，故不能将本轮判为 PASS。FAIL 仅表示证据不可用，本轮没有对上游资源状态作出反向断言；未执行修复。

## 已验证

### 1. 任务与授权边界

- 测试部收件箱仅列本任务为当前在办，任务 JSON 的验收出口、失败路径、`user_confirmed` 授权范围和“不切换真值/provider/Collector/发布”边界已读取。
- 任务要求仅使用飞书 CLI 用户身份、只读验收；本轮没有切换 bot，也没有访问浏览器或真实资源写接口。

### 2. 统筹部脱敏鉴权收据

- `lark-cli 1.0.68`、`identity=user`、`verified=true`、用户 token 状态为 `valid`，并列出 Base 相关租户级 scope。
- 收据自身的证据边界已核对：它不证明目标 Base 存在、资源级 ACL、具体表/视图/表单/记录状态，也不证明 Base API 实读或写入已经成功。
- 当前本地可见的统筹部收据是鉴权收据；没有找到目标 Base 结构或记录读回的脱敏收据。因此不把鉴权摘要当作 Base 回读结果。

### 3. 离线载荷静态复核

以下均为本地文件自洽性检查，不代表真实 Base 已匹配：

- 主信源字段定义：33 个字段；手机捕获字段定义：9 个字段。
- 批量载荷：59 行、每行 33 列；顶层字段数组与行宽一致。
- `source_id` 59 个且唯一；`canonical_url` 59 个且唯一；`enabled=false` 共 59 条。
- `migration_batch_id` 仅有固定批次 `M3-20260801-X59-01`，无本地越批值。
- 离线字段定义名称与批量载荷字段数组一致；未发现 token、open_id、appId 或认证原始 JSON。
- 本地载荷 SHA-256（用于可重复核对）：主信源字段 `b743abaee8ef68db586148e58afdcc9dec2b6fe07bab61ad7c779327d4076870`；手机捕获字段 `378b2c2b0c1369198dfa84e8961d7ad1a4aa247238a1389c213dbf7dc37ea6b6`；批量载荷 `e73b8d6b8a9b1a018dc7d30c90bfe3111b10caeb6fee28486edf27f176a05de5`。

### 4. 当前 CLI 阻断证据

在不提权的受限环境中，以 `--as user` 运行目标 Base 标题解析的只读命令，CLI 返回 `ok=false`，错误为 `keychain Get failed: keychain not initialized`。因此在当前允许路径中未获得可用于后续读取的 Base token，也没有执行 table/view/form/record 读取；任何不符合该执行边界的结果均未纳入本报告证据。

## 补充验收口径与现场判定

以下是本次补充要求的逐项合同。产品执行包中的名称、过滤器、题目定义和字段定义只能作为待比对基线；由于真实 Base 读取被 Keychain 阻断，所有标注“现场状态”的项目仍为 `unknown`。

### 5.1 手机捕获表记录与表单提交

- 现场硬门槛：`手机捕获` 表记录数必须严格为 `0`，并且必须有证据证明没有发生真实手机表单提交。
- 任意一条捕获记录、任意真实提交痕迹或无法排除提交的状态，都应使该项 FAIL；不能用“表单未分享”的描述代替记录数检查。
- 本轮没有读取捕获表记录列表、表单提交历史或表单事件；记录数和“无真实提交”均未验证。

### 5.2 三个 grid 视图逐项合同

每个视图都必须逐个读回并同时核对精确名称、`type=grid`、过滤器表达式和 visible field 顺序：

1. **`M3｜影子59对账`**（主信源）
   - filter：`logic=and`，唯一条件为 `migration_batch_id == M3-20260801-X59-01`。
   - visible fields 顺序：`source_id`、`canonical_url`、`raw_url`、`entity_type`、`content_focus`、`priority`、`identity_status`、`monitorability`、`lifecycle_status`、`enabled`、`migration_batch_id`。
2. **`维护｜门禁待复核`**（主信源）
   - filter：`logic=or`，条件依次为 `enabled == false`、`dedup_status disjoint [unique]`、`adapter_status disjoint [ready]`、`adapter_authorization_status disjoint [valid]`、`monitorability disjoint [monitorable]`、`source_stop_status disjoint [clear]`。
   - visible fields 顺序：`source_id`、`priority`、`normalization_status`、`dedup_status`、`identity_status`、`relevance_status`、`monitorability`、`adapter_status`、`adapter_authorization_status`、`collection_onboarding_status`、`enabled`、`source_stop_status`。
3. **`手机捕获｜待处理`**（手机捕获）
   - filter：`logic=or`，条件依次为 `normalization_status empty`、`normalization_status intersects [pending, invalid, needs_review]`、`dedup_status empty`、`dedup_status intersects [pending, needs_review]`。
   - visible fields 顺序：`capture_id`、`raw_url`、`capture_note`、`captured_at`、`normalization_status`、`normalization_error`、`dedup_status`、`dedup_match_source_id`、`source_id`。

上述三个视图的真实名称、`type`、filter 和 visible field 顺序本轮均未现场读回，不能以产品执行包中的合同文本代替验收结果。

### 5.3 手机表单问题、隐藏字段与底层字段

- 表单精确名称：`新增信源（手机）`。
- 表单说明必须精确为：`提交后只进入候选处理，不代表信源已验证、启用或可公开。`。
- 问题集合必须严格按以下顺序且只有两题：
  1. `raw_url`：描述 `粘贴 X 账号页或帖子分享链接`，`required=true`。
  2. `capture_note`：描述 `可选：简要说明为何希望加入`，`required=false`。
- 五个内部字段必须存在于底层字段定义并在表单中 `visible=false`：`normalization_status`、`normalization_error`、`dedup_status`、`dedup_match_source_id`、`source_id`。
- `capture_id` 与 `captured_at` 不得出现在问题集合；它们仍必须保留在捕获表底层字段中，底层字段总数仍严格为 9。
- 表单必须保持 `shared=false`。本轮没有读取表单详情或问题列表，因此题目顺序、描述、required、五个隐藏字段、`capture_id/captured_at` 排除、底层 9 字段和 `shared=false` 均未验证。

### 5.4 本地配置与业务门禁边界

- 本地静态检查显示 `app/` 仍只有地基占位说明，没有运行时代码或配置；M3 产品包、Spec、进度和 accepted 路线均保留影子态，未授权 Base 业务真值切换、`active_source_config_provider`、`source_config_epoch`、Collector、采集、同步或发布门禁变化。
- 本测试任务没有修改这些本地配置或上游文档，也没有执行任何业务链路切换。该结论仅覆盖本地仓库边界；它不证明真实 Base 或外部运行环境没有变化。
- Base ACL 不在当前可读证据范围内。CLI 无法读取 ACL 时，ACL 必须保留为未验证的残余风险，不能报告为已关闭。

## 未验证 / 未覆盖

以下项目必须由目标 Base 的用户身份现场读回才能闭环，本轮均标为 `unknown`：

1. 目标标题是否唯一解析、运行时 Base token SHA-256 是否与任务要求相符。
2. 真实 Base 是否恰有两张目标表，且表名、table_id 与任务合同一致。
3. 两张表是否分别为 33/9 字段，字段顺序、类型、枚举、必填/默认值是否与离线定义逐项一致。
4. 三张 grid 视图是否逐个满足精确名称、`type=grid`、filter 表达式和 visible field 顺序合同。
5. 手机捕获表真实记录数是否严格为 0，是否无真实手机表单提交。
6. 手机表单是否精确满足名称、说明、两题顺序/描述/required，五个内部字段 `visible=false`，`capture_id/captured_at` 不在问题集合且底层仍 9 字段，以及 `shared=false`。
7. 主表真实总计是否为 59，本批 `M3-20260801-X59-01` 是否为 59。
8. 真实记录的 `record_id`、`source_id`、`canonical_url` 是否分别满足 59 唯一；是否有重复记录、重复 source、越批记录或其他批次。
9. 真实 59 行的 33 个字段是否与离线载荷逐字段、逐行一致。
10. 真实记录是否全部 `enabled=false`，以及关键保守状态、停止状态、授权/适配器/采集接入状态是否符合合同。
11. 真实 Base ACL 是否可读且符合预期；若 CLI 无法读取，ACL 继续是未验证的残余风险。
12. 是否存在任何 Base 真值、`active_source_config_provider`、`source_config_epoch`、Collector、采集、同步或发布门禁变化。

## 必测失败路径结果

- Keychain/CLI 不可用：已触发停止条件；没有改用 bot、浏览器或提权路径。
- 未取得目标 Base 现场数据：不尝试补写、删除、修复、重试或推断资源状态。
- 手机捕获表记录非 0、出现真实表单提交、任一 grid 视图的名称/type/filter/visible 顺序不符、或表单题目/隐藏字段/shared 状态不符，均属于立即 FAIL 条件；本轮无法读取，保持未验证。
- Base ACL 无法由 CLI 读取时，保留为残余风险，不得写成“权限门禁已关闭”或“ACL 已通过”。
- 任何 Base 真值、provider、epoch、Collector、采集、同步或发布门禁变化都超出本轮授权；本地静态边界通过不替代外部配置读回。
- 记录级重复、越批、字段不一致、`enabled=true`：因没有现场数据，保持未验证；不能写成“无此问题”。

## 错题自检

- 没有把租户级 `token valid` 或 Base scope 误写成目标 Base 存在、资源 ACL 或 API 实读成功。
- 没有把本地 59×33 静态通过误写成真实 Base 的 59/59 对账通过。
- 没有把产品包中的视图/表单合同误写成真实 view/form 读回；手机捕获表 0 记录和无真实提交仍明确为未验证。
- 没有把本地未发生配置改动误写成真实 Base ACL 或外部运行门禁已关闭；ACL 无法读取时保留残余风险。
- 没有把任务 JSON 中“统筹部已回读”的描述替代测试部独立现场读回；本轮明确标记“未独立现场读取”。
- 没有在 CLI 阻断后切换 bot、浏览器或提权命令。
- 没有写入真实飞书资源，没有修改上游产出，没有保存敏感认证原文。
- `FAIL` 仅表示本轮验收证据链未闭环；不声称真实 Base 必然存在结构或数据缺陷。

## 后续复验入口

在不改变任务范围的前提下，只有当受限环境能够以 `--as user` 运行只读 CLI 且 Keychain 阻断消除后，才可重新领取/重开独立现场验收；届时应从标题解析开始，逐页读取结构、视图、表单和记录并保存脱敏计数与逐字段差异，仍不得自动修复。

## TASK_STATE_OK

本报告已独立提交，`decision=fail`、已验证/未验证清单和错题自检均已写明；任务状态随后按脚本协议标记为 `complete`，并返回 `TASK_STATE_OK`。
