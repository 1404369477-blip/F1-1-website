---
type: audit_report
department: 测试部
target: TASK-20260810-875B6C冻结候选
status: final
date: 2026-08-10
related_task: TASK-20260810-A3293F
decision: fail
tags: [M5, source-management, HTTP, csrf, recovery]
summary: 候选与正确x-f1-csrf-token静态门通过；唯一3019实例完成绝大部分HTTP、CSRF、幂等、恢复与冲突矩阵，最终list_after后的客户端overlay origin断言误用local_overlay而合同为local_synthetic，触发首错停止。未建立产品缺陷，但destroy收口路径未运行，P0=0/P1=1/P2=0。
p0: 0
p1: 1
p2: 0
---

# TASK-20260810-A3293F 修复后独立 HTTP 恢复矩阵报告

## 结论

`FAIL`，P0=0、P1=1、P2=0。37CA8F历史FAIL保持不变。

独立客户端静态门确认只使用精确 `x-f1-csrf-token`，canonical body hash、method/path绑定和断言状态均在启动前检查通过。唯一实例运行到最终 `list_after=200` 后触发 `OVERLAY_INVARIANT`。根因是客户端把本地来源 `meta.origin` 断言为 `local_overlay`；冻结产品类型与Repository精确值为 `local_synthetic`。SQLite独立回读确认本地行确实存在且为 retired/stopped/disabled。该首错属于测试断言错误，没有建立产品缺陷；合同禁止本任务边跑边改与重跑，因此 session destroy/销毁后401未执行，不能判PASS。

## 已验证

- 报告、manifest、security、raw-target test、server/no-egress/repository、closed DB SHA全部命中。
- 独立59+0临时profile，最终 integrity=ok、59 baseline保持只读、1 local synthetic存在；operation receipt=5、audit event=5。
- health 200/externalCalls=0；未认证401；session create/get/refresh；CSRF重放、错path/method/body均403且reason精确；add 202与response-loss replay逐字等价；operation/get；validate 200；stale 412；activate 200与response-loss replay等价；identity conflict 409；stop/retire 200；requeue conflict 409；source/operation 404；最终list 200；全部响应no-store。
- 正式DB、receipt与冻结候选零漂移；实例停止、3019无监听。

## 未验证 / 错题自检

未验证：session destroy 204及destroy后401，因首错停止。closed receipt仅做冻结文件、自哈希字段和正式DB SHA交叉核对，未重跑生成命令。

- 未将客户端 origin 枚举写错归为产品缺陷。
- 未修 harness 后重跑；实例数严格为1。
- 未跑全量/build/typecheck；未修改产品、正式DB、依赖或外部资源。
- 原始逐步账本见 [raw-http-receipt.json](TASK-20260810-A3293F-evidence/raw-http-receipt.json)，机器汇总见 [evidence-manifest.json](TASK-20260810-A3293F-evidence/evidence-manifest.json)。

TASK_STATE_OK
