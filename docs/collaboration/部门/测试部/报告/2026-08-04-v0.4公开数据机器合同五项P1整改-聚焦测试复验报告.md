---
type: audit_report
department: 测试部
target: TASK-20260803-D53BF3；data/mvp-contract-v0.4-public-synthetic
status: final
date: 2026-08-04
related_task: TASK-20260804-00972D
decision: pass
tags: [M4, public-synthetic, v0.4, deterministic-generation, single-source, fault-injection, read-only]
summary: 两套隔离副本各连续两次生成与验证、9项SHA、唯一data-native输入驱动、12套批准发布图、DTO exact、v0.3与M3冻结基线及32类故障注入全部通过；正式包字节未变且无缓存或临时残留。P0=0、P1=0、P2=1，依据门禁本轮decision=pass。
---

# v0.4 公开数据机器合同五项 P1 整改聚焦测试复验报告

## 1. 唯一结论

`decision=PASS`。

本轮严格只读复验 `TASK-20260803-D53BF3` 的 final 报告和 9 项正式机器产物，并以首轮测试 `TASK-20260803-FB0D41`、首轮安全 `TASK-20260803-8C85B4` 两份 `decision=fail` 报告作为缺陷回归输入。

独立复验结果：

| 级别 | 数量 | 结论 |
|---|---:|---|
| P0 | 0 | 未发现绕过批准发布链、混入 M3、改写冻结基线、越界输出或外部 I/O |
| P1 | 0 | 首轮 5 项 P1 均已关闭，指定旧故障和新增攻击路径全部 fail closed |
| P2 | 1 | 宿主仍没有原生 Draft 2020-12 JSON Schema 引擎；当前 schema 使用关键字由项目 validator、独立结构检查和反向注入覆盖 |
| 唯一判定 | PASS | 满足 `P0=0 且 P1=0` 门禁 |

本报告只放行这 9 项精确 SHA 对应的数据机器候选进入后续开发接线门禁。它不证明 SQLite、migration/seed、Repository/API、前端接线、真实外部平台或部署已经实现或通过。

## 2. 审核范围、验证层级与写入边界

### 2.1 审核输入

- 数据部 final 报告：`docs/collaboration/部门/数据部/报告/2026-08-04-v0.4公开数据机器合同五项P1整改报告.md`；
- 9 项正式产物：`data/mvp-contract-v0.4-public-synthetic/`；
- 首轮测试 FAIL：`docs/collaboration/部门/测试部/报告/2026-08-03-v0.4公开数据机器合同与12条发布图-独立测试报告.md`；
- 首轮安全 FAIL：`docs/collaboration/部门/安全部/报告/2026-08-03-v0.4公开数据机器合同独立安全审查.md`；
- accepted 合同：`ADR-M4-PUBLIC-READ-001`；
- v0.3 冻结 manifest 声明的 11 项 artifact；
- M3 59×39 Source 投影、seed validator 和权威 hash。

### 2.2 独立方法

1. 先读取正式 9 项字节并计算 SHA-256，冻结测试快照；
2. 创建两套互相独立的 Python `TemporaryDirectory` 最小项目副本：第一套写入一份故意修改过的 `app/src/features/stories/demo-data.ts`，第二套完全不含 `app/`；
3. 每套连续运行两次 generator、两次 validator，并比较每次生成后的 9 项 SHA；
4. 在第三套副本修改唯一 `stories.input.json`，连续生成两次，检查变更是否确定性进入对应领域输出；
5. 独立解析生成器 AST 和 12 条 input，检查完整 `STORIES` 副本、`UI_INPUT` 运行依赖和内嵌 12 标题；
6. 独立重算 12 套 FK/hash/fence/approved→published 图、DTO 字段集合、v0.3 11 项和 M3 projection；
7. 每个负向用例都从干净副本开始；需要时同步重算非目标 fixture graph、artifact、manifest 和 ledger 外层收据，使 validator 到达目标语义门；
8. symlink、非 regular 和越界 sentinel 探针只在临时目录执行；
9. 结束后重新计算正式 9 项 SHA，并检查正式包没有 `__pycache__`、`.pyc` 或 `.*.tmp`。

正式项目内仅新增本测试部 final 报告并更新任务状态。未修改 `data/`、`app/`、Spec、accepted ADR、design、lockfile、SQLite 或外部资源。

## 3. 正式 9 项 SHA-256 独立收据

| 产物 | 独立 SHA-256 | 与数据部 final |
|---|---|---|
| `stories.input.json` | `42188f0b621bdfa9333fbe8b7541b284faf5e82ed655369b4a2feda383a4dcb1` | 一致 |
| `schema.json` | `4b2d1abec6c6315c92fd2ae70c6422b894e380850a9b9408d35a385c777ca11f` | 一致 |
| `fixtures.public-synthetic.json` | `c7d9d88b170214b283a214625d6fd2028fd8eb3a6a2701c556cb2364eb9941e4` | 一致 |
| `public-dto-mapping.json` | `285bfe529b492c81fe883097615ebae34effbe6a9123a1bfd0184973f3fe7be2` | 一致 |
| `profile-boundary.json` | `c9a83ddc7b99386181bcde3b9286f3c6d6fd83acff599a09a8d1a4639e025e90` | 一致 |
| `manifest.json` | `3b296868dc0c0000fb94856b334ff7d1f698e3e80d4bb02e7062142dc1a0e554` | 一致 |
| `profile-ledger.json` | `1f7719490a18a49842427907b53c3dbde5813709a2ad611f7cfaca891880caf1` | 一致 |
| `generate_public_fixture.py` | `34ecfa83fec1f89a22d877e554c4ce5c4d11c1bad6b7f09f123fea3ede1cb81a` | 一致 |
| `validate_public_fixture.py` | `058be83bdded7f5c60028f0a2e537c510e9386934284684fffb119d2e487360c` | 一致 |

fixture 的 `canonical-json-v1` graph hash 为：

```text
4be9f7e868a8bf21551bdcdc05d6b0d027e1a0ea43fd16dd2c7ea2b2ff9ba526
```

测试结束后再次计算 9 项 SHA，逐项与测试前完全相等，证明正式数据包没有被生成或攻击探针污染。

## 4. 双副本四次生成与唯一输入证明

### 4.1 两套副本各连续生成两次

| 副本 | `app/demo-data.ts` 状态 | generator #1 | validator #1 | generator #2 | validator #2 | 两次 9 项 SHA | 与正式 9 项 SHA |
|---|---|---:|---:|---:|---:|---:|---:|
| A | 存在，但内容被故意改成无关占位 | exit 0 | PASS | exit 0 | PASS | 全等 | 全等 |
| B | 整个 `app/` 不存在 | exit 0 | PASS | exit 0 | PASS | 全等 | 全等 |

四次 generator 都返回同一结果：

```json
{"contract_version":"mvp-local-v0.4","graph_hash":"4be9f7e868a8bf21551bdcdc05d6b0d027e1a0ea43fd16dd2c7ea2b2ff9ba526","public_ids":12,"result":"GENERATED"}
```

四次 validator 都返回 `result=PASS`，并逐次报告 1 Source、12 CapturedItem/Content/Summary/Bundle/Decision/Publication/Projection、10 MediaCandidate、12 个合法公开 ID、v0.3 11 项、M3 59×39、`external_calls=0`、`writes_to_base=false`。

该结果独立证明：修改或删除 `app/src/features/stories/demo-data.ts` 都不影响再生成，旧前端文件只保留一次性 provenance，不是运行时输入。

### 4.2 修改唯一 input 后确定性改变对应输出

在第三套干净副本中，只给第一条 `stories.input.json` 的 `title` 追加测试标记，然后连续运行两次 generator + validator：

- 两次生成后的 9 项 SHA 完全相等；
- 两次 validator 均 PASS；
- 相对正式基线，精确改变 4 项：`stories.input.json`、`fixtures.public-synthetic.json`、`manifest.json`、`profile-ledger.json`；
- `schema.json`、`public-dto-mapping.json`、`profile-boundary.json` 和两份 Python 源码保持不变；
- 新标题精确进入对应 `Content.normalized_title` 和 `Summary.title_zh`；
- graph hash 和 ledger/manifest 绑定随领域图确定性变化。

另做“只改 input、重绑全部外层收据但不重新生成领域图”的反向探针，validator 以 `story input Content projection drift` 拒绝。这证明 input 不只是被 hash 收据引用，validator 还逐条核对了它到领域图的实际投影。

### 4.3 源码静态单一来源检查

独立 AST/字节检查结果：

- generator 不存在名为 `STORIES` 的赋值；
- 12 条 input 标题在 generator 源码中的完整内嵌命中数为 0；
- generator 不存在 `UI_INPUT`；
- `demo-data.ts` 只在一次性 provenance 常量出现 1 次，且 `runtime_dependency=false`；
- 正式包内 `stories*.json` 精确只有 `stories.input.json` 一份；
- 实际执行入口先调用 `load_story_input()`，生成循环使用其返回的 12 条记录。

首轮测试 P1“生成器依赖旧 app 文件且内嵌第二份 STORIES”已关闭。

## 5. Schema、计数、公开身份和完整批准发布图

### 5.1 Schema closed 检查

- fixture 根 schema 为 `additionalProperties=false`，根 `required` 全部有对应 property；
- v0.3 的 13 个领域定义全部保留；
- 13 个领域对象均为 `additionalProperties=false`，required 是 properties 子集；
- 删除 Content required、写入非法 editorial category、给 fixture 根增加未知字段三个反向用例分别被 missing-required、enum、unknown-property 门拒绝；
- 项目 validator 对当前 schema 实际使用的 `$ref/anyOf/type/const/enum/pattern/format/minLength/maxLength/minimum/minItems/maxItems/uniqueItems/items/properties/required/additionalProperties` 进行本地验证。

### 5.2 精确计数与公开身份

| 集合 | 实际 | 要求 |
|---|---:|---:|
| Source | 1 | 1 |
| CapturedItem | 12 | 12 |
| Content | 12 | 12 |
| Summary | 12 | 12 |
| MediaCandidate | 10 | 10 |
| ReleaseBundle | 12 | 12 |
| ReviewDecision | 12 | 12 |
| Publication | 12 | 12 |
| PublishedProjection | 12 | 12 |

12 个 `public_id` 唯一、全部匹配 `public-demo-[a-z0-9-]+`，并与 accepted ADR 表列的 12 个 ID 逐项、逐序一致。

### 5.3 12 套批准发布图独立重算

对每条内容独立完成以下检查：

- CapturedItem → Content、Content → Summary、Content → Bundle、Bundle → Decision、Content → Publication、Publication → Projection 的 FK 唯一且一致；
- Source 只为 `src-active`，不属于 M3 的 59 个 Source ID；
- Content、Summary、payload、bundle、decision 的 `canonical-json-v1` hash 从落盘输入重新计算一致；
- `decision_hash_input` 精确由顶层 Decision 身份、批准 bundle、版本、决定、canonical rule 和五个 fence 构造；
- Bundle immutable/approved，Decision approved，Publication published 且 `emergency_stop=false`，Projection published；
- Decision 与 Publication 的 approved bundle hash 精确等于 Bundle hash；
- Publication 的 approved Content/Summary version hash 与对应实体一致；
- Projection 的 bundle、published version hash 与 Publication 一致；
- Bundle、Decision、Publication、payload 四处的五个 fence 均逐项为 `[1,1,1,1]`；
- 12 条链全部通过，未出现未批准 Projection。

## 6. Public DTO exact 复验

`public-dto-mapping.json` 复验结果：

- `feed_item` 精确 17 个 accepted 字段；
- `detail_extension` 精确 3 个 accepted 字段：`leadZh/bodyZh/keyPointsZh`；
- `page_size=12`；
- 每项 mapping 精确由 `kind/owner/json_pointer/transform` 四个键构成；
- 所有公开 pointer 均不含 mapping 自身声明的 blocked private token；
- validator 将完整 mapping 与内置 accepted machine allowlist 做整体相等比较。

把 `publicId` 改指 `ReleaseBundle.canonical_payload.original_url`，并同步重算 DTO artifact、manifest、ledger 外层收据后，validator 以 `DTO mapping must equal the closed machine allowlist` 拒绝。删除 `source.byline` 并同步重算外层收据同样被拒绝。首轮安全 P1“只校验 DTO key set”已关闭。

## 7. v0.3 与 M3 冻结基线

独立重算结果：

- v0.3 manifest SHA-256：`8a371102c28eaa557d33df8672338cb3aba7b7ae1fe75c0c357c8edaa23b2cde`；
- v0.3 manifest 声明的 11/11 artifact 实际字节 SHA 全部一致；
- M3 seed 为 59 行 × 39 字段；
- 59 条全部 `enabled=false`；
- 对 `{fields, rows: source_id Unicode code point 升序}` 执行 `canonical-json-v1`，得到 `e7a8312c70a9a49922aedb3cfbeaa190db8f5dce8d4ab45db1570748fc329f17`；
- `src-active` 不在 M3 59 个 Source ID 中；
- M3 validator `--repeat 2` 返回 PASS、`independent_reload=true`。

旧 artifact 单独漂移、以及协调改写旧 schema + 旧 manifest + successor 声明两种探针均被拒绝。

## 8. 32 类故障注入收据

### 8.1 首轮测试 12 类回归

| # | 注入 | 独立结果 |
|---:|---|---|
| 1 | 删除一个 CapturedItem | reject：`below minItems` |
| 2 | 制造重复 capture ID | reject：`capture_id must be unique` |
| 3 | editorial category 改为 `rumour` | reject：`enum mismatch` |
| 4 | Content version hash 改错 | reject：`content hash mismatch` |
| 5 | Publication policy fence 改为 2 | reject：`fence mismatch policy_epoch` |
| 6 | ledger contents count 改为 11 | reject：`profile ledger must be closed and exact` |
| 7 | v0.3 schema 字节漂移 | reject：`v0.3 frozen artifact drift` |
| 8 | 协调重打包 v0.3 schema/manifest/successor 声明 | reject：`manifest v0.3 receipt shape drift` |
| 9 | 删除 Content required 字段 | reject：`missing required` |
| 10 | fixture 根增加未知字段 | reject：`unknown properties` |
| 11 | approved Decision 改 rejected 并重算 decision hash | reject：`decision approval binding mismatch` |
| 12 | DTO 删除 `source.byline` | reject：`DTO mapping must equal the closed machine allowlist` |

### 8.2 Decision input 五个 fence 逐项分裂

分别只把 `decision_hash_input` 内的下列 fence 改为 2，保留顶层为 1，并重算 decision hash、fixture graph、artifact、manifest 和 ledger 收据：

- `source_config_epoch`；
- `source_safety_epoch`；
- `authorization_version`；
- `policy_epoch`；
- `recovery_epoch`。

5/5 均以 `decision hash input does not exactly match top-level approval/fences` 拒绝。首轮安全 P1“hash-input 与顶层 fence 可分裂”已关闭。

### 8.3 manifest/profile/ledger 语义重打包

以下每项都同步重算可控的外层 artifact/manifest/ledger hash：

| 注入 | 结果 |
|---|---|
| manifest `external_calls=1` | reject：manifest capability drift |
| public profile SQLite 路径改为 `/tmp/public.sqlite` | reject：profile closed exact |
| public profile Source allowlist 指向一个 M3 Source ID | reject：profile closed exact |
| `sqlite_attach_allowed=true` | reject：profile closed exact |
| 删除 `SQLITE_ATTACH_M3_SHADOW` forbidden reason-code | reject：profile closed exact |
| ledger `external_calls=1` | reject：schema const mismatch |

首轮安全 P1“manifest/profile 安全语义未封闭验证”已关闭。

### 8.4 input、DTO、FK、媒体和输出安全

| 注入 | 结果 |
|---|---|
| 修改 input 并重绑外层收据、不重生成领域图 | reject：input projection drift |
| 新增第二份 `stories.shadow.json` | generator 和 validator 均 reject |
| DTO `publicId` 重定向 internal original URL | reject：machine allowlist drift |
| CapturedItem Source FK 指向 M3 Source | reject：source FK drift |
| Bundle fence 改为 2 | reject：fence mismatch |
| Media asset ref 改成 HTTPS URL | reject：schema pattern mismatch |
| 输出 `schema.json` 预置为指向项目外 sentinel 的 symlink | generator reject；sentinel 保持 `SAFE` |
| 输出 `schema.json` 预置为目录 | generator reject |
| 整个 `data/` 父链改为 symlink | generator reject；9 项输出字节不变 |

合计 32 类独立故障结果全部为预期拒绝。每类使用全新临时副本；没有依赖前一探针留下的状态。

## 9. 静态执行面与无残留

源码 import/call 检查：

- generator 只使用标准库 `copy/hashlib/json/os/re/stat/uuid/pathlib/typing`；
- validator 只使用标准库 `datetime/hashlib/json/re/sys/pathlib/typing`；
- 两者均无 `socket/urllib/requests/httpx/aiohttp/subprocess/os.system/popen/eval/exec/compile/importlib`；
- validator 无写文件路径；
- generator 输出仅走固定 allowlist、同目录私有临时 regular file、`fsync`、`os.replace` 和目录 `fsync`。

所有 Python 执行均设置 `PYTHONDONTWRITEBYTECODE=1`。最终检查：

```text
formal_package_sha_unchanged=true
formal_artifacts=9
temporary_directory_cleaned=true
formal_leftovers(__pycache__|*.pyc|.*.tmp)=0
git_diff_check=PASS
TASK_DOCTOR_OK | tasks=114 | full_history_validated=true
```

## 10. 首轮五项 P1 关闭矩阵

| 首轮缺陷 | 本轮证据 | 状态 |
|---|---|---|
| 测试 P1：旧 app 文件只验 SHA，generator 内嵌第二份 STORIES | 修改/删除 app 均可四轮再生成；唯一 input 实际驱动输出；AST 无 STORIES/12 标题副本 | 已关闭 |
| 安全 P1：Decision hash-input 与顶层 fence 可分裂 | 五个 fence 逐项分裂、重算全部可控 hash 后 5/5 reject | 已关闭 |
| 安全 P1：manifest/profile 关键语义可重打包绕过 | capability/path/allowlist/ATTACH/forbidden/ledger 六类重打包全部 reject | 已关闭 |
| 安全 P1：DTO 映射值可重定向内部 URL | closed machine allowlist exact；internal URL redirect 重绑后 reject | 已关闭 |
| 安全 P1：输出 symlink 可越界覆写 | target symlink/目录、data 父链 symlink 均 reject；sentinel 未改 | 已关闭 |

## 11. P2、未验证与后续门禁

### P2-01：宿主没有原生 Draft 2020-12 JSON Schema 引擎

当前 Python 解释器导入 `jsonschema` 返回 `ModuleNotFoundError`。本轮没有联网安装新依赖。项目 validator 覆盖当前 schema 实际使用的关键字，并由 required/enum/additionalProperties 等反向注入提供 fail-closed 收据，因此该项维持 P2，不阻断本机器候选的 P0/P1 门禁。

### 仍未验证

- 后续应用代码中的 manifest/ledger/generator/validator root hash pin；
- `m3-shadow` 与 `public-synthetic` 两个真实 SQLite 文件、不同 inode、禁止 ATTACH 和每进程单 profile；
- migration/seed、单事务回滚、重启幂等、Repository/API/cursor/Problem JSON/DB busy；
- 前端移除静态运行分支并只消费 API DTO 后的新快照；
- 真实 Base/provider/Collector、平台授权、采集、AI、真实媒体、审核身份、外部发布、部署与自动发布。

这些项目属于后继开发、测试和安全任务，本报告不推导其通过。

## 12. 错题自检

- 没有采信数据部 `FAULTS_REJECTED=29` 作为本部门证据；本轮重新创建独立临时副本并亲自执行 32 类故障。
- 每个语义攻击都尽量重绑非目标外层收据，避免把单纯 artifact SHA mismatch 当作语义门通过。
- “修改 input 后再生成 PASS”和“修改 input 但不再生成 reject”分别验证输入驱动和 input/graph 漂移拒绝，没有混淆两种预期。
- `demo-data.ts` 在源码中仍作为一次性 provenance 字符串出现；本报告只证明它不被运行时读取，没有误报为源码完全不含该路径。
- 输出 symlink、非 regular 与父链探针都在临时目录执行；没有触碰正式项目外 sentinel。
- 没有把数据机器合同 PASS 写成 SQLite/API/UI、真实平台、部署或发布授权。
- 生成器使用 UUID 仅命名同目录私有临时文件，最终 9 项输出字节在四轮中保持确定性；没有把临时文件名的非确定性误报为正式产物不确定。
- P2 原生 schema 引擎缺失已如实保留，没有为了得出 PASS 隐去未覆盖层级。

## 13. 收口

- 本测试任务：`TASK-20260804-00972D`；
- 唯一结论：`decision=pass`；
- 等级：`P0=0 / P1=0 / P2=1`；
- 正式 9 项候选允许进入统筹核收与后续开发接线门禁；
- 本部门不 ACK 数据整改任务、不触发开发或发布。

TASK_STATE_OK
