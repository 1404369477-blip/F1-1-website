---
type: audit_report
department: 测试部
target: TASK-20260802-FFC67A / TASK-20260802-7A9C48 当前 app 快照
status: final
date: 2026-08-02
related_task: TASK-20260802-FFC67A
decision: pass
tags: [M4, VS-0, Node24, argv, cli-redaction, read-only-regression]
summary: 固定 Node24 下六组附加 argv 全部 fail closed，四类 CLI 失败均为四字段脱敏 JSON，正常 loopback start/health、完整 check、重复 migration/seed 与静态 hash 零漂移通过；P0=0、P1=0，P2=2 为既有缓存观察和任务范围外安全能力。
---

## 审核对象与边界

本报告是测试部对开发部 `TASK-20260802-7A9C48` 当前 app 快照的独立只读回归，开发报告只作为线索。只验证本任务指定的 VS-0 启动参数边界、CLI 失败脱敏、正常本地启动、完整 Node24 check、重复 migration/seed/hash 及进程清理；不把 R5、R12 或真实外部能力写成通过。

未修改 app、Spec、ADR、data、design、package-lock；未安装依赖、访问 registry、联网、调用真实飞书/Base/provider/Collector、提交表单或发布内容。为避免完整 check 生成 `.next`、`.vite`、`tsconfig.tsbuildinfo` 和 SQLite 测试副作用，完整回归在 `/private/tmp/f1plus1-ffc67a.nvUoFg` 的隔离 app 副本执行；共享工作区只做只读核对和本报告/任务状态写入。

## 结论与计数

- `decision=pass`
- `P0=0`
- `P1=0`
- `P2=2`
  - `P2-01`：当前共享 app 快照在本任务开始前已存在 `.next`、`node_modules/.vite`、`tsconfig.tsbuildinfo`（mtime 约 22:48，任务创建时间 22:55）；本轮不删除、不重建、不把它们混入受保护静态 hash。
  - `P2-02`：R5 恶意同 UID 竞争、R12 OS/系统调用级 no-egress，以及真实 Base/provider/采集/AI/媒体/发布/部署能力仍按项目合同 pending/closed，超出本任务验收范围。

## 固定环境与当前快照 hash

固定执行工具链：项目 Node `v24.18.0`、npm `11.16.0`、Node 内置 SQLite `3.53.1`、arm64。全局 Node 不作为证据。

当前快照在独立复制前读取的关键 hash：

| 文件/集合 | SHA-256 |
|---|---|
| `app/package.json`（当前 7A9C48 快照） | `cb81c6be9db1772537dfc2a53214bf03d7f92f763a701d20c68789d68d1fd452` |
| `app/package-lock.json` | `89807e33818cfb851b57da65a5675e413e5593d01d6773c3a71a6cc6ea3d85d3` |
| `app/scripts/serve.ts`（当前快照） | `21c251dd2c7875b46a076a586195477b4c5f3850f62ae14133b88dcba9713100` |
| `app/src/server/security/cli.ts` | `1126cca2471da18160b8d870bd4a1d030610324e5b0c1e1ccbf78e4c7ce42e85` |
| `app/src/server/security/log.ts` | `f69347a4d054cb4ce05f92bd333fba4b80b00dfe21108db110da12c7e41e4b00` |
| `app/src/tests/p1-cli.test.ts` | `6994365c1450998d2b1e95808a997a0dcb234242cfb9cbc2bd5533cae9cad011` |
| `app/src/tests/vs0.test.ts` | `96a8a69f61b997bf680ff95ec614aee758efa62ecaba19e3cc256753434bdeca` |
| `app/migrations/0001_local_foundation.sql` | `9c8c083b8f3c566023e9438c254d5b1c09d87430dec08f6e6905ed84b6fb3176` |
| `app/migrations/0002_source_fixture.sql` | `12a755754744689f977ac8b8d5d4443ec63cd5612aaff50eb920badf1ebfb031` |
| app static tree（排除 `.local/node_modules/.next/.vite/tsconfig.tsbuildinfo`，62 files） | `2c6132f78523d3361902d2f65c03ce927f1d2f30c304496ecb6f1ee7f0d1a706` |

开发报告中列出的 `app/package.json`/`serve.ts` 迁移前 checkpoint（`95e2…`/`ce42…`）属于整改前线索；当前 7A9C48 快照已包含 `test:p1` 与最新入口内容，最终以本轮实际读取的当前 bytes 验收。package-lock、security cli/log、VS-0 test 与 migration hash 均与冻结值一致。

## 验收一：六组 argv fail closed 与零监听

固定 Node24、隔离 app、最小环境下分别执行：

```text
npm run dev   -- --port 3101
npm run dev   -- --hostname 0.0.0.0
npm run dev   -- positional
npm run start -- --port 3101
npm run start -- --hostname 0.0.0.0
npm run start -- positional
```

六组均满足：

- 进程退出码 `1`（非零），无 timeout、signal 或残留子进程。
- stdout 为空。
- stderr 恰为一行 JSON，字段顺序及集合均为 `event/status/reasonCode/externalCalls`：

```json
{"event":"cli_failure","status":"rejected","reasonCode":"CLI_ARGUMENTS_FORBIDDEN","externalCalls":0}
```

- 每次执行前后 `127.0.0.1:3000`、`127.0.0.1:3101` 均不可连接。
- 附加参数在 runtime readiness、Next 动态导入和监听之前被拒绝，未进入 Next 参数解析。

开发报告所称六组负例由本轮逐命令重跑确认，未只依赖 `p1-cli.test.ts`。

## 验收二：四类 CLI 脱敏 JSON

以下四类真实 Node24 子进程负例均由本轮执行，输出逐字核对，stdout 为空，stderr 只有单行四字段 envelope；扫描结果没有绝对路径、`file://`、HTTP(S) URL、`Error:`、stack `at …`、源码行列号、Bearer 或注入 secret：

| 场景 | 命令/注入 | 实得 reasonCode | 结果 |
|---|---|---|---|
| DB 不存在 | `runtime:assert-ready` + 唯一临时 `F1_DB_PATH` | `HEALTH_DB_MISSING` | PASS |
| 禁用环境名与 secret | `verify:env` + `FEISHU_TOKEN=super-secret-value` | `ENV_FORBIDDEN` | PASS |
| fixture 路径失败 | `verify:env` + 唯一不存在 `SOURCE_FIXTURE_PATH` | `FIXTURE_PATH` | PASS |
| migration ledger 漂移 | 临时 SQLite 先迁移，再改写唯一 ledger hash 后重跑 `db:migrate` | `MIGRATION_DRIFT` | PASS |

四类 envelope 均为：

```json
{"event":"cli_failure","status":"rejected","reasonCode":"<allowlist-code>","externalCalls":0}
```

迁移负例数据库、WAL/SHM/journal sidecar 均在临时目录验证后清除，未触及共享两份 SQLite；清理后残留探针为 `false`。

## 验收三：正常 start/health 与进程清理

在隔离副本完成 build 后，固定 Node24 运行 `npm run test:p1`，真实生产 start 子进程返回：

```json
{"command":"test:p1","status":"ok","bindHost":"127.0.0.1","port":3000,"health":"ready","externalCalls":0}
```

实际 HTTP health 为 `200`，DTO 满足 `scope=local-only`、`status=ready`、`reasonCode=ok`、`externalCalls=0`；监听只允许 loopback `127.0.0.1:3000`，未开放 `3101` 或 `0.0.0.0`。测试结束后：

- Python loopback 探针：3000=`LISTENING false`，3101=`LISTENING false`。
- `lsof -nP -iTCP:3000 -sTCP:LISTEN` 和对应 3101 均无输出。
- `/private/tmp/f1plus1-ffc67a.nvUoFg` 进程扫描无残留子进程。

受限执行环境首次直接 start 曾返回 `listen EPERM`；该收据归为宿主沙箱能力失败。随后在用户授权的隔离 loopback 路径取得上述真实 start/health PASS，没有把 `EPERM` 误判为应用失败，也没有继续扩大权限或访问外部资源。

## 验收四：完整 Node24 check

在隔离副本使用最小环境、项目 Node24/npm11.16.0 和 `NEXT_TELEMETRY_DISABLED=1` 执行 `npm run check`，最终 exit `0`：

| 阶段 | 实得 |
|---|---|
| `verify:env` | `fixture/mock/manual_only`、externalCalls=0、Node24、loopback:3000 |
| `db:migrate` | `applied=[]`、user_version=2、SQLite3.53.1、WAL、synchronous=2、busyTimeout=250、foreign_keys=1、tempStore=2 |
| `seed:fixtures` | fieldCount=39、rowCount=59、enabledFalseCount=59、inserted=false、writesToBase=false、externalCalls=0；sourceArtifactHash=`d4da9fc24c792c0471bcd24c525a46dcef1e521b36a870fd111e7310243888b2`；projectionHash=`e7a8312c70a9a49922aedb3cfbeaa190db8f5dce8d4ab45db1570748fc329f17` |
| `runtime:assert-ready` | status=ready、scope=local-only |
| `test` | 2 files、42/42 passed，其中 VS-0 32、P1 10 |
| `lint` / `typecheck` | exit 0 |
| `build` | exit 0；路由 `/`、`/_not-found`、`/api/health` |
| `test:p1` | exit 0；normal start/health、127.0.0.1:3000、externalCalls=0 |

隔离副本初次未复制项目根 `data/` 时得到 `FIXTURE_PATH`，这是测试夹具准备错误；补齐与当前快照相同的 `data/` 后重跑，完整 check exit 0。该错误未触碰共享 app，已在错题自检中保留。

## 验收五：重复 migration/seed 与 hash 零漂移

完整 check 后在同一隔离副本重复执行两轮 `db:migrate` 与 `seed:fixtures`：

- 两轮 `db:migrate` 均 `applied=[]`、exit 0、user_version=2。
- 两轮 `seed:fixtures` 均 `inserted=false`、39 fields、59 rows、59 disabled、writesToBase=false、externalCalls=0。
- 两轮 projection hash 均为 `e7a8312c70a9a49922aedb3cfbeaa190db8f5dce8d4ab45db1570748fc329f17`，source artifact hash 均为 `d4da9fc24c792c0471bcd24c525a46dcef1e521b36a870fd111e7310243888b2`。
- 重复操作前后、排除 `.local/node_modules/.next/.vite/tsconfig.tsbuildinfo` 的 62-file app static tree digest 均为 `2c6132f78523d3361902d2f65c03ce927f1d2f30c304496ecb6f1ee7f0d1a706`。
- package-lock、package、serve、security cli/log、P1/VS-0 test、两份 migration 的 before/after hash 均相等；当前 package 与 serve hash 分别为 `cb81c6be…d1fd452`、`21c251dd…9713100`。

## 保护边界与未验证项

已验证：当前 app 只读快照、六组 argv、四类 CLI envelope、正常 start/health、完整 Node24 check、42/42 tests、lint、typecheck、build、重复 migration/seed、projection/source hash、静态 tree 零漂移、3000/3101 无监听、临时子进程清理。

未验证且保持关闭：R5 恶意同 UID 竞争、R12 OS/系统调用级 deny-all、真实 Base/provider/Collector、真实平台采集、AI/媒体、表单提交、自动发布、部署和付费能力。本任务没有把这些项目写成通过。

## 错题自检

- 开发报告的旧 checkpoint hash 只作定位线索；本轮读取当前快照 hash，确认 package/serve 已是 7A9C48 之后的版本，lockfile 与关键安全文件保持冻结值，验收以当前实际 bytes 为准。
- 首次隔离副本未复制根 `data/`，导致预期 `FIXTURE_PATH`；补齐 data 后重跑，不把夹具准备错误计为 app 失败。
- 首次完整 check 在默认受限环境遇到 loopback `EPERM`；保留为宿主环境负例，随后取得一次授权隔离 loopback 收据并确认 test:p1/health 通过。
- 测试脚本 wrapper 首次使用 zsh 保留变量名 `status`，得到 shell wrapper 错误；改用 `rc` 后重跑，未把 wrapper 错误计入 app 结论。
- 六组 argv 和四类 CLI 均逐命令重跑，未只采信开发报告或单元测试；未放宽 allowlist、正则、Node 版本或环境门禁。
- 未改 app、Spec、ADR、data、design、lockfile；未安装、联网或执行任何真实外部能力。

## 最终结论

当前 7A9C48 app 快照在任务要求的 VS-0 P1 范围内通过独立回归：`decision=pass`、`P0=0`、`P1=0`、`P2=2`。报告已落盘，任务随后按协议 complete 并由 doctor 复核；R5/R12 与真实能力继续保持 pending/closed。

## TASK_STATE_OK

TASK_STATE_OK
