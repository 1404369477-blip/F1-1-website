---
type: audit_report
department: 测试部
target: TASK-20260910-95D001 Hermes 固定 f1 直出候选技术准备
status: final
date: 2026-09-11
related_task: TASK-20260911-FAB9DA
decision: pass
tags: [Hermes, Weixin, readonly, independent-review, direct-command]
summary: 固定十文件候选、无参直出 CLI、安装 helper、本地失败边界和 M1 暂存及只读 dry-run 通过技术准备审查；入口选择、正式安装、Gateway 激活与真实微信手机出口均未完成。
---

# 固定 /f1 直出候选独立审查

**结论：技术准备范围 pass；真实微信出口尚未验收。** 本报告绑定 `candidate-site-status-20260911-direct-r1`，不撤销[既有 r4 微信准确性 fail](2026-09-11-TASK-20260911-FAB9DA-Hermes微信回复r4准确性附录.md)。开发任务 `TASK-20260910-95D001` 继续等待用户选择入口；本报告不能据此将开发整体完成。审核者 `测试部/agent_options_research` 未参与 CLI 或安装 helper 实现，未安装候选、修改生产配置、调用模型、发送消息或操作 Gateway。

候选仅增加 `app/scripts/f1-status-report.py`。原 r4 的九个 payload、严格 DTO 校验和中文 formatter 均逐字保持。CLI 每次加载经过固定 SHA 验证的同目录 r4 模块，只执行一次固定采样，校验后输出完整中文报告；不启动 MCP server，不消费参数，不调用模型。日期与北京时间、有效期、失败项以及 Admin 登录、容量变化率、任务存活和根因的未知范围沿用已审核的确定性正文。

| 冻结对象 | SHA-256 |
|---|---|
| 10 文件 / 107,769 字节 manifest | `481246328bc9d88826e8d3e755ea782e3afb60b9b95aaef3761b94eb9c1569f4` |
| 新 CLI | `68ee75878989b35f431e4117d878c12d13d88c1623644d4390b5f0ef5cc21408` |
| 安装 helper | `acf7f8ce5b8d538c38b1e3e33d51948bf803f06a42959d209aa0bb75bb043ff0` |
| 冻结低层 helper | `670d2268e1fbe5ffe9b3a8c554fe38092c8af92b0f28368a70dcc10769cee61d` |
| 状态配置 | `ed93f3acf8fb468cec5c4c575ec36b00ffed3703c5e3c06fe62095992eb8be7f` |
| 正式测试最终版本 | `319d4fc68a32a139625222a2df2e156a61d5812b9f858ed816b696e37ac7f8e7` |

## 必要验证与反例

审核者执行或复核的定向验证如下；未重复已冻结的旧 Node 全量测试。

| 验证 | 结果及证据 |
|---|---|
| CLI 14 项验收 | 原 12 项通过；最终正式 fixture 的 2 项真实超时清理用例在原 scratch/TMPDIR 通过，25.303 秒。覆盖无参、固定依赖、读取后替换、异常/输出限制、过期 DTO、未知/失败、环境隔离、启动阻塞及父子进程清理。见 `direct-report-tests-r1.log`、`direct-report-tests-r1-final-timeouts.log`。 |
| 首次 fixture 失败归因 | 首次两个用例因临时 `producer.ts` 继承 CommonJS 语义，在启动 PID 前报语法错误；CLI 安全返回 unknown。作者只把正式 fixture 改为 `producer.mts`，payload 未变，原目录复验通过。隔离记录及显式 module 环境的辅助复跑保留为 `direct-report-environment-r1.json`、`direct-report-tests-r1-module.log`。 |
| 安装 helper | 最终 helper 独立复跑 18/18；追加配置保留全部原字节与原 r4 MCP，十文件闭包、权限/链接/哈希、旧 r4 实体、CAS、原子替换失败、回退与异常输出均覆盖。见 `direct-helper-tests-r1.log`。 |
| 独立额外反例 | 2/2：提供新的当前 SHA/身份不能越过固定原配置；提供新的 post SHA 不能绕过回退前后配对。拒绝后保留外部改动及原备份。见 `adversarial-direct-helper-r1.json`。 |
| 已安装 Hermes 原生路径 | 实际 M1 源码提取后的完整 handler、解析器、redactor 与微信 formatter 以内存 fixture 执行，6 组通过。空闲合法入口在创建新 agent、读取业务对话历史前返回完整报告；尾部参数不进入固定命令。见 `native-f1-source.json`、`native-f1-path-probe.json`。 |
| 前置分流反例 | 2/2 复现：忙碌 steer 会把完整消息交给现有模型；pending update 会把消息当作更新回答。均未进入 native exec。见 `native-f1-preemption-probe.json`。 |
| 候选一致性 | 本地新 CLI 与 stage 相等，九个旧 payload 与 r4 相等；M1 暂存的 12 个文件、全部 SHA 与权限匹配。见 `stage-direct-r1-review.json`、`m1-direct-r1-readonly.json`。 |

原生 exec 的两个实测边界已落实到固定候选：stdout 为空时 Hermes 会使用 stderr，可能显露普通路径，最终固定 command 因此追加常量 `2>/dev/null`；解释器或脚本启动失败只得到原生 `Command returned no output.`，表示未取得报告。Gateway 的 30 秒超时本身不负责清理子进程；CLI 在 main 内使用 27 秒总时限，固定采样器使用 24 秒时限及进程组清理，两条真实超时路径均已复验。解释器进入 main 之前的卡死不属于内部时限保证，未将 Gateway 的超时夸大为进程清理保证。

## M1 已执行范围

主控在 M1 私有 inactive stage 执行了一次真实只读 CLI：北京时间 `2026-09-11 00:35:04.733` 采样，有效至 `00:36:04.733`，用时 2.181 秒，退出码 0、stderr 0，报告 1,051 字符 / 2,253 字节。审核者复算了证据及报告 SHA、字节数、60 秒有效期和重要未知边界；报告为有效的站点 `failed`，没有把超龄成功记录解释为任务停运。该样本已过期，只用于本次历史验收。证据为准备目录的 `m1-inactive-cli-probe.json`，独立复核为 `m1-direct-r1-artifact-validation.json`。

审核者另在北京时间 `00:42:09` 亲自执行了 M1 固定 helper 的只读 dry-run，并核对实际暂存文件。配置前后保持 `03941a9b10e3f967c13c005bbe710c9744589b185076fafd52941e982d990fee`；计划 post 为 `007fd2cc87885a4d625292d8f55089237228e3a656987e25b5e7ccf5649006a5`，固定 command SHA 为 `f8d03ac1bdc4825404907ded3e7ec108064ffd45fa9cf9847868c40823f709a8`。新 release 和独立配置备份均不存在，未正式 apply。只读检查程序及结果保存为 `m1-direct-r1-readonly-check.py/json`。

安装 helper 只允许对固定 r4 前像追加 `quick_commands.f1`，要求该顶层字段原先完全不存在。正式执行前仍须重新取得紧邻本次 apply 的 dry-run 身份；已有配置漂移不得沿用旧成功结果。其单操作者 advisory lock 与最终检查至 replace 之间的短暂并发间隙，沿用既有已披露限制；未声称能约束所有同 UID 写者。

## 尚未完成的出口

本次准备方案的适用前提为：已授权的微信会话处于空闲状态，没有等待回复的 `/update`，前置 hook 没有改写或截获消息。只有确实进入 native exec 后，固定 `/f1` 才具有不消费尾参、按固定 CLI 原文返回的保证；不能保证任意状态下的每条 `/f1` 都绕过模型。真实验收还需要用户接受该入口、主控安装及激活配置、核实这些前提、取得对应微信请求和实际手机回复。自然语言旧入口的 r4 准确性失败仍保留，不由本报告关闭。

具体候选、M1 路径、安装及回退步骤以已冻结的 [INSTALL.md](../../../../../scratch/hermes-site-status-direct-20260911/INSTALL.md) 为准，SHA 为 `fd3b171355b6fba4b693785e6ff7ba17139e7c977b7642dc7508a04c3bda9652`。本报告中的独立证据位于 `scratch/hermes-site-status-review-20260910/`，主控暂存证据位于 `scratch/hermes-site-status-direct-20260911/`。新证据清单为 `evidence-manifest-direct-r1.json`；旧 r3/r4 报告及相关冻结证据已复算保持原字节。审核者未修改正式任务记录，分层任务转换由主控处理。
