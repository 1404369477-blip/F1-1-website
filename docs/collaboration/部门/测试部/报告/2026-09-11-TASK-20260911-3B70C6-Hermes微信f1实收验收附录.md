---
type: audit_report
department: 测试部
target: TASK-20260910-95D001 Hermes 微信 f1 实收验收
status: final
date: 2026-09-11
related_task: TASK-20260911-3B70C6
decision: pass
tags: [Hermes, Weixin, readonly, independent-review, receipt]
summary: 21时59分的实际 f1 请求，经重启后的固定配置、对应入站与发送窗口、独立正文复算及用户完整回传交叉核验，通过已授权范围的原生入口和实收验收；未直接捕获此次父链、服务器 stdout、模型 API 线级记录或精确手机送达时间。
---

# 微信 /f1 实收验收附录

**结论：本次实际 `/f1` 请求及用户确认的微信收件通过验收。** 本附录补齐[安装与加载报告](2026-09-11-TASK-20260911-3B70C6-Hermes固定f1安装与加载独立审查.md)当时缺少的真实出口证据，结论限于用户已经接受的空闲、无更新待答、前置 hook 不截获的固定入口。原安装报告及其 26 件证据保持原字节；旧 r4 自然语言回复的准确性失败不改写。本次没有要求用户重复查询，也未追加代码、日志写入或巡检调用。

## 同一请求的交叉证据

审核者独立只读检查 M1：Gateway 已从原 PID 1080 / runs 1 变为 PID 5281 / runs 2，进程于北京时间 `21:58:19` 启动。当前配置仍为 `007fd2cc…`，分发源码仍为 `43420c40…`，与已验收的固定命令及加载路径一致。此前完整 loader 已验证仅有 `f1/exec`，command SHA 为 `f8d03ac1bdc4825404907ded3e7ec108064ffd45fa9cf9847868c40823f709a8`，没有 managed overlay 覆盖。

| 北京时间 | 已核实事实 |
|---|---|
| 21:58:19 | 当前 Gateway 新进程启动，PID 5281。 |
| 21:58:22.889 | 新启动窗口内登记固定 `f1_status` 的一个 MCP 工具；此记录用于启动关联，不将其当作原生 `/f1` 工具调用。 |
| 21:59:10.496 | 微信收到一条 DM 文本入站，无媒体。 |
| 21:59:11.107 | 用户回传报告标明本次采样时间，位于入站之后、发送尝试之前。 |
| 21:59:13.523 | 同窗口记录准备发送 1,050 字符；入站至此为 3.027 秒。 |
| 22:01:00.742 左右 | 用户在本对话回传完整微信正文，证明此前已收到该报告；此时间不作为手机实际送达时间。 |

审核者只查询 `21:58–22:01` 的微信日志和 SQLite 消息/工具元数据，没有读取其他聊天正文或输出账户、token。该时间窗中，Weixin 的持久消息、工具结果和 assistant tool-call 记录均为 0，符合原生 quick command 在模型对话处理之前返回的源码路径。**记录为 0 本身不独立证明模型 API 从未调用。** 原生路径结论来自新 Gateway、未变的固定配置与源码、对应请求/采样/发送窗口、匹配的完整报告和无对应模型 turn 的共同支持。

此前 180 秒进程观察已经于 `21:55:09` 结束，因此没有捕获 `21:59` 这次调用的父链。本次也没有直接保存服务器 stdout 或原始 DTO。现有原生实现没有常规首发成功的持久正文回执；`Sending response` 仅证明发送尝试，手机收件依据用户明确回传的完整正文。已有交叉证据足以支持本次验收，无需补采某一种指定证据才认可结果。

## 正文独立校验

用户回传为 1,050 字符、2,252 UTF-8 字节、20 行，无末尾换行，SHA-256 为：

`a9acd145f4f5f0cf32064ba90e59be241da0f8e538eefdc8dcd7b6a4d7088524`

观测时间为 `2026-09-11 21:59:11.107 UTC+8`，有效至 `22:00:11.107 UTC+8`，精确相差 60 秒。发送尝试发生在有效期内；没有证据确定手机的精确送达时刻。审核者按报告列明的成功/恢复点时间独立复算并取整，四个年龄均正确：

| 项目 | 正确年龄 |
|---|---|
| Motorsport 最近成功 | 35 小时 38 分 32 秒 |
| The Race 最近成功 | 35 小时 38 分 28 秒 |
| Sky Sports 最近成功 | 35 小时 38 分 25 秒 |
| 已登记备份恢复点 | 35 小时 50 分 27 秒 |

以安装后 `21:44` 实际 command stdout 为比较基准，移除其唯一末尾换行，并仅替换两个观测/有效期时间及四个数学派生年龄后，结果与本次用户正文逐字符相等。这证明模板与其余内容一致，不自称捕获了本次原始 stdout。

全部重要范围均完整保留：站点总体 failed、本机公开入口 200 与 M1 公网请求失败的区别、当前控制值及未知来源、历史/当前未知操作、备份清单和两类验签、未重读异机密文或执行恢复、容量变化率未知、Admin 私网登录未知、进程存在不代表业务正常，以及任务存活未采样、根因未查。没有新增任务停运、人工停止或调度器根因等推断。此报告现已过期，只用于这次验收，不能当作持续当前状态。

本附录支持主控按已确认的 `/f1` 范围完成实施和审核任务。它不扩大为繁忙会话、pending update、其他 hook 分流或自然语言旧入口的普遍保证，也不表示报告中的 RSS、备份或公网业务故障已被修复。

本次独立新增证据位于 `scratch/hermes-site-status-activation-review-20260911/` 的 `wechat-215911-*` 文件，用户原文位于主控目录 `scratch/hermes-site-status-activation-20260911/user-weixin-reply-215911.json`。新增冻结清单为 `wechat-215911-evidence-manifest.json`，原安装清单 `evidence-manifest.json` 保持 SHA `98dc9a12…`。审核者没有修改正式任务记录，主控负责最终核收及转换。
