---
type: audit_report
department: 测试部
target: TASK-20260910-95D001 Hermes 固定 f1 安装与加载
status: final
date: 2026-09-11
related_task: TASK-20260911-3B70C6
decision: pass
tags: [Hermes, Weixin, readonly, independent-review, activation]
summary: 固定 direct-r1 的实际安装、原配置保留、安装收据、正式命令样本及新进程配置加载通过独立审查；现役 Gateway 激活、微信原生 f1 实际执行与手机实收均未验证，开发继续等待用户操作。
---

# 固定 /f1 安装与加载独立审查

**结论：安装与新进程 loader 层 pass；现役 Gateway 激活、微信原生执行和手机收件均为 NOT_VERIFIED。** 用户已接受 `/f1` 正式入口及空闲、无更新待答的前提。本次承接[已冻结的技术准备 PASS](2026-09-11-TASK-20260911-FAB9DA-Hermes固定f1直出候选独立审查.md)，仅核查现场身份、安装和加载，不重复整套旧测试。审核者 `测试部/agent_options_research` 未参与实现，未 apply、重启 Gateway、调用模型或发送微信；主控承担安装和实际 CLI 调用。

## 安装身份与结果

北京时间 `2026-09-11 21:44:47`，审核者直接在 M1 只读核查新 release 的 13 件文件，确认 10 个 payload 共 107,769 字节、文件 SHA、单链接与所有者、目录 `0700` 和文件 `0600` 全部符合冻结候选。旧 r4 九个 payload 原字节保留；原配置的每一个字节仍是新配置的原前缀，仅追加固定 `quick_commands.f1`。原 MCP 与其他配置值不变，原配置私有备份及实际安装收据完整。

| 对象 | 已复算身份 |
|---|---|
| candidate | `candidate-site-status-20260911-direct-r1` |
| manifest | `481246328bc9d88826e8d3e755ea782e3afb60b9b95aaef3761b94eb9c1569f4` |
| 配置前像 / 独立备份 | `03941a9b10e3f967c13c005bbe710c9744589b185076fafd52941e982d990fee` |
| 配置后像 | `007fd2cc87885a4d625292d8f55089237228e3a656987e25b5e7ccf5649006a5` |
| M1 release 内安装收据 | `014690ddc86b2797a3ca7274214f3b33c49b3d850cd414e41fe4ee9999af9618` |
| 固定 shell command | `f8d03ac1bdc4825404907ded3e7ec108064ffd45fa9cf9847868c40823f709a8` |

上表收据 SHA 对应 M1 release 内的 `install-receipt.json`；主控本地 `installation-receipt.json` 是格式化后的安装返回，属于另一份证据，不能将两者文件 SHA 混同。独立程序和结果为本轮审查目录的 `installed-files-check.py`、`installed-files.json`。

主控实际执行了安装后配置中的完整固定 command，`21:44:03.726` 取得本次报告，3.094 秒完成，退出码 0、stderr 0，输出 1,051 字符 / 2,253 字节。审核者复算了主控原始证据及报告 SHA `0060d69ec1e92d1fd38675d34c3be706905c5f5638d93a6149ad1170bb3a8201`，核对北京时间、60 秒有效期、failed 与重要未知范围；没有任务停运或根因外推。该历史样本仅用于此次运行验证，已于 `21:45:03.726` 过期。审核者未另跑 CLI，避免与随后微信进程观察混淆。

## 当前源码与配置加载

当前 M1 `gateway/run.py` 仍为 `43420c40…`，MessageEvent 解析、微信 formatter、redactor 分别仍为 `ee22548e…`、`0f478335…`、`2f172d22…`，与旧审查完全相同。当前配置 loader 源码 SHA 为 `d8c3b167…`，源码及必要片段保存为 `hermes-source-current.json`。

在 `21:49:11`，审核者通过新进程执行实际 `load_gateway_config()`，完整返回仅有 `f1` 的 `exec` 配置，最终 command SHA 精确匹配上表；managed scope 不存在，声明的 quick_commands 与加载结果完全相同，配置后像保持不变。检查带只读守卫：66 次已存在目录的重复 mkdir 按 EEXIST 处理，66 次相同权限 chmod 按原结果处理；仅允许 urllib3 导入时的一次 IPv6 loopback 临时端口能力探测。未产生新文件、实际权限变更、外部连接、子进程启动或 Gateway 服务调用。首次守卫拦截及最终脚本/结果保留，未把中断执行当作加载成功。

源码明确区分新进程加载与现役服务：`GatewayRunner.__init__` 在 run.py:2540 保存 `self.config`，原生 exec 在 8853 起读取这份内存配置；`/reload-mcp` 只重建 MCP 连接和工具缓存，不更新 `self.config.quick_commands`。新增 `/f1` 需要重新构造 GatewayRunner；按现有服务方式应在重启后加载。新进程 loader PASS 不证明现役 Gateway 已加载。

## 实际出口仍待验证

主控进行了一次限时只读进程观察：`21:52:09.758–21:55:09.997`，180.245 秒，开始与结束的 Gateway 均为 PID 1080 / runs 1，配置后像不变，未捕获固定 reporter 或 sampler。0.4 秒轮询可能漏掉短进程，这一结果只说明本观察窗口没有捕获，不能外推 `/f1` 未执行。审核者限定于同一轮起点之后的微信日志元数据读取截至 `21:54:45`，未取得可关联的新入站或发送事件；没有读取其他聊天正文或输出账户标识。

已核实原生收据边界：微信入站日志记录脱敏发送方、类型及媒体数；通用发送日志记录准备发送的字符数。原生 exec 本身不记录命令完成或出站正文 SHA，也不生成对应 LLM 对话历史。首次发送成功直接返回，只有重试成功才另有日志；`_record_delivery` 是本轮内存布尔值。因此 `Sending response` 只证明发送尝试，无法独立证明手机收到。

接续仍需用户重启后在约定前提下发送 `/f1`，并回传完整报告。应结合重启后的 Gateway 身份、固定脚本父链、相应入站/发送尝试时间以及手机正文，核验同一请求、时间和有效期、报告完整性与未知边界。单独的 CLI 样本、脚本名、Node 进程或发送尝试日志都不足以关闭真实出口。开发任务 `TASK-20260910-95D001` 继续等待用户操作；后续证据另加附录，不覆盖本报告，也不撤销旧 r4 自然语言回复的 fail。

本轮独立证据位于 `scratch/hermes-site-status-activation-review-20260911/`，主控安装、报告和限时观察证据位于 `scratch/hermes-site-status-activation-20260911/`。冻结清单为前者的 `evidence-manifest.json`，包含主控 `installed-check.json`、`installed-report.txt`、观察源码及 JSONL；持续变化的主状态文档不纳入。旧技术准备的 41 件冻结证据保持原字节，正式任务转换由主控负责。
