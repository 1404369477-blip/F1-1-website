---
type: audit_report
department: 测试部
target: TASK-20260802-574A6D 新 Mac 迁移与清理边界（pre-clean + post-clean）
status: final
date: 2026-08-02
related_task: TASK-20260802-574A6D
decision: pass
tags: [migration, pre-clean, read-only, M5, macOS26.6]
summary: pre-clean 的旧机凭证清理门禁已由授权处置关闭；post-clean 保留边界、Node24、SQLite、任务真值与 9/9 Codex 线程核对通过，P0=0/P1=0，剩余项为外部能力关闭及本机进程可见性 Unknown。
---

## 结论摘要

报告保留 pre-clean 的失败快照，并追加本轮 post-clean 的独立只读终验。pre-clean 时目标工作区存在迁移明确排除的 `.obsidian/plugins/obsidian-local-rest-api/data.json`，旧门禁因此关闭；该文件当时只检查了元数据，没有读取内容或凭证值。统筹部在获得用户授权、确认无进程/句柄/监听后删除旧文件并保留插件，本轮确认删除结果与忽略规则。

post-clean 最终结论为 `decision=pass`，P0=0、P1=0。统筹清理收据记录项目释放约 `298.6 MiB`、Homebrew 旧 simdjson 释放约 `6.6 MB`；本轮只读复核当前项目约 `951M`、migration 约 `560K`，没有重建归档或缓存，也没有再次运行会留下构建缓存的完整 check。两次 post-clean 完整 check 收据由统筹提供并与本地静态、数据库、工具链和任务证据交叉核对。

## 审核标准与阶段边界

- pre-clean：只读核对硬件/系统、Git 与 dirty、task doctor、9 个 Codex 任务的磁盘映射、会话/manifest、portable/warm 归档及隔离解包、Node24/npm、完整检查、关键 hash、skills、飞书 CLI 和精确清理边界。
- post-clean：已在用户授权和统筹精确清理完成后执行，只读核对结果写入本报告。
- 失败路径：归档/关键 hash、Git/任务/会话数量或完整 check 不符即停止清理；清理不得触及 node_modules、精确 Node24、SQLite 业务状态、项目文档/对话/任务真值、全局凭证或不可证明可重建文件；飞书 CLI、Codex 同步和工具链故障须与迁移完整性分开记录。

## 独立证据与逐项结果

| 检查项 | 结果 | 独立证据 |
|---|---|---|
| 硬件/系统 | PASS | `Darwin arm64`；MacBook Air `Mac17,3`、Apple M5、10 cores、16 GB；macOS `26.6`、Build `25G72`；SIP Enabled；Xcode CommandLineTools；git `2.50.1`；Python `3.14.6`；rg `15.1.0`。与目标环境 M5/macOS26.6/arm64 相符。 |
| Git 基线 | PASS（存在预期迁移 dirty） | root `[M5-HOME]/Documents/F1+1`；branch `main`；HEAD `a9691e71b1552592cc5ded8d5db66c336262301c`；remote 为空；pre-clean dirty 为 532 条，post-clean 只读复核为 375 条；两次 `git diff --check` 均 exit 0，未执行 reset/checkout/clean。 |
| task doctor | PASS（阶段差值已解释） | pre-clean `TASK_DOCTOR_OK | tasks=92` 为冻结基线 91 加本任务 `574A6D`；post-clean `TASK_DOCTOR_OK | tasks=93 | full_history_validated=true`，新增安全任务 `026EC3` 的增量已由任务真值解释。 |
| 9 个 Codex 任务可见性 | PASS（应用层 + 磁盘侧） | post-clean 使用 deferred `list_threads` 获取 9/9 个目标线程，随后对 9 个 UUID 分别调用 `read_thread`；全部 `cwd=[M5-HOME]/Documents/F1+1`。目标包括主线程 `019fb2b1…32947`、统筹 `019fb368…d1cd1c`、产品 `019fb369…42ead`、研究 `019fb36c…bd710`、设计 `019fb36e…6ba23`、数据 `019fb371…19520`、开发 `019fb374…e7f`、安全 `019fb377…76d77`、测试 `019fb379…936c4`。pre-clean 阶段未调用该 deferred 接口的记录已纠正，不再写成“无线程接口”。 |
| 关键任务状态 | PASS（约束保持） | `TASK-20260802-574A6D` 已由本会话 claim；`TASK-20260802-7A9C48` 仍为 `claimed` 且无报告，未写成 complete/PASS；`TASK-20260802-6F7563` 仍为 `queued`；未改写其他任务。 |
| 会话/协议 manifest | PASS | `docs/collaboration/协议版本.json` 管理的 27 个文件逐项 mode/SHA 校验 `bad_count=0`；启动状态为 8 部门 active/registered，协议 `1.4.10`，UUID 与 THREAD-MAP 相符；warm symlink 29/29 目标存在且均落在项目边界内；portable symlink manifest 为空。 |
| portable/warm 归档外层 hash | PASS | `bash migration/bundles/verify-bundles.sh`：`CONVERSATIONS-INDEX.md`、`CURRENT-HANDOFF.md`、`DEPARTMENT-HANDOFF-PROMPTS.md`、`README-FIRST.md`、`README-M5-macOS26.6.md`、`RESUME-PROMPT.md`、`SECURITY-EXCLUSIONS.md`、restore/verify 脚本均 OK；portable 成员 `3700`，warm 成员 `29157`。portable tar SHA-256 `5bff483c6fc042db6f74b10e8c3daf8edced8398caaa53b18f1db657ed92aa5a`；warm tar SHA-256 `ddd54f1117eac6b03e3d5f9b1f149bc18aab702b3dfe58862be92106729cc14e`。 |
| 隔离解包与完整回归 | PASS（隔离临时目录） | `bash migration/bundles/verify-bundles.sh --deep --run-check` 在临时解包副本完成：restore symlink OK；隔离 task doctor 为冻结基线 `91`；Node `v24.18.0`、npm `11.16.0`；SQLite `3.53.1`、WAL、`synchronous=2`、busyTimeout 250、foreign_keys=1；seed `39 fields × 59 rows`、59 disabled、externalCalls 0；Vitest `32 passed`；lint/typecheck/build 全部 exit 0；`RESTORE_VERIFY_CHECK_OK`、`RESTORE_VERIFY_OK`、`BUNDLE_VERIFY_DEEP_OK`。为避免写入当前项目，未在当前工作区执行会生成 `.local/.next` 的完整 check。 |
| 当前目标工作区 restore verifier | pre-clean FAIL；post-clean PASS（收据交叉核对） | pre-clean 的 `verify-restored-project.sh` exit 4 原因已由用户授权处置关闭。统筹提供的 post-clean `verify-restored-project.sh --run-check` 收据显示 Node24、32/32、lint、typecheck、build、task doctor=93 全部通过；本轮确认旧 `data.json` 不存在、插件文件仍在、根 `.gitignore` 精确规则命中。 |
| Node/npm 工具链 | PASS（项目路径）；机器全局不作项目依据 | 项目精确路径 `app/.local/toolchains/node-v24.18.0-darwin-arm64/bin/node` 为 `v24.18.0`，npm `11.16.0`，Mach-O arm64；运行时报告 SQLite `3.53.1`。当前 Homebrew 全局 Node 为 `v26.5.1`、npm `11.17.0`；全局版本不满足项目 Node24 合同，隔离回归使用并验证了项目 warm Node24。 |
| 关键 checkpoint hash | PASS | `app/.node-version`/`app/.nvmrc` `55075b5ec4e8b31936cbbc282b8829116d1fd48f2f2f1856dee592a6650700ce`；`app/package.json` `95e2e7403c612bd6dac7375c8444c43d920b1c7a34949d3c047a4413093d5ac2`；`app/package-lock.json` `89807e33818cfb851b57da65a5675e413e5593d01d6773c3a71a6cc6ea3d85d3`；`app/.npmrc` `be7906950cc4de765c6933ebada746d495050d9a6b4b61ddf0d261d0a8c747b1`；`app/scripts/serve.ts` `ce42e19e707dc306abbcafe1cdec143abaddb1e989cda9524334652f069e787a`；security cli `1126cca2471da18160b8d870bd4a1d030610324e5b0c1e1ccbf78e4c7ce42e85`；security log `f69347a4d054cb4ce05f92bd333fba4b80b00dfe21108db110da12c7e41e4b00`；VS-0 test `96a8a69f61b997bf680ff95ec614aee758efa62ecaba19e3cc256753434bdeca`；pre-clean `migration/bundles/SHA256SUMS` `b9f5614b311f5fc433a683e1515b2ee8933e68605530e566be741441c4b628cd`；post-clean `migration/conversations/SHA256SUMS` `40481017e7d5ea6b7330a210e4c3ecb7cd13e42316cec14629e310e33fcf1797`。代码/工具链关键点与 CURRENT-HANDOFF 一致。 |
| skills | PASS | vendored 与已安装 `agent-team/SKILL.md` SHA 均为 `86941df11a1f8951d4eb06554e3363bb974c6c578832c0f77e87eebb2560f302`；`vibe-project-foundation/SKILL.md` 均为 `ac48d9b6b71fd58b760eb6978b4d89b9e1cb7038be203dacbe083f89a33e0a28`（安装副本与迁移副本一致）。其他插件按迁移要求由账户/插件管理器重获，不由本包复制。 |
| 飞书 CLI | 版本 PASS；外部能力按合同 closed | `/opt/homebrew/bin/lark-cli --version` exit 0，`1.0.68`。OAuth/Keychain 按迁移合同刻意不迁移，`auth status` 未初始化属于待用户重新登录的外部能力状态；未登录、未初始化、未更新、未写入真实资源，也未输出 token/auth JSON。项目启动不依赖真实飞书认证，未发现启动阻断，因此不把该状态计为项目迁移 P1。 |

## pre-clean 归档输入 manifest 的残余漂移（历史快照）

外层归档和深度隔离校验通过，但当前工作区相对冻结输入 manifest 存在可解释的迁移态差异，不能声称工作区 byte-identical：

- portable input：3252 项中 3248 OK；4 项差异，包含测试部收件箱因本任务 claim 重新生成的 hash，以及 3 个已不存在的 `.git/refs/codex/turn-diffs/...` 临时引用。
- warm input：25815 项中 25812 OK；3 项差异，包含 `app/node_modules/.vite/vitest/.../results.json` 缓存 hash 漂移，以及 `app/.local/f1plus1.sqlite-wal`、`app/.local/f1plus1.sqlite-shm` 两个可随 SQLite 关闭而消失的瞬态 sidecar 缺失。
- pre-clean 工作区曾存在 `app/${TMPDIR:-/tmp}/node-compile-cache/v24.18.0-arm64-cf738c9d-501/` 的 93 个编译缓存文件（约 524 KiB）；post-clean 已按授权删除，当前路径不存在。

上述残余不影响已验证归档 tar 的完整性或隔离回归，但会阻止“当前工作区已完全恢复且可立即清理”的无条件声明。

## pre-clean 阶段 P0/P1/P2 快照

### P0

- `P0-01 / FAIL / 安全清理门禁`：被迁移安全排除的 `.obsidian/plugins/obsidian-local-rest-api/data.json` 在目标工作区重新出现。任务失败路径明确禁止清理全局凭证或无法证明可重建的文件；在未由用户控制的安全处置、重新生成与复验前，不批准任何清理。

### P1

- `P1-01 / pre-clean 记录，post-clean 关闭`：pre-clean 时飞书 CLI 认证读取因 Keychain 未初始化失败；post-clean 按迁移合同改列为外部能力 `closed/待用户重新登录`，不作为项目迁移 P1。CLI 版本持续为 `1.0.68`，项目可使用 warm Node24 启动。
- `P1-02 / FAIL / 迁移态`：portable/warm 输入 manifest 存在上述 7 项差异（含授权 claim 变更、瞬态 turn-diff 引用、Vitest cache 变更、SQLite sidecar 缺失）。归档 tar 与隔离回归已通过；差异需在 post-clean 再复核并解释/收口。

### P2

- `P2-01 / 观察`：dirty 条目由冻结 metadata 的 519 增至当前 532；差值与本轮 task claim、迁移恢复残余和缓存相关。未发现 `git diff --check` 错误，未执行任何清理或回滚。
- `P2-02 / pre-clean 临时 Unknown，post-clean 已关闭`：pre-clean 本会话尚未调用 deferred 线程接口；post-clean 已用 `list_threads/read_thread` 独立核对 9/9。
- `P2-03 / 已关闭`：pre-clean 实际释放空间为 `0 bytes`；post-clean 已按授权清理并取得约 `298.6 MiB` 项目空间收据，另有 Homebrew 旧 simdjson 约 `6.6 MB` 收据。

## post-clean 阶段 P0/P1/P2 结论

- `P0=0`：旧 Local REST `data.json` 已不存在；插件代码和 manifest 保留；根 `.gitignore` 的精确规则命中；未发现旧凭证值进入报告、任务 JSON 或仓库。
- `P1=0`：清理边界未触及 `app/node_modules`、精确 Node24、两份 SQLite、Git、任务/报告、对话/交接；当前 task doctor=93；9/9 Codex 线程已由 `list_threads/read_thread` 实时核对；统筹 post-clean 完整 check 收据通过。
- `P2-POST-01 / Unknown`：本会话的 `pgrep` 进程枚举受本机 `sysmond service not found` 限制，无法独立复现进程列表；统筹已在删除前确认无 Obsidian 进程、目标文件句柄及 27123/27124 监听。本地未获得相反证据，保留为环境可见性 Unknown，不升级为阻断。
- `P2-POST-02 / 信息项`：lark CLI 认证仍按合同保持 `closed/待用户重新登录`；本轮未访问真实飞书资源，真实资源状态保持 Unknown/closed。

### post-clean 资源与数据证据

- 对 `app/.local/f1plus1.sqlite` 与 `app/.local/vs0-acceptance.sqlite` 均以 SQLite read-only URI 打开：`integrity_check=ok`、`journal_mode=wal`、`user_version=2`；`source_config_fixture` 均为 39 列、59 行、59 行 `enabled=0`。没有写入数据库，也没有删除 SQLite sidecar 以外的业务状态。
- `migration/conversations/SHA256SUMS` 在其自身目录执行校验，10/10 通过；当前对话目录、任务/报告目录和 `.git` 均存在。
- `.obsidian/plugins/obsidian-local-rest-api/manifest.json` 保留，SHA-256 为 `b830eab4e34bf1a390223bf14fb04de3a413ae58d661d3c67081f98a2433ea6f`；`.obsidian/community-plugins.json` 仍列出 `obsidian-local-rest-api`；`git check-ignore -v` 命中根 `.gitignore` 第 18 行精确规则。
- 删除边界抽查：`migration/bundles`、`migration/manifests`、`migration/portable-assets`、`migration/scripts`、`app/.local/toolchains/downloads`、`app/.next`、literal TMPDIR compile cache、`app/node_modules/.vite`、`app/tsconfig.tsbuildinfo` 均不存在；`app/node_modules`、精确 Node24、两份 SQLite、`.git`、任务/报告、对话保留。

## 精确清理边界（本轮只读）

本轮 post-clean 已按授权清理已验证的归档、manifest、portable-assets/scripts、Node 下载缓存、`.next`、literal TMPDIR compile cache、Vitest/TS/pycache/DS_Store，并保留可恢复所需的收据与对话校验文件。`app/node_modules/`、精确 Node24 工具链、`app/.local/f1plus1.sqlite`、`app/.local/vs0-acceptance.sqlite`、`.git`、任务/报告、对话/交接均为明确保留项，后续不得列入删除候选。Local REST `data.json` 由用户授权处置后保持删除状态；插件在缺失配置时按自身路径重新生成私有材料，禁止把其值写入报告或仓库。

永不删除或改写：app 实现、Spec、ADR、data、design、`.git`、任务 JSON/收据/会话对话、Codex 认证/SQLite/cache/log、飞书 OAuth/token/Keychain、浏览器 profile、SSH/GPG、真实 env 文件及不可复现输入。清理前不得改变外部能力开关。

## 必测失败路径与反向探针

1. 让目标工作区直接走 restored-project verifier：命中被排除的 Local REST 配置并 exit 4，证明清理门禁确实拦截了凭证残留。
2. 对 portable/warm 归档执行 `verify-bundles.sh --deep --run-check`，使用临时解包副本并将 warm Node24 bin 前置到 PATH；归档在全局 Node26 影响 npm shebang 的条件下仍以 Node24 完成隔离 PASS，证明项目合同依赖 warm Node24。
3. 对 9 个 UUID、8 个部门启动状态、27 个协议管理文件和 29 个 symlink 做逐项映射/hash 检查，并通过 deferred `list_threads/read_thread` 取得应用层 9/9 证据；进程枚举的 sysmond 限制单列为 P2 Unknown。
4. 使用正确/错误两种 CLI 调用观察边界：首次误传不支持的 `--format json` 仅得到参数校验错误；随后正确 `auth status` 暴露 Keychain 未初始化。该误用已记录，不将其当作认证结果。

## 已验证 / 未验证

已验证：目标硬件/OS、Git HEAD/branch/diff-check、当前 task doctor 与关键任务状态、9 个磁盘会话映射、协议和 symlink manifest、两份归档外层 hash、隔离解包、Node24/npm、隔离完整 npm check、关键 checkpoint hash、vendored/installed skills hash、lark-cli 二进制版本、精确清理禁止边界。

未验证或保持 closed：进程列表的独立现场可见性（受 sysmond 限制，统筹处置收据已核对）、Keychain 重新登录后的飞书 auth/真实资源只读能力、任何真实 Base/文档访问、自动发布或外部写入。当前这些能力均保持 closed/Unknown；post-clean 业务回归完整 check 采用统筹收据交叉核对，本轮没有再次运行会生成缓存的命令。

## 错题自检

- 已纠正 CLI 首次调用的参数错误：`auth status --format json` 不受当前 CLI 支持；后续使用无 `--format` 的只读命令，并且没有把错误输出当作认证成功。
- 已纠正迁移合同口径：OAuth/Keychain 刻意不迁移，`auth status` 未初始化归为外部能力 closed/待登录，不作为项目迁移 P1。
- 已区分全局 Node `v26.5.1/npm11.17.0` 与项目 warm Node `v24.18.0/npm11.16.0`；隔离回归使用后者，没有用全局 Node 作为项目 PASS 依据。
- 已把 pre-clean task doctor 92 与冻结 91 的差值解释为本任务 574A6D，并把 post-clean 93 解释为新增安全任务 026EC3；没有改写、完成或覆盖 7A9C48，也没有领取 6F7563。
- 已保留归档 tar 的独立 PASS，同时单列当前工作区 manifest 漂移和 Local REST 凭证残留；没有因为隔离副本通过而错误放行当前清理。
- post-clean 读取对话校验时首次从项目根执行 `shasum -c migration/conversations/SHA256SUMS`，因清单相对路径产生 open failure；随即改在 `migration/conversations/` 目录重跑，10/10 通过，未修改清单内容。
- 已纠正插件清单路径为 `.obsidian/community-plugins.json`；插件目录、manifest 和精确 ignore 规则均以只读方式复核。
- 清理后没有将 `node_modules`、精确 Node24 或两份 SQLite 列入删除候选；没有重建已删除归档/缓存，也没有运行会留下大缓存的 full check。
- 本轮没有删除、移动、安装、联网、外部写入或修改受保护产物。

## 阶段结论与后续闸门

pre-clean 的 `P0-01` 旧凭证门禁已通过授权处置关闭；post-clean 保留边界、关键 hash、SQLite 2× integrity、59×39/59 disabled、Node24/npm、lark CLI 版本、9/9 Codex 线程和 task doctor=93 均已核对。最终结论为 `decision=pass`、P0=0、P1=0；P2 仅保留进程现场可见性与外部认证 closed/Unknown。任务已通过协议写入 `TASK_STATE_OK`，并保留 `7A9C48=claimed`、`6F7563=queued` 的真值。

TASK_STATE_OK
