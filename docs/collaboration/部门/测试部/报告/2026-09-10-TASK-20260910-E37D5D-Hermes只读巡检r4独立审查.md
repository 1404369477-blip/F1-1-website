---
type: audit_report
department: 测试部
target: TASK-20260910-95D001 Hermes 微信只读巡检 r4 中文报告与升级
status: final
date: 2026-09-10
related_task: TASK-20260910-E37D5D
decision: pass
tags: [Hermes, MCP, readonly, independent-review]
summary: 固定 r4 的格式化、协议边界、定向升级及 M1 安装后实际工具调用通过独立审查；Gateway 新版与 r4 微信准确性、手机收件未验证，开发任务保留等待用户输入。
---

# Hermes 只读巡检 r4 独立审查

**本报告的最终结论为有明确范围的 pass：冻结代码、升级及 M1 工具层通过；Gateway 新版与微信准确性、手机收件尚未验证。** 该分层出口符合审核任务 `TASK-20260910-E37D5D` 的既定验收约定，实施任务 `TASK-20260910-95D001` 继续等待用户输入，不能整体完成。审核者 `测试部/agent_options_research` 未参与实现或安装 helper 开发，未修改生产配置、数据或运行服务；安装由主控执行。

本报告承接[既有合同](../../../../reviews/2026-09-10-Hermes微信只读巡检.md)、[r3 独立审查](2026-09-10-TASK-20260910-E37D5D-Hermes只读巡检r3独立审查.md)及随后真实微信回复准确性为 `fail` 的 [r3 附录](2026-09-10-TASK-20260910-E37D5D-Hermes微信回复r3准确性附录.md)。旧报告和证据均保持原字节；r4 工具层修复通过不自动关闭旧版微信出口发现，实际新版回复需另行补验并提交附录。

## 已核实结论

r4 已修复 r3 微信回复暴露的两个准确性问题：把 UTC 钟面写成北京时间；依据过旧成功记录和当前停止开关推断任务未运行或人为原因。Python MCP 现在返回确定性的中文 `content`，所有业务时间均有完整日期、北京时间 UTC+8 和结果有效期，同时以 `structuredContent` 保留原有严格 DTO。工具描述要求按给定报告回复，明确当前观测、未知范围及尚未查明的根因。采样 Node、状态阈值、固定输入、备份校验及生产读取范围均未变化。

审核中新增的控制单项失败反例曾暴露首版 formatter 未显示 control 的问题：其他组件健康、全局停止为 `stopped` 时，总体失败却没有对应文字。最终 Python `9521cce2…` 已显示控制状态、阶段、停止/恢复/门控当前值，并标明操作来源和根因未查。反例按真实 Node 采样 DTO 重新验证通过。

本地候选、升级 helper、M1 安装字节及实际 SDK 调用没有剩余阻断发现。Gateway 热加载与新微信回复尚待用户完成。工具结果中的网站 `failed` 为有效的巡检发现，不代表 MCP 协议错误，也不表示本次任务已修复网站问题。

## 候选身份

| 对象 | 已复算 SHA-256 / 身份 |
|---|---|
| release | `candidate-site-status-20260910-r4` |
| payload | 9 文件、103,473 字节；安装目录共 12 文件 |
| manifest | `5970667e5bcb5d5c7210d0a09cd02470e8b6f04c72ec9ff21bbda3c56a7c7ad3` |
| status config | `ed93f3acf8fb468cec5c4c575ec36b00ffed3703c5e3c06fe62095992eb8be7f` |
| MCP Python | `9521cce27a50e4c5632c5f3b3769c0fb5bcd17d000ec987814705852322faaba` |
| 升级 helper | `2945eaf5fc5299d889d9e78e5b0a7d4cbc59e41f91f9446531bf18616fa776b1` |
| 冻结基础 helper | `670d2268e1fbe5ffe9b3a8c554fe38092c8af92b0f28368a70dcc10769cee61d` |
| 升级前 Hermes config | `a01adaea5172e2a72972c53377cbbafec7775da63bcc985e65dae64c866c8a1d` |
| 升级后 Hermes config | `03941a9b10e3f967c13c005bbe710c9744589b185076fafd52941e982d990fee` |
| 安装回执 | `1f9a501312113935653e3184fd04c7ebd95d0338f707c452484e4994c985638e` |

审核者重新复算 stage/source 字节，确认相对 r3 只有 `app/scripts/f1-status-mcp.py` 变化。七个 Node/TypeScript 文件和 ESM 元数据逐字节不变，导入闭包沿用已通过的 r3；没有新增 Python 依赖。因此未机械重复此前的 69 项 Node 用例和已关闭的备份反例。

## 定向验证

| 验证 | 审核结果 | 证据 |
|---|---|---|
| 独立格式化及边界 | 10/10：真实历史 DTO 时区、北京时间跨午夜、四类 unknown/null、Admin/M1/备份范围、双通道不变性、控制单项失败、handler 拒绝参数 | `adversarial-format-r4-9521cce27a50.json` |
| 独立实际 stdio 协议探针 | 7/7：URL/command/path/env/args/未知工具均拒绝且不启动子进程；空参数仍为固定 argv、净化环境 | `adversarial-protocol-r4.json` |
| 升级 helper 隔离自测 | 最终 helper 13/13；仅临时合成目录 | `upgrade-selftest-r4-configrevision.log` |
| 独立升级反例 | 最终 helper 5/5：Unicode/引号/CRLF 精确保留；旧 manifest 漂移；CAS 不能绕过固定前像；外部 YAML alias；回退错配 | `adversarial-upgrade-r4-configrevision.json` |
| stage/source/旧版字节 | 唯一 Python 变更，9 文件闭包通过 | `stage-r4-review.json` |
| 安装前配置漂移复核 | 旧配置和实际配置的完整 YAML 值及类型相同，f1_status 相同；只更新固定 SHA 常量，未放宽检查 | `r4-config-drift-independent.json`、`upgrade-constant-revision-r4.json` |
| M1 暂存候选实际 SDK | SDK 1.26.0，唯一空参数工具，严格 DTO 通过，标准文本精确等于冻结 formatter | `m1-staged-sdk-r4-validation.json` |
| M1 安装后独立文件/config/进程 | 新旧 release payload、文件集合/权限、固定入口、新回执、两个配置字节及四 PID 全部通过 | `m1-installed-files-r4.json` |
| M1 安装后审核者实际 SDK | 23:47:56.147 采样；严格 DTO 通过；标准文本精确匹配；`isError=false` | `m1-installed-sdk-r4.json` |
| Gateway/微信初次元数据检查 | 23:49:07 Gateway 仍挂 r3；没有 r4 注册或新工具结果 | `gateway-metadata-r4-initial.json` |

证据均位于 `scratch/hermes-site-status-review-20260910/`。主控与开发方另外报告当前 Python 测试 23/23 通过；上表单独列出审核者实际执行的检查，未把开发方声明当成独立验证。旧候选的控制遗漏复现和首次 helper 测试继续保留，最终结论绑定上表哈希。

## 升级和实际状态

安装前只读预检发现 Hermes config 字节从旧 r3 安装后的 4,434 字节变为 2,539 字节。审核者在 M1 以冻结基础 helper 读取原配置备份并重建旧 r3 后像，确认 SHA `1af5a2cc…`，然后严格比较现配置的 YAML 值及类型，结果完全相同。写入者身份未确定。升级 helper 保留原稿，仅将固定前像 SHA 改为 `a01adaea…`；把这一常量换回后，完整源码精确还原此前审查通过的 `a465b6ff…`。最终 helper 的隔离测试和独立反例重新通过，实际 r4 dry-run 在重新绑定固定前像之后通过。

升级只修改 `mcp_servers.f1_status.args[1]` 和 `env.F1_STATUS_CONFIG_PATH` 两个标量，从 r3 路径切到 r4。真实配置恰好仅两个字符 `3` 变为 `4`，其他字节、配置值、单工具白名单和关闭项保持一致。原配置先保存在私有备份中，回退继续绑定前后 SHA 和对应升级对。基础 helper 通过固定 hash 后才载入，r3 manifest/config/payload 固定不漂移，r4 目录排他创建、权限 0700/0600，无符号链接或额外文件。

审核者在 `2026-09-10T15:46:45.507095+00:00` 重新执行 M1 只读安装检查，确认配置、新回执和冻结候选身份，原 Gateway/Public/Admin/tunnel 仍分别为 PID 1080/1070/20563/48316。安装没有重启它们。单操作者窗口、advisory lock 与最终检查到 POSIX replace 间不具有内核条件替换保证的已披露限制沿用 r3，未在本次夸大为任意同 UID 并发写者隔离。

审核者随后亲自通过 M1 已安装 SDK 1.26.0 调用唯一 `get_site_status({})`，观测于北京时间 `2026-09-10 23:47:56.147`，有效至 `23:48:56.147`。这份历史样本总体 `failed`：RSS 成功记录过旧、恢复点过旧、公网在 M1 视角网络请求失败；本机公开入口 200，备份 manifest 和两类验签均通过，Admin loopback 404 仅验证边界拒绝，私网登录未知。输出完整保留这些区分及未知根因。此采样已过 60 秒有效期，不能作为持续当前状态。

## Gateway 与微信待补验

已核 M1 `gateway/run.py` 的 `_execute_mcp_reload`：重连后会对每个缓存 agent 调用 `refresh_agent_mcp_tools`，工具名称相同也刷新描述和定义。因此 r4 可使用用户手动 `/reload-mcp` 热加载，不需要重启。此源码事实与实际加载状态分开记录。

截至北京时间 `2026-09-10 23:49:07`，一次只读元数据检查发现 Gateway 1080 的固定 wrapper 子进程仍为 r3 的 68208；安装之后没有 r4 对应注册事件或新的微信 `mcp_f1_status_get_site_status` 结果。最新微信消息时间仍为 r3 最终回复的 `23:15:35.942465`。审核者只读取固定工具名、时间/角色元数据和注册事件，没有读取其他聊天正文或账户标识，也未轮询、代用户热加载、发送微信或调用模型。

主控已请用户在同一微信会话热加载并发送普通状态查询。待用户反馈后，需要补充：r4 固定子进程与本次注册事件；微信查询→唯一工具调用→携带标准文本与同一 DTO 的结果→紧随最终回复；北京时间/失败/未知/原因的准确性；用户收件确认。旧 r3 的手机回复存在已确认错误，不能替代 r4 证据。该末段未补齐之前，实施任务继续等待，不应整体完成。

本报告与所列证据由 `scratch/hermes-site-status-review-20260910/evidence-manifest-r4.json` 绑定，随后取得的新 Gateway/微信证据另加附录，不覆盖本次冻结文档。审核者未修改正式任务 JSON，任务转换由主控执行。
