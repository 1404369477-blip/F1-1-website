---
type: audit_report
department: 测试部
target: M4 B层工程结构与可复现锁基线
status: final
date: 2026-08-02
related_task: TASK-20260802-F23239
decision: fail
tags: [M4, B层, package-lock, 依赖基线, 只读, 机械复验]
summary: "B层静态基线本身全部通过（P0=0、结构P1=0），但当前 Spec 与 accepted ADR 仍把已完成的 app/package-lock 初始化写成仅有 README/初始化未完成，构成唯一实施状态时态 P1；按任务出口给 FAIL。Node24、npm ci、build、test 保持 C 层 pending。"
---

# TASK-20260802-F23239：M4 B 层工程结构与可复现锁基线复验报告

## 1. 唯一结论

**`decision=FAIL`（唯一问题为 Spec/accepted ADR 的实施状态时态 P1）。**

- B 层机器与文件静态基线：**P0=0，结构 P1=0**。
- 当前任务总出口：**P0=0，P1=1，FAIL**。唯一 P1 是现行 `docs/spec.md` 与 accepted M4 ADR 仍把已完成的 B 层初始化写成“仅有 README/初始化收据未完成/无 lockfile”。
- `app/` 具备唯一 `package.json`/`package-lock.json`，依赖集合、直接依赖精确版本、根 engines 和 `packageManager` 可由本地 JSON 重算。
- `package-lock.json` 为 `lockfileVersion=3`；399 个非根 package 条目全部有 `resolved` 和 `integrity`，其中 `399/399` 的 `resolved` 指向 `https://registry.npmjs.org/`，`399/399` 的 `integrity` 为 `sha512-*`。
- Node `24.18.0`、npm `11.16.0`、Next `16.2.11` 栅栏，版本文件、`engine-strict`/`ignore-scripts`、安全环境示例、accepted ADR canonical 目录、README 当前叙述、pending 命令非零和 `node_modules` 缺失均通过。
- Node24 实机、`npm ci`、SQLite、构建、测试、lint/typecheck、运行时安全和业务切片属于 C 层，仍保持 `pending`。本报告没有把当前机器的 Node25 或静态 lock 证据当作 C 层运行通过。

如果只判断 B 层落盘机器基线，结论为 PASS；任务要求现行实施状态同步，故上述唯一 P1 使本轮总判定为 FAIL。未发现第二个 P1，也未发现 P0。

## 2. 范围、依据与只读约束

### 2.1 任务与依据

本轮正式领取 `TASK-20260802-F23239`，只评 B 层静态结构与可复现锁基线。核对输入为：

1. `docs/spec.md`；
2. `docs/progress.md`；
3. `docs/decisions/system/2026-08-01-F1+1-M4本地Kickoff系统路线-accepted.md`；
4. `docs/collaboration/tasks/TASK-20260802-F8BF72.json` 与 `TASK-20260802-D2724D.json`；
5. `docs/collaboration/部门/开发部/报告/2026-08-02-M4-B层本地Web工程初始化报告.md`；
6. `docs/collaboration/部门/开发部/报告/2026-08-02-M4-B层lockfile恢复与门禁闭合报告.md`；
7. `app/package.json`、`app/package-lock.json`、版本文件、配置、README、脚本与目录。

accepted ADR 的 B 层合同要求创建单包 Next.js App Router 工程，固定 Node/npm/Next 版本，使用 npm lockfile，建立 `src/app`、`src/modules`、`src/server`、`src/styles`、`src/tests`、`migrations`、`fixtures`、`scripts` 等 canonical 目录，并让 C 层未实现命令 fail closed。

### 2.2 只读边界

本轮使用 Python 标准库解析 JSON、文本和路径，并进行独立静态断言；没有执行 `npm ci`、`npm install`、`npm run build`、`npm test` 或其他 C 层命令，没有安装依赖、没有运行 lifecycle、没有联网、没有改动系统，也没有访问真实 Base、provider、Collector、平台、AI、媒体、发布或部署服务。

除本测试报告和完成任务收据外，没有修改上游 `app/`、Spec、ADR、progress、lockfile 或开发部报告。

## 3. package/lock 依赖合同

### 3.1 根合同与直接依赖

| 项目 | `package.json` | `package-lock.json` | 结果 |
|---|---|---|---|
| name/version | `f1-plus-1-app` / `0.0.0` | `f1-plus-1-app` / `0.0.0` | PASS |
| lockfileVersion | — | `3` | PASS |
| Node engine | `24.18.0` | `24.18.0` | PASS |
| npm engine | `11.16.0` | `11.16.0` | PASS |
| packageManager | `npm@11.16.0` | 根合同一致 | PASS |
| root requires | — | `true` | PASS |

直接运行依赖精确集合为：

```text
next       16.2.11
react      19.2.0
react-dom  19.2.0
```

直接开发依赖精确集合为：

```text
@types/node        24.0.0
@types/react       19.0.8
@types/react-dom   19.0.3
eslint             9.39.4
eslint-config-next 16.2.11
typescript         5.9.3
```

独立解析结果：

- `package.json` 依赖集合与 lock 根 `dependencies`/`devDependencies` 完全一致；
- 9 个直接依赖对应的 `node_modules/*` lock 条目均存在，版本逐项精确相等；
- package/lock 没有第二套根依赖集合或未声明直接依赖。

### 3.2 399 个解析与完整性条目

对 `package-lock.json.packages` 去除根条目 `""` 后得到 399 个非根 package 条目：

| 检查 | 实际 | 结果 |
|---|---:|---|
| 非根 package 条目 | 399 | PASS |
| 有 `resolved` | 399/399 | PASS |
| `resolved` 为官方 npm registry | 399/399 | PASS |
| 有 `integrity` | 399/399 | PASS |
| `integrity` 为 `sha512-*` | 399/399 | PASS |
| 缺失 resolved/integrity | 0 | PASS |

没有发现以下来源或浮动标记：

- `latest`、`canary`、`preview`；
- `git`、`git+`、`github:`、`git://`、`file:`、`workspace:`、`link:`；
- `http://` 或非 `https://registry.npmjs.org/` 的 resolved。

因此本地 lockfile 的解析来源和 integrity 均可追溯，未发现非官方源或浮动依赖。

### 3.3 lifecycle 与 optional/native 元数据

静态 lock 元数据列出两个 `hasInstallScript` 条目：

| package | version | 本轮判定 |
|---|---:|---|
| `sharp` | `0.34.5` | 仅为 lock 元数据；未运行 install script |
| `unrs-resolver` | `1.12.2` | 仅为 lock 元数据；未运行 install script |

另有 64 个 optional 条目，包含平台相关的 Next SWC、sharp/libvips、unrs-resolver 等包。它们属于解析元数据，不构成 lifecycle 已执行证据。本轮没有执行安装或脚本；`app/.npmrc` 的 `ignore-scripts=true` 也保持开启。

## 4. 版本文件、配置与安全默认值

### 4.1 版本栅栏

| 文件/字段 | 实际值 | 结果 |
|---|---|---|
| `app/.nvmrc` | `24.18.0` | PASS |
| `app/.node-version` | `24.18.0` | PASS |
| `package.json.engines.node` | `24.18.0` | PASS |
| `package.json.engines.npm` | `11.16.0` | PASS |
| `packageManager` | `npm@11.16.0` | PASS |
| `next`/`eslint-config-next` | `16.2.11` | PASS |

当前工作机的 Node25/npm11.8 仅作为环境事实；没有把它们用于运行或构建验收。

### 4.2 npm、TypeScript、Next 与 ESLint 配置

- `app/.npmrc`：`engine-strict=true`、`ignore-scripts=true`、`audit=false`、`fund=false`；
- `app/tsconfig.json`：JSON 解析通过，`strict=true`、`noEmit=true`、bundler module resolution、`@/*` → `./src/*` 路径存在；
- `app/next.config.ts`：NextConfig 类型导入和 `reactStrictMode: true` 存在；
- `app/eslint.config.mjs`：使用 `eslint-config-next/core-web-vitals`，并忽略 `.next`、`out`、`build` 等生成目录；
- `app/.gitignore`：保留源码与 `.env.example`，忽略 `node_modules`、构建输出、本地数据库和真实 `.env`。

上述检查均为文件结构/内容检查，未执行 TypeScript、Next、ESLint 或构建命令。

### 4.3 `.env.example` 默认值

`.env.example` 只表达 local/loopback/fixture/mock/manual-only 安全默认值：

```text
APP_ENV=local
APP_BIND_HOST=127.0.0.1
APP_PUBLIC_ORIGIN=http://127.0.0.1:3000
SOURCE_CONFIG_PROVIDER=fixture
ADAPTER_MODE=mock
SUMMARY_MODE=fixture
MEDIA_MODE=fixture
PUBLISH_MODE=manual_only
REAL_FEISHU_IO=false
REAL_EXTERNAL_IO=false
REAL_FORM_SUBMIT=false
ADMIN_ACCESS_MODE=local_dev_only
```

文件未发现 token、secret、password、API key、代理变量或真实 provider 配置；`SOURCE_FIXTURE_PATH` 指向仓库内 M3 脱敏载荷。

## 5. canonical 目录、README 与 pending 命令

### 5.1 canonical 目录

accepted ADR 中列出的 25 个 canonical 目录全部存在，包含：

- `src/app/`、`src/app/(public)/`、`src/app/(admin)/`、`src/app/api/`；
- `src/modules/` 及 `source-registry`、`ingestion`、`normalization`、`dedupe`、`summary`、`media`、`review`、`publication`、`shared`；
- `src/server/` 及 `config`、`db`、`providers`、`workers`、`security`；
- `src/styles/`、`src/tests/`、`migrations/`、`fixtures/`、`scripts/`。

所需入口文件 `package.json`、`package-lock.json`、`.env.example`、`.nvmrc`、`.node-version`、`.npmrc`、`tsconfig.json`、`next.config.ts`、`eslint.config.mjs`、`README.md` 与 `.gitignore` 全部存在。

### 5.2 README 当前状态

`app/README.md` 已描述当前工程为 M4 B 层单包 Next.js App Router 静态脚手架，并准确写出：

- Node `24.18.0`、npm `11.16.0`、Next `16.2.11`、React `19.2.0`；
- lockfileVersion 3、399/399 官方 resolved、399/399 sha512 integrity；
- `sharp`/`unrs-resolver` 的 install-script 仅为元数据，未运行 lifecycle；
- `node_modules` 不生成；
- Node24、`npm ci`、构建、测试和业务切片仍属于 C 层。

README 中没有“app 尚未初始化”“无 lockfile”“DNS 阻断”等过期现行表述。

### 5.3 pending 命令必须非零

`package.json` 的以下命令均指向 `scripts/pending.mjs`：

```text
verify:env  db:migrate  seed:fixtures  worker:mock
test        test:contract  check
```

静态读取 pending 脚本确认其打印 C 层仍 pending，并设置 `process.exitCode = 1`。脚本没有 fetch、HTTP、child process 或其他网络/外部调用。`dev`、`build`、`start`、`lint`、`typecheck` 仍是正式工程命令，尚未在本任务运行。

### 5.4 `node_modules` 与生成物

`app/node_modules/` 不存在。本轮没有执行依赖安装，因此没有 lifecycle、构建输出、缓存或本地数据库生成物可被误认为 C 层收据。

### 5.5 Spec/accepted ADR 实施状态时态（唯一 P1）

落盘 B 层已完成的事实与两个当前合同入口的状态描述不一致：

| 当前文件与位置 | 现行文字的事实含义 | 与现场的差异 |
|---|---|---|
| `docs/spec.md:12` | `app/` 当前仍只有地基 README | 现场已有 package/lock、配置、canonical 目录和 scaffold |
| `docs/spec.md:93` | 开工预检称 `app/` 仍只有 README | 同上；该段也保留“无 lockfile”的旧状态 |
| `docs/spec.md:142-143` | B 初始化收据未完成，C 行称 app 仍仅 README | B 静态初始化和 lockfile 已由 F8BF72/D2724D 交付并 ACK |
| `docs/spec.md:157` | 依赖风险称当前没有 lockfile | `app/package-lock.json` 已存在且 399 条解析可重算 |
| `docs/decisions/system/2026-08-01-F1+1-M4本地Kickoff系统路线-accepted.md:101` | 根目录 app 仍只有基础 README | 现场已有 B 层 scaffold |
| accepted ADR `:333-334` | B 初始化收据未完成、C 表称 app 仍只有 README | B 层静态收口已完成，C 仍应只描述运行收据 pending |

这些位置属于当前 Spec/accepted ADR 的实施状态描述，已不是本轮应重新解释的历史报告。`docs/progress.md:10-11` 与开发部 D2724D 报告已经记录 B 层 PASS；旧 F8BF72 PARTIAL 和 2026-08-01 历史段落继续保留，不计作新的 P1。该跨合同时态漂移是一个唯一 P1，需由产品/统筹部后续同步文件；测试部不修改上游。

## 6. B 层验收矩阵

| 类别 | 证据 | 判定 |
|---|---|---|
| P0：package/lock 唯一性与 JSON 可解析 | 两文件解析、根依赖集合、直接版本逐项一致 | PASS |
| P0：精确版本栅栏 | Node24.18.0、npm11.16.0、Next16.2.11、版本文件和 packageManager 一致 | PASS |
| P0：锁来源与完整性 | 399/399 官方 registry，399/399 sha512 integrity | PASS |
| P0：禁止浮动/非官方来源 | latest/canary/preview/git/file/workspace/link/非官方 HTTP 扫描为 0 | PASS |
| P1：配置与目录 | `.npmrc`、安全 env、TypeScript/Next/ESLint、25 个 canonical 目录 | PASS |
| P1：README 与命令边界 | README 当前准确；pending 命令静态 exit code=1 | PASS |
| P1：安装边界 | `hasInstallScript` 仅元数据；无 node_modules；本轮未安装 | PASS |
| P1：Spec/accepted ADR 实施状态 | 当前文本仍写 app 仅 README/初始化未完成/无 lockfile | **FAIL（唯一 P1）** |

**B 层静态落盘基线为 P0=0、结构 P1=0；当前任务总计 P0=0、P1=1，唯一结论 FAIL。**

## 7. 已验证与未覆盖

### 已验证

- package/lock 根合同、依赖集合、精确版本和直接 node_modules 条目一致；
- lockfileVersion=3，399/399 resolved，399/399 integrity；
- 所有 resolved 为官方 npm registry，未发现浮动 tag、git/file/workspace/link 或非官方源；
- Node/npm/Next 精确版本栅栏及 `.nvmrc`、`.node-version`、`.npmrc`；
- TypeScript、Next、ESLint 配置存在且静态内容符合合同；
- `.env.example` 的 fixture/mock/loopback/manual-only/REAL_* false 安全默认值；
- accepted ADR 的 canonical 目录和所需入口文件；
- README 已从旧初始化阻断叙述更新为当前 lockfile 状态；
- pending 命令静态设置非零退出，`node_modules` 缺失；
- `hasInstallScript`/optional native 元数据已区分为静态 lock 信息，没有安装执行事实。
- Spec/accepted ADR 的实施状态漂移已定位为唯一 P1；没有把 B 层静态文件缺陷误报成 P1。

### 未覆盖（C 层 pending/Unknown）

- Node `24.18.0` 实机与 npm `11.16.0` 实机运行；
- `npm ci` 安装收据、生命周期隔离的实际行为及依赖运行时加载；
- `node:sqlite`、WAL、事务、锁竞争、migration、recovery；
- `verify:env`、`lint`、`typecheck`、`build`、`test`、`test:contract`、`check` 的运行结果；
- Next 真实 loopback 端口、UI/API、mock worker、四页纵向切片、响应式/无障碍/AT；
- 运行时 XSS/SSRF/提示注入、deny-all/no-egress、审计和失败恢复；
- 真实 Base/provider/Collector、采集、表单提交、自动发布、部署和外部 IO。

这些项目继续保持 C 层 pending，不构成额外 P0/P1；当前总 FAIL 仅由第 5.5 节的实施状态时态漂移造成。

## 8. 错题自检

1. 只统计 lockfile 根条目之外的 399 个 package 条目，并分别核对 resolved 与 integrity；没有把根条目漏算或重复计数。
2. 同时比较 package 根 `dependencies`/`devDependencies`、lock 根依赖和 9 个直接 `node_modules/*` 条目，确认集合与精确版本一致。
3. 将 `sharp`、`unrs-resolver` 的 `hasInstallScript` 和 64 个 optional 条目视为静态元数据；没有据此声称脚本已执行，也没有把它们隐瞒为无风险。
4. 通过静态读取 pending 脚本确认非零退出；没有运行 Node25、npm、build 或 test 来越过 C 层边界。
5. 使用 accepted ADR 的 `src/tests/` canonical 目录，未采用旧报告中可能出现的 disposable `app/tests/` 槽位。
6. 没有把开发部 F8BF72 的旧 PARTIAL 事实改写为当前历史消失；本轮复验的是 D2724D 已恢复的 lockfile 基线，前序历史仍可追溯。
7. 对当前 Spec/accepted ADR 的“仅 README/初始化未完成/无 lockfile”文字进行了单独状态检查；没有将 2026-08-01 历史段落或 C 层 pending 误算为额外 P1。
8. 未修改 `app/` 或上游合同、未联网、未安装、未创建 `node_modules`，仅写入本报告和任务完成收据。

## 9. 任务收口

本报告提交后，将 `TASK-20260802-F23239` 标记为 `completed`，写入已验证、未覆盖与错题自检收据，并返回 `TASK_STATE_OK`。后续应先由产品/统筹部同步 Spec 与 accepted ADR 的 B 层实施状态，再在 Node24/npm11.16.0 的受控环境中另行执行 C 层；本报告不提前宣称 C 层通过。
