---
type: audit_report
department: 测试部
target: M4 A轴 v0.3 时态修订与机器基线
status: final
date: 2026-08-02
related_task: TASK-20260802-ABB9F8
decision: pass
tags: [M4, A轴, v0.3, 时态复验, 机器基线, 只读]
summary: "聚焦复验 PASS：当前时态一致，P0=0、P1=0；11 个 artifact SHA、6/5/9/3、16/4 与 M3 33/9/59/59 机器基线重算通过。Spec/ADR 仍 proposed，A 轴仍 NO-GO，B/C pending，外部能力保持关闭。"
---

# TASK-20260802-ABB9F8：M4 A 轴 v0.3 时态修订后聚焦测试复验报告

## 1. 唯一结论

**`decision=PASS`（A 轴静态候选聚焦复验）。**

- 当前任务出口：**P0=0，P1=0**。
- `TASK-20260802-D80846` 当前任务状态为 `acknowledged`，产品、数据、Spec、progress 和 M4 proposed ADR 的现行叙述已统一引用该 ACK。
- 首轮 v0.3 安全/测试复验的 `P0=0、P1=1、FAIL` 已作为历史记录保留，没有被本轮报告覆盖或改写。
- 当前候选仍是 `proposed`/并行输入；A 轴仍为 `NO-GO`，B/C 仍 `pending`/未验证，外部能力保持关闭。上述边界按任务口径单列，不升级为本轮 A 轴失败。

本报告只覆盖 A 轴现行时态与机器基线。没有把 B/C 的运行收据缺失解释成 A 轴 P0，也没有把 proposed 或 NO-GO 文字改判为 accepted。

## 2. 范围、输入与只读约束

只读核对的输入为：

1. `docs/spec.md`；
2. `docs/progress.md`；
3. `docs/decisions/system/2026-08-01-F1+1-M4本地Kickoff系统路线-proposed.md`；
4. `docs/collaboration/部门/产品部/报告/2026-08-02-M4-A轴第二轮产品合同修订报告.md`；
5. `docs/collaboration/部门/数据部/报告/2026-08-02-本地MVP数据合同v0.3-A轴阻断闭合.md`；
6. `docs/collaboration/tasks/TASK-20260802-D80846.json`、`TASK-20260802-BA178A.json`、`TASK-20260802-04E0A2.json`；
7. `data/mvp-contract-v0/` 与 `data/m3-base-shadow-import-v0/` 的 v0.3 机器产物；
8. 首轮测试/安全 FAIL 报告，用于证明历史保留。

本轮仅用本地只读解析与独立重算脚本读取 JSON、CSV、报告和任务收据。未运行数据生成器入口，未安装依赖，未联网，未访问浏览器、飞书、Base、provider、Collector、真实账号或其他外部资源；未修改上游产物。新增内容仅为本测试报告、任务完成收据及其事件记录。

## 3. 时态与门禁复验

### 3.1 D80846 及相关 ACK

| 对象 | 现行状态 | 独立核对结果 |
|---|---|---|
| `TASK-20260802-D80846` | `acknowledged`，revision 4 | 与数据部收口报告及统筹部 ACK 事件一致 |
| `TASK-20260802-BA178A` | `acknowledged`，revision 4 | 与产品 v0.3 修订报告一致 |
| `TASK-20260802-04E0A2` | `acknowledged`，revision 4 | 与数据 v0.3 收口报告一致 |
| 数据 v0.3 收口报告 | 当前 ACK 已写入 | 没有现行“待统筹 ACK”措辞 |
| 产品 v0.3 修订报告 | 当前 ACK 已写入 | 仍明确 `proposed`、A `NO-GO`、B/C pending |

针对当前状态文件的定向过期措辞扫描：

```text
仍在进行中 / 尚未交付 / 尚未 ACK / 待统筹 ACK ：0 命中
```

历史 FAIL 报告没有纳入“当前状态过期措辞”扫描的判定集合；它们按第 3.2 节作为不可覆盖的历史证据保留。

### 3.2 首轮 FAIL 历史保留

以下两个独立报告仍存在，frontmatter 和正文均保留首轮结论：

- `docs/collaboration/部门/测试部/报告/2026-08-02-M4-A轴v0.3跨合同定版复验报告.md`：`decision=fail`，`P0=0、P1=1`，唯一 P1 为 D80846 文档时态漂移；
- `docs/collaboration/部门/安全部/报告/2026-08-02-M4-A轴v0.3安全合同定版复验报告.md`：`decision=fail`，`P0=0、P1=1`，同一唯一时态问题。

本轮没有编辑、删除、覆写或把上述 FAIL 改成 PASS。当前报告只记录修订后聚焦复验的状态，时间顺序可由历史报告、D80846 ACK 收据和本报告共同重建。

### 3.3 proposed、NO-GO、B/C 与外部关闭边界

- `docs/spec.md`、M4 proposed ADR 和 `docs/progress.md` 均仍将 Spec/ADR 标记为 `proposed`/候选，并明确 A 轴 `NO-GO` 等待本轮复验及后续定版裁决。
- B 层初始化、package/lockfile、Node 24、SQLite、真实端口和 app 工程收据未验证；C 层 UI/API、运行时安全、构建、测试、恢复和真实部署收据未验证。它们维持 `pending`，没有被本轮升级为 A 轴 FAIL。
- 真实 Base/provider/Collector、真实采集、真实表单提交、AI/媒体外部调用、自动发布、部署、付费和外部 IO 均保持关闭。

因此本轮通过的是“时态修订后的 A 层静态复验出口”，不构成 Spec/ADR accepted 或 B/C 实现验收。

## 4. 机器基线独立重算

### 4.1 manifest 与合同计数

| 检查项 | 期望/合同值 | 实际值 | 结果 |
|---|---:|---:|---|
| manifest version | `mvp-local-v0.3` | `mvp-local-v0.3` | PASS |
| artifact SHA 条目 | 11 | 11，逐项重算相等 | PASS |
| state machines | 6 | 6 | PASS |
| idempotency key classes | 5 | 5 | PASS |
| invariants | 9 | 9 | PASS |
| published projections | 3 | 3 | PASS |
| security cases | 16 | 16 | PASS |
| reconcile cases | 4 | 4 | PASS |

`manifest.json` 引用的 11 个 artifact SHA 全部与落盘文件重算值相等，`artifact_hash_mismatch=[]`。没有发现合同目录漂移、第二套 v0.3 数据产物或生成器未冻结的迹象。

### 4.2 M3 影子导入基线

M3 复验结果为 **33/9/59/59**：

- `Source` 字段：33；
- `CapturedItem` 字段：9；
- 导入行数：59；
- `enabled=false` 行数：59（59/59）。

输入 CSV、payload 和 M3 manifest 的 SHA/行数/字段数/默认值逐项一致；`m3_errors=[]`。没有越批、重复、启用漂移或 M3 业务真值切换。

### 4.3 task、lease、operation 与 fence

- 11 个 task/operation/lease/payload/idempotency 记录的各自唯一计数均为 11；
- `task_id`、`operation_id`、`lease_token`、`payload_hash`、`idempotency_key` 均无重复；
- lease token 满足 32 字节十六进制形式，epoch、时间窗口、attempt 和 fence 校验通过；
- `fence_errors=[]`；16 个 source 状态覆盖 16/16，`paused` 未进入 onboarding enum，仅存在于 lifecycle 记录。

### 4.4 strict release hash、Publication/key 与内部 schema

- Content、Summary、ReleaseBundle、ReviewDecision 的可重建 hash 链错误数为 0；
- strict release snapshot / canonical payload 的字段重算通过，`hash_chain_errors=[]`；
- Publication/key 校验错误数为 0；Publication 总数 5，无重复 `(bundle, approved_hash)`、无重复 `public_id`；
- 现有发布任务与对应 Publication 的 canonical key 绑定通过，`publication_key_errors=[]`；
- internal contract schema SHA 独立重算为 `462605a2258d2922d9b982f490aeda3a1395f9e1dcf718fb8745e49db2afade8`，落盘引用一致；internal-only 记录为 2 个 SourceObservation、3 个 AuditEvent，未引入第二套领域实体或 M3 字段。

### 4.5 seed subset 与 no-egress

顶层 seed 层为 3 层，且子集 hash 均按其实际 canonical 对象重算通过：

| 子集 | 声明/实际 | hash |
|---|---:|---|
| `published-projection-seed` | 3/3 | PASS |
| `snapshot-failure-seed` | 1/1（object） | PASS |
| `internal-fixtures-seed` | 5/5（object） | PASS |

`external_calls=0` 的检查覆盖 manifest、fixtures、security、seeds、internal 五个范围，五项均为 0。没有发现真实 URL、凭证、真实媒体下载或外部网络调用。

## 5. P0/P1 对照表

| 类别 | 结果 | 判定依据 |
|---|---:|---|
| P0-01–P0-09 当前静态复验 | 0 | 时态已修订，机器合同、hash、计数、状态与 no-egress 复算通过 |
| P1 当前问题 | 0 | D80846 ACK 与现行文档已统一；首轮问题仅作为历史保留 |
| 首轮历史 P1 | 1（历史） | 两份首轮 FAIL 报告均保留，不计入当前出口 |
| B/C 未验证项 | 多项，pending | 按 A/B/C 分层保留，不倒置为本轮 A 失败 |

按照任务出口，当前唯一结论为 **PASS**。

## 6. 已验证与未覆盖

### 已验证

- D80846、BA178A、04E0A2 当前 `acknowledged` 及对应报告/文档时态一致；
- 首轮安全/测试 FAIL 历史存在且未被覆写；
- Spec/ADR `proposed`、A `NO-GO`、B/C `pending`、外部能力关闭边界未漂移；
- 11 个 artifact SHA；6/5/9/3、16/4；M3 33/9/59/59；
- task/lease/operation/idempotency 唯一性与 fencing；
- strict release hash 链、Publication/key、internal schema SHA；
- 三层 seed、三组 seed subset hash、全部 no-egress=0；
- `source_observation`/`AuditEvent` 仍为 internal-only，M3 不反写为业务真值。

### 未覆盖（保持 Unknown/pending）

- 原生 JSON Schema 引擎在目标运行时的执行收据；
- B 层安装、lockfile、Node 24、app 初始化、SQLite/WAL/CAS、真实 loopback 端口；
- C 层页面/API、构建、运行时 deny-all、XSS/SSRF/提示注入、并发/崩溃恢复和部署收据；
- 真实 Base/provider/Collector/采集、真实表单提交、自动发布、外部平台授权、生产和网络环境；
- 直接协作者 ACL 现场读取、OAuth scopes 最小化、平台条款/费用、AI 数据区域和图片权利。

这些项目在当前任务中保持未验证，不影响本次 A 轴 P0/P1 出口。

## 7. 错题自检

1. 重新检查 seed subset 的数据形状：`snapshot-failure-seed` 与 `internal-fixtures-seed` 是 canonical object，按 object 重算；没有把它们误当 list 造成假失败。
2. 把首轮 `P0=0、P1=1、FAIL` 视为历史证据，未因历史报告中的旧叙述重新制造当前 P1。
3. 将 `proposed`、A `NO-GO`、B/C pending 与“当前复验 FAIL”分开计数，未把待后置运行证据倒置为 A 轴 P0。
4. 对 D80846、产品、数据和 Spec/progress/ADR 进行同一时点交叉核对，确认现行 ACK 与历史 FAIL 的时间顺序可重建。
5. 未运行生成器、未写入上游文件、未安装依赖、未联网、未触发外部能力；所有数值均来自本地只读落盘对象的独立重算。

## 8. 任务收口

本报告提交后，按任务要求将 `TASK-20260802-ABB9F8` 标记为 `completed`，并写入独立 verified/unverified/mistake-check 收据。报告与任务状态收据均保留上述边界；后续若要把 A 轴从 `NO-GO` 推进到 accepted，须由统筹部在另行授权后收取产品/安全 PASS 及 B/C 实现证据。
