---
type: audit_report
department: 测试部
target: "TASK-20260812-E44650 / commit 95b21d Admin 运行闭包"
status: final
date: 2026-08-12
related_task: TASK-20260812-E44650
decision: fail
severity_count: { P0: 0, P1: 1, P2: 0 }
tags: [admin, git-closure, static-review, launchagent, credentials]
summary: "FAIL。commit 95b21d 的 admin:start 、Admin UI 资产、migration、生产依赖与 LaunchAgent 最终入口静态闭合；admin:prepare-macos 与 admin:status 共同引用的 app/scripts/admin-install-macos.ts 未进入 commit，为唯一确定性 Git 闭包缺口（P1）。最小白名单只增加该 4099-byte 文件；当前本地 commit 尚未推送，建议 amend 后以新 SHA 推送。动态机器路径、运行密钥和安装后 node_modules 保持 Unknown，列为部署前硬门。"
---

# Admin 检查点运行闭包只读核对

## 1. 结论

**FAIL；P0=0，P1=1，P2=0。**

`95b21d106c95afe076847df8a74442eb7c240de8` 不是可独立复现的完整 Admin 检查点：`app/package.json` 中两个命令直接指向未进入 commit 的 `app/scripts/admin-install-macos.ts`。从干净 checkout 执行 `npm run admin:prepare-macos` 或 `npm run admin:status` 会在入口解析阶段失败。

`admin-install-macos.ts` 是本轮找到的唯一确定性 Git 闭包缺口。`admin:start` 的递归本地 import、Admin UI 静态资产、两个 migration、LaunchAgent 最终进程入口以及 Admin 实际使用的第三方生产依赖均在 commit 中闭合。

## 2. 冻结身份与只读方法

| 项 | 值 |
| --- | --- |
| commit | `95b21d106c95afe076847df8a74442eb7c240de8` |
| parent | `5d5963671550b45e9c01fbc727bc6aeac73447e4` |
| tree | `9d71a028293a19b2742da48e1a7af2679708eb36` |
| commit 变更路径 | 33，全为文本文件 |
| 核对方式 | `git show/ls-tree/cat-file/diff-tree`、递归静态 import 解析、动态路径人工追踪、package-lock 静态解析、只读模式扫描 |
| 明确未做 | 0 应用运行、0 测试、0 build、0 浏览器、0 网络、0 修改/stage/commit |

复现时以上述 commit 为唯一 tree 输入：先从 `app/package.json` 取三个 `admin:*` root，递归解析 `.ts/.js/.json/index` 本地 import，再追踪 `readFile/resolve/join` 与 LaunchAgent `ProgramArguments`，最后对每个路径执行 `git cat-file -e 95b21d:<path>`。

## 3. 入口裁定

| package script | 指向 | commit 状态 | 裁定 |
| --- | --- | --- | --- |
| `admin:start` | `scripts/admin-service.ts` | 存在 | 静态闭合 |
| `admin:prepare-macos` | `scripts/admin-install-macos.ts` | **缺失** | **P1** |
| `admin:status` | `scripts/admin-install-macos.ts --status` | **缺失** | **同一 P1 的第二个可见后果** |

`app/ADMIN-SERVICE-PREP.md` 同样要求执行后两个命令，因此这一缺口同时打断 runbook 的准备与状态查询路径。

## 4. 可复现运行闭包

### 4.1 `admin:start` 递归本地 import：26/26 在 commit

```text
app/scripts/admin-service.ts
app/src/server/admin-service/auth.ts
app/src/server/admin-service/deployment.ts
app/src/server/admin-service/runtime.ts
app/src/server/admin-service/server.ts
app/src/server/admin-service/storage.ts
app/src/server/admin-service/webauthn.ts
app/src/server/config/env.ts
app/src/server/db/database.ts
app/src/server/db/profile.ts
app/src/server/providers/source-fixture.ts
app/src/server/review-real/backend.ts
app/src/server/review-real/error.ts
app/src/server/review-real/mapping.ts
app/src/server/review-real/migration.ts
app/src/server/review-real/projection.ts
app/src/server/review-real/repository.ts
app/src/server/review-real/routes.ts
app/src/server/review-real/schema.ts
app/src/server/review-real/security.ts
app/src/server/rss/repository.ts
app/src/server/rss/types.ts
app/src/server/security/cli.ts
app/src/server/security/log.ts
app/src/server/source-management/security.ts
app/src/server/source-management/types.ts
```

### 4.2 加入工作树 installer 后：28 文件，只有 1 个 worktree-only

installer 对上述闭包只新增：

- `app/scripts/admin-install-macos.ts` — **WORKTREE_ONLY / commit 缺失**；
- `app/src/server/runtime-config.ts` — commit 已存在。

工作树 installer 的所有其他本地 import 均指向上述 commit 文件；未发现第二个缺失的相对 import。

### 4.3 动态资产路径：5/5 在 commit

| 路径 | 用途 | 结果 |
| --- | --- | --- |
| `app/src/admin-ui/index.html` | Admin 入口 | 存在 |
| `app/src/admin-ui/app.css` | UI 样式 | 存在 |
| `app/src/admin-ui/app.js` | UI 交互 | 存在 |
| `app/migrations/rss-real/0001_rss_real.sql` | RSS 基础 schema | 存在 |
| `app/migrations/rss-real/0002_admin_review_publish.sql` | Admin/review schema | 存在 |

## 5. LaunchAgent 与 runbook

`deployment.ts` 渲染的 plist 使用以下最终进程参数：Node 路径、`--experimental-strip-types`、`<appRoot>/scripts/admin-service.ts`、`--manifest <manifestPath>`。`admin-service.ts` 与它的静态闭包都已在 commit；plist 明确为 `RunAtLoad=false` 与 `KeepAlive=false`。

LaunchAgent 的生成入口在缺失的 installer 内。所以 commit 具有最终服务入口，但干净 checkout 无法按 runbook 生成禁用态 plist、初始私有目录或查询候选状态。

## 6. 生产依赖静态闭包

Admin 服务非 Node builtin 的实际 import 为 `@simplewebauthn/server` 和 `zod`：

| package | package.json | lockfile | integrity |
| --- | --- | --- | --- |
| `@simplewebauthn/server` | production dependency | `13.3.2` | 有 |
| `zod` | production dependency | `4.4.3` | 有 |

lockfile 中同时存在根生产依赖 `fast-xml-parser@5.10.1`、`next@16.2.11`、`react@19.2.0`、`react-dom@19.2.0` 及 integrity。`tinyglobby` 依赖的 `fdir@6.5.0` 按 package-lock v3 结构存在于 `node_modules/tinyglobby/node_modules/fdir`，不是缺失依赖。

`node_modules` 实际安装结果本轮未运行确认，应在目标机使用 lockfile 安装收据硬门判定。

## 7. 凭据与本机值只读扫描

对 commit 的 33 个变更文件完整文本 blob，以及 worktree-only installer，扫描了 private-key header、GitHub token、AWS access key、JWT、`/Users/<name>` 绝对路径、高风险 secret 字面量、已知 LAN IP/SSH alias/Quick Tunnel host。结果：

- installer 中 0 个高置信凭据命中，0 个已知本机值命中；
- installer 仅包含环境变量名，机器路径通过 `homedir()`、`process.execPath` 和 `appRoot` 在运行时求值；
- 1 个 `lease_token='lease-data-test-1'` 模式命中位于测试 fixture，为明确合成值，不是凭据；
- `docs/progress.md` 含 1 个公开 Quick Tunnel 历史 hostname；该行不在 95b21d 新增 diff 中，且不参与 Admin 运行闭包。它属运营信息，不属凭据。

本轮未发现需要从最小 Git 增量剔除的凭据或机器私有值。

## 8. P1 与最小修正

### P1-01 — package/runbook 引用未提交 installer

**证据：** `admin:prepare-macos`、`admin:status` 和 `ADMIN-SERVICE-PREP.md` 均指向 `app/scripts/admin-install-macos.ts`；`git cat-file -e 95b21d:app/scripts/admin-install-macos.ts` 不成立，工作树有该常规文件。

**最小白名单增量：仅 1 个文件。**

| 路径 | 大小 | mode | SHA-256 |
| --- | ---: | --- | --- |
| `app/scripts/admin-install-macos.ts` | 4099 bytes | regular `0644` | `6ea70cf82d402af8e156113c6c28f96910ad6a5f70c5196071480172e55f5a67` |

不需要同步增加 package、UI、migration、runbook 或依赖文件；它们已在 `95b21d` 中且与工作树一致。

## 9. amend / 第二 commit 建议

**建议 amend 当前未推送的 `95b21d`，然后以新 commit SHA 执行非 force 推送。**

理由：

1. installer 与已提交的 package scripts、runbook、deployment 属同一个原子 Admin 检查点；
2. 当前仓库证据显示远程 ref 仍在 parent `5d5963…`，`95b21d` 因 Git Data API commit SHA 不一致未更新远程 ref；本轮依任务禁令未联网重查；
3. amend 可避免在历史中保留一个已知无法从干净 checkout 执行的中间检查点；
4. remote 仍在 parent 时，新 amend commit 可直接 fast-forward，不需要 force push。

执行者在 amend 前只需一次远程 ref 预检。如果远程已经含有 `95b21d`，则改用单独第二 commit，并禁止改写共享历史。

## 10. Unknown 与运行前硬门

下列数据只能在固定 M1 准备/部署时确定，本报告保持 **Unknown**：

- 生产 `appRoot`、Node 绝对路径、manifest 绝对路径与 `staticRoot`；
- data root、session key、recovery fence、projection verify key、bootstrap generation/hash；
- canonical origin、trusted identities、operator refs 与 passkey credential 文件；
- lockfile 安装后的实际 `node_modules`、目录权限和禁用态 plist 内容；
- LaunchAgent 真实加载、3101 loopback 服务、Mac/iPhone 私网访问。

上述项必须在真实准备收据中按精确路径、mode、owner、hash 和状态逐项判定；静态 Git 闭包报告不对它们给出 PASS。

## 11. 错题自检

- 只写入本报告与任务状态，未修改/stage/commit 任何 App 或发布文件；
- 未运行应用、测试、build、浏览器或网络请求；
- 未将 worktree-only installer 写成已进入 commit；
- 未将测试 fixture token 写成真实凭据，也未隐藏历史 Quick Tunnel hostname 的存在；
- 未把静态闭包结论扩大成目标机运行、安全或生产就绪结论。

TASK_STATE_OK
