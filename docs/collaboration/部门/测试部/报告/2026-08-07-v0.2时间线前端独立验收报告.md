---
type: audit_report
status: final
date: 2026-08-07
department: 测试部
target: TASK-20260807-6CD2FB v0.2 时间线公开信息流前端
related_task: TASK-20260807-4451C2
decision: pass
tags: [v0.2, timeline, public-ui, playwright, independent-acceptance]
summary: 独立只读验收通过：功能矩阵、feed/detail HTTP 闭环、对比度复核、1440/1024/390 三档视觉证据与可访问抽查全部完成（Playwright 64+19 断言全过、test:public-http 全绿），无 P0/P1，1 个 P2 reduced-motion 小缺口已记录建议。
task_id: TASK-20260807-4451C2
accepted_task: TASK-20260807-6CD2FB
domain_stage: v0.2-timeline-frontend-acceptance
p0: 0
p1: 0
p2: 1
---

# TASK-20260807-4451C2｜v0.2 时间线前端独立验收报告（测试部）

> 验收对象：开发部 `TASK-20260807-6CD2FB` 产出（v0.2 时间线落地公开信息流前端）
> 基线：`design/ui/F1+1-v0.2-实现级设计合同.md`、`design/ui/F1+1-v0.2-全站设计/F1+1-v0.2-对比度验证.md`
> 开发实现报告：`docs/collaboration/部门/开发部/报告/2026-08-07-v0.2时间线落地公开信息流实现报告.md`（含 6 项偏差声明）
> 方式：**只读验收，未修改任何实现文件**；全程本地 127.0.0.1，无外网。

## 0. 总结论

**通过（PASS）**。六项验收内容全部达成，未发现 P0/P1 阻断缺陷：

- **功能矩阵**：时间线条目 / lightbox / 7 状态 / 搜索 / URL 参数全部按合同落地并经浏览器实测。
- **HTTP 闭环**：`/api/public/feed`、`/api/public/stories/{publicId}`、首页 `/`、详情页 `/stories/{id}` 均可访问；`externalCalls=0`、零泄漏。
- **对比度复核**：独立 OKLCH→WCAG 计算与对比度验证表逐项一致，深/浅双主题文本 AA 全部达标，非文本边框 ≥3:1。
- **三档视觉证据**：1440 / 1024 / 390 三档 PNG 已产出（`scratch/visual/`），可直接打开预览。
- **可访问抽查**：键盘可达、可见焦点、44px 触控、reduced-motion / reduced-transparency / forced-colors 降级均已落实（含 1 个 P2 小缺口，见 §6.5）。
- **开发部 6 项偏差**：复核后全部接受；其中 2 项标注「待真实内容/DTO 复验」。

自动化证据：Playwright 断言 64 项全过 + 状态/深链断言 19 项全过 + `test:public-http` 全绿。

---

## 1. 验收环境与方法

| 项 | 值 |
|---|---|
| Node | **v24.18.0**（`/tmp/node240/node`，npx node@24.18.0 精确二进制；默认 v26.5.1 被 engines 拒绝） |
| npm | **11.16.0**（`/opt/homebrew/opt/node@24/bin/npm`） |
| 干净环境 | `env -i PATH="/tmp/node240:/opt/homebrew/opt/node@24/bin:/usr/bin:/bin" HOME="$HOME"`，排除 `HTTPS_PROXY/HTTP_PROXY/ALL_PROXY/NO_PROXY/*_KEY/*_TOKEN/CLAUDE` 等；未运行 `npm run dev` |
| 运行模式 | 生产 `npm run start`（`scripts/serve.ts`，127.0.0.1:3000，Next 16.2.11） |
| 数据 | public-synthetic（`app/.env` 未改动），migration `public-synthetic-0003`，seed `12-public-synthetic` |
| 浏览器 | Playwright 1.58.2（chromium 无头，`~/Library/Caches/ms-playwright`） |
| 只读约束 | 未修改 `app/` 源码、`.env`、`.npmrc`、`package.json`；未新增依赖 |

验收脚本（测试部工具，放 `scratch/`）：

- `scratch/verify-contrast.mjs` —— OKLCH→WCAG 独立对比度计算
- `scratch/acceptance-visual.mjs` —— 三档视觉 + 功能/可访问断言（64 项）
- `scratch/acceptance-states.mjs` —— 状态矩阵 + URL 深链断言（19 项）
- `scratch/theme-check.mjs` —— 主题持久化与优先级断言

---

## 2. 功能矩阵（PASS）

### 2.1 时间线条目

| 合同条款 | 验收点 | 结论 |
|---|---|---|
| §3.1 时间左列 | `.tl-time[aria-hidden="true"]`；`.tl-t` 时刻 mono 11px 上对齐分类行、`.tl-d` 日期 display 20px/600 下对齐标题行 | ✅ 实测 `aria-hidden=true`、`HH:MM`/`MM-DD` 正确、字号/字体符合 |
| §3.1 主图 | `.ph-main` `tabindex=0 role=button aria-label="放大图片"`；原始比例 `max-width:100%` + `max-height:255px`、4px 圆角、`cursor:zoom-in` | ✅ 实测存在且可聚焦 |
| §3.1/§4.3 缩略图 | 单图内容不渲染缩略图（`imgs.length===1` 时无 `.ph-thumbs`） | ✅ 生产 DTO 恒单张 synthetic → 实测 `thumbs=0`，符合合同；真实多图路径未触发（见 §8） |
| §4.2 就地展开 | 原生 `<button>`、`aria-expanded`/`aria-controls=det-{id}`、`0fr→1fr`、**一次一个**、`#tl.is-focusing` 高亮+其余 `opacity:.38+grayscale(.85)`（触屏 .5/.7）、`scrollIntoView({block:'start'})+scroll-margin-top:20px`、点击外部自动收起+移除 URL `open` | ✅ 实测：展开/一次一个/外部收起/`open` 参数移除全部通过 |
| §4.7 证据行 | `信源 · [作者] · 时间`；无作者时跳过作者段；不带「来源/作者」标签；原文为真实 `<a href>` | ✅ 结构正确；原文分支当前因 DTO 恒 null 显示「原文暂不可用」（见 §7 偏差 2） |

### 2.2 lightbox

| 合同条款 | 验收点 | 结论 |
|---|---|---|
| §4.4 打开 | 点主图 / 主图 Enter/Space → 从源位置缩放放大（340ms）；`.app` 灰度+模糊+压暗 | ✅ `body.lb-open .app { grayscale(.85) blur(4px) opacity(.35) }`；实测打开 |
| §4.4 内容/角色 | `role="dialog" aria-modal="true" aria-label="图片放大预览" tabindex=-1`，打开后容器获焦 | ✅ 实测焦点移入 lightbox |
| §4.4 可见关闭按钮 | `.lb-close`（48px） | ✅ 存在且为唯一可聚焦项时可闭环焦点陷阱 |
| §4.4 Esc / 背景点击 | Esc 与点击背景（容器空白）均关闭，淡出 180ms，移除 `body.lb-open` | ✅ 实测 Esc 关闭 |
| §4.4 焦点返回触发点 | 关闭后 `lastTrigger.focus()` 回主图 | ✅ 实测关闭后焦点回到 `.ph-main` |
| §4.4 焦点陷阱 | Tab/Shift+Tab 在 lightbox 内循环（可聚焦项：翻页/关闭） | ✅ 实测连续 Tab 5 次焦点始终在 lightbox 内 |
| §4.4 方向键 | ArrowLeft/Right 翻页，序号取模回绕；单图时隐藏翻页 | ✅ 单图下方向键不翻页、不崩溃 |
| reduced-motion | 跳过缩放/淡入淡出动画 | ✅ JS 判定 `prefers-reduced-motion` 后跳过；实测可正常开合 |

### 2.3 7 状态

| 状态 | 触发路径 | 结论 |
|---|---|---|
| loading | 初始骨架（时间块 + 3 行文本 78%/100%/54%，shimmer 1.4s，`aria-busy`） | ✅ 实测（路由拦截延迟 feed 2.5s）骨架渲染、`aria-busy=true` |
| empty | feed 0 条 → `0`「暂无内容」/「当前没有可显示的聚合内容。」/ 刷新 | ✅ 实测（拦截返回空 items） |
| error | 首屏失败 → `!`「加载失败」/「聚合服务暂时不可用…」/ 重试 | ✅ 实测（拦截返回 500 Problem） |
| 404 | `404`「未找到这条内容」/「返回时间线」 | ✅ 代码存在（`STATE_COPY["404"]`）；**生产无自然触发路径，仅 dev `state` 参数 QA 驱动**，静态验证 |
| nomore | `∅`「已无更多内容」/「返回顶部」 | ✅ 生产 `hasMore=false` 时 `nomore-box` 渲染；实测 44px 操作按钮 |
| partial | `i`「部分内容加载成功」/「重试失败项」 | ✅ 代码存在（`loadMoreFailed` 触发）；**当前数据集 `hasMore=false` 无自然触发路径**，静态验证 + dev QA 参数 |
| offline | `ⓘ`「离线 / 受限」；有数据时保留时间线 + 离线横幅 | ✅ 实测（`context.setOffline(true)` 后 `#offline-box` 出现且 12 条保留） |

状态盒样式与合同一致：`state-box` 居中、44px display 状态码 `--accent`、16px/600 标题 `--fg`、13.5px 消息 `--muted`、44px 操作链接。每状态仅一条返回路径。

### 2.4 搜索

- ✅ 实时客户端过滤（输入即过滤 `#tl .tl-item`，大小写不敏感子串）
- ✅ 无匹配显示「没有匹配的条目」（`role="status"`），实测
- ✅ 过滤词同步进 URL `search` 参数；重渲染后重新应用
- ✅ `#search=<词>` 深链初始过滤，输入框值同步，实测

### 2.5 URL 参数（生产只暴露 theme/open/search）

| 参数 | 结论 |
|---|---|
| `theme` | ✅ 初始主题（URL 优先→localStorage `f1p1-theme`→缺省 dark）；写 `html[data-theme]`+`body[data-theme]`；切换持久化；实测默认深/切浅/ls 恢复/URL 参数优先均通过 |
| `open` | ✅ 按 feed 项 `public_id` 预展开并高亮（`#tl.is-focusing`），不自动滚动（滚动依赖展开 effect，命中才滚）；无匹配忽略；实测深链展开+高亮+无匹配忽略 |
| `search` | ✅ 见 §2.4 |
| `width` / `state` / `reveal` | ✅ **生产构建被 `IS_DEV` 显式隔离**；实测生产 `#state=loading&width=390&reveal=1` 被忽略（时间线正常渲染、无 `html[data-reveal]`、无 `body[data-width]`） |

hash 参数：`parseHashParams` 用 `location.hash` 分隔 `&`/`=`、值 `decodeURIComponent`（畸形段忽略）；写回 `history.replaceState`（沙箱失败 noop）。`state!=='timeline'` 时删除 `open`。

---

## 3. HTTP 闭环（PASS）

### 3.1 接口层（`npm run test:public-http`，本地 127.0.0.1:3000）

全部断言通过（矩阵全绿，`failures=[]`）：

- 健康：`scope=local-only / status=ready / reasonCode=ok / dataGate=accepted-public-synthetic / externalCalls=0 / migration=public-synthetic-0003 / seed=12-public-synthetic`
- feed：200、`cache-control:no-store`、无 CORS 头、schema `public-read-v0.1`、12 条、`published_at/public_id` 双键降序、page closed（`hasMore=false, nextCursor=null`）、`originalLink.enabled=false & url=null`
- 过滤：4 个 `contentType` 过滤正确且 no-store；`source=src-active`（12 条）/ `src-missing`（0 条）
- cursor：11 条、首条排除；`source`+cursor 作用域不匹配 → 400 `PUBLIC_CURSOR_SCOPE_MISMATCH`
- 非法查询：8 组（`limit/source 空/重复 source/contentType 未知/source 超长/cursor 单键/cursor 畸形`）全部 400 + `application/problem+json` + 对应 `reasonCode` + 正确 `instance` + 不回声
- detail：200 结构校验（story + relatedItems≤3 且互异且不含自身）、404 `PUBLIC_STORY_NOT_FOUND`、400 `PUBLIC_ID_INVALID`
- 泄漏：`absolute_path=0, scheme_url=0, runtime_keyword=0, unexpected_field=0`（zero-leak）
- 收尾：SIGINT 停止、3000/3001 端口清空、`externalCalls=0`

### 3.2 页面层（Playwright）

- 首页 `/`：200，`body` 含 `F1+1` 与「时间线」标记 ✅
- 详情页 `/stories/{publicId}`：200，内容渲染 ✅

---

## 4. 对比度复核（PASS，独立计算）

用 `scratch/verify-contrast.mjs` 从 `globals.css` 实际 token（OKLCH）独立换算 OKLab→线性 sRGB→WCAG 相对亮度→对比度，与 `F1+1-v0.2-对比度验证.md` 逐项对比：

| 项 | 深色 实测值 | 深色 基线 | 浅色 实测值 | 浅色 基线 | 结论 |
|---|---|---|---|---|---|
| `--fg` on `--bg` | 16.60 | 16.60 | 15.87 | 15.87 | ✅ |
| `--fg-strong` on `--bg` | 18.96 | 18.96 | 18.26 | 18.26 | ✅ |
| `--muted` on `--bg` | 6.88 | 6.88 | 6.53 | 6.53 | ✅ |
| `--faint` on `--bg` | 5.02 | 5.02 | **4.64** | **4.64** | ✅（浅色临界，未再降） |
| `--accent` on `--bg` | 8.36 | 8.36 | 6.13 | 6.13 | ✅ |
| `--accent` on `--surface` | 7.56 | 7.56 | 6.68 | 6.68 | ✅ |
| `--signal` on `--bg` | 6.93 | 6.93 | 5.91 | 5.91 | ✅ |
| `--border-strong` on `--bg` | **3.03** | 3.03 | **3.08** | 3.08 | ✅（非文本 ≥3:1） |

说明：`--border-strong` 实测屏幕色为深 `#585e65` / 浅 `#888d92`（由 `oklch(0.48 0.014 252)` / `oklch(0.64 0.01 250)` 精确换算），与验证表页内标注的近似 hex（`#4a5159`/`#a9afb4`）不同，但**比值与表完全一致且 ≥3:1**——表中 hex 为近似展示值，验收以 CSS 实值 + 比值判定为准。浅色 `--faint` 4.64 处于临界下限，实现未再降透明度/亮度，符合合同约束。

**抽查关键文本/边框 token 全部达标。** 未覆盖：真实摄影最差裁切区文字对比、玻璃表面文字（生产无玻璃浮层），见 §8。

---

## 5. 三档视觉证据（PASS，PNG 可直接打开）

产出目录：`scratch/visual/`（共 11 张，全部经像素统计确认非空白、内容丰富）：

| 证据 | 文件 | 尺寸 |
|---|---|---|
| **1440 深色全页** | `scratch/visual/v0.2-timeline-1440.png` | 1440×5953 |
| **1024 深色全页** | `scratch/visual/v0.2-timeline-1024.png` | 1024×5953 |
| **390 深色全页** | `scratch/visual/v0.2-timeline-390.png` | 390×6564 |
| 390 移动视口（isMobile+touch） | `scratch/visual/v0.2-timeline-390-mobile.png` | 390×6564 |
| 浅色 1440 | `scratch/visual/v0.2-timeline-light-1440.png` | 1440×5953 |
| 就地展开态 1024 | `scratch/visual/v0.2-timeline-expanded-1024.png` | 1024×6162 |
| lightbox 打开 1024 | `scratch/visual/v0.2-lightbox-open-1024.png` | 1024×768 |
| 状态：loading | `scratch/visual/v0.2-state-loading-1440.png` | 1440×900 |
| 状态：empty | `scratch/visual/v0.2-state-empty-1440.png` | 1440×900 |
| 状态：error | `scratch/visual/v0.2-state-error-1440.png` | 1440×900 |
| 状态：offline | `scratch/visual/v0.2-state-offline-1440.png` | 1440×900 |

断言（Playwright 实测）：三档均 12 条、主图存在、单图无缩略图、**无页面级水平滚动**（`scrollWidth<=clientWidth`）；390 下时间/日期并排（`flex-direction: row-reverse`）、日期 18px、DRAFT 徽标不渲染；浅色 `html[data-theme]=light`。

> 预览提示：PNG 为全页长图（高 5900–6600px），可直接用系统「预览」打开；如需快速查看首屏，可自行裁剪顶部视口。

---

## 6. 可访问抽查（PASS，含 1 个 P2 建议）

### 6.1 键盘可达

- ✅ 条目为原生 `<button>`（Enter/Space 原生触发）
- ✅ 主图/缩略图 `tabindex=0` + Enter/Space 打开 lightbox（实测 Enter 打开）
- ✅ 搜索、主题 seg、lightbox（Esc/方向键/焦点陷阱）均可达

### 6.2 可见焦点

- ✅ `:focus-visible { outline: 3px solid var(--focus); outline-offset: 2px }`（全局，含条目按钮）
- ✅ Playwright 实测聚焦 `.tl-summary-btn` 后 computed outline 为 3px solid `--focus` 色 + 2px offset

### 6.3 触控目标（390 移动视口实测）

- ✅ `.tl-summary-btn`、`.seg button`、`.search`（外层命中区）、`.sb-action`、`.ph-main` 均 ≥44px
- ✅ lightbox `.lb-close`/`.lb-nav` CSS 48px
- 说明：`#search-input` 字形高度约 18px，但**命中区为 `.search` 外层（min-height:44px + padding）**，实测外层 44px，满足合同「搜索框 44px」；`load-more` 按钮在当前数据集（`hasMore=false`）不渲染，由 `nomore` 状态盒 44px 主动作替代（预期行为）

### 6.4 reduced-motion / reduced-transparency 降级

- ✅ `@media (prefers-reduced-motion: reduce)`：`.tl-live` 呼吸动画停、`.trace`/`.tl-caret` 取消 scale、`.search input` 保持展开态（实测 caret transform none、live 动画 none）
- ✅ JS 侧：lightbox 缩放/淡入淡出跳过、滚动 `behavior:'auto'` 且不延迟
- ✅ `prefers-reduced-transparency: reduce` 与 `[data-rt]`：玻璃 → 不透明 `--surface`、`backdrop-filter:none`（生产无玻璃浮层，机制在位）
- ✅ 低性能 `@media (update:slow),(pointer:coarse) and (max-width:700px)`：blur 8px 降级
- ⚠️ **P2 建议**：合同 §6.5 要求 reduced-motion 下「过渡/动画 → .01ms、展开动画跳过」；实现已覆盖 `.tl-live/.trace/.tl-caret/.search` 及 JS 动画，但 **`.tl-collapse`（手风琴 `grid-template-rows` 240ms）与 `.tl-item`/`.tl-summary-btn` 等 CSS transition 未在 media 查询内归零**，reduced-motion 下展开仍会轻微过渡。影响小、不阻断验收；建议开发部在 `@media (prefers-reduced-motion: reduce)` 中追加 `* { transition-duration: .01ms !important; animation-duration: .01ms !important; }` 一类全局兜底（由开发部在后续任务处理，非本任务范围）。

### 6.5 forced-colors

- ✅ CSS 已含 `@media (forced-colors: active)`：顶栏/轴节点/图片/lightbox 改用 `Canvas`/`CanvasText`
- ⚠️ 真实强制色模式未在本环境复验（无系统级 forced-colors 开关），见 §8

### 6.6 语义

- ✅ `main id="main-content"` + `section` + `ol/li`；时间列 `aria-hidden`（真实时间在证据行）；lightbox `role=dialog aria-modal`；状态 `role="status"`；搜索无匹配 `role="status"`；骨架 `aria-busy`
- ✅ 展开/离线等状态均有文字+图标（不单靠颜色）

---

## 7. 与开发部 6 项偏差的复核意见

| # | 开发部偏差声明 | 测试部复核 | 结论 |
|---|---|---|---|
| 1 | 主图/lightbox 用合成占位图（tone 渐变 data-URI SVG） | 与合同「真实摄影未测」一致；`<img>` data-URI 无外联；支撑了 lightbox/键盘交互 | ✅ 接受 |
| 2 | 「前往原文」当前为禁用态（DTO `originalLink` 恒 `enabled:false/url:null`） | 前端保留真实 `<a href>` 分支（`hasOriginalUrl`），当前显示「原文暂不可用」，**不伪造外链**（M4 fail-closed）；`feed:originalLink` 断言全 false/null 佐证 | ✅ 接受；**待真实 DTO 出现有效 URL 后复验 `<a>` 渲染与普通导航** |
| 3 | 作者段判定（`unknown`/空隐藏） | `hasAuthor()` 正确（trim+lower 后剔除 `unknown`/`未知`）；当前 byline 恒非空，语义分支未实际触发 | ✅ 接受；**待真实内容复验** |
| 4 | 搜索置于顶栏（仅首页渲染） | shell 在 `pathname==="/"` 渲染搜索；详情页无时间线可过滤，合理 | ✅ 接受 |
| 5 | `next/image` 未使用（lint 3 警告） | 合成 data-URI 需原生 `<img>` + `tabindex/role` 支撑焦点/角色；未加 eslint-disable 隐藏，诚实保留 | ✅ 接受 |
| 6 | theme localStorage 键改 `f1p1-theme`（旧键不迁移） | 与合同 §4.1 一致；实测持久化/URL 优先级正确；历史存储不迁移属轻微用户体验影响 | ✅ 接受（合同明确） |

6 项偏差全部接受，未发现绕过 M4 门禁的行为；`externalCalls=0`、`REAL_*` fail-closed 保持。

---

## 8. 未覆盖项（如实保留）

1. **VoiceOver / NVDA / TalkBack 实机**：本环境无法做真实读屏，语义与 ARIA 仅做了 DOM 级抽查。
2. **原生 200% 缩放**、**真实 forced-colors 强制显示**：未在浏览器/系统层做原生缩放与强制色实测（CSS 规则已就位）。
3. **真实摄影最差裁切对比度、玻璃表面文字对比**：生产无玻璃浮层；真实图片未接入。
4. **真实多图缩略图导航 / lightbox 翻页**：当前 DTO 恒单张 synthetic 占位 → 生产无缩略图、无翻页路径；`ph-thumb` DOM/翻页逻辑仅静态核对。
5. **partial / 404 状态的生产自然触发**：当前数据集 `hasMore=false` 且无自然 404 路径，仅 dev `state` 参数 QA 驱动（代码/文案已静态验证）。
6. **用户最终体验确认**：待用户（最终确认人）打开 §5 三档视觉证据后给出体验结论。

---

## 9. 错题自检

- 已检查错题集（`docs/collaboration/错题集.md`，当前为空模板）：**无命中**。
- 只读验收：未修改任何实现文件；未新增依赖；未运行 `npm run dev`；未触发外网。
- 测试部临时工具仅写入 `scratch/`（`verify-contrast.mjs`、`acceptance-visual.mjs`、`acceptance-states.mjs`、`theme-check.mjs`、`visual/*.png`），不属于 app 交付物。

---

## 10. 交付路径

- 本报告：`docs/collaboration/部门/测试部/报告/2026-08-07-v0.2时间线前端独立验收报告.md`
- 三档视觉证据（可直接打开预览）：`scratch/visual/v0.2-timeline-1440.png`、`scratch/visual/v0.2-timeline-1024.png`、`scratch/visual/v0.2-timeline-390.png`
- 其余视觉证据：`scratch/visual/`（浅色、展开、lightbox、4 状态）
- 自动化断言矩阵：`scratch/visual/acceptance-matrix.json`（64 项全过）、`scratch/visual/acceptance-states-matrix.json`（19 项全过）
- 对比度计算：`scratch/verify-contrast.mjs`
- 受验代码：`app/src/features/stories/feed-experience.tsx`、`app/src/features/stories/hash-params.ts`、`app/src/features/stories/timeline-search.ts`、`app/src/features/stories/public-api.ts`、`app/src/components/f1/f1-page-shell.tsx`、`app/src/components/f1/theme-preference.ts`、`app/src/app/globals.css`、`app/src/app/page.tsx`、`app/src/app/stories/[publicId]/page.tsx`

TASK_STATE_OK
