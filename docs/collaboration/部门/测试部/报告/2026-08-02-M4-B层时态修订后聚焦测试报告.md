---
type: audit_report
department: 测试部
target: M4 B层时态修订与C层预检边界
status: final
date: 2026-08-02
related_task: TASK-20260802-7746F8
decision: pass
tags: [M4, B层, C层, 时态复验, package-lock, 只读]
summary: "聚焦复验 PASS：现行 Spec、progress 与 accepted ADR 已统一为 B=completed/PASS、C=local preflight pending；F8BF72 PARTIAL 与 F23239 首轮 FAIL 历史均保留；399/399 官方 resolved 与 sha512 integrity 未漂移。P0=0、P1=0。"
---

# TASK-20260802-7746F8：M4 B 层时态修订后聚焦测试报告

## 1. 唯一结论

**`decision=PASS`，P0=0、P1=0。**

本轮确认 `TASK-20260802-7F3D22` 的状态同步已经生效：

- 当前 B 层为 `completed/PASS`，静态 scaffold、唯一 `package.json`/`package-lock.json`、版本文件、安全默认值和 canonical 目录已完成并通过审计；
- 当前 C 层为 `local preflight pending`，Node24、npm ci、SQLite、UI/API、build/test/security 和真实 loopback 运行收据仍未验证；
- 当前 Spec、progress、accepted ADR 均不再把 `app/` 写成仅有 README，不再把 lockfile 写成不存在，也不再把 B 初始化写成未完成；
- F8BF72 的 B 层 `PARTIAL` 历史和 F23239 首轮唯一 P1 `FAIL` 历史均保留；
- 真实 Base/provider/Collector、真实采集、真实表单提交、AI/媒体外部调用、自动发布、部署、付费和其他外部 IO 继续关闭。

## 2. 范围、依据与只读边界

本轮正式领取 `TASK-20260802-7746F8`，只读核对：

1. `docs/spec.md` 当前状态栏、开工预检证据、A/B/C 表与命令合同；
2. `docs/progress.md` 的 2026-08-02 当前记录与历史段落；
3. `docs/decisions/system/2026-08-01-F1+1-M4本地Kickoff系统路线-accepted.md` 的应用边界、A/B/C 矩阵、未验证项和变更记录；
4. `docs/collaboration/tasks/TASK-20260802-7F3D22.json` 及其产品状态同步报告；
5. `TASK-20260802-F8BF72.json`、`TASK-20260802-F23239.json` 及两份对应报告；
6. `app/package.json`、`app/package-lock.json` 和 `app/node_modules/` 状态。

本轮只使用本地文本、JSON、路径和 lockfile 解析；没有联网、安装依赖、运行项目、执行 `npm ci`、build、test 或 C 层预检命令，没有访问或操作任何外部资源，也没有修改上游文件。

## 3. 现行 Spec/progress/accepted ADR 时态

### 3.1 Spec 当前状态

当前 `docs/spec.md` 已形成一致状态：

- 状态栏将当前阶段写为 **M4 C 层本地工具链与能力预检窗口**，并明确 B 层静态初始化与 package-lock 已完成、C 层不宣称运行通过；
- 当前摘要写明 B 层静态初始化与 package-lock 已通过审计，`app/node_modules/` 尚不存在，Node24/npm ci/SQLite/UI/API/build/test/security 仍 pending；
- 开工预检证据写明 B scaffold 与 lockfile 已完成，399/399 resolved/integrity 完整，未运行 lifecycle；
- A/B/C 表中 B 为 `completed`，C 为 `local preflight pending`；
- 依赖风险段落写明 `app/package-lock.json` 已生成并完成静态审计，目标网络安装、Node24/npm ci、真实端口和 SQLite runtime 仍未验证；
- 命令合同明确命令已写入 `app/package.json`，执行收据属于 C 层预检/验收，不写成已通过；
- 真实外部能力仍关闭。

### 3.2 progress 当前记录与历史分层

`docs/progress.md` 的 2026-08-02 当前段落已明确：

- D2724D：B 层 lockfile 审计 `PASS`，399/399 resolved、399/399 integrity、依赖一致；
- 7F3D22：B 静态初始化与 package-lock 已完成并通过开发/安全/测试审计；
- 当前阶段：C 层本地工具链与能力预检窗口，Node24/npm ci/SQLite/UI/API/build/test/security/真实端口 pending；
- 外部 Base/provider/Collector/采集/表单/AI/发布/部署/外部 IO 继续关闭；
- A 轴 accepted、B 静态初始化与 lock 已完成、C 运行收据 pending。

历史段落仍保留 2026-08-01 的旧环境描述，例如当时 `app/` 只有 README；这些行处于历史日期段，任务失败路径明确要求不将其计为当前 P1。

### 3.3 accepted ADR 当前状态

`ADR-M4-KICKOFF-001 accepted` 已同步：

- 应用边界写明 `app/` 已具备 B 层静态 scaffold、唯一 package/lock、版本文件、安全默认值和 canonical 目录，`node_modules` 尚不存在；
- A/B/C 矩阵将 B 标记为 `completed`，将 C 标记为 `local preflight pending`；
- C 预检的 Node24、npm ci、SQLite、UI/API、build/test/security 运行收据仍列为未验证；
- 进入 C 轴前置条款写明 B 层静态初始化、package/lock/版本文件和最小 scaffold 已完成；
- 当前阶段变更记录收录 7F3D22，只同步实施状态，不改变 accepted 核心；
- 外部 provider、Base、Collector、采集、发布、部署和 IO 继续关闭。

### 3.4 当前过期措辞扫描

对现行段落扫描 `仍只有`、`只有 README`、`无 lockfile`、`初始化收据未完成`、`B 层未完成`：

- `docs/spec.md` 当前段落：0 个相关过期状态命中；
- `docs/progress.md` 2026-08-02 当前段落：0 个相关过期状态命中；
- accepted ADR 正文及当前 A/B/C 矩阵：0 个相关过期状态命中；
- 仅保留 2026-08-01 历史段落中的旧描述，按任务口径不计当前 P1。

## 4. 历史保留核对

### 4.1 F8BF72 PARTIAL

`TASK-20260802-F8BF72.json` 仍保留原始 B 层 `PARTIAL` 历史，原报告继续记录官方 registry/DNS 阻断导致的 lockfile 未生成事实。该历史没有被改写为 PASS；当前 B PASS 依据来自后继 D2724D、4CE4F0、F23239 和 7F3D22 审计链。

### 4.2 F23239 首轮 FAIL

`TASK-20260802-F23239.json` 及测试部报告仍保留 `decision=fail`、`P0=0、P1=1` 的首轮结论。唯一 P1 是当时 Spec/accepted ADR 对 B 实施状态的时态漂移；7F3D22 后续同步已关闭该当前问题，但没有改写 F23239 的首轮历史。

### 4.3 其他当前依据

`TASK-20260802-7F3D22.json` 当前为 `acknowledged`，产品状态同步报告为 `decision: pass`，明确 B completed、C preflight pending、历史保留和外部关闭。

## 5. package-lock 399/399 抽查

本轮再次独立解析 `app/package-lock.json` 与 `app/package.json`：

| 检查项 | 实际结果 | 判定 |
|---|---:|---|
| lockfileVersion | `3` | PASS |
| 非根 package 条目 | `399` | PASS |
| 有 resolved | `399/399` | PASS |
| 官方 `https://registry.npmjs.org/` resolved | `399/399` | PASS |
| 有 integrity | `399/399` | PASS |
| `sha512-*` integrity | `399/399` | PASS |
| package/lock 根依赖一致 | `true` | PASS |
| 直接依赖版本一致 | `true` | PASS |
| `latest/canary/preview/git/file/workspace/link/http://` 命中 | `0` | PASS |
| `app/node_modules/` | 不存在 | PASS |

本次抽查同时确认 `sharp@0.34.5` 与 `unrs-resolver@1.12.2` 的 `hasInstallScript` 仍只是 lock 元数据，未据此声称生命周期已运行；lockfile 未出现上游依赖集合或来源漂移。

## 6. P0/P1 结论与边界

| 类别 | 结果 | 说明 |
|---|---:|---|
| 当前实施状态时态 | PASS | Spec/progress/accepted ADR 已同步 B completed、C preflight pending |
| B 静态基线 | PASS | package/lock、399/399、版本/目录/配置均无漂移 |
| 历史保留 | PASS | F8BF72 PARTIAL、F23239 首轮 FAIL 未覆写 |
| C 边界 | PASS | 仅预检 pending，未写成 C 通过 |
| 外部边界 | PASS | 真实外部能力继续关闭 |
| P0 | 0 | 未发现当前阻断 |
| P1 | 0 | 历史旧措辞有明确历史标识，不计当前 P1 |

因此本轮唯一结论为 **PASS / P0=0 / P1=0**。

## 7. 已验证与未覆盖

### 已验证

- 7F3D22 状态同步后的 Spec、progress、accepted ADR 当前实施状态一致；
- B=completed/PASS，C=local preflight pending，外部能力关闭；
- F8BF72 PARTIAL 与 F23239 首轮 FAIL 历史存在且未覆写；
- lockfile 399/399 官方 resolved、399/399 sha512 integrity、根依赖和直接版本未漂移；
- `node_modules` 不存在，未运行 lifecycle；
- 当前文档没有把静态 lockfile 写成 npm ci/Node24/C 层运行通过。

### 未覆盖（保持 pending/Unknown）

- Node24.18.0 实机与 npm11.16.0 运行；
- `npm ci`、SQLite runtime/WAL/事务/recovery、真实 loopback 端口；
- UI/API、deny-all、session/Origin/CSRF、build/test/security、业务纵向切片；
- 真实 Base/provider/Collector、真实采集、表单提交、AI/媒体、自动发布、部署和其他外部 IO。

## 8. 错题自检

1. 将 `docs/progress.md` 2026-08-01 旧描述与 2026-08-02 当前段落分开读取，没有把历史旧状态计为当前 P1。
2. 读取 F8BF72 原始任务/报告和 F23239 首轮报告，确认历史未被后继 PASS 覆写。
3. 将 B 静态 package/lock 证据与 C 层 Node24/npm ci/运行收据分开；没有把静态审计写成安装或运行完成。
4. 抽查 399 个非根 lock 条目，分别验证 resolved、integrity、根依赖和直接版本；没有把“399 条”当作唯一证据。
5. 未修改 Spec、progress、accepted ADR、app、lockfile 或历史报告；未联网、未安装、未运行项目或外部能力。

## 9. 任务收口

本报告提交后将 `TASK-20260802-7746F8` 标记为 `completed`，写入 verified/unverified/mistake-check 收据并返回 `TASK_STATE_OK`。C 层仍需后续独立收据，真实外部能力继续关闭。
