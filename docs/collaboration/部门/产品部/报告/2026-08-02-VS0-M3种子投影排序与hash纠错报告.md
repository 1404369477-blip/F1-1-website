---
type: product_implementation_correction_report
status: final
decision: PASS
date: 2026-08-02
department: 产品部
task_id: TASK-20260802-E1CFC2
decision_id: ADR-M4-VS0-SEED-002
---

# TASK-20260802-E1CFC2｜VS-0 M3 种子投影排序与 hash 收据纠错报告

## 1. 结论

本任务已完成产品合同层的最小纠错，结果为 `PASS`。已建立 accepted successor [`ADR-M4-VS0-SEED-002`](../../../decisions/system/2026-08-02-F1+1-VS0-M3种子投影-successor-accepted.md)，以 `source_id` Unicode code point 升序算法为唯一权威；`e7a8312c70a9a49922aedb3cfbeaa190db8f5dce8d4ab45db1570748fc329f17` 为有效 sorted projection hash。前任 `ADR-M4-VS0-SEED-001` 仅更新 frontmatter 为 `superseded` 并保留 successor 指针，历史正文未改。

`docs/spec.md` 仅完成 VS-0 当前 ADR 引用和变更记录同步。未写入 `data/`、`app/` 或 Base，未切换真值/provider，未启动采集、表单、发布或任何真实外部能力。

## 2. 根因

前任 ADR §2.4 已规定 `rows: enriched_rows sorted by source_id ascending`，并明确 Unicode code point 排序；此前 `96d5caf625f62d059cc51a41d7c3b6a1db623d07cea00c4d256e2d841c693aa2` 来自 M3 原行序（`first=x_formula24hrs`、`last=x_autosport`、`sorted=false`）。因此历史收据与已接受排序算法不一致，形成机械合同冲突。

## 3. 机械复算证据

对当前 `data/m4-vs0-seed-enrichment-v0/source-seed-enriched.json` 做离线只读复算，未写回文件：

| 项目 | 结果 |
|---|---|
| rows | 59 |
| fields | 39 |
| 原始 first / last | `x_formula24hrs` / `x_autosport` |
| 原序是否已排序 | `false` |
| 原序 hash | `96d5caf625f62d059cc51a41d7c3b6a1db623d07cea00c4d256e2d841c693aa2` |
| 排序规则 | `sorted(key=source_id)`，Python 字符串/Unicode code point 升序 |
| 排序后 first / last | `x_afcorse` / `x_zhouguanyu24` |
| 排序后有效 hash | `e7a8312c70a9a49922aedb3cfbeaa190db8f5dce8d4ab45db1570748fc329f17` |

canonical 对象固定为：

```text
projection_object = {
  fields: Source.required in frozen schema order,
  rows: enriched_rows sorted by source_id Unicode code point ascending
}
canonical_projection_hash = SHA-256(canonical-json-v1(projection_object))
```

## 4. 影响、回退与未验证项

- 影响范围限于 VS-0 local fixture projection 的行序与实现收据；M3 原始 batch/fields/manifest、冻结 v0.3 data schema/state machine、6 组状态机、9 条不变量和既有领域实体不变。
- `96d5` 只作决策前 unsorted historical candidate，永远不得 PASS 或作为当前 seed 输入。若后续复算不等于 `e7a8`，必须 fail closed，保持不导入 seed；不能回退到 `96d5`。
- 已验证产品合同、前任正文保留和 Spec 引用的一致性；未验证数据部后续 validator 是否已用新收据回写 manifest，也未验证任何 seed import、Repository、UI/API、完整 R12、Base/provider 或真实外部链路。
- 回退需要新 ADR；禁止原地改动 accepted successor 或恢复旧未排序收据。

## 5. 错题自检与出口

- 错题：把 M3 原行序 hash 当成符合已冻结排序算法的当前收据。
- 修正：先固定 `source_id` Unicode code point 升序，再对同一 59×39 对象按 canonical-json-v1 复算；以 `e7a8` 为唯一有效收据。
- 边界复核：没有修改 data/app/Base，没有扩展 VS-0，没有把机械收据误写成真实资源或 seed import 放行。

## 6. TASK_STATE_OK

`TASK_STATE_OK`
