---
type: work_report
department: 产品部
task_id: TASK-20260809-B10D8D
title: 初版全功能追踪矩阵与零缺项实现合同报告
status: completed
observation_date: 2026-08-09
scope: 只更新产品合同、Spec、MVP、产品索引与系统 draft；不改 app/data/design/accepted ADR
---

# 初版全功能追踪矩阵与零缺项实现合同报告

## 1. 结论

产品部已把 `COR-20260809T102214-960E65` 固化为初版功能完整性合同。矩阵当前登记 50 个 Function ID，每行均含用户入口、后端/数据依赖、视觉锚点、成功/失败/恢复、责任部门/实施落点、独立证据和授权轴；状态只使用 `complete`、`user-gated`、`P1-blocker`。

当前计数：

- `complete`：12；
- `user-gated`：18；
- `P1-blocker`：20。

这些数量是当前证据时态，不是完成率目标。已确认功能没有因实现冲突而被删除；缺少真实用户出口或正式复验的能力继续列为 P1。真实外部请求、自动发布、部署、真实媒体、生产权限和跨网络 Admin 继续使用独立用户门禁。

## 2. 交付物

1. `docs/spec/F1+1-初版全功能追踪矩阵-v0.1.md`
2. `docs/decisions/system/2026-08-09-F1+1-公开多媒体数组successor实施合同-draft.md`
3. `docs/mvp.md`
4. `docs/spec.md`
5. `docs/spec/F1+1-v0.2-最终实现级产品合同.md`
6. `docs/spec/README.md`
7. 本报告。

## 3. 产品真值修订

### 3.1 最终公开 App

- 最终 v0.2 正式 App 落地任务 `TASK-20260808-33533E` 已 ACK。
- 独立测试 `TASK-20260809-34476E` 当前为 `FAIL / P0=0 / P1=3`：React #418、lightbox 焦点返回、正式多图数据出口。
- 浏览器七状态、分类筛选、分页 append/partial、inline 与独立详情错误恢复、主题持久化、实体安全区及辅助技术按证据拆成独立 P1，未借 API 机器测试或静态代码分支外推为用户完成。
- API/Repository 的 fail-closed 继续标 `complete`，浏览器错误与恢复单列 P1；两层没有合并。

### 3.2 多图 successor

- 多图 P1-03 的数据前置 `TASK-20260809-33B8F5` 已 ACK/PASS；0/1/4 图、V1/V2 兼容、mapping、fixture、validator、manifest 和 hash 只认该机器包。
- 数据 PASS 只关闭机器前置。独立 `public-multimedia-synthetic` profile、V2 Repository/API/App 接线与浏览器多图仍无运行收据，P1-03 继续开放并转交后继 `TASK-20260809-55302B` 合同任务。
- v0.4 保持不可改写历史与回退基线；当前没有修改 App、SQLite、migration 或 accepted ADR。
- 组件预留、前端复制、静态数组、测试注入或 `NOT_RUN` 均不能关闭多图 P1。

### 3.3 Admin 与 RSS

- Admin 被拆为 `ADMIN-DECISION` 与 `ADMIN-VISUAL` 两个独立轴。review-synthetic 系统 successor 仍为 draft，Admin 候选预览仍未取得用户视觉确认；发问前还须冻结不可变候选路径、SHA-256 和一个精确批准/拒绝问题，相关功能保持 `user-gated`。
- RSS/XML 解析实现只在用户确认精确 `fast-xml-parser@5.10.1` 与 lock 图后允许 fixture-only disposable spike；当前保持 `user-gated`，未写成完成。
- RSS 真实三次固定 URL GET 使用第二个独立用户问题。确认依赖不会自动授权真实请求。

## 4. 给各部门的直接拆分

### 开发部

1. `DEV-PUB-P1`：React #418、lightbox 焦点返回、筛选、七状态、分页、两类详情错误恢复、主题持久化、实体安全区。
2. `DEV-MEDIA-VNEXT`：只在 55302B 固化独立 profile、显式 V1/V2 协商与接线出口后实施已 ACK 的 0..4 媒体数组；删除复制/静态/注入路径，做正式四图交互。
3. `SOURCE-MGMT-001`、`COLLECT-MOCK-002`、`CONTENT-PROCESS-003`、`SUMMARY-MOCK-004`：可按本地 synthetic 边界拆分；不得触发真实 provider 或外部 I/O。
4. Admin 与 RSS 分别等待各自用户门禁；不得提前修改依赖、review profile 或 Admin App。

### 数据部

- 33B8F5 已交付 0/1/4 图 successor 机器包，复用 MediaCandidate/ReleaseBundle/Projection 并保持 v0.4 全字节零漂移；数据部后续只在正式派单后支持 55302B 的运行接线证据，不另造字段或第二真值。
- 为分页和故障状态提供正式可达的版本化 synthetic fixture 时，不改变 accepted 领域对象或创建第二真值。

### 设计部

- 公开页以固定 HTML/hash 为唯一基线，重点复核四图、实体安全区、错误恢复和辅助技术视觉。
- Admin 候选在用户确认前不得成为 App 实施真值。

### 测试部与安全部

- 所有任务必须引用 Function ID 并绑定精确候选 hash。
- API 通过不外推浏览器通过；静态分支不外推真实可达；任一 mandatory 缺证据保持 P1。
- 对多图篡改、同一响应混合 V1/V2、未协商返回 V2、冻结 profile 混跑、rights/hash/order 不一致、第五张、焦点、手势和零外联做失败注入。

## 5. 已验证

- 机械解析得到 50 个唯一 Function ID，无重复 ID、无空列、无非法状态。
- 状态分布为 complete=12、user-gated=18、P1-blocker=20。
- 每个功能行都有九个业务字段：Function ID、状态、用户入口、依赖、视觉锚点、成功/失败/恢复、Owner/落点、证据、授权轴。
- 冻结设计 HTML SHA-256 本地复算为 `5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1`。
- 任务 `33B8F5` 已 ACK/PASS；矩阵明确只把数据机器前置写为已完成，没有把 App/SQLite/API/UI 或 P1-03 写成完成。
- `TASK-14C467` 已 ACK，但报告明确包未安装/运行；矩阵因此使用 `user-gated`。
- 目标文档 `git diff --check` 通过；链接目标和必要输入路径存在。
- 未修改 `app/`、`data/`、`design/`、accepted ADR、飞书资源或外部系统；没有外部 I/O。

## 6. 未验证与当前阻断

- 矩阵的 20 个 P1 功能行仍需后继实现与独立收据；本任务只建立产品真值，不关闭实现缺陷。
- 0/1/4 图数据机器包已验证；独立 profile、App/SQLite/migration/Repository/API/UI 接线仍未验证。
- Admin successor 与 Admin 视觉尚未获用户确认，全部实现和运行收据为空。
- RSS 解析依赖未获用户确认、未安装、未执行；真实 RSS 请求仍为 0。
- 真实来源、真实媒体、Base/provider/Collector、自动发布、部署、付费和生产权限继续未授权。

## 7. 错题自检

- 首轮矩阵曾把 API cursor 机器证据外推为用户分页 append 完成；已拆为 `PUB-FEED-005 P1-blocker`。
- 首轮把搜索与四类筛选合并为 complete；已保留搜索 complete，四类筛选独立为 P1。
- 首轮把 inline detail 正常展开与 404/error/retry 合并；已拆为正常展开 complete、两类详情恢复 P1。
- 首轮把 API fail-closed 与浏览器错误恢复合并；已限定 `RECOVER-PUBLIC-001` 只覆盖 API/Repository，UI 恢复仍是 P1。
- `PUB-DETAIL-002` 只覆盖已证的单次正常展开；互斥、整条居中、外点关闭与 reduced-motion 几何出口统一留在 `UI-ACCORDION-001 P1-blocker`，读取失败重试单列 `PUB-DETAIL-004`。
- 主题切换与刷新后持久化、三档响应式与实体安全区也分别拆开，避免由一次浏览器证据扩大完成范围。
- 没有把 33533E 的开发 PASS 覆盖 34476E 的测试 FAIL，也没有用任务状态代替业务质量。
- 没有把 33B8F5 数据机器 PASS 外推为 App/SQLite/API/UI 完成，也没有把 Admin/RSS draft 或研究推荐写成 accepted/implemented。

## 8. 对抗审查

三路只读对抗审查最终均为 `P0=0 / P1=0`：

- 产品审查纠正独立详情 UI、手风琴与错误恢复的证据外推，并把旧的绝对 DTO 禁令窄化为“UI 不得自造字段；版本化 successor 必须走机器包、独立 profile、accepted 窄合同与显式协商”。
- 领域审查补齐回到顶部、内容 normalization/dedupe/低质量过滤、固定 operator 收据，并校正 0 图与 restricted 的唯一状态优先级。
- 安全审查冻结三态转移、RSS 精确依赖问题与独立真实请求门禁；Admin 视觉发问前必须绑定不可变快照/hash。审查范围只限本任务产品文档，未修改 App、data、design 或 accepted ADR。
