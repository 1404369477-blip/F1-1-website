---
type: product_completion_report
status: final
date: 2026-08-12
department: 产品部
task_id: TASK-20260812-0196EB
decision_id: ADR-M5-REAL-PROJECTION-RUNTIME-003
external_calls: 0
app_changes: 0
---

# 不可变 release 与唯一持久数据、回退根解耦产品报告

## 结果

已接受 `ADR-M5-REAL-PROJECTION-RUNTIME-003`，把首版运行真值冻结为四个独立根：新版本 release 只承载代码；Admin 与现有 RSS collector 共用旧 live app 下同一已存在私库；旧 live app 的 `BUILD_ID + synthetic SQLite` 只作成对回退锚；public projection/data root继续使用 home-scoped稳定根。

开发下一步没有用户未决：deployment manifest升级v3并显式携带`reviewDatabasePath + dev/inode`，Admin HTTP与sender使用existing-only opener，缺库禁止创建；public prepare分开`targetReleaseAppRoot`与`syntheticRollbackAppRoot`，新plist只指new release，rollback只读核验old live root。长期迁移数据库另立停采原子迁移任务，本轮不复制、不`ATTACH`、不双写。

## 已验证

- 逐文件静态核对Admin runtime/sender、RSS LaunchAgent/collector、public installer/receiver/reader的路径推导；确认当前Admin会从new release创建第二库，public prepare会把new release误当old rollback root。
- 吸收开发部只读勘察`TASK-20260812-8BA759`的独立调用链与最小改动事实；结论与任务约束一致。
- ADR已给出v3字段、精确M1首版路径、owner/mode/link/dev/inode/schema门、CLI变量、prepare/load/cutover/rollback顺序和长期迁移边界。
- Spec与Function矩阵已同步；状态不冒进：Admin/恢复/部署仍为`P1-blocker`，`auto-publish=0`、人工批准与第二次显式发布保持。

## 未验证

本任务没有修改`app/`、没有SSH、没有打开/迁移M1数据库、没有prepare/load/cutover。deployment-v3、existing-only opener、real-mode readiness、同inode原位migration、M1真实sender→active、公网页面200和old synthetic回退均等待开发及独立安全/测试执行。Tailscale HTTPS/设备身份与Passkey实机门仍需其独立任务闭合。

## 错题自检

- 没有复用含session/Passkey/recovery资产的`dataRoot`冒充数据库根；字段固定为单一`reviewDatabasePath`。
- 没有把版本release、私库、public投影根或rollback资产互相复制。
- 没有要求活跃RSS数据库字节hash恒定；首版以路径/dev/inode/owner/mode/link/schema识别唯一库，内容允许900秒任务合法变化。
- 没有让长期Application Support迁移混入快速上线；旧live app在正式回退演练完成前不得清理。
- 没有改写002的人工审核、sender、bootstrap与公开读语义，只取代其路径运行细节。

TASK_STATE_OK
