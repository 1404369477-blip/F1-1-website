---
type: data_delivery_report
status: final
date: 2026-08-01
department: 数据部
task_id: TASK-20260801-5A90E3
domain_stage: M3-Base影子建表
execution_mode: offline_only
---

# M3 飞书 Base 影子导入离线载荷报告

## 1. 结论

已按产品部 M3 执行包和 `data/x-source-inventory-v0.csv` 生成确定性离线载荷：主信源表 33 字段 JSON、手机捕获表 9 字段 JSON、59 行批量记录 JSON 和 manifest。全部 JSON 可由标准解析器读取；字段定义与产品执行包逐对象完全一致，批量记录符合 `lark-cli base +record-batch-create --json` 的 `fields + rows` 形状。

本任务没有调用飞书 API、没有读取或写入任何真实 Base，也没有创建外部资源。载荷没有保存资源令牌、用户凭证、外部资源 ID 或个人标识。

## 2. 合同预检

| 检查项 | 结果 |
|---|---|
| 输入 CSV | `data/x-source-inventory-v0.csv` |
| CSV SHA-256 | `bbb84a7f8f625e8106ae6e0b2714a363940dbf3f20dcbbf300fa6552de1ac01b`，与产品合同一致 |
| CSV 数据行 | 59 |
| `source_id` 大小写不敏感唯一值 | 59 |
| `canonical_url` 大小写不敏感唯一值 | 59 |
| 身份状态 | 59/59 为 `unknown` |
| 可监控性 | 59/59 为 `unknown` |
| 生命周期 | 59/59 为 `proposed` |
| 产品执行包 SHA-256 | `86ce6de47711ae912059a1ea4122a1885b7990385f6930cd050749feebf118c1` |
| 固定迁移批次 | `M3-20260801-X59-01` |

## 3. 产物

| 文件 | 用途 | SHA-256 |
|---|---|---|
| `data/m3-base-shadow-import-v0/main-source-fields.json` | 主信源表 33 字段定义；可作为 `--fields` 参数值 | `b743abaee8ef68db586148e58afdcc9dec2b6fe07bab61ad7c779327d4076870` |
| `data/m3-base-shadow-import-v0/mobile-capture-fields.json` | 手机捕获表 9 字段定义；可作为 `--fields` 参数值 | `378b2c2b0c1369198dfa84e8961d7ad1a4aa247238a1389c213dbf7dc37ea6b6` |
| `data/m3-base-shadow-import-v0/main-source-record-batch.json` | 主信源 59 行批量导入对象；可作为 `+record-batch-create --json` 参数值 | `e73b8d6b8a9b1a018dc7d30c90bfe3111b10caeb6fee28486edf27f176a05de5` |
| `data/m3-base-shadow-import-v0/manifest.json` | 输入、合同、载荷哈希、数量、默认值和安全边界清单 | `c6d681ba9428107801e7077a65e265176d684575d8d2e16c6958d19722791e29` |
| `data/m3-base-shadow-import-v0/generate_payloads.py` | 确定性离线生成器；合同或输入哈希变化时停止 | `fdf4ae9522995ddc881d9edb2780b9761705b96994a9b9c85c99846e385af31f` |

字段 JSON 均为顶层数组；批量记录 JSON 顶层只有 `fields` 与 `rows`。执行时真实父资源标识应由外部创建/读回步骤临时传入，不能写入这些载荷文件。

## 4. 载荷校验

### 4.1 结构

- 主信源字段：33；第一项为 `source_id`。
- 手机捕获字段：9；第一项为 `capture_id`。
- 批量记录字段顺序：33 项，与主信源字段名称及产品执行包顺序完全一致。
- 批量记录：59 行，每行严格 33 个值。
- 4 个交付 JSON 均通过标准 JSON 解析。
- 生成器连续执行两次，4 个 JSON 的 SHA-256 完全相同。

### 4.2 59 行映射

- CSV 每行映射到且只映射到一条载荷记录。
- `source_id`、`handle`、`canonical_url`、`entity_type`、`content_focus`、`priority`、`identity_status`、`monitorability`、`lifecycle_status`、`added_at`、`evidence_url` 和 `notes` 均按合同映射。
- `raw_url` 与 CSV 的 `evidence_url` 一致。
- 载荷中 `source_id` 和 `canonical_url` 继续保持 59 个大小写不敏感唯一值。
- 59 行的 `migration_batch_id` 全部为 `M3-20260801-X59-01`。

### 4.3 默认值

| 默认字段 | 期望值 | 通过数 |
|---|---|---:|
| `dedup_status` | `pending` | 59/59 |
| `verification_status` | `pending` | 59/59 |
| `identity_status` | `unknown` | 59/59 |
| `relevance_status` | `unknown` | 59/59 |
| `monitorability` | `unknown` | 59/59 |
| `adapter_authorization_status` | `unknown` | 59/59 |
| `lifecycle_status` | `proposed` | 59/59 |
| `enabled` | `false` | 59/59 |
| `platform_account_id` | `null` | 59/59 |
| `authorization_checked_at` | `null` | 59/59 |
| `authorization_expires_at` | `null` | 59/59 |
| `onboarding_operation_id` | `null` | 59/59 |
| `manual_disable_at` | `null` | 59/59 |

`canonical_url_valid=true`、`normalization_status=valid`、`adapter_status=unchecked`、`collection_onboarding_status=validating`、`source_stop_status=clear` 和 `source_safety_epoch=1` 也严格沿用产品执行包，没有自行改变。

## 5. 安全与范围自检

- 扫描 4 个 JSON，未发现凭证键、用户凭证模式、外部资源 ID 模式或个人标识模式。
- 没有生成真实 Base、表、视图、表单或记录 ID。
- 没有调用任何飞书读写命令；仅查看了本地 CLI 帮助，以机械确认字段 JSON 与批量记录 JSON 的参数形状。
- 没有把 `enabled` 改为 `true`，没有把身份、相关性、可监控性或跨批查重状态升级为已验证。
- 没有修改 CSV、产品执行包、Spec、ADR、业务真值、provider、Collector 或 `app/`。

## 6. 已验证 / 未验证

### 已验证

- 输入哈希、59 行、双唯一性与产品执行包一致。
- 两张表的字段定义逐对象等于产品合同。
- 59 行载荷逐行 33 值，字段顺序、映射、枚举、布尔值、空值、批次 ID 和关键默认值均符合合同。
- 4 个 JSON 可解析，manifest 中的三个载荷哈希与文件实值一致。
- 重复生成结果一致；`git diff --check` 通过。

### 未验证

- 真实 Base 是否存在、资源级权限、表字段创建和平台实际返回。
- 59 条记录的真实写入、分页读回、59/59 外部对账与部分成功恢复。
- 视图和手机表单的真实创建及读回行为。
- Base 业务真值切换、A/D provider、Collector 和 15 分钟采集能力。

上述项目均需要后续获授权的外部执行与只读回查，本任务不作推断。

## 7. 错题自检

- 已读取当前错题集，未发现正式错题条目。
- 已检查没有把 M3 资源授权扩写为业务真值、provider、自动采集或自动公开授权。
- 已检查批次内 URL 唯一性没有被写成平台稳定账号 ID 或跨批次查重完成；`dedup_status` 保持 `pending`。
- 已检查临时分类与账号名称没有被升级为身份事实；相关状态保持 `unknown`。
- 已检查所有记录 `enabled=false`、`lifecycle_status=proposed`，载荷不含运行遥测或外部资源凭证。
