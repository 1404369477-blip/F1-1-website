# M4 VS-1 公开前端本地浏览器验收收据

- 关联任务：`TASK-20260802-C29BB8`
- 验收部门：统筹部
- 验收时间：`2026-08-02T23:45:06+08:00`
- 运行边界：本机 `127.0.0.1:3000`、固定 Node `24.18.0`、Next.js `16.2.11`、生产构建；未访问外部站点
- 当前结论：浏览器与正常启动验收 `PASS`；本收据只补齐开发部窗口无法执行的可视、交互、HTTP 与正常 `test:p1` 证据，不替代测试部独立回归，也不改变另案启动失败日志脱敏 P1 的 `FAIL` 结论

## 1. 验收快照

本次在以下实现 SHA-256 上执行浏览器验收：

| 文件 | SHA-256 |
|---|---|
| `app/src/app/globals.css` | `265b17237efe9ceef60473edef3860ff9123b011165f0d20e698af59731ee9e9` |
| `app/src/app/layout.tsx` | `01f6fbf532f9918b63334d59fb8ac048ba34682fd29b0b194b17a5a7226e36a1` |
| `app/src/app/page.tsx` | `3b7a5ff1b913153afae4de3b316fad5f4dbdbc83708659b8e4404390b21b3a23` |
| `app/src/app/stories/[publicId]/page.tsx` | `e9131c11a6714df2048d01ece632685f62e8b8cf54e2d5185cd77a3173edc150` |
| `app/src/app/stories/[publicId]/not-found.tsx` | `4887682f407b5866060ac90ff74d035f17f87c48572b18f1b0f57c7945e036ad` |
| `app/src/components/f1/f1-page-shell.tsx` | `8680cc5c63857af81e0465ca757af4130419e98c760a8c180ea347f390899b0f` |
| `app/src/components/f1/story-parts.tsx` | `4d920cc9d34c48a23417f2731b5497d0d87fef29632d8c200586d65f4d51cc7f` |
| `app/src/features/stories/demo-data.ts` | `e6a8fb65374ec71722f8cf0c3234dfdef4ea8df691991d3f6615fdbf6de9881b` |
| `app/src/features/stories/feed-experience.tsx` | `c95c85e00e464616f9476e81f9da2aee8b0a5854d4d5abc311babb3423c64133` |
| `app/src/tests/public-ui.test.ts` | `241c7a4d9ef71aeaa18b667215865b83fc717be80a8951462d9a31b5658eadb6` |

固定 Node24 的完整 `npm --silent run check` 为 exit 0：`verify:env`、migration、fixture seed、runtime readiness、45/45 tests、lint、typecheck、build 和 `test:p1` 全部通过；构建路由为 `/`、`/_not-found`、`/api/health`、`/stories/[publicId]`。完整检查后上表 UI SHA 与 P1/lockfile 冻结 SHA 均未漂移。

## 2. HTTP 与监听收据

| 检查 | 实得 | 结论 |
|---|---:|---:|
| `GET /` | HTTP 200 | PASS |
| `GET /stories/demo-qualifying-window` | HTTP 200 | PASS |
| `GET /stories/not-a-real-public-id` | HTTP 404 | PASS |
| `GET /api/health` | HTTP 200 | PASS |
| 正常生产监听 | 仅 `127.0.0.1:3000` | PASS |
| 同时检查 `3101` | 无监听 | PASS |
| `npm --silent run check` | exit 0；3 files、45/45 tests，lint/typecheck/build/test:p1 全通过 | PASS |
| `npm --silent run test:p1` | exit 0；`bindHost=127.0.0.1`、`port=3000`、`health=ready`、`externalCalls=0` | PASS |
| 浏览器与 `test:p1` 停止后 | 3000/3101 均无监听，生产 PID 不存在 | PASS |

## 3. 1440px 桌面端

- 首页视口为 `1440×1000`，`documentElement` 与 `body` 均为 `clientWidth=1440`、`scrollWidth=1440`，未发生页面级横向溢出。
- 默认暗色主题：`data-theme=dark`；页面含 12 个资讯 `article`，12 个原文按钮均为 disabled。
- 首页当前导航只有 `/` 的“资讯流”被标记为当前页；无标签的可交互元素计数为 0。
- 浅色切换后：`data-theme=light`，按钮可访问名称变为“切换至深色主题”，body 背景为 `rgb(245,245,247)`、正文色为 `rgb(29,29,31)`，宽度仍无溢出。
- 详情页桌面布局为两列，实测 `gridTemplateColumns=885.328px 442.664px`；来源侧栏可见，页面宽度无溢出。
- 详情页全站导航没有错误的 `a[aria-current=page]`；面包屑有且只有一个当前详情节点。

## 4. 390px 移动端

- 首页视口为 `390×844`，`documentElement` 与 `body` 均为 `clientWidth=390`、`scrollWidth=390`，未发生页面级横向溢出。
- 桌面导航隐藏、菜单按钮可见；本轮可见且可交互的按钮、链接、输入框、下拉框中，高度低于 44px 的元素计数为 0。
- 筛选入口打开后呈现 `role=dialog`，含关闭按钮、四类筛选、搜索框、状态预览和结果确认按钮。
- 选择“车手社交”并输入“车库”后，结果精确为 1 条；关闭面板后保留“分类：车手社交”和“关键词：车库”两个可清除 Chip，页面仍无横向溢出。
- 清除筛选后恢复 12 条、`全部` pressed、关键词为空、状态为 default、Chip 为 0。
- 状态预览：
  - `restricted`：2 条受限演示内容，显示“状态：受限来源” Chip 和受限说明，页面不存在 HTTP(S) 原文链接；
  - `empty`：0 条内容，显示“暂时没有符合条件的演示内容”和清除筛选出口；
  - `no-more`：12 条内容，显示“已无更多内容”和“没有后台加载或外部请求”。
- 移动详情页含唯一 H1、中文摘要、关键点、来源与原文状态、3 条相关内容；390px 下宽度无溢出，4 个原文入口均 disabled。
- 移动菜单打开后焦点自动进入“资讯流”；按 `Escape` 后菜单移除、`aria-expanded=false`，焦点返回“菜单”按钮。

## 5. 404、资源与运行错误

- 未知 `publicId` 的浏览器页面显示“这条演示内容不存在或已下架”，返回资讯流链接为 `/`，页面无错误的当前导航标记；对应真实 HTTP 状态为 404。
- 首页和详情 DOM 中可观察到的 HTTP(S) 图片、脚本、样式资源 URL 为 0；原文入口保持 disabled。
- 浏览器控制台 `error`/`warn` 日志为 0。
- 本结论证明页面 DOM、浏览器交互和本次命令没有使用外部 URL；不把它扩写成 R12 操作系统级 no-egress 证明。

## 6. 边界与后续

1. 本收据可供开发部补入 `TASK-20260802-C29BB8` 最终报告，并完成该任务的浏览器、HTTP、正常启动和端口清理条目。
2. 公开 UI 仍使用 12 条本地合成演示数据；尚未接入 SQLite 公开读取 API、真实采集、审核、发布或部署。
3. `TASK-20260802-68F43E` 发现的 Next 启动后置错误输出旁路继续为独立 P1；已另排 `TASK-20260802-3760F6`，本收据的正常 happy path PASS 不覆盖该失败路径。
4. `git diff --check` 在本轮验收快照上通过。
