---
type: closure_report
department: 统筹部
status: final
date: 2026-08-02
related_task: TASK-20260802-49B4AF
domain_stage: M4-migration-cleanup-closure
decision: pass
tags: [migration, macOS26.6, cleanup, handoff, work-resume]
summary: M5/macOS 26.6 迁移、旧凭证授权处置与清理后终验已同步进项目交接真值；已删除迁移临时产物不再作为现存入口，现存施工任务及所有安全和外部能力门禁保持准确。
---

# 新 Mac 迁移清理与施工恢复收口报告

## 1. 结论

本次文档真值收口通过。依据已 ACK 的测试终验 `TASK-20260802-574A6D` 与安全处置审查 `TASK-20260802-026EC3`，已把项目交接从 2026-07-30 的地基状态刷新为 Apple M5 / macOS 26.6 post-clean 后的 M4 施工断点。

本结论只确认迁移、清理和交接时态完成，不改变以下门禁：VS-0 最新独立安全结论仍为 FAIL；R5 同 UID 威胁模型仍待用户决定；R12 OS/系统调用级 no-egress 仍 pending；`TASK-20260802-7A9C48` 已由开发部完成但尚待统筹核收，后继测试 `TASK-20260802-FFC67A` 已 claimed 且测试后仍需安全复验，`TASK-20260802-6F7563` 仍在办；真实 Base/provider/Collector、平台采集、AI/媒体、公开发布、部署、付费和其他真实外部能力继续 closed/Unknown。

## 2. 证据基线

- 测试部 `TASK-20260802-574A6D` 已 ACK：post-clean `decision=pass`、P0=0、P1=0；M5/macOS 26.6、Git、Node24/npm、两份 SQLite、关键 hash、9/9 Codex 任务、清理边界和完整检查收据均通过。
- 安全部 `TASK-20260802-026EC3` 已 ACK：该任务是删除前只读审查，结论 `decision=fail`、P0=0、P1=2、P2=1；它只给出处置顺序，没有自行删除或输出任何凭证值。
- 后续用户授权和删除前门禁满足后，旧 Local REST `data.json` 被精确删除；测试部 post-clean PASS 验证删除结果、插件保留和精确忽略规则。
- 测试终验记录清理释放约 `298.6 MiB` 项目空间及约 `6.6 MB` Homebrew 旧 `simdjson`。

## 3. 已更新产物

1. `docs/handoff.md`
   - 从 M0 地基旧状态更新为 M4 / VS-0 整改断点。
   - 写明目标机终验、删除/保留边界、当前任务和外部能力 closed。
2. `docs/progress.md`
   - 在 2026-08-02 顶部追加迁移终验、清理收据、旧凭证时态和当前施工门禁。
3. `migration/CURRENT-HANDOFF.md`
   - 改为 post-clean 断点；保留历史归档与代码 checkpoint hash，同时撤销已删除路径的现存入口语义。
4. `migration/README-FIRST.md`
   - 从“迁移包使用说明”改为目标机现存项目的交接入口；明确 bundles/manifests/portable-assets/scripts 已删除。
5. `migration/README-M5-macOS26.6.md`
   - 改为迁移终验与清理记录；保留删除前验证 hash、当前保留项、任务状态、安全门禁与未验证项。

未修改 `docs/spec.md`、任何 ADR、`app/`、`data/` 或 `design/`；未创建或重建迁移归档、manifest、portable assets、恢复脚本或缓存；未接触凭证值和外部资源。

## 4. 删除与保留边界

### 清理后持续不存在，且不得描述为现存

- `migration/bundles/`
- `migration/manifests/`
- `migration/portable-assets/`
- `migration/scripts/`
- Node 下载缓存和 literal TMPDIR compile cache
- 旧 Local REST `data.json`
- Homebrew 旧 `simdjson`

归档、manifest 和 checkpoint hash 作为历史证据保留在文档中；所有表述均明确对应文件已经删除，没有提供当前可执行命令或下载入口。

### 清理时删除、当前可重建

post-clean 终验时删除了 `.next`、Vitest cache、`tsconfig.tsbuildinfo`、Python cache 和 `.DS_Store`。本轮只读核验观察到 `.next`、Vitest cache 与 `tsconfig.tsbuildinfo` 已随开发恢复再次出现；它们是可重建施工缓存，不是迁移归档或恢复脚本。本统筹任务没有创建、删除或修改这些缓存。

### 当前明确保留

- `app/node_modules/`、精确 Node 24.18.0/npm 11.16.0 工具链
- `app/.local/f1plus1.sqlite`、`app/.local/vs0-acceptance.sqlite`
- `.git/`、dirty worktree、任务/报告、Spec/ADR、代码、数据、设计和研究材料
- 迁移交接文档、`migration/conversations/` 及其校验清单
- Obsidian Local REST 插件本体、启用记录与精确 `.gitignore` 规则

## 5. 当前任务与门禁

- `TASK-20260802-7A9C48`：开发部 `completed`。开发报告以 Node24 完整 check、42/42 测试、真实 argv/CLI 负例、固定 loopback health 与零漂移收据判定两项 P1 整改 PASS；尚未统筹 ACK，也没有后继独立安全 PASS。
- `TASK-20260802-FFC67A`：测试部 `claimed`。正在只读复验两项 P1 整改、完整 check 与零漂移；测试结论不替代后继独立安全复验。
- `TASK-20260802-6F7563`：安全部 `claimed`。只形成 VS-1 本地管理 API session/CSRF 安全合同候选；候选不等于实现或放行。
- 9/9 个 Codex 任务已同步并通过目标机应用层和磁盘侧核对；`migration/conversations/` 保留恢复证据。
- VS-0、R5、R12 和所有真实外部能力继续保持各自 FAIL/pending/closed 状态，迁移终验不跨轴继承为业务或安全授权。

## 6. 验证

- 五份目标文档的 M5/macOS 26.6、约 `298.6 MiB`、约 `6.6 MB`、9/9 同步、已删除路径、明确保留项、`7A9C48` 和 `6F7563` 状态已交叉核对。
- 已删除路径只出现在“已删除”或“历史证据”语境，没有作为现存链接、命令或恢复入口。
- 历史 portable/warm、bundle manifest、conversation manifest 和 VS-0 checkpoint hash 均保留，没有删除历史证据。
- 未把安全部 `026EC3` 的处置前 FAIL 覆写为 PASS；post-clean PASS 明确归属于测试部 `574A6D`。
- 未把迁移、build、check 或 task doctor 的 PASS 延伸为 `7A9C48`、VS-0、R5、R12、VS-1 或真实外部能力放行。
- `git diff --check` 通过；六份交付文件无尾随空白；当前 Markdown 链接目标存在；`migration/conversations/SHA256SUMS` 复算 10/10 通过。
- 完成前任务队列校验为 `TASK_DOCTOR_OK | tasks=96 | full_history_validated=true`；最终 complete 后将再次运行 doctor。

## 7. 未验证项

- 测试部受 `sysmond service not found` 限制，无法独立复现进程列表；统筹删除前检查无 Obsidian 进程、目标句柄及 27123/27124 监听，测试部未发现相反证据，继续保留为 P2 环境可见性 Unknown。
- 飞书 OAuth/Keychain 重新登录后的 auth、scope、具体资源 ACL 与可读性未验证，相关能力 closed/Unknown。
- Local REST 新凭证是否按需重生成、客户端是否重配、证书信任和实际监听状态未验证。
- 已完成开发任务和仍在办安全任务的最终项目结论等待统筹核收与必要独立复验。

## 8. 错题自检

- 已区分迁移/清理 PASS、任务执行状态、业务阶段和用户授权三条轴。
- 已区分清理当时删除的可重建缓存与当前持续不存在的迁移临时产物；没有把开发恢复后再次生成的缓存误报为迁移归档重建。
- 已保留 `026EC3=fail` 历史及 portable/warm/checkpoint hash，没有用后续收口改写历史。
- 已按实时任务 JSON 写明 `7A9C48=completed/待ACK`、`FFC67A=claimed/独立测试中`、`6F7563=claimed`，并明确测试后仍需安全复验；没有把开发或测试结论写成 VS-0、R5、R12 或真实外部能力放行。
- 已确认写入范围只含任务 impact 指定的五份业务文档和本统筹部报告；没有修改 Spec、ADR、app、data、design。
- 已确认没有重建归档、manifest、portable assets、脚本或缓存，没有记录、复制或读取凭证值，没有外部写入。

TASK_STATE_OK
