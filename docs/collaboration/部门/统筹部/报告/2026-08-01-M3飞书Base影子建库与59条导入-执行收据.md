---
type: execution_receipt
department: 统筹部
status: final
decision: acl_public_permissions_pass_with_unknown_collaborators
date: 2026-08-01
subject: M3飞书Base影子建库与59条导入
related_tasks:
  - TASK-20260801-71E193
  - TASK-20260801-5A90E3
  - TASK-20260801-83CEBE
  - TASK-20260801-6086D8
  - TASK-20260801-57E98D
  - TASK-20260801-EE36EF
  - TASK-20260801-812839
  - TASK-20260801-508D03
---

# M3 飞书 Base 影子建库与 59 条导入执行收据

## 1. 结论

截至 `2026-08-01T22:14+08:00`：

- **资源与数据执行通过。** 已只用飞书 CLI 用户身份创建 1 个 M3 影子 Base、2 张表、3 个 grid 视图、1 个未分享表单，并一次导入 59 条 `enabled=false` 的影子信源；写后全量回读与离线载荷逐字段一致。
- **公共 ACL 收紧已执行并回读通过。** 用户明确授权后，统筹部只用飞书 CLI 将六项公共权限一次收紧到已审核的最小目标；fresh read 与目标逐项一致，当前维护者仍有 `manage_public=true`。
- **协作者边界仍为 Unknown。** 当前授权无法列出直接协作者清单，因而不能证明资源只对预期个人维护者开放，也不能证明存在外部协作者；公共权限目标通过不消除该证据缺口。
- **业务链路保持未切换。** 本轮没有把 Base 设为业务真值，没有连接 `base_direct`、`base_snapshot`、Collector、采集、同步或发布链路，也没有把任何信源设为启用。
- **OAuth 风险未随 ACL 收紧消除。** 当前用户令牌仍有 142 个 scopes；缩减、重授权或撤权都需用户另行确认。

因此，本轮 M3 的“资源创建与数据导入”及“公共权限最小化”已经完成并通过后继测试核收；直接协作者清单仍未验证，OAuth 最小权限继续单独跟踪。

## 2. 执行边界

- 飞书编辑与读取只使用 `/opt/homebrew/bin/lark-cli`；未使用浏览器或飞书 UI。
- 执行身份固定为 `user`；未切换 bot。
- 真实 Base token、token、refresh token、open_id、appId 和认证原始 JSON 均未写入仓库。
- 表单字段显隐只使用安全部批准的固定官方 Bitable v1 字段 PATCH 白名单；其余 raw API 写入继续禁止。
- 记录写入只执行一次 typed `base +record-batch-create --as user`；ACL 收紧只执行一次 typed `drive permission.public patch --as user --yes`。未执行记录 update/delete、协作者增删、workflow、真实表单提交或盲重放。

## 3. 脱敏资源目录

| 资源 | 名称 | 脱敏标识 / 数量 |
|---|---|---|
| Base | `F1+1 信源库｜M3影子` | Base token SHA-256：`7d6638223761b3ddfd542b36cc96177160ed72e8010e2e9cb0f1752940316122` |
| 表 | `主信源` | `tblaUkA0PJDLvU5H`；33 字段 |
| 表 | `手机捕获` | `tblThc1oQWRe96g0`；9 字段 |
| grid | `M3｜影子59对账` | `vew60xtVMG`；1 个筛选条件；11 个可见字段 |
| grid | `维护｜门禁待复核` | `vew32cY825`；6 个筛选条件；12 个可见字段 |
| grid | `手机捕获｜待处理` | `vewEEgEMuP`；4 个筛选条件；9 个可见字段 |
| form | `新增信源（手机）` | `veww7BZNx4`；`shared=false`；`submit_limit_once=false` |

CLI 回读显示 Base 只有上述 2 张表；主表只有上述 2 个 grid，手机捕获表只有 1 个 grid 和 1 个 form。

## 4. 视图合同回读

### 4.1 `M3｜影子59对账`

- 类型：`grid`
- 筛选：`migration_batch_id == M3-20260801-X59-01`
- 可见字段顺序：
  `source_id`、`canonical_url`、`raw_url`、`entity_type`、`content_focus`、`priority`、`identity_status`、`monitorability`、`lifecycle_status`、`enabled`、`migration_batch_id`

筛选器与产品合同字面一致。

### 4.2 `维护｜门禁待复核`

- 类型：`grid`
- 逻辑：`or`
- 条件顺序：
  `enabled == false`、`dedup_status` 不含 `unique`、`adapter_status` 不含 `ready`、`adapter_authorization_status` 不含 `valid`、`monitorability` 不含 `monitorable`、`source_stop_status` 不含 `clear`
- 可见字段顺序：
  `source_id`、`priority`、`normalization_status`、`dedup_status`、`identity_status`、`relevance_status`、`monitorability`、`adapter_status`、`adapter_authorization_status`、`collection_onboarding_status`、`enabled`、`source_stop_status`

CLI 原始操作符均为产品合同定义的 `==` 或 `disjoint`；筛选器与产品合同字面一致。

### 4.3 `手机捕获｜待处理`

- 类型：`grid`
- 逻辑：`or`
- 条件顺序：
  `normalization_status empty`、`normalization_status` 含 `pending/invalid/needs_review`、`dedup_status empty`、`dedup_status` 含 `pending/needs_review`
- 可见字段顺序：
  `capture_id`、`raw_url`、`capture_note`、`captured_at`、`normalization_status`、`normalization_error`、`dedup_status`、`dedup_match_source_id`、`source_id`

飞书 API 给两个 `empty` 条件自动补入尾部 `null`，形成三元组；产品合同使用二元组。按 CLI 官方可选 `value` 规则去除尾部 `null` 后语义与顺序完全一致，Base 无需因该序列化差异修改。

## 5. 手机表单回读

- 名称：`新增信源（手机）`
- 说明：`提交后只进入候选处理，不代表信源已验证、启用或可公开。`
- `shared=false`
- `submit_limit_once=false`

官方表单字段 GET 返回 7 个问题字段、`has_more=false`：

| 顺序 | 字段 | visible | required | 描述 |
|---|---|---:|---:|---|
| 1 | `raw_url` | `true` | `true` | `粘贴 X 账号页或帖子分享链接` |
| 2 | `capture_note` | `true` | `false` | `可选：简要说明为何希望加入` |
| 3 | `normalization_status` | `false` | `false` | 空 |
| 4 | `normalization_error` | `false` | `false` | 空 |
| 5 | `source_id` | `false` | `false` | 空 |
| 6 | `dedup_status` | `false` | `false` | 空 |
| 7 | `dedup_match_source_id` | `false` | `false` | 空 |

`capture_id` 与 `captured_at` 不在表单问题集合；手机捕获底层仍完整保留 9 个字段。手机捕获表全表记录数为 0、`has_more=false`，支持“尚未发生真实表单提交或自动入表”的 Base 侧结论。

## 6. 59 条影子导入与全量对账

### 6.1 固定输入与载荷

| 文件 | 当前 SHA-256 |
|---|---|
| `data/x-source-inventory-v0.csv` | `bbb84a7f8f625e8106ae6e0b2714a363940dbf3f20dcbbf300fa6552de1ac01b` |
| `data/m3-base-shadow-import-v0/main-source-fields.json` | `b743abaee8ef68db586148e58afdcc9dec2b6fe07bab61ad7c779327d4076870` |
| `data/m3-base-shadow-import-v0/mobile-capture-fields.json` | `378b2c2b0c1369198dfa84e8961d7ad1a4aa247238a1389c213dbf7dc37ea6b6` |
| `data/m3-base-shadow-import-v0/main-source-record-batch.json` | `e73b8d6b8a9b1a018dc7d30c90bfe3111b10caeb6fee28486edf27f176a05de5` |

- 批次：`M3-20260801-X59-01`
- 写入：单次 typed `+record-batch-create --as user`
- 写入行数：59
- 每行字段数：33
- 服务端返回 record ID：59 个；本收据不持久化具体 ID

### 6.2 写后结果

- 主表全表：59 条，`has_more=false`
- 目标批次：59 条，`has_more=false`
- 其他批次：0
- 唯一 record ID：59
- 唯一 `source_id`：59
- 唯一 `canonical_url`：59
- 唯一 `raw_url`：59
- 59×33 逐字段规范化比较：全等，差异 0
- `enabled=false`：59/59
- `dedup_status=pending`：59/59
- `verification_status=pending`：59/59
- `identity_status=unknown`：59/59
- `relevance_status=unknown`：59/59
- `monitorability=unknown`：59/59
- `adapter_authorization_status=unknown`：59/59
- `collection_onboarding_status=validating`：59/59
- `lifecycle_status=proposed`：59/59
- `source_stop_status=clear`：59/59

逐字段比较只做以下飞书返回规范化：单选数组还原为单值；Markdown URL 包装还原为 URL 文本。没有忽略业务字段或状态差异。

### 6.3 哈希时态说明

离线 manifest 锁定的产品合同哈希为 `86ce6de47711ae912059a1ea4122a1885b7990385f6930cd050749feebf118c1`。导入载荷生成后，产品合同仅追加了“用户撤销 UI 回退、飞书编辑只用 CLI”和固定表单 PATCH 例外说明，当前文件哈希为 `0a25cc1f5b307a1b12b54591ee38476fd3696e08a60d6f36b409b32b9046c377`。三份载荷哈希保持不变；已锁定 manifest 不回写。

manifest 中 `external_resource_identifiers_present=false` 的历史意图是“生成时不含飞书资源 ID 或凭证”；载荷仍包含公开 X URL、handle 和项目内 source ID。后续 schema 应改用更精确的字段名，本轮不修改哈希锁定文件。

## 7. ACL 与 OAuth 残余风险

### 7.1 Base 公共权限

用户授权前的只读 typed CLI 回读与授权后的目标、fresh read 如下：

| 设置 | 收紧前 | 收紧后 fresh read | 结果 |
|---|---|---|---|
| `link_share_entity` | `tenant_readable` | `closed` | 通过 |
| `external_access` | `true` | `false` | 通过 |
| `invite_external` | `true` | `false` | 通过 |
| `share_entity` | `anyone` | `only_full_access` | 通过 |
| `security_entity` | `anyone_can_view` | `only_full_access` | 通过 |
| `comment_entity` | `anyone_can_view` | `anyone_can_edit` | 通过；这是当前 typed schema 可表达的最严格评论档位 |

执行前 fresh read、schema 校验与 `manage_public=true` 均通过；精确请求体先以 `--dry-run` 验证，再执行一次 high-risk typed PATCH，返回成功且没有重试。写后 fresh read 的六项值全部命中目标，`manage_public=true` 保持不变。授权事实已登记为 `DEC-20260801T215054-C95B18`。

Base 没有启用高级权限；自定义角色为 0。typed CLI 当前没有列出分享成员的只读命令，官方协作者列表端点在当前用户授权下返回授权错误。因此以下事实仍未知：

- 当前协作者数量与身份；
- 是否已有外部协作者；
- 每个协作者的阅读、编辑或管理权限；
- 直接协作者是否受本次公共权限收紧影响。

公共权限设置已经达到已审核的最小目标；证据链仍不能证明不存在未确认的直接协作者，因此只能判定“公共 ACL 通过、协作者边界 Unknown”，不能扩写为“仅 owner 可访问”。

### 7.2 OAuth scope

为取得表单能力使用 `--recommend` 重授权后，当前用户 scope 总数为 142，Base 域包含 app、field、form、record、role、table、dashboard、workflow 等广泛 create/read/update/delete 能力。本轮没有用额外 scope 执行业务动作，任务级白名单也不能从 token 能力层移除这些权限。

安全部已将其评为高等级 P1 预防性授权风险。最小 scope 重授权、旧 token 撤权或保留窗口都需用户另行确认；不得自动删除 Base 或记录。

## 8. 无业务切换证据

- `app/` 只有占位 `README.md`，没有应用实现或运行时配置。
- 非文档区域没有 `active_source_config_provider`、`source_config_epoch`、`base_direct`、`base_snapshot`、Collector、采集器、同步器或发布器实现。
- 工作区没有 `.env`、`package.json`、Dockerfile、YAML、TOML 或 INI 形式的相关运行配置。
- 本轮没有设置 Base 为业务真值，没有创建 workflow，没有启动 Collector，没有改变公开内容人工审核门禁。

该结论只覆盖当前工作区；不能证明仓库外是否存在独立部署的系统。

## 9. 验收证据链

| 证据来源 | 结论 | 证据边界 |
|---|---|---|
| 统筹部 CLI 现场读回 | 资源结构、视图、表单、59 条数据与手机表 0 条通过 | 同一执行会话，独立性有限 |
| 收紧前独立现场对抗 Agent | 结构、视图、表单、59 条数据、手机表 0 条与本地无切换通过；当时 ACL 项 FAIL | 当时六项公共权限较宽；协作者列表不可读，未证明外部协作者存在 |
| 测试部 `TASK-20260801-57E98D` | `decision=fail` | 测试部 Keychain 未初始化，未能独立现场读 Base；不构成资源缺陷断言 |
| 安全部 `TASK-20260801-EE36EF` | OAuth 风险审查条件式 PASS，风险仍为高 | 没有访问飞书，不替代资源验收 |
| 安全部 `TASK-20260801-812839` | typed CLI 最小 ACL 方案条件式 PASS | 只验证方案与 schema；没有执行权限写入，协作者边界仍为 Unknown |
| 统筹部 ACL 写后复核 | 六项公共权限逐项命中最小目标；`manage_public=true`；资源、表单与 59×33 数据不变量继续通过 | 同一执行会话；直接协作者仍为 Unknown |
| 收紧后独立现场对抗 Agent | PASS；fresh read 六项公共权限、`manage_public=true`、资源、表单、59×33、手机表 0 条及本地无切换全部通过 | 只证明公共 ACL 与当前工作区；直接协作者列表仍为 Unknown |
| 测试部后继 `TASK-20260801-508D03` | `decision=pass`，已由统筹部核收 | 测试会话 Keychain 仍阻断第二次现场读取；按任务失败路径审计带来源 fresh-read 收据，独立性限制已明示 |

原测试任务保持失败历史，不覆写为 PASS。测试后继任务已经完成并核收；由于任务工具不允许对已 `completed` 的旧任务执行 `supersede`，旧任务继续作为收紧前历史，当前主链以已核收的后继任务为准。

## 10. 停止点与下一确认

本次授权仅覆盖六项公共权限收紧。后续在取得用户新的明确确认前：

- 不继续修改 Base 公共权限或协作者；
- 不重授权、缩减或撤销 OAuth token；
- 不把 Base 设为业务真值；
- 不连接 provider、Collector、采集、同步或发布；
- 不启用任何一条信源；
- 不公开表单或执行真实表单提交。

下一项权限治理决策是是否缩减或重新授权当前 142-scope 用户令牌；该决定与已完成的 Base 公共 ACL 收紧分开，不自动授权。直接协作者名单若继续不可读，必须保留为未验证风险。

## 11. 自检

- 没有把 `form.shared=false` 推导成整个 Base 私有。
- 没有把收紧前的 `external_access=true` 写成已经存在外部协作者或已经泄露。
- 没有把测试部 Keychain 阻断写成真实 Base 缺陷。
- 没有把 59 条影子导入扩写成启用、采集或发布授权。
- 没有在仓库保存完整 Base URL、Base token、用户标识或认证原始 JSON。
- 没有修改已锁定 manifest、accepted ADR 核心或正式应用。

## 12. ACL 收紧执行补充

### 12.1 授权与命令边界

- 用户明确授权记录：`DEC-20260801T215054-C95B18`。
- 授权只覆盖六项固定公共权限的 typed CLI PATCH、`--yes` 与写后 fresh read；不覆盖协作者变更、OAuth scope 变更、业务真值、provider、Collector、采集或发布切换。
- 执行身份为 `user`，全程仅使用飞书 CLI，未使用浏览器。
- 写前完成标题、资源类型、Base token 哈希、schema、当前权限和 `manage_public=true` 核对；dry-run 的方法、端点和请求体与授权边界一致。
- 实际 PATCH 只执行一次，`patch_rc=0`，没有自动重试或回滚。

### 12.2 写后不变量

- 六项公共权限 fresh read 全部命中目标，`acl_after_ok=true`，维护者仍为 `manage_public=true`。
- Base 标题、2 张表、主表 33 字段、手机捕获表 9 字段、3 个 grid 与 1 个 form 均保持不变。
- 表单仍为 `shared=false`、`submit_limit_once=false`；只显示 `raw_url` 与 `capture_note`，五个内部字段继续隐藏。
- 主表仍为 59 条，批次仍为 `M3-20260801-X59-01`，59 条全部 `enabled=false`；按 fresh 字段目录映射后，59×33 与离线载荷全等，差异 0。
- 手机捕获表仍为 0 条；没有发现真值、provider、Collector、采集或发布切换。

### 12.3 保留风险

- 直接协作者列表仍不可读，状态保持 Unknown；本收据不声称 Base 仅 owner 可访问。
- OAuth 用户令牌仍有 142 个 scopes，继续作为独立 P1 预防性授权风险处理。
