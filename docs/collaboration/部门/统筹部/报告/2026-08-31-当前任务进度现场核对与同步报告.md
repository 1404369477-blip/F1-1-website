# 2026-08-31 当前任务进度现场核对与同步报告

## 结果

本轮已同时核对 M5 工作区、正式任务 JSON、M1 生产运行状态与公网用户入口，并把当前事实同步到 `docs/当前生产状态与执行待办.md`、`docs/progress.md`、`docs/handoff.md`、`docs/roadmap.md` 与 `docs/spec.md` 顶部覆盖层。

当前结论：

- Public Beta 正常在线，但继续读取上次签名投影 LKG。
- 15 分钟 SNAP 备份、异机密文、恢复演练和合法恢复点登记已运行。
- RSS 与 X 持续采集尚未恢复。
- 当前唯一需要用户确认的下一生产切片是 `ECADB7` R3：先现场绑定实际 release/manifest/DB/migration 身份，再执行 0011 additive migration、`quick-launch --until ready`、schema10 RSS plist installed-not-loaded。

## M1 现场只读核验

核验时间：2026-08-31 08:57，09:14 增量复核（Asia/Shanghai）。

### 服务与入口

| 项目 | 现场状态 |
| --- | --- |
| `com.f1plus1.public-beta` | loaded / running |
| `com.f1plus1.quick-tunnel` | loaded / running |
| `com.f1plus1.backup-snapshot` | loaded；间歇任务当时 not running；last exit=0 |
| `com.f1plus1.rss-collector` | not loaded |
| `com.f1plus1.rss-refiner` | not loaded |
| M1 loopback `/`、`/api/health`、`/api/public/feed` | 200 / 200 / 200 |
| M5 经公网 Quick Tunnel 的同三入口 | 200 / 200 / 200 |

公网 URL 保持 `[EPHEMERAL-TUNNEL-URL]` 占位符，不把临时域名重新写入公开仓库。

### 数据库与恢复点

| 项目 | 现场状态 |
| --- | --- |
| `PRAGMA user_version` | 10 |
| control | `phase=disabled / global_stop=stopped / recovery=fenced` |
| `valid_backup_recovery_point_v1` | 5 行 |
| 有效性 | 5/5 `off_host_verified=1 / encrypted=1 / restore_drill_state=verified / rpo_seconds=900` |
| 最新恢复点 | `2026-08-31T01:03:04.724Z` |
| Admin recovery fence | `clockTrusted=true / writerReady=true / lastSuccessfulRecoveryPointAt=1788138184724` |
| projection anchor | generation=141 / version=5 |

另发现一个历史 Admin profile 的 fence 仍为 false/null；当前生产 Admin profile 的 fence 为 true。文档只记录当前生产 profile，历史 profile不得被 runtime 选中。

### RSS、发布与投影

- 四条 RSS 的 route/release/manifest/authorization/policy 仍为 `111…555` 占位 hash。
- Motorsport 与 The Race 最近一次成功 ingest 仍是 2026-08-28；Autosport 最近为 HTTP 405；RaceFans 仍 stopped/blocked。
- publication：`published=954 / queued=1609`。
- projection outbox：`reconcile_wait snapshot_sync=1`，另有历史 succeeded/terminal_failed。
- collector/refiner 未加载，因此本轮没有新 RSS 内容进入处理链。

## 任务真值

| Task | 状态 | 当前含义 |
| --- | --- | --- |
| `TASK-20260829-FCC322` | superseded | Backup V2 增量路线已由 SNAP 取代，不领取 |
| `TASK-20260830-370AD2` | acknowledged | SNAP 候选与 disposable 恢复演练已核收 |
| `TASK-20260830-C917F4` | acknowledged | G1–G5 恢复门合法工具链已核收 |
| `TASK-20260829-BBFF2A` / `TASK-20260829-082F2C` | blocked legacy | 合同过时，禁止恢复；任务协议因历史替代链不允许再次 formal supersede |
| `TASK-20260831-F7B06E` / `TASK-20260831-C5189A` | locked legacy | R2 正文含旧 supersede/authorization 时态，已设 `user_required`，禁止恢复 |
| `TASK-20260831-ECADB7` | queued / user_required | RSS/恢复门 R3；等待用户确认三项 A 类生产动作 |
| `TASK-20260831-AC5CC2` | queued / user_required | 两源 canary R3；等 ECADB7 完成与独立 canary 确认 |
| `TASK-20260829-0ED611` | blocked / user_required | 等 ECADB7；生产 registry 写入与真实 X canary 分别等用户确认 |
| `TASK-20260829-E59ACA` | blocked | 等采集链后完成双语、人工审核、Public 与完整 Admin |

独立审查发现旧 `BBFF2A / 082F2C` 的验收条件把 `reconcile=0` 写成总量，和生产既有 1 条 `reconcile_wait` 冲突；也固定引用了未重新绑定的旧 candidate。首轮 R2 虽修正不变式，正文仍残留错误 supersede/authorization 时态；任务协议不允许直接改写 details，因此已将旧任务和 R2 全部设为 `user_required` 机械锁，并建立措辞自洽的 `ECADB7 / AC5CC2` R3。没有执行任何生产动作。

## Git 与工作区

- 当前分支：`codex/first-public-release`。
- 本地 HEAD 与远端同为 `3a8fe968b13cabcdb832c1617e88469244d7591c`。
- 工作区仍有既有未提交文档改动；本轮只做增量同步，未回滚、覆盖或清理其他改动。

## 本轮修改

- 更新 `docs/当前生产状态与执行待办.md` 的现场时间、Public状态、恢复点数量、最新恢复点、projection anchor 与任务状态。
- 更新 `docs/progress.md`，追加本轮现场核验记录并校正 `BBFF2A` 时态。
- 更新 `docs/handoff.md`、`docs/roadmap.md` 与 `docs/spec.md` 顶部当前覆盖层。
- 通过任务工具把旧 BB/Canary、R2 与 `0ED611` 全部置于 `user_required` 机械授权门；建立 `ECADB7 / AC5CC2` R3（均 queued/user_required）。
- 独立审查首轮结论为 `BLOCK / P0=1 / P1=5 / P2=3`；本轮已收敛其任务授权、reconcile 不变式、历史时态、SNAP 后继证据和 LaunchAgent 区间态问题。

## 未验证与限制

- 没有执行 0011 apply、`--until ready`、RSS plist安装、collector canary或任何生产数据库写入。
- 09:14 已验证第五个自然900秒轮：`SNAPSHOT_OK`、`OFFHOST_MIRROR_OK`、`REGISTER decision=SUCCESS`；没有等待第六轮。
- 没有验证新内容时效，因为 RSS/X collector 均未运行。
- 没有验证完整 Admin Mac/iPhone 运行功能。
- 当前环境无法读取用户提供的旧 M1 Codex thread ID；本轮通过 SSH 直接读取 M1 运行事实，未依赖对话自述。

## 下一步

等待用户明确确认后，授权并领取 `TASK-20260831-ECADB7`，只按以下顺序执行：

1. 现场复核实际 Admin/RSS release、manifest SHA、DB identity 与 0011 migration identity；
2. apply 0011，将 Motorsport 与 The Race 的真实 RSS identity 写入 additive v2 配置；
3. 执行 `quick-launch --until ready`，停在 `phase=disabled / global_stop=stopped / recovery=ready`；
4. 安装 schema10 RSS plist，保持 installed-not-loaded；
5. 核对既有 1 条 `reconcile_wait` identity/status 不变且新增 automatic review/publish/reconcile delta=0，再为 `AC5CC2` 单次 canary另取确认。
