# 自动化部 · 交接班文档

> **2026-09-06 08:13–08:25 只读回扫覆盖**：生产备份当前故障，`backup-snapshot` 最近 exit=1，遗留锁 `STALE_LOCK` 持续阻断；最新本地快照为 9 月 5 日 14:37，最新已登记恢复点为 9 月 1 日 08:13（北京时间）。不能继续以“已上线 / fence=true”宣称当前 RPO 合格。公开 reader 可用、三源最近采集槽成功；cutoff 后待审核已增至 40 条。当前优先级为恢复备份与登记链，再收敛任务合同及新稿持续链。详见 [本轮审计与推进方案](../../../reviews/2026-09-06-整体回扫与推进方案.md)。本地缺陷修复未部署，TASK 状态未升级。

> 角色 ID:`auto` ·最近更新:2026-09-05
> 这是本部门的**语义交接**(给接班的人看),不是任务状态真值或流水账。任务所有权、状态和产物只认 `../../tasks/TASK-*.json`;若两者冲突,以任务 JSON 为准并修正本文件。
> 铁律:从这里删掉的档案级事实,必须先用 `../../scripts/agent_team_log.py append` 追加到本周日志末尾,绝不直接丢;普通过程不记。

## 当前任务补充说明

> 只记录任务 JSON 不适合承载的恢复信息。没有时写“无”;干活时不刷收件箱。

- 已做到: `AC5CC2` 用户确认后已进 live、两源 canary 成功、collector/refiner 已加载。生产 0012 Sky 已 apply。公开 `:3000` 与投影 `:3102` 已切 `1408`。cutoff allowlist 已 sidecar refine/approve/publish 上站（Sky 22 / Motorsport 23 / The Race 16）。公开 generation **201**。snapshot 投递改为运行根 sidecar 进程内 `ProjectionReceiver.receive()`。
- 关键中间结论: JSON 仍 `claimed / user_confirmed`，**不要 complete**。收件箱同时列出锁定的 `BBFF2A`/`082F2C`/`C5189A`/`F7B06E`，禁止恢复，只续 `AC5CC2`。collector WD 必须留 `0145`；opener 读 `1408`。不要 HTTP POST `:3102`（10s idle timeout）。签名 caps 保持关。
- 相关产出路径: [`docs/collaboration/当前工作分工与交接.md`](../../当前工作分工与交接.md)、[`docs/当前生产状态与执行待办.md`](../../../当前生产状态与执行待办.md)、生产运行根 `[M1-HOME]/F1-1-website/.ac5cc2/runtime/`。

## 已定、不再回退的决策

- 固定部门正式名为“自动化部”，M1运行单元称“M1运行办公室”。
- 共享 docs 只承载治理；M1 owner-only OpsHandoff 承担机器合同、lease 和收据。
- 生产采集/提炼/发布走运行根 sidecar，不翻签名 `automaticReview`/`automaticPublish`。
- 流量监控从隐私最小化聚合开始，原始身份/认证/管理路径字段默认不持久化。

## 下一步

- 新 pending_review（Motorsport 4 / Sky 2 / The Race 3）走 pause 窗口补 candidate fence → refine → sidecar approve → publication fence → 进程内 send。
- `AC5CC2` 核收仍按合同：两个成功自然 900s（kickstart / RunAtLoad / gap 不算）。现场已有多个三源槽，等统筹按口径核收后再动 JSON。
- 抬 `:3102` HTTP timeout 或把 `findCommitted` 改成 O(1) 需要新签名 candidate，禁止热改 1408 密封文件。

## 已知坑 / 未决问题

- iCloud `Documents/F1+1` 会让 vitest/rsync/`cat` `EAGAIN`；生产脚本必须在 `[M1-HOME]/F1-1-website/.ac5cc2/runtime/`。
- 1408 `receiver-http.ts` 在 `PUBLIC_RELEASE_RUNTIME_FILES` 内，改 timeout 会破 `publicRuntimeClosureSha256`。
- `source_updated` 后再 approve 会留下旧 bundle 的 `queued` publication 行；不进公开 snapshot，不要误发布。
- `--upgrade-collaboration --role-policy-overlay-file` 在协议已经是 1.4.10 时直接 `UPGRADE_NOT_NEEDED`；禁止手改 `岗位说明.md` 或 `协议版本.json` 绕过事务。
- File Provider 两机副本可内容同步但不具备分布式锁语义；活动 task/lease/outbox 必须落 M1 owner-only 控制根。

## 关键文件指针

- [`docs/collaboration/当前工作分工与交接.md`](../../当前工作分工与交接.md)
- [`docs/当前生产状态与执行待办.md`](../../../当前生产状态与执行待办.md)
- `docs/collaboration/部门/自动化部/README.md`
- `docs/collaboration/部门/自动化部/M1运行办公室补充章程.md`
- `docs/collaboration/部门/自动化部/RUNBOOK.md`
- `docs/collaboration/部门/自动化部/STATUS.md`
- `scratch/TASK-20260815-M1-CODEX-LAUNCHD-HANDOFF/`（建部历史，不是当前运行钉）
