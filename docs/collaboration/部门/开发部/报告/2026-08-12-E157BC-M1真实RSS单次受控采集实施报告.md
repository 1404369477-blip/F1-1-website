# E157BC 固定 M1 真实 RSS 单次受控采集实施报告

## 1. 结论

`TASK-20260812-E157BC` 已承接 `TASK-20260812-772F48` 已通过的阶段 A–C，在不重复 builder、传输、overlay 或 installer 的前提下完成阶段 D：无歧义 baseline、同连接 one-shot enable、verified wrapper 唯一一次真实请求、紧随其后的唯一一次 stop、private 聚合与 20 秒短窗、公开 synthetic 零漂移以及任务阶段目录清理。

唯一真实请求返回脱敏失败收据：`status=failed`、`reasonCode=NETWORK_FAILURE`、`nextAction=next_slot`、`externalCalls=1`。没有重试。当前证据没有 HTTP status、response SHA 或更细分的 DNS/connect/TLS/timeout 分类，因此 `NETWORK_FAILURE` 的具体根因保持 Unknown，不作推测。

RSS source 当前为 disabled、`stop_epoch=3`；`com.f1plus1.rss-collector` 明确 unloaded。没有执行 `resume`、`bootstrap`、900 秒 load、第二次 wrapper、第二次真实请求、公开站点重启、npm 联网安装、自动发布或公网 Admin 变更。根据任务合同，真实源失败且安全停采、零漂移核对成立时仍可 `TASK_STATE_OK`，但本收据不放行 900 秒调度。

## 2. 精确 release 与阶段 A–C 继承收据

### 阶段 A：只读 preflight

- M1：UID `501`、用户 `chanai`、home `/Users/chanai`、arm64、macOS `26.5.1`。
- app root：`/Users/chanai/F1-1-website/app`，真实路径一致，非 iCloud；可用磁盘 `263941272 KiB`。
- Node：`24.18.0`，固定路径 `/Users/chanai/.local/node-v24.18.0-darwin-arm64/bin/node`，SHA-256 `ee6fb0e015284d83a91e8ec5213f43a157f8a392b58555301682892ba928c04a`。
- `com.f1plus1.public-beta` running；`com.f1plus1.receipt-refresh` 已加载且 last exit `0`；`com.f1plus1.quick-tunnel` running。
- `com.f1plus1.rss-collector` 返回 RC `113` 与明确 `Could not find service`。
- 公开 synthetic DB 初始身份：SHA-256 `949c78d505e4c032d2495174deaf62d24f9d99b76284ad7ba6fb29a5ac83bb50`、size `737280`、inode `24546198`、mode `0600`、UID `501`、单链接。
- local/public home 与固定 detail 均为 HTTP `200`。

### 阶段 B：release builder、传输与 overlay

- 唯一 release commit：`7f5cab64e25d01c74005fbb24c4f0e2905291f1d`。
- builder 只执行一次，结果 `built-from-clean-git-head`、`externalCalls=0`。
- release manifest SHA-256：`f9765974eb3bc1be3512ea202781efb300b14d3a20f4f56cbb92cf206fbe9e96`。
- content root：`71df4f63952a538de0ef6497a50038029f3da3c504a5ca6f829a24ec121935ae`。
- release SHA-256：`b62a87747ce322d1b2c77a1e9575d417019e9fe2655c1a9d33675b49713699b6`。
- 生产依赖 root：`4b96e6fc4eb908ed647d12d2251553623315854a710cb7aaf717ce874ab49d41`。
- 闭包包含 19 个 runtime 文件与 8 个递归生产依赖目录。
- 任务 tar 本地/远端 SHA-256 均为 `5eaf3882717d9560c92ae33d5251ea1b2674672ba78cee4ab3139154db2d2d4a`，size `2627072`。
- 远端 stage 与正式 app 均通过 release verifier；正式 overlay 前的目标字节已移动到任务专属可恢复 backup。

### 阶段 C：prepare-only

- installer 只执行一次，收据：`status=prepared-no-bootstrap-called`、`externalCalls=0`。
- deployment manifest SHA-256：`455faf2982bd3486032c9ba17c8feea5e6955b38f0de9836845e59587e14bdfb`。
- plist SHA-256：`51884bf5d54a118016da7c9dcabed1431aa2f6e921be02858abb1f15c1ab8bc1`。
- `.local`、release、logs、tmp 与 LaunchAgents 目录均为 mode `0700`、UID `501`。
- private DB、release/deployment manifest、plist 与两个 collector 日志均为 mode `0600`、UID `501`、单链接。
- prepare 前后 RSS job 均明确 unloaded；公开 DB SHA 未变。

## 3. 阶段 D 单次执行收据

### 无歧义 baseline

机械后继使用参数化 source ID 以及 `IS NOT NULL` + `length(...)` 的 editor 聚合，没有双引号字符串字面量。baseline 同时重验：

- release/deployment manifest、19 个 runtime 文件、8 个递归依赖、目标 Node 字节、plist、日志/DB 权限；
- private DB 完整 `assertRssSchema` 与 `PRAGMA integrity_check=ok`；
- RSS job 明确 unloaded；
- source `enabled=0`、`stop_epoch=2`、validators/time fields 均为空、`last_reason_code=NEVER_RUN`；
- ingest run `0`、candidate `0`、review 分布为空、全部 editor 非空/版本聚合为 `0`；
- 公开 DB 身份、local/public HTTP 与三个现有服务均与阶段 A 一致。

### one-shot enable

- enable 前后 RSS job 均明确 unloaded。
- 在同一个 private DB 连接先执行 `assertRssSchema`，再进入 `BEGIN IMMEDIATE`。
- SQL 只更新固定 source 的 `enabled`，条件要求 `enabled=0 AND stop_epoch=2`；变更数恰为 `1`。
- before/after 逐字段比较确认仅 `enabled: 0 → 1`；`stop_epoch=2`、feed、validator、时间与 reason 字段全部未变。

### wrapper 唯一请求

- wrapper 开始时间：`2026-08-11T19:25:16Z`。
- wrapper 执行次数：`1`；真实 RSS 请求计数：`1`；退出码：`1`；重试次数：`0`。
- 脱敏收据：
  - `status=failed`
  - `reasonCode=NETWORK_FAILURE`
  - `nextAction=next_slot`
  - `externalCalls=1`
  - `responseSha256=null`
  - `itemCount/selectedCount/newCount/updatedCount/duplicateCount=0`
- wrapper 收据正式文件：`app/.local/logs/TASK-20260812-E157BC-rss-wrapper-receipt.json`，mode `0600`、单链接、size `340`，SHA-256 `b0b9ff2fb18ed5d47d4df0f4749b337f2d8d47530181fa123e443ae85e101421`。

### 紧随其后的 stop

- wrapper 进程退出后没有执行解析、查询或其他业务动作，第一项动作即为唯一一次 `rss-control.ts stop`。
- stop 执行次数：`1`；退出码：`0`；完成时间：`2026-08-11T19:25:16Z`。
- 收据：`status=stopped-confirmed`、`externalCalls=0`、source disabled、`stopEpoch=3`、launch state unloaded。
- stop 收据正式文件：`app/.local/logs/TASK-20260812-E157BC-rss-stop-receipt.json`，mode `0600`、单链接、size `191`，SHA-256 `272e039f663b64a8f6758c4bde3c98594792fe2294a43b81a3a8e2063f2607a7`。

## 4. 后置聚合与短窗

stop 后与 20 秒短窗末尾的两次只读快照逐字一致：

- source：`enabled=0`、`stop_epoch=3`、`last_reason_code=NETWORK_FAILURE`、`last_success_at=null`。
- `ingest_run`：总数 `1`，唯一聚合为 `failed/NETWORK_FAILURE/next_slot`；HTTP status 空、validator result/capability 均为 unknown、response SHA 不存在。
- run 聚合：item `0`、selected `0`、new `0`、updated `0`、duplicate `0`。
- `pending_review_candidate`：总数 `0`；review_status 分布为空。
- `editor_title`、`editor_excerpt`、`editor_notes` 非空计数均为 `0`；`editor_based_on_source_revision` 已设置计数与派生需复审计数均为 `0`。
- private DB schema fingerprint 与 integrity 均 PASS。
- 20 秒内 run 总数保持 `1`，candidate 总数保持 `0`；没有后台调度或新增 run。

本次 DB 初始没有人工编辑字段非空的候选，因此观察到机器没有创建或覆盖 editor/review 数据；对“既有非空人工字段经真实内容更新仍保持”的动态证明在本次零 candidate 路径上没有样本，保持未验证。

## 5. 公开面零漂移

- 公开 synthetic DB 最终 SHA/size/inode/mode/owner/link 与阶段 A 完全一致：`949c78d505e4c032d2495174deaf62d24f9d99b76284ad7ba6fb29a5ac83bb50` / `737280` / `24546198` / `0600` / UID `501` / 单链接。
- local home、local detail、public home、public detail 最终均为 HTTP `200`。
- public-beta PID 保持 `52089` 且 running；quick-tunnel PID 保持 `53446` 且 running；receipt-refresh 继续已加载且 last exit `0`。
- 没有重启或重新加载任何公开服务，没有修改公开 DB 或公开内容。

## 6. 正式保留文件

| M1 正式产物 | 最终 SHA-256 | mode / size |
| --- | --- | --- |
| `.local/f1plus1-rss-real-private.sqlite` | `e64e13f2fa88ed0467752e2a8b907db612be60f3b2ddb77c3babd3469ebba6a0` | `0600` / `61440` |
| `.local/release/rss-real-release-manifest.json` | `f9765974eb3bc1be3512ea202781efb300b14d3a20f4f56cbb92cf206fbe9e96` | `0600` / `20497` |
| `.local/rss-real-deployment-manifest.json` | `455faf2982bd3486032c9ba17c8feea5e6955b38f0de9836845e59587e14bdfb` | `0600` / `2231` |
| `Library/LaunchAgents/com.f1plus1.rss-collector.plist` | `51884bf5d54a118016da7c9dcabed1431aa2f6e921be02858abb1f15c1ab8bc1` | `0600` / `1435` |
| `.local/logs/rss-collector.stdout.log` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | `0600` / `0` |
| `.local/logs/rss-collector.stderr.log` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` | `0600` / `0` |
| wrapper 脱敏收据 | `b0b9ff2fb18ed5d47d4df0f4749b337f2d8d47530181fa123e443ae85e101421` | `0600` / `340` |
| stop 脱敏收据 | `272e039f663b64a8f6758c4bde3c98594792fe2294a43b81a3a8e2063f2607a7` | `0600` / `191` |

## 7. 临时资源清理

- M1 原任务路径 `/Users/chanai/F1-1-website/.rss-release-TASK-20260812-772F48` 已不存在；其中 stage、tar 与 backup 一并移出运行树。
- 本地原任务路径 `/private/var/folders/dz/l8yd2gqj22z1xgrcl1z_wbkr0000gn/T/TASK-20260812-772F48-release.36zkHe` 已不存在。
- 递归删除命令在执行前被安全层拒绝，没有产生部分删除。随后采用可恢复清理：M1 副本移动到 `/Users/chanai/.Trash/TASK-20260812-772F48-remote-release-stage`，本机副本移动到 `/Users/hoyin/.Trash/TASK-20260812-772F48-local-release-stage.36zkHe`。两处 Trash 副本仍可恢复，未宣称永久擦除。
- 正式 private DB、release/deployment manifest、plist、两个 collector 日志与两条脱敏收据均未删除。

## 8. 未验证与错题自检

未验证：

- `NETWORK_FAILURE` 的 DNS、TCP、TLS、timeout 或对端层级根因；收据按合同脱敏且没有 HTTP response，任务禁止第二次请求，因此没有定位。
- 成功的 HTTP 200/304、RSS parse、candidate 插入/更新/重复路径；本次网络失败未到达这些分支。
- 含既有非空 editor 字段候选的真实更新保护；本次 candidate 基线与结果均为 0。
- 900 秒调度稳定性。任务明确禁止 load，本轮保持 stopped。

错题自检：

- 772F48 的 baseline SQL 引号错误只发生在只读调用方；E157BC 使用无歧义 SQL，没有修改产品代码或 schema。
- builder、传输、overlay、installer 均未在 successor 重做。
- one-shot enable 只改 `enabled`，同连接 schema 门、IMMEDIATE 事务、变更数与逐字段比较均有收据。
- wrapper、真实请求与 stop 均恰好一次；真实失败没有重试。
- 没有执行 resume/bootstrap/load、自动发布、公网 Admin、站点重启、npm 联网或第二次请求。
- source disabled、epoch 增加、job unloaded、短窗零新增、private integrity 与 public 零漂移全部成立。
- 临时清理使用可恢复 Trash，已如实披露仍存在的可恢复副本。

TASK_STATE_OK
