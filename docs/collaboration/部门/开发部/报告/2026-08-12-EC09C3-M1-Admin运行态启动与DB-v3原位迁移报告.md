# TASK-20260812-EC09C3｜M1 Admin 运行态启动与 DB-v3 原位迁移

## 结论

`status=final`，`decision=pass`。固定 M1 的 projection receiver 与 Admin 各执行一次 `bootstrap`、一次 `kickstart`，现均为 running；3102/3101 各只有一个 `127.0.0.1` listener。唯一 review DB 从 `user_version=1` 在原 path/dev/ino 追加迁移至 v3，启动门接受三个互异 sourceRef。无可信 Tailscale 身份的 loopback 请求返回 401 且 DB SHA 前后一致。显式 sender-once 返回确定 `idle`，未创建第二常驻 sender。

## 运行收据

- Receiver `com.f1plus1.public-projection`：`state=running`，`runs=1`，精确监听 `127.0.0.1:3102`。
- Admin `com.f1plus1.admin-service`：`state=running`，`runs=1`，精确监听 `127.0.0.1:3101`。
- 固定 DB：path未变，dev `16777233`、ino `24570709`、uid501、nlink1、mode0600；`user_version 1 → 3`，`integrity_check=ok`，`journal_mode=wal`。
- `foreign_keys=ON` 由 Admin 真实启动链的 `openExistingSafeDatabase` 连接门与 `openReviewAdminDatabase` 运行门强制核对；该门未通过时 3101 不会开始监听。
- 第二同名 review DB 候选路径计数 `0`。
- 三设备配置：trusted identity 精确一行，sourceRefs `3` 项且互异；原值未进入报告或 Git。
- 无身份请求：`GET /admin/reviews` 在精确 Host 下返回 `401`；探针前后 DB SHA 逐字节相等。
- Sender 一次收据：`outcome=idle`、`deliveryId=null`、`externalCalls=0`、`loopbackCallsMaximum=1`；当前 outbox 无待投递工作。

M1 脱敏执行收据保存于 owner-only 路径：

`[M1-HOME]/Library/Application Support/F1Plus1/ActivationReceipts/TASK-20260812-2889EA`

## 旧业务状态与边界

- 旧 public-beta 继续 running，PID 保持 `52089`，3000 仍只监听 `127.0.0.1:3000`。
- RSS collector 仍为已加载的自然周期任务，本任务未 kickstart、resume、stop 或改写 RSS。
- Public deployment/read mode/live release 未切换；本次启动的 receiver 只提供 loopback 投影接收端。
- Tailscale policy、Grant、Serve、Funnel 写入均为 `0`。

## mistake-check / 未验证

- 2084BB 中 disabled plist 只 bootstrap 不会运行，已完整 bootout/隔离；本任务只补了各一次明确 kickstart，未改 plist 或业务代码。
- 完成主链后一个额外只读 SQL 摘要候选使用了不存在的表名 `admin_passkey_credential`，只读连接报 `no such table`；未写 DB、未影响已通过的启动/迁移/sender 收据，也没有重跑主链。
- 尚未执行 Tailscale Serve 或真实 M5/iPhone/iPad 访问，因此 Passkey 真机注册/登录、三设备 app-cap 头及撤销路径仍未验证。
- sender 本次为确定 no-op；生成第一代真实投影需要后续审核/发布事实，本任务没有伪造一条待投递任务。
