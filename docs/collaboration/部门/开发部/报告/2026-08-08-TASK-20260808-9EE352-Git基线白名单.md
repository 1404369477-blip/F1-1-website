# TASK-20260808-9EE352｜安全 Git 基线白名单

> 状态：执行清单草案。当前任务只产出清单，以下命令 **未执行**。执行需另开 Git 基线任务并由统筹确认当前 HEAD、设计冻结及产品合同状态。逐文件路径真值为同目录 `2026-08-08-TASK-20260808-9EE352-Git基线路径清单.tsv`；禁止用目录名替代该清单。

## 0. 强制前置

```sh
test "$(git rev-parse HEAD)" = "a9691e71b1552592cc5ded8d5db66c336262301c"
test -z "$(git diff --cached --name-only)"
```

任一失败即停止。禁止 `git add .`、`git add -A`、`git clean`、`git reset --hard`。

## 1. 逐文件执行器

```sh
MANIFEST='docs/collaboration/部门/开发部/报告/2026-08-08-TASK-20260808-9EE352-Git基线路径清单.tsv'
awk -F '\t' '$1 == "INCLUDE_EVIDENCE" { print $6 }' "$MANIFEST" |
while IFS= read -r path; do git add -- "$path"; done
```

对其他批次，只替换分类名：

- `INCLUDE_TRACKED_DOC`
- `INCLUDE_APP`
- `INCLUDE_DATA`
- `INCLUDE_MIGRATION_CORE`
- `CONDITIONAL_DESIGN`（仅满足 §6 条件后）

`DEFER_CONTRACT`、`DEFER_DOC_RECONCILIATION`、`UNKNOWN_PRESERVE`、`EXCLUDE_GIT` 永远不能传给执行器。

清单同时记录当前 SHA-256、来源类别、敏感性判断和生成物类别；清单自身的 hash 标为 `SELF`。任务 JSON 会在本任务 complete 时更新内容，因此其清单 hash 会按协议产生预期漂移。实际 Git 基线任务必须先重新计算所选批次 SHA-256：除任务状态正常漂移和获批后继任务外，其他差异一律停止并回查。

## 2. 协作协议与正式证据层

使用 `INCLUDE_EVIDENCE` 的逐文件清单。纳入后必须确认不存在 `docs/collaboration/.locks/`。任务 JSON、正式报告、日志与索引属于项目证据；不得只纳入报告而漏掉任务真值。

## 3. 已跟踪项目文档修改层

使用 `INCLUDE_TRACKED_DOC` 的逐文件清单。

`design/README.md`、`design/ui/README.md`、`docs/overview.md`、`docs/roadmap.md`、`docs/spec.md` 当前列为 `DEFER_DOC_RECONCILIATION`。其中 roadmap 的阶段时态与 M4 handoff/progress 不一致；必须等待 `TASK-20260808-00A9C9` 或独立文档一致性裁决，不能随 tracked 修改一并纳入。

## 4. 已验收 app 层

使用 `INCLUDE_APP` 的逐文件清单。禁止 `git add app/src` 或 `git add app`；目录级命令会误纳三个待合同文件或未来并发新增项。

当前允许随该层纳入的两项专项文件及冻结 hash：

- `app/src/features/stories/public-api.ts` — `a8baccbaa3cf95b997113d0cb00db1ab902c230759959d454d268d9e2bece033`
- `app/src/components/f1/f1-page-shell.tsx` — `4237c0ac004858831e14ff669e17eb1bf1560e2b3a96dc18b218ba459b2f21ae`

明确暂缓：

- `app/src/tests/public-ui.test.ts`
- `app/src/app/globals.css`
- `app/src/features/stories/feed-experience.tsx`

三者等待最终设计合同、开发落地任务和独立复验，不删除、不回滚。

## 5. 数据合同层

使用 `INCLUDE_DATA` 的逐文件清单，禁止 `git add data`。

该层只能单独提交。暂存后必须确认提交说明明确：`mvp-contract-v0` 中的 secret-like 值是 synthetic security fixture，禁止误认成真实凭据。

## 6. 迁移核心证据层

使用 `INCLUDE_MIGRATION_CORE` 的逐文件清单，禁止 `git add migration`。

本批精确排除并保留待裁决：

- `migration/bundles/`
- `migration/manifests/`
- `migration/portable-assets/`
- `migration/scripts/`

## 7. 最终设计层（条件式）

仅当 `TASK-20260808-54BB47.execution_state=completed` 且 task JSON 登记 final 报告与 hash 清单后执行：

使用 `CONDITIONAL_DESIGN` 的逐文件清单。

当前 `index.html` 与 final HTML hash 相同，但 `index.html` 是活动设计入口；待冻结任务决定是否同时纳入。其余 `evidence/v0.2*` 中间证据保留，等待大文件归档策略，不能自动删除。

## 8. 永久排除路径

```text
app/.env
app/.local/
app/node_modules/
app/.next/
app/tsconfig.tsbuildinfo
app/Library/
app/~/
app/--disable-gpu/
app/--full-page/
app/--headless=new/
app/--hide-scrollbars/
app/--screenshot=/
app/--user-data-dir=/
app/--window-size=1024,1600/
app/--window-size=1440,2000/
app/-la/
app/http:/
app/ls/
.codex/
.obsidian/
docs/collaboration/.locks/
scratch/
```

## 9. 每批暂存后的机械核对

```sh
git diff --cached --name-status
git diff --cached --stat
git diff --cached --check
git status --short
```

必须人工确认：

- 没有永久排除路径；
- 没有三个待合同前端文件；
- 没有迁移四组冲突目录；
- 没有中间设计 PNG；
- 没有 `.env`、SQLite、日志、工具链、缓存或本机插件状态；
- 未知项仍留在工作区，没有被删除。
