---
type: development_delivery_report
status: final
date: 2026-08-04
department: 开发部
task_id: TASK-20260804-538692
domain_stage: 公开前端API接线
decision: delivered_for_formal_test_and_design_review
---

# 公开前端 API 单一数据源接线实现报告

## 结论

开发交付完成。首页与详情浏览器运行时只消费 closed 公开 DTO：相对同源 `GET /api/public/feed` 与 `GET /api/public/stories/{publicId}`。旧 `demo-data.ts` 已删除；客户端没有 fixture、SQLite、Repository、Host/Forwarded、自建绝对 URL、外联或静态 fallback。API 错误统一失败闭合。

## 改动文件

- 新增 `app/src/features/stories/public-api.ts`：closed DTO 校验、相对路径请求、分类/游标查询、视图模型映射与安全错误类型。
- 重写 `app/src/features/stories/feed-experience.tsx`：API 驱动的 loading/error/empty/no-more/retry、四类筛选、游标加载、受限与无图状态。
- 新增 `app/src/features/stories/story-detail-experience.tsx`，调整 `app/src/app/stories/[publicId]/page.tsx` 与 `not-found.tsx`：详情、related、API 404/500 与重试映射。
- 调整 `app/src/components/f1/story-parts.tsx`、`f1-page-shell.tsx`、`app/src/app/layout.tsx`、`globals.css`：公开 DTO 展示、来源状态、主内容锚点及既有主题/可访问性合同延续。
- 删除 `app/src/features/stories/demo-data.ts`；重写 `app/src/tests/public-ui.test.ts` 为本任务聚焦集成测试。

## 唯一验证收据

- 固定 Node.js 24.18.0：`vitest run src/tests/public-ui.test.ts --config vitest.config.ts`，exit 0，1 个文件、8/8 tests PASS。
- 固定 Node.js 24.18.0：`tsc --noEmit --incremental false`，exit 0。
- 按任务约束未运行 full check、build、浏览器测试，也未启动端口或访问外部网络；本任务未创建 DB、`.next` 或临时文件。工作区已有 `app/tsconfig.tsbuildinfo` 的修改时间为 2026-08-04 15:33:39，早于本任务验证，未改写或清理。

## 已知残余与交接

- 聚焦测试验证 API 适配器、closed DTO、相对 no-store 请求、筛选/游标、详情/related、404/500、安全回退边界、静态标记、主题与部分可访问性合同；未挂载页面 hooks 驱动真实 HTTP 状态迁移。
- 客户端将 API 404 映射为 404 视图；未声明页面文档本身返回 HTTP 404。
- “加载更多”失败时整页进入安全错误态，重试从第一页开始，属于保守失败闭合行为。
- 真实 HTTP/交互由原测试部窗口验收；390/1440、深浅主题、键盘与视觉由原设计部窗口验收。两部门 P0/P1 清零前，本开发部不 ACK。

## 错题检查

- 未改 Repository、API、migration、seed、data、Spec、accepted ADR、design 或 lockfile；未新增依赖。
- 未运行被禁止的 full check/build/浏览器；验证次数严格为一次聚焦测试与一次 typecheck。
- 只读对抗审查未发现 P0/P1 阻断；以上未验证项已如实保留，没有扩大 PASS 声明。

TASK_STATE_OK
