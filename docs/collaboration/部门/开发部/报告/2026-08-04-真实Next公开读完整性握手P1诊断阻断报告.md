---
type: development_block_report
status: blocked
date: 2026-08-04
department: 开发部
task_id: TASK-20260804-16B34F
domain_stage: p1_remediation
decision: blocked_after_bounded_restart_budget
---

# 真实 Next 公开读完整性握手 P1 诊断阻断报告

## 结论

任务未达到 HTTP 验收出口，不能回传 `TASK_STATE_OK`。两次允许的启动均在监听前结束；当前没有任务进程或端口残留。按照任务失败路径停止第三次启动，并将任务置为 blocked。

## 已定位并修复的首错

任务专属 `/tmp/TASK-20260804-16B34F/project/app/.local/f1plus1-public-synthetic.sqlite` 仅 seed 一次，结果为 1/12/12/12/10/12/12/12/12、`externalCalls=0`。

首次启动前的 readiness 返回 `PROFILE_MIX: Source seed requires m3-shadow`。根因是 `app/src/server/health.ts` 无条件调用 `assertSourceFixtureSeeded`，没有按 `dataProfile` 分流。实现已最小修复：

- `m3-shadow` 继续执行原 59 行 Source 完整性断言；
- `public-synthetic` 执行 `assertPublicSyntheticSeeded`，没有关闭或放宽 integrity；
- health DTO 对 public profile 使用准确的 `accepted-public-synthetic`、`public-synthetic-0003`、`12-public-synthetic` 标签。

修复后的无监听内部 readiness 在同一 DB 上通过：Node 24.18.0、SQLite 3.53.1、`dataProfile=public-synthetic`。

## 第二个阻断

唯一纠正性重启仍在监听前由安全包装器返回 `CLI_INTERNAL_ERROR`。Next 本轮痕迹只记录 `NEXT_CLI_SESSION_STOPPED`：Next 16.2.11、Node 24.18.0、`cliCommand=dev`、约 449ms；没有监听 127.0.0.1:3000，也没有可用 HTTP 请求。包装器按安全合同吞掉子进程 stdout/stderr，现有日志没有提供更具体的内部异常。

由于两次启动预算已经耗尽，本窗口没有运行第三次启动，也没有用直启 Next 绕过包装器。因此 feed 200/12、有效详情 200、未知详情 404、malformed/Problem/no-store/零泄露的唯一真实 HTTP 验收均为 `NOT_RUN`。

## 验证与清理

- 固定 Node 24 typecheck：一次，exit 0。
- seed：一次；没有第二轮 seed。
- 启动：两次，均未监听；没有额外测试、full check、build 或浏览器。
- 本任务进程、`/tmp/TASK-20260804-16B34F`、临时 DB/sidecar、临时 `.next`、诊断脚本与收据在任务置 blocked 前清理；工作区 `.next` 未创建。

## 最小下一步

下一轮需由统筹部明确恢复启动预算，并在不泄漏 HTTP 的前提下，让 `scripts/serve.ts` 将 Next 子进程启动失败归一化为本地受控诊断收据（稳定错误阶段/退出码，不含路径、stack、URL 或 secret）。随后使用一次启动确认子进程退出根因，再决定是否需要局部修复。当前证据不足以安全猜测 Next 子进程为何在约 449ms 后退出。

## 错题检查

- 没有关闭 integrity、吞掉 Repository 异常或增加 fallback。
- 没有启动第三次进程、重复 seed、执行 HTTP 伪验收或扩大 PASS 声明。
- 未修改 API、Repository、数据合同、Spec、accepted ADR、依赖或 lockfile。
