# 自动初审与人工恢复生产启用报告

日期：2026-08-14

## 结果

- 固定 M1 Admin 每 60 秒执行一次确定性自动初审，单次最多 100 条。
- 安全通过只生成不可变审核版本、approved decision 与 queued publication；自动公开发布保持关闭。
- 缺少当前来源版本中文稿的内容保持等待，不写拒绝决定。
- 自动拒绝原因写入既有 `review_decision.rejection_reason` 与 hash-chain `audit_event`，Admin 的“已拒绝”筛选可回看。
- “恢复到待审核”会生成新 revision 并保留旧拒绝记录；相同 source revision/hash 不再自动打回，来源版本变化时重新初审。
- refiner 每个 900 秒周期最多补齐 20 条缺失 DeepSeek 中文稿；collector/refiner 两个 LaunchAgent 均已恢复。

## 生产收据

- 启用前 SQLite 一致性快照：`[M1-HOME]/F1-1-website/.auto-review-20260814/preflight.sqlite`，SHA-256 `2cd8e666adb9503f4d23589f1605585060a7e3ea5aff4c4f904b4e3400a48d9d`，mode `0600`，UID `501`，nlink `1`。
- 自动初审累计：approved `37`、rejected `0`、failed `0`；当前中文稿缺失 `0`。
- 当前候选：approved `37`、published `2`；approved decisions `45`。
- 当前 publication：queued `39`、published `6`。
- 公开边界零漂移：`published_projection=6`、`projection_outbox=6`，本任务没有创建公开投递。
- SQLite：`user_version=4`、`integrity_check=ok`、`foreign_key_check` 无结果。
- 服务：Admin `127.0.0.1:3101`、receiver `127.0.0.1:3102`、public `127.0.0.1:3000` 均为唯一 loopback listener。
- collector/refiner：`StartInterval=900s`，恢复后的主动周期均 `last exit code=0`。

## 代码与回退

- M1 Admin release `c8e1b263…62097a`：
  - `repository.ts` SHA-256 `73e5ed08455888290e7810991cccfab25653ff8771d22c193801b48595a42e8e`
  - `runtime.ts` SHA-256 `f34364b290f5b522e6e749575640e36626f593aacb9a749d02697b5bbc623a30`
  - `app.js` SHA-256 `7ca4d195be144675aff2e52824b4539e352d11d9d0fa7e421dff2b2e86e43434`
- M1 refiner release `fb50b6a…c789`：`rss-refine-once.ts` SHA-256 `3139ef109660620fdbd66f1d7b0abf3a412db110979703525113481426a36a96`。
- 上一版四个文件保存在 `[M1-HOME]/F1-1-website/.auto-review-20260814/deploy/backup/`。代码回退时原子恢复这四个文件并重启 Admin；数据库中的审核、拒绝与审计记录不删除。

## 验证

- 固定 Node `24.18.0` 聚焦 Vitest：4 files / 5 tests PASS。
- 固定 Node `24.18.0` TypeScript `tsc --noEmit` PASS。
- 限定文件 `git diff --check` PASS。
- 生产数据库快照预演：第一次 approved `22`、waiting `15`、rejected `0`、failed `0`；第二次只保留 waiting，证明幂等且公开投影/outbox 零变化。
- 生产静态资源已回读“恢复到待审核”入口。

## 未扩大边界

- 没有自动公开发布、没有自动 sender 投递、没有删除候选或拒绝记录。
- 没有改变 Passkey、Tailscale、projection key、public reader、receiver、公开站或数据库 schema。
- 内容相关性、文风、重复价值与编辑质量暂不作为自动拒绝条件，后续依据实际分布另行微调。
