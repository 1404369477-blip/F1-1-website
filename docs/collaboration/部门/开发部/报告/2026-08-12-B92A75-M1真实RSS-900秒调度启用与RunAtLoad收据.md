# TASK-20260812-B92A75 M1 真实 RSS 900 秒调度启用与 RunAtLoad 收据

## 结论

M1 固定真实 RSS source 已从 `disabled / stopEpoch=4 / unloaded` 经唯一一次既有 `rss-control.ts resume` 切换为 `enabled / stopEpoch=4 / loaded`。LaunchAgent 回读显示 `RunAtLoad`、`run interval = 900 seconds`、`runs = 1`、`last exit code = 0`。RunAtLoad 已形成一条成功收据；公开数据库、四个页面探针和三项公开服务均与固定基线一致，因此未执行紧急 stop。

## 固定输入与次数

- release manifest SHA-256：`34c1032cc6adca96990cf35b092c2d61b940a2bda8fdd53a0c8ccfbb6e7c8d95`
- M1 Node：`[M1-HOME]/.local/node-v24.18.0-darwin-arm64/bin/node`
- control pre-status：1 次
- control resume：1 次
- 等待：20 秒，不超过 30 秒
- control post-status：1 次
- control stop：0 次
- 手动 wrapper：0 次
- 第二次 resume：0 次
- 新脚本、inline Node、代码或数据库人工修改：0 次

## 控制收据

### Pre-status

```json
{"command":"rss:control","action":"status","status":"observed","label":"com.f1plus1.rss-collector","launchState":"unloaded","source":{"enabled":false,"stopEpoch":4},"externalCalls":0}
```

前置状态精确满足 `disabled / epoch4 / unloaded`，允许进入唯一 resume。

### Resume

```json
{"command":"rss:control","action":"resume","status":"resumed-confirmed","label":"com.f1plus1.rss-collector","launchState":"loaded","source":{"enabled":true,"stopEpoch":4},"externalCalls":0}
```

### Post-status

```json
{"command":"rss:control","action":"status","status":"observed","label":"com.f1plus1.rss-collector","launchState":"loaded","source":{"enabled":true,"stopEpoch":4},"externalCalls":0}
```

## RunAtLoad 收据

collector stdout 尾部只有一条 JSON，stderr 尾部为空：

```json
{"schemaVersion":"rss-real-receipt-v1","profile":"rss-real-private","sourceId":"motorsport-f1-news","runId":"rss-run-1984983","slotKey":1984983,"status":"succeeded","reasonCode":"OK","nextAction":"next_slot","externalCalls":1,"responseSha256":"09754466c27b4a1cdac5af9f6b5817fc716c0db08814d3a6f0dde3e5d3017d7f","itemCount":50,"selectedCount":20,"newCount":0,"updatedCount":0,"duplicateCount":20}
```

该 RunAtLoad 运行完成一次真实请求，解析 50 条、选择 20 条，新增 0、更新 0、重复 20；未手动触发或重试。LaunchAgent 只加载调度定义，单次进程完成后显示 `state = not running / active count = 0` 属于预期；同时 `runs = 1`、`last exit code = 0`、`run interval = 900 seconds`、`properties = runatload`。

## 公开面零漂移

- 公开数据库 SHA-256：`949c78d505e4c032d2495174deaf62d24f9d99b76284ad7ba6fb29a5ac83bb50`
- 数据库身份：size `737280`、inode `24546198`、mode `0600`、uid `501`、nlink `1`
- local home：HTTP 200
- local detail：HTTP 200
- public home：HTTP 200
- public detail：HTTP 200
- `com.f1plus1.public-beta`：running，PID `52089`
- `com.f1plus1.quick-tunnel`：running，PID `53446`
- `com.f1plus1.receipt-refresh`：已加载，当前不运行，last exit `0`

以上身份和健康状态与任务固定基线一致，未触发 stop 条件。

## 已验证、未验证与错题自检

已验证：唯一 resume 成功；source enabled、epoch 维持 4；LaunchAgent loaded；RunAtLoad 完成且 exit 0；StartInterval 900 秒；公开 DB、页面和服务零漂移。

未验证：后续每个 900 秒自然周期的持续稳定性、未来上游网络可用性和既有非空人工字段遇真实更新时的动态保持，留待后续自然运行监测。

错题自检：未新增脚本，未使用 inline Node，未手动运行 wrapper，未执行第二次 resume，未执行 stop，未删除收据、备份、数据库、manifest、plist 或日志，未发布站点或执行 Admin 操作。collector stdout/stderr 各自读取不超过 5 行；另为公开入口健康核对只读查看 quick-tunnel stderr 尾部 5 行，未修改服务。
