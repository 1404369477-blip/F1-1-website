---
type: work_report
department: 开发部
target: "TASK-20260812-2B6D52 / M1 RSS 旧 stage 与迁移残留精确清理"
status: final
date: 2026-08-12
related_task: TASK-20260812-2B6D52
decision: blocked
tags: [RSS-REAL-001, M1, cleanup, partial-delete, tool-policy, first-error-stop]
summary: "四个目标均通过路径、owner、内容哨兵与打开句柄门；M1 两个旧残留已永久删除，本机首个删除命令在进程创建前被执行工具安全策略拒绝；按首错纪律未换方式重试，本机两个路径仍在，任务未达成四路径全部不存在的验收出口。最新 325056 回退备份、正式运行文件、900 秒调度与公开面均正常。"
---

# 2B6D52 M1 真实 RSS 旧 stage 与迁移残留精确清理首错阻断报告

## 1. 结论

本任务以 **BLOCKED / 部分清理** 收口。

删除前，四个精确目标均存在且为 UID 501 所有的真实目录；目录内未发现 symlink、非预期 owner 或打开句柄，任务哨兵文件的 SHA-256/大小也与既有报告相符。M1 上的两个目标已按精确绝对路径永久删除。执行本机第一个精确删除时，执行工具在进程创建前拒绝 `rm -rf`，没有发生本机部分删除。任务合同要求“删除首错立即停止”，因此没有改用 Python、`find -delete`、Trash 或其他写法重试，也没有继续删除第二个本机目标。

已确认 M1 最新回退备份、正式 app/private DB/release manifest/deployment manifest/plist/logs/公开 DB 仍在；RSS source 继续 `enabled=1 / stop_epoch=4 / OK`，900 秒 LaunchAgent 继续 loaded，本地与公网四个页面均为 HTTP 200，三项公开服务无漂移。本任务没有触发采集、重启、改 DB 或改服务。

## 2. 删除前四路径身份门

`du -sk` 以 1024 字节块折算为下表的已分配字节数。`lsof +D` 对四个精确根均为无输出/exit 1，表示当时未发现打开句柄。

| 目标 | 规范化绝对路径 | 根身份 | 条目 | 已分配字节 | 内容哨兵 |
| --- | --- | --- | ---: | ---: | --- |
| M1 D460D7 stage | `[M1-HOME]/F1-1-website/.rss-release-TASK-20260812-D460D7` | directory, UID 501 `chanai`, mode 0700, inode 24578627, nlink 13 | 188 | 3,379,200 | `release.tar.gz` SHA `da03e816…7963`, size 626012；stage manifest SHA `34c1032c…c8d95` |
| 本机 D460D7 tar | `/private/var/folders/dz/l8yd2gqj22z1xgrcl1z_wbkr0000gn/T/TASK-20260812-D460D7-release.1qPEeZ` | directory, UID 501 `hoyin`, mode 0700, inode 5136356, nlink 3 | 2 | 626,688 | `rss-second-release.tar.gz` SHA `da03e816…7963`, size 626012 |
| M1 772F48 Trash | `[M1-HOME]/.Trash/TASK-20260812-772F48-remote-release-stage` | directory, UID 501 `chanai`, mode 0700, inode 24570073, nlink 6 | 204 | 5,591,040 | `TASK-20260812-772F48-release.tar` SHA `5eaf3882…d2d4a`, size 2627072 |
| 本机 772F48 Trash | `[M5-HOME]/.Trash/TASK-20260812-772F48-local-release-stage.36zkHe` | directory, UID 501 `hoyin`, mode 0700, inode 5112664, nlink 4 | 180 | 5,259,264 | `TASK-20260812-772F48-release.tar` SHA `5eaf3882…d2d4a`, size 2627072 |

四个根的删除前已分配空间合计为 **14,856,192 字节（约 14.17 MiB）**。四个根都没有目标自身 symlink；递归检查也未输出 symlink 或非预期 owner 条目。

## 3. 实际删除时间线

1. 精确删除 M1 `[M1-HOME]/F1-1-website/.rss-release-TASK-20260812-D460D7`：exit 0。
2. 精确删除 M1 `[M1-HOME]/.Trash/TASK-20260812-772F48-remote-release-stage`：exit 0。
3. 精确删除本机 `/var/folders/dz/l8yd2gqj22z1xgrcl1z_wbkr0000gn/T/TASK-20260812-D460D7-release.1qPEeZ`：执行工具在创建进程前拒绝，原始错误分类为 `rm -f style commands are not permitted. Use a safer approach`。
4. 按任务首错纪律停止；未尝试其他删除方式，未执行本机 Trash 的删除。

后置只读确认的当前状态：

- M1 D460D7 stage：不存在。
- M1 772F48 Trash：不存在。
- 本机 D460D7 tar 根：仍存在。
- 本机 772F48 Trash：仍存在。

因此，已永久删除的 M1 两根按删除前 `du -sk` 合计释放 **8,970,240 已分配字节（约 8.55 MiB）**，无法通过本任务恢复。本机仍保留 **5,885,952 已分配字节（约 5.61 MiB）**，未释放。本报告没有用全盘剩余空间变化反推删除量，避免把并发系统活动误认为本任务释放。

## 4. 明确保留项与最小健康复核

最新回退备份 `[M1-HOME]/F1-1-website/.rss-release-TASK-20260812-325056-backup` 仍为 UID 501/mode 0700 的真实目录：

- 旧 transport SHA-256：`747c3fa238dab55bb9eb247b3337ae2b8e7918e45d68a9d9033fcaa62064cd09`。
- 旧 release manifest SHA-256：`f9765974eb3bc1be3512ea202781efb300b14d3a20f4f56cbb92cf206fbe9e96`。

正式 M1 运行项仍在，关键身份如下：

- private DB：UID 501/mode 0600/inode 24570709/nlink 1/size 86016；删除前 SHA-256 `82b3d34b…2242`。
- 当前 release manifest SHA-256：`34c1032c…c8d95`。
- deployment manifest SHA-256：`1f681429…ad50`。
- RSS plist SHA-256：`60cd3b0d…787e`。
- collector stdout/stderr：均 UID 501/mode 0600/单链接，文件仍在。
- 公开 DB：SHA-256 `949c78d5…bb50`，UID 501/mode 0600/inode 24546198/nlink 1/size 737280，与固定基线一致。

后置最小健康复核：

- source：`motorsport-f1-news | enabled=1 | stop_epoch=4 | reason=OK`。
- `com.f1plus1.rss-collector`：loaded，周期空闲态 `state=not running`，`runs=2`，`last exit=0`，`run interval=900 seconds`。
- `com.f1plus1.public-beta`：running，PID 52089。
- `com.f1plus1.quick-tunnel`：running，PID 53446，保留既有 last exit 1。
- `com.f1plus1.receipt-refresh`：loaded/周期空闲，last exit 0。
- local home/local detail/public home/public detail：全部 HTTP 200。

## 5. 未验证、不可恢复性与错题自检

### 未验证

- 本机两个剩余路径尚未永久删除；四路径全部不存在的验收出口未达成。
- 未尝试任何替代删除机制；需由统筹部重新裁定是否以新任务使用执行工具允许的精确、不跟随 symlink 的清理实现。
- 已删除的 M1 两根是永久删除，没有 Trash 或任务内回收站；只有正式保留的 325056 最新回退备份可用于当前 release 回退，它不等于已删除历史 stage 的逐字恢复。

### 错题自检

- 删除前已逐项固定规范化路径、owner、类型、symlink、跨 owner、哨兵 SHA/大小和打开句柄；没有 glob、没有清空整个 Trash。
- 删除首错后没有尝试变更命令、重试、重启、重载或修复。
- 删除前有两个只读核对命令本身出错：首先假定 325056 backup 内还带原始子目录，实际为扁平两文件；随后一条 SQLite `WHERE` 引号在 SSH shell 中丢失。两次都在写入/删除前停止，没有改文件、DB 或服务；改用实际扁平路径与无 `WHERE` 的单 source 只读查询后才继续。
- 没有把两个 M1 成功删除扩大为整个任务完成；本报告和任务状态都明确保留本机两路径未清理的事实。

TASK_BLOCKED: LOCAL_DELETION_TOOL_POLICY_REJECTED_AFTER_REMOTE_PARTIAL_CLEANUP
