---
type: work_report
department: 开发部
target: "TASK-20260812-49B22E / 本机两项旧 RSS 残留精确删除"
status: final
date: 2026-08-12
related_task: TASK-20260812-49B22E
decision: pass
tags: [RSS-REAL-001, local-cleanup, exact-path, depth-first-delete]
summary: "两个本机精确目标通过路径、owner、symlink 和打开句柄门后，使用两条固定绝对路径的 depth-first 不跟随删除命令分别成功永久删除；回读均 absent，按删除前 du 记录释放 5,885,952 个已分配字节。"
---

# 49B22E 本机两项旧 RSS 残留精确永久删除报告

## 1. 结论

**PASS。** 前任 `TASK-20260812-2B6D52` 因本机 `rm` 执行工具策略在进程创建前拒绝而保留的两个精确路径，已分别使用任务授权的 depth-first、默认不跟随符号链接删除方式永久删除。两条删除命令均 exit 0，后置精确回读均为 absent。

本任务没有运行 RSS、页面、DB 或服务测试，没有连接 M1，没有触碰 325056 最新回退备份、项目工作区内容或 Trash 中其他项目。

## 2. 删除前精确身份门

| 目标 | 规范化绝对路径 | 根身份 | 条目 | symlink/异主 | 已分配字节 |
| --- | --- | --- | ---: | --- | ---: |
| D460D7 本机 tar 根 | `/private/var/folders/dz/l8yd2gqj22z1xgrcl1z_wbkr0000gn/T/TASK-20260812-D460D7-release.1qPEeZ` | directory, UID 501 `hoyin`, mode 0700, inode 5136356, nlink 3 | 2 | 0 / 0 | 626,688 |
| 772F48 本机 Trash | `[M5-HOME]/.Trash/TASK-20260812-772F48-local-release-stage.36zkHe` | directory, UID 501 `hoyin`, mode 0700, inode 5112664, nlink 4 | 180 | 0 / 0 | 5,259,264 |

两根都与 2B6D52 的身份和大小记录一致。删除前串行执行 `lsof +D` 时，两根均无输出/exit 1，未发现打开句柄。

## 3. 唯一删除执行

只执行了下列两条删除，没有 glob、`rm`、宽路径、整个 Trash 或其他备用删除机制：

1. `/usr/bin/find -x /var/folders/dz/l8yd2gqj22z1xgrcl1z_wbkr0000gn/T/TASK-20260812-D460D7-release.1qPEeZ -depth -delete`：exit 0。
2. `/usr/bin/find -x [M5-HOME]/.Trash/TASK-20260812-772F48-local-release-stage.36zkHe -depth -delete`：exit 0。

`find` 使用精确固定根、`-x` 限制同一文件系统、`-depth` 先删子项后删目录，且未使用 `-L`；删除前还已确认根本身与递归内容均没有 symlink。

后置只回读两个精确路径：

- D460D7 本机 tar 根：`absent`。
- 772F48 本机 Trash：`absent`。

按删除前 `du -sk` 的 1024 字节块记录，本任务删除的已分配空间精确合计为 **5,885,952 字节（约 5.61 MiB）**。删除为永久删除，本任务不提供恢复方式。

## 4. 边界与错题自检

- 只写入两个任务指定的本机绝对路径；未运行 SSH，未触碰 M1、325056 回退备份、工作区业务文件、其他 Trash 内容、RSS、DB 或服务。
- 删除命令均首次成功，没有重试或替代机制。
- 首次执行门检时，并行的 `lsof` 曾短暂观察到同一门检的只读 `find` 进程持有目录句柄。该扫描进程完成后，在任何删除前串行复核两根，均无打开句柄。没有在存在句柄的观察瞬间执行删除。
- 没有运行冗余服务测试；前任 2B6D52 已在 M1 清理后完成 RSS 与公开面最小健康复核，本任务的写目标与 M1 物理分离。
- 未用全盘剩余容量反推删除量；字节数来自删除前两个精确根的 `du -sk` 已分配空间。

TASK_STATE_OK
