# SOURCE-MGMT-001 纯后端纵切实现首错阻断报告

- 任务：`TASK-20260809-BDBD33`
- 部门：开发部
- 日期：2026-08-09
- 状态：`blocked`
- 决策：唯一最终验收批次在 `typecheck` 首个结构性错误处非零退出；依任务失败路径停止，未继续 build 或 loopback HTTP。

## 1. 固定输入

| 输入 | SHA-256 | 结果 |
|---|---|---|
| SOURCE-MGMT-001 v0.3 | `90ee4ed30d325b7b2833582cc0ac8134aefc7fbc2dcd43ec9d20c0f726b2f1fe` | MATCH |
| 后端先行裁决 A | `8dbcf669b7bb0e6d67fb0b1dd395b6c52a7a6cd5ad9ecd5fabf319389bcfc77f` | MATCH |
| 数据合同 | `4498eea95e8d2461bd3c4bbb1e3ff67de2247a65c0da99951a194b2b9e9d3d95` | MATCH |
| 安全 PASS 报告 | `495bcf8a670cf275c88a67056370e62ec238fc5b38e3a3165dfbb82f3c8ebc6d` | MATCH |
| M3 baseline 文件 | `d4da9fc24c792c0471bcd24c525a46dcef1e521b36a870fd111e7310243888b2` | MATCH |
| `app/package-lock.json` | `89807e33818cfb851b57da65a5675e413e5593d01d6773c3a71a6cc6ea3d85d3` | MATCH；未修改 |

## 2. 已形成的候选产出

候选已经落下以下纯后端能力，但因验收首错不得宣称完成：

- 独立 `source-management-synthetic` profile、canonical SQLite 路径、显式三段 migration selector、migration ledger、schema fingerprint、profile ledger、59 行 baseline 只读引用和 local overlay。
- 单进程 profile lock、单 SQLite handle、单 writer transaction lane、closed-receipt 生成逻辑。
- command/business/job/task/live-lease 身份域、Source ID 全 hash 与 96-bit 前缀处理。
- raw peer/request-target/Host/Origin/proxy/no-egress 前门；单 ACTIVE 内存 session；一次性、method/path/body-hash 绑定 CSRF。
- 显式 Admin API：session create/get/refresh/destroy、CSRF、sources list/add/get、validate/activate/stop/retire/requeue、operation receipt。
- `source_activation` 唯一 Outbox 类型、TaskAttempt、Inbox、dead letter、requeue、CAS 与 five-fence worker。
- 后端聚焦测试和本地 worker CLI。

主要新增/修改范围：

- `app/migrations/profiles/source-management-synthetic/0003_source_management_runtime.sql`
- `app/src/server/db/source-management-synthetic.ts`
- `app/src/server/source-management/**`
- `app/src/app/api/admin/**`
- `app/src/tests/source-management-backend.test.ts`
- `app/scripts/source-management-worker.ts`
- profile/env/health/migrate/seed/serve/closed-receipt 接线文件与 `app/package.json`

边界核对：没有创建 `/admin/sources` 页面、浏览器客户端或 CSS；未修改根 layout、globals、公开 Repository/API、design、data、Spec、ADR 或 package-lock；未新增依赖；未访问真实 Base/provider/AI/媒体/发布或外网。

## 3. 唯一最终验收批次

固定工具链：Node `24.18.0`、npm `11.16.0`；`env -i` 只传最小系统键和 canonical 本地 synthetic 配置；`externalCalls=0`。

按序结果：

1. `db:migrate`：exit 0；应用 `0001`、`0002`、scoped `0003`；`user_version=3`；SQLite `3.53.1`；WAL、FULL、busy timeout 250、FK 均就绪。
2. `seed:fixtures`：exit 0；`baselineRows=59`、`localRows=0`、`inserted=true`；effective root=`e137661fc1141981288467007f3f821d7ed1ce498080b3ff4f5492360de897d9`。
3. `runtime:assert-ready`：exit 0；`ready/local-only`。
4. `test:source-management`：exit 0；1 个测试文件、5/5 PASS。覆盖 profile/pin、baseline mutation 403、add/validate/activate/worker/stop/resume/retire/requeue、幂等/response-loss、stale、dead-letter、identity collision、raw/session/CSRF/no-egress。
5. `lint`：exit 0；0 errors，3 个既有公开 UI `<img>` warning，与本任务文件无关。
6. `typecheck`：exit 2；停止。
7. `build`：`NOT_RUN`，因为前一步非零。
8. loopback HTTP：`NOT_RUN`，因为候选未通过静态类型门。

## 4. 首个阻断

首错：

```text
app/src/server/db/source-management-synthetic.ts(145,102): error TS2769:
No overload matches this call.
Argument of type 'unknown' is not assignable to parameter of type 'SQLInputValue'.
```

同一根因还出现在 source-management DB/repository 的多处 Node 24 `node:sqlite` 参数绑定，以及 `http.ts` 的 response header 联合类型收窄。运行期聚焦用例通过不能替代 TypeScript 门；这些错误属于当前候选的结构性 P1，因此本任务不能 complete。

恢复门槛：后继或恢复任务只需在不改变运行语义、合同、依赖和公开字节的前提下，把所有 SQLite bind 值显式收敛到 `SQLInputValue`，并修正 header narrowing；随后重新执行任务授权的一次完整验收批次。不得通过 `any`、禁用 typecheck、放宽 tsconfig 或跳过 build 关闭本阻断。

## 5. 收口与零漂移

- canonical source-management DB 已生成：`app/.local/f1plus1-source-management-synthetic.sqlite`，权限 `0600`，大小 `217088` bytes。
- 未生成 closed receipt，因为完整链在 typecheck 前停止；不能把 DB 写成 closed/canonical PASS。
- 无任务 server、端口或 lock 残留；未启动 loopback 实例。
- 现场未见该 DB 的 WAL/SHM sidecar。
- 保护文件复核：package-lock=`89807e…85d3`；根 layout=`d7f0d2…99aa`；globals=`9aee74…c191`；public Repository=`c33772…01eb`；public HTTP=`348b6f…8a0`。

## 6. 已验证、未验证与错题自检

已验证：固定输入、profile migrate/seed/readiness、59+0、聚焦 server golden、lint、no-egress 测试边界、保护文件当前 hash。

未验证：typecheck PASS、build、真实 raw HTTP adapter、一次 loopback session/CSRF/API、closed receipt、第二实例/端口完整验收、独立测试部与安全部复验。UI 按产品裁决保持关闭，不能宣称 SOURCE-MGMT-001 整体完成。

错题自检：开发期使用 Node strip-types 的运行测试先暴露 SQL/状态错误并已修正，但没有在唯一最终批次前发现 `node:sqlite` 的静态 bind 类型问题。最终批次严格在首错停止，没有补跑、重复 build、启动服务或把 5/5 运行测试外推成完整 PASS。
