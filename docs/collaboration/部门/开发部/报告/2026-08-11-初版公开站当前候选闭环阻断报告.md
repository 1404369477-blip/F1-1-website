# 初版公开站当前候选闭环阻断报告

- 任务：`TASK-20260811-0F65C1`
- 部门：开发部
- 日期：2026-08-11
- 结论：`BLOCKED`
- 首错：`RECEIPT_STALE`
- 风险结论：源码静态对抗复核 `P0=0 / P1=0`；最终运行门存在 `P1=1`
- 机器清单：[manifest.json](../../../../../app/evidence/TASK-20260811-0F65C1/manifest.json)

## 1. 结论先行

当前公开核心候选已经关闭两项实现缺口：默认本地运行 profile 已切到 ACK 的 24 条 `public-multimedia-synthetic`，且 feed 根状态、inline detail、独立详情的重试焦点均在成功、再次错误和 404 终态有稳定恢复位置。只读对抗复核对最新源码给出 `P0=0 / P1=0`。

唯一最终验证批次在聚焦测试的第一个失败处停止。`public-ui.test.ts` 为 `14/14 PASS`；`public-multimedia-backend.test.ts` 为 `4/5 PASS`。失败来自旧 closed receipt 超出运行时接受的 24 小时 freshness 窗口：

```text
ConfigError: RECEIPT_STALE: closed receipt is outside the accepted freshness window
at app/src/server/db/public-multimedia-synthetic.ts:326
```

该门禁是现行 integrity 合同的一部分。按照任务失败路径，本任务没有刷新、伪造或放宽 receipt，没有追加重跑，也没有继续 lint、typecheck、build 或 production HTTP。因此当前候选不能交付为“可运行冻结”，任务必须如实 `block`。

## 2. 冻结输入与候选身份

| 项目 | SHA-256 / 身份 |
| --- | --- |
| 冻结设计 HTML | `5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1` |
| 最终产品合同 | `b07a07f80b9857c2172da3334a97673570e2344fa2f6fba72497e3dcbe9da79c` |
| 初版 Function 矩阵当前文件 | `66a57c58bdae2cf045f58c89285ad72316787ddcfb35a8293ae9efb2be854882` |
| runtime graph | `1eddfe54394757ff1cf00dce12ec2409772817256fe40177bc04e5a54989608b` |
| data manifest | `38d51764568cc9d3943e0c31388bf125c1f3b207cde0fc2f37db22becfbed6eb` |
| canonical SQLite（前/后） | `eb2d7ad2787a290f7a13adcb063215d58654bc9f66d1d8ff60b98f14592b9551` / 相同 |
| package lock | `89807e33818cfb851b57da65a5675e413e5593d01d6773c3a71a6cc6ea3d85d3` |
| 当前 source/data 聚合 | `9c0a46991000d393c1c7960a95535d44f9353512162096f4638b5c4b5c364c7c` |

F67080 报告中的旧 11 文件 aggregate 为 `7b1e8977…65281`。按原文件顺序复算当前候选得到 `ccf74c90…352f`；漂移来自后续后端演进和本任务精确焦点修复，旧 aggregate 没有被冒充为当前身份。完整路径顺序和逐项 hash 见机器清单。

当前本地运行三项为：

- `F1_DATA_PROFILE=public-multimedia-synthetic`
- `F1_DB_PATH=.local/f1plus1-public-multimedia-synthetic.sqlite`
- `SOURCE_FIXTURE_PATH=../data/mvp-contract-v0.6-public-multimedia-pagination-synthetic/runtime-graph.public-multimedia-pagination-synthetic.json`

前端仍只发送同源 `Accept: application/json`，以 strict `public-read-v0.1` 解析并映射为 `0/1` 张本地 synthetic data-URI；没有接入 V2 媒体数组，也没有把 `MEDIA-NAV-003`、`MEDIA-LIGHTBOX-004` 写成完成。

## 3. 本任务精确改动

| 文件 | 改动 | 当前 SHA-256 |
| --- | --- | --- |
| `app/.env` | 从旧 12 条 profile 切到已 ACK 的 24 条 canonical profile；文件为未跟踪本地运行配置，不含 secret | `411a5809…e386` |
| `app/src/features/stories/feed-experience.tsx` | feed 根 error/404/success，以及 inline detail error/404/success 的重试焦点恢复；用户中途关闭时清除待恢复身份 | `0c669d2d…1135` |
| `app/src/features/stories/story-detail-experience.tsx` | 独立详情成功、错误、404 重试终态的焦点恢复 | `85234bb7…517` |
| `app/src/tests/public-ui.test.ts` | 增加上述稳定恢复路径的聚焦合同断言 | `3f75db61…142` |

未改 package/lock、Spec、accepted ADR、design、data、Repository/API 语义、Admin、真实 provider/Base、发布或部署；未执行 Git stage/commit/reset/clean。

## 4. Function ID 状态

以下结论严格区分“实现已落盘”“本轮有效运行验证”和“后继独立浏览器/视觉验证”。本任务因 freshness 首错 blocked，任何一项均未外推为最终核收 PASS。

| Function ID | 正式路由 / 用户操作 | 正常、失败与恢复实现 | 本轮证据状态 |
| --- | --- | --- | --- |
| `PUB-FEED-003` | `/`；全部与四类筛选按钮 | 切 scope 取消旧请求、清展开和分页状态；同源 V1 `contentType` 请求 | 源码与 public UI 聚焦测试 PASS；production HTTP NOT_RUN |
| `PUB-FEED-004` | `/`；loading/empty/error/404/nomore/partial/offline | 每态正式动作；离线/失败保留已加载内容；再次失败聚焦新动作按钮 | 实现与聚焦断言 PASS；真实七态浏览器 NOT_RUN |
| `PUB-FEED-005` | `/`；加载更多 | canonical 24 条 profile；12+12、拒绝重复/游标漂移；partial 保留第一页并重试同 cursor | profile 已切；后端相关测试 4/5，receipt freshness 首错；HTTP NOT_RUN |
| `PUB-DETAIL-001` | `/stories/[publicId]` | 标题、中文详情、关键点、来源、最多 3 条相关内容、返回 feed | 适配器聚焦测试 PASS；production route NOT_RUN |
| `PUB-DETAIL-004` | `/` 内联展开 | 404 与通用错误分离；重试同 ID；时间线保留；终态焦点回同条摘要/重试 | 实现与静态对抗复核 PASS；浏览器 NOT_RUN |
| `PUB-DETAIL-005` | `/stories/[publicId]` | 404/错误分离；重试同 URL；成功回 main，失败回当前重试按钮 | 实现与静态对抗复核 PASS；HTTP/浏览器 NOT_RUN |
| `UI-THEME-002` | 页面工具主题按钮 | SSR cookie + `useSyncExternalStore`；localStorage getter/setter 异常闭合并保守回退 | public UI 聚焦测试 PASS；刷新/重开实机 NOT_RUN |
| `UI-RESP-002` | `≤1100` Dock 与页面底部 | `viewport-fit=cover`、safe-area、末条底部空间、blur fallback | CSS 合同断言 PASS；实体 safe-area/六格 NOT_RUN |
| `UI-TOP-001` | 页面工具“回到顶部” | 鼠标/键盘；reduced-motion；完成后焦点回 `#main-content` | 源码合同断言 PASS；浏览器 NOT_RUN |
| `UI-ACCORDION-001` | feed 摘要按钮 | 单一展开、外点关闭、整条居中、reduced-motion | 源码合同断言 PASS；浏览器几何 NOT_RUN |
| `UI-RUNTIME-001` | production `/` 与 `/stories/[publicId]` | SSR snapshot 与同源 API 边界已实现 | build/start/console 因首错 NOT_RUN |
| `UI-A11Y-001` | 全公开页 | 44px、键盘、焦点恢复、forced-colors、200%/reduced-* CSS 路径 | 聚焦合同与对抗复核 PASS；AT/200%/实体运行 NOT_RUN |

## 5. 唯一最终批次收据

固定运行时为项目内 Node `24.18.0`，clean-room 环境只保留 `HOME`、精确 toolchain `PATH`、`TMPDIR=/tmp`、`LANG=C`、`LC_ALL=C`、`NEXT_TELEMETRY_DISABLED=1`。

执行顺序为：

1. 聚焦 Vitest：`public-ui.test.ts` + `public-multimedia-backend.test.ts`；
2. lint；
3. typecheck；
4. production build；
5. 任务专用 `127.0.0.1:3000` production HTTP。

第 1 步 exit `1`，结果为 `18/19 PASS`：

- `public-ui.test.ts`：`14/14 PASS`；
- `public-multimedia-backend.test.ts`：`4/5 PASS`；
- 唯一失败：`keeps the legacy closed bytes frozen and selects only the exact three migrations`；
- 首错：`RECEIPT_STALE`；
- lint/typecheck/build/HTTP：`NOT_RUN`；
- App external calls：`NOT_RUN`；
- 已运行的聚焦测试路径 external calls：`0`；
- canonical DB 前后 SHA 相同，未写入或漂移。

旧 receipts 的现场 mtime 为：M3/public-synthetic/data receipts 均 `2026-08-09T16:28:04+0800`，当前任务运行于 2026-08-11，确已超过代码固定的 24 小时窗口。该信息只记录键名、时间、大小和权限，没有输出 receipt 内部载荷。

## 6. 阻断、恢复门槛与未验证项

阻断项只有一个：由有权任务重新生成并独立核对 M3 与 public-synthetic closed receipts，证明旧 canonical DB、schema/ledger/counts/root 与数据 manifest 仍精确闭合，且 freshness 进入现行 24 小时窗口。新 receipt 必须由既有安全工具生成，禁止手写、改时间、放宽 freshness 或跳过 legacy gate。

恢复后本任务或精确 successor 应从未运行阶段继续，避免重复已记录的首轮失败：

- 固定 Node24 lint、typecheck、build；
- 单次 `127.0.0.1` production start 与真实 API 12+12、四类、detail、404、no-store、health/externalCalls=0；
- 同候选 source/data/build hash 前后零漂移；
- 停止任务进程并确认 3000/3001 无残留。

按任务原文，以下继续由独立测试/设计 successor 完成，开发不得自证放行：1440/1024/390 × 深浅六格、真实七态与 partial、浏览器 hydration/console、主题刷新/重开与 Storage 异常、回顶/手风琴几何和焦点、实体 safe-area、forced-colors、200%、reduced-motion/reduced-transparency 与读屏。

真实 provider/Base、外部 I/O、Admin、发布、部署、V2 多图前端和 integrity 视觉 successor 继续关闭。

## 7. 错题自检

- 已承认 F67080 旧 11 文件 aggregate 漂移，没有把旧 hash 当作当前候选。
- 已在运行前完成一次只读对抗审查；两项实现 P1 修复后复核为 `P0=0 / P1=0`。
- 聚焦测试出现首错后立即停止，没有为了完成任务刷新 receipt、降低 integrity、追加测试或继续 build/HTTP。
- 没有把测试构造响应、静态分支、未运行浏览器或历史六格证据计作当前 Function PASS。
- 没有吸收尚未视觉确认的多图 successor；V1 前端仍保持 0/1 图。
- 没有把 App external calls 写成 `0`；production 未启动，故如实为 `NOT_RUN`。
- canonical SQLite、data、package lock 未漂移；工作区既有 dirty/untracked 状态未被清理或重置。

## 8. 四件套

- 产物：本报告、机器 manifest、三处焦点/profile 精确修订。
- 已验证：冻结设计 SHA、当前合同/矩阵身份、v0.6 graph/manifest/DB/lock hash、public UI `14/14`、其余 backend `4/5`、源码对抗复核 `P0=0/P1=0`、`git diff --check`。
- 未验证：lint、typecheck、build、production start/HTTP、App external calls、独立浏览器与视觉/AT 出口。
- 状态：`BLOCKED`；任务状态工具落账后回传字面 `TASK_STATE_OK`，该字面只证明状态持久化，不表示验收通过。
