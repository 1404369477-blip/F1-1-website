---
type: development_audit_report
status: final
date: 2026-08-08
department: 开发部
task_id: TASK-20260808-9EE352
domain_stage: git-baseline-readonly-audit
decision: conditional_baseline_ready
summary: 当前仓库不能使用全量暂存；五个目标文件中两项可由正式任务链归属，三项属于验收后且尚未获得实现合同的改动；已形成分层 Git 白名单、排除清单、迁移冲突清单和脱敏敏感文件检查结果。
---

# TASK-20260808-9EE352｜前端验收后改动溯源与安全 Git 基线报告

## 0. 结论

本次只读审计结论为 **CONDITIONAL READY**：可以按本报告的分层白名单准备 Git 基线，但当前不得执行 `git add .`、`git add -A`、`git clean` 或目录级删除。

核心原因：

- 当前 `HEAD=a9691e71b1552592cc5ded8d5db66c336262301c`，Git 历史只有两次地基提交，当前正式 app、任务 JSON、报告、数据合同与设计产物大多尚未进入历史。
- 当前工作区有 **11 个已跟踪修改、765 个未跟踪文件**；五个目标前端文件均为 `??`，现有 Git 历史和索引不能提供逐字节基线。
- 测试部 `TASK-20260807-4451C2` 与安全部 `TASK-20260807-F01A13` 于 `2026-08-07 20:20+08:00` ACK。`public-ui.test.ts`、`globals.css`、`feed-experience.tsx` 当前修改时间为 23:01–23:03，且内容对应后来停在用户体验确认门的 v0.2b 媒体方向；缺少开发实现任务、最终合同和重新测试/安全收据。
- 8 月 8 日最终 Demo 已获用户确认，但冻结任务 `TASK-20260808-54BB47` 当前仍为 `claimed`，产品合同同步任务 `TASK-20260808-00A9C9` 因冻结收据未完成而 `blocked`。当前 app 仍是早期时间线快照，不应把设计 Demo 自动视为已落地 app。
- `migration/CURRENT-HANDOFF.md` 明确记录 `migration/bundles/`、`manifests/`、`portable-assets/`、`scripts/` 已删除；这些路径当前重新出现并占约 240 MiB。它们与正式迁移证据重叠，来源无法从 Git 归属，必须保留并交统筹裁决，不能自动清理或纳入首个基线。

本任务未修改 `app/`、Spec、ADR、data、design 或其他项目文件；未删除、暂存、提交、重置、回滚；未运行测试或构建。

## 1. 证据边界

使用的证据仅包括：

1. 当前工作区的文件内容、元数据、SHA-256 与忽略状态；
2. 当前 Git `HEAD`、两次现有提交、索引和工作区状态；
3. `TASK-*.json` 任务状态与授权链；
4. 开发、测试、安全、设计正式报告；
5. `migration/CURRENT-HANDOFF.md`、`SECURITY-EXCLUSIONS.md` 等迁移正式材料。

未使用文件修改时间单独证明作者身份；修改时间只用于识别“正式验收前后”的时序差异。没有 Git blob 或正式冻结 hash 的文件，均不宣称逐字节归属。

## 2. 五个前端文件溯源

| 文件 | 当前 SHA-256 | 当前状态 | 证据与最终设计关系 | 分类与动作 |
| --- | --- | --- | --- | --- |
| `app/src/features/stories/public-api.ts` | `a8baccbaa3cf95b997113d0cb00db1ab902c230759959d454d268d9e2bece033` | 未跟踪；mtime `2026-08-07 20:54:15+08:00` | `TASK-20260807-6CD2FB` 开发报告明确记录新增 `publishedAtIso`/`originalUrl` 派生字段；测试、安全报告均审查该闭集 DTO 适配器。当前 hash 未在报告冻结。 | **保留 / 可纳入 app 基线**。任务级归属明确，逐字节归属未验证；纳入前由统筹保存本表 hash。 |
| `app/src/components/f1/f1-page-shell.tsx` | `4237c0ac004858831e14ff669e17eb1bf1560e2b3a96dc18b218ba459b2f21ae` | 未跟踪；mtime `2026-08-07 20:55:58+08:00` | 6CD2FB 报告明确记录极简顶栏、首页搜索与主题切换；测试报告包含顶栏/主题/搜索验收。最终 Demo 后续增加 Dock/设置等尚未落 app。 | **保留 / 可纳入 app 基线**。该文件是已验收早期快照；未来最终设计落地另开任务修改。 |
| `app/src/tests/public-ui.test.ts` | `914deb43d44fef88640d5f74373fa621aa0a66cf74151ab7a3cc9ba563a64074` | 未跟踪；mtime `2026-08-07 23:01:14+08:00` | 6CD2FB 的测试文件有正式归属，但当前文件在测试/安全 ACK 后变化；缺少当前 hash 的测试报告或开发任务。 | **待合同 / 保留 / 首个基线排除**。先由后继开发任务按最终合同重写或确认，再交独立测试。不得删除或回滚。 |
| `app/src/app/globals.css` | `77e94dff98bef04837436e51be0df9f77f97eabdb0307159c5258b6b96c99018` | 未跟踪；mtime `2026-08-07 23:01:40+08:00` | 当前媒体样式为右侧 80px/56px 竖向缩略图、主图 320px/220px；v0.2b 设计报告明确把它当作“当前 app”并指出数值问题。该方向随后被 8 月 8 日最终 Demo 的单主图/缩略图/移动/Dock 方案取代。 | **待合同 / 保留 / 首个基线排除**。与最终设计不一致，不具备正式 app 落地收据。 |
| `app/src/features/stories/feed-experience.tsx` | `eb84d0b399fdb00be0394e704d144dbbe5847f10a7ea8e9f7ef37c0329155b79` | 未跟踪；mtime `2026-08-07 23:03:08+08:00` | 当前文件含 pointer 48px lightbox 滑动与右侧竖向缩略图；v0.2b 设计报告称这些是当时 app 的隐式实现，同时明确“未进入正式实现”。8 月 8 日最终方向进一步改变媒体、原文、移动布局与 Dock，产品合同仍待冻结。 | **待合同 / 保留 / 首个基线排除**。可能包含有价值的交互探索，来源无法完整归属；不得删除、回滚或宣称已验收。 |

### 2.1 解释限制

- `public-api.ts` 与 `f1-page-shell.tsx` 的“可纳入”只表示当前内容与正式报告描述一致，不能替代缺失的验收 blob hash。
- 后三项中既有 6CD2FB 的正式实现，也混入验收后修改；现有 Git 历史无法做安全拆分。完整文件按最保守分类保留并暂缓纳入。
- 最终 Demo 当前 `index.html` 与 `F1+1-v0.2-final-20260808.html` SHA-256 均为 `5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1`。该事实证明两个文件当前相同，不证明冻结任务已经完成。

## 3. Git 基线清单

精确、可执行的命令清单见同目录的 `2026-08-08-TASK-20260808-9EE352-Git基线白名单.md`；逐文件路径、当前 SHA-256、来源类别、敏感性判断与生成物类别见 `2026-08-08-TASK-20260808-9EE352-Git基线路径清单.tsv`。基线应拆分，禁止一次性全纳入：

1. **协议与正式证据层**：现有协作协议、任务 JSON、正式报告；排除 `.locks/`。
2. **已验收 app 层**：app 配置、lockfile、迁移、scripts、server/public 等正式实现；本轮明确排除三个待合同文件。
3. **数据合同层**：`data/` 的冻结合同与 synthetic fixture；先复核生成器和 manifest 的正式报告指针。
4. **迁移核心证据层**：只纳入当前 handoff 明确保留的根说明和 `migration/conversations/`；重新出现的四组旧归档路径不纳入。
5. **最终设计层**：只在 `TASK-20260808-54BB47` 完成、最终 hash/报告登记后纳入不可变 HTML、六张 final PNG、清单与 final 报告。中间版证据先保留，等待仓库大文件策略。
6. **最终 app 落地层**：待产品合同同步、开发实现及测试/安全复验后，再纳入后三个前端文件的新冻结版本。

任何执行者都应先确认：

```sh
test "$(git rev-parse HEAD)" = "a9691e71b1552592cc5ded8d5db66c336262301c"
test -z "$(git diff --cached --name-only)"
```

若任一条件失败，应停止并重新生成清单。

## 4. 明确排除与保留

### 4.1 永不进入 Git

- `app/.env`
- `app/.local/`
- `app/node_modules/`
- `app/.next/`
- `app/tsconfig.tsbuildinfo`
- `app/~/.npm/_logs/` 与 `app/~/.npm/_update-notifier-last-checked`
- `app/Library/Preferences/nextjs-nodejs/config.json`
- `.obsidian/plugins/obsidian-local-rest-api/data.json`
- `.codex/`、`.obsidian/` 的本机状态与插件副本
- `docs/collaboration/.locks/`
- `scratch/` 中脚本、截图和临时验收矩阵

### 4.2 自动清理候选（仅供后续独立清理任务）

下列路径有正式安全报告支持，且当前均为 0B 空目录或本机日志/telemetry 残留。**本任务没有删除它们**：

- `app/--disable-gpu/`
- `app/--full-page/`
- `app/--headless=new/`
- `app/--hide-scrollbars/`
- `app/--screenshot=/`
- `app/--user-data-dir=/`
- `app/--window-size=1024,1600/`
- `app/--window-size=1440,2000/`
- `app/-la/`
- `app/http:/`
- `app/ls/`
- `app/Library/`
- `app/~/`
- `app/.next/`
- `app/tsconfig.tsbuildinfo`

执行清理前仍需新任务重新核对路径类型、大小、符号链接状态，并逐个删除；不得使用宽泛 glob 或递归清理 app 根目录。

### 4.3 归属未知，保留并等待统筹裁决

- `migration/bundles/`（约 236 MiB，含两份 tar.gz；hash 与 handoff 所列历史 hash 相符）
- `migration/manifests/`
- `migration/portable-assets/`
- `migration/scripts/`
- `.obsidian/plugins/` 及 `.obsidian` 其他未跟踪状态
- `design/ui/F1+1-v0.2-全站设计/evidence/` 下除 `final-20260808/` 外的多轮中间 PNG
- 当前三个待合同 app 文件中的验收后增量

迁移四组目录虽像可清理残留，但与正式迁移证据重叠，严格从自动清理列表移除。中间设计 PNG 是正式设计任务证据，不得当作本机缓存删除；是否进入 Git、LFS 或外部归档由统筹决定。

## 5. 潜在敏感文件检查

### 5.1 已确认

- `git remote -v` 无输出，当前没有 remote URL 或嵌入式 remote credential。
- `app/.env` 存在且被 `app/.gitignore` 的 `.env` 规则忽略；未输出其内容。
- `app/.local/f1plus1-public-synthetic.sqlite` 存在且被 `.local/` 忽略；未扫描或导出业务行。
- Local REST 私密路径 `.obsidian/plugins/obsidian-local-rest-api/data.json` 当前不存在，根 `.gitignore` 保留精确忽略规则。
- 常见私钥头、AWS/GitHub/OpenAI 形态扫描只输出路径。命中位于插件源码、合成 fixture、测试计划和迁移 hash 清单；没有确认到应提交的真实生产凭据。

### 5.2 需保守处理的路径

- `.obsidian/plugins/realclaudian/main.js`、`styles.css` 出现私钥/token 形态的静态源码匹配；插件来源及提交必要性未建立，分类为 **归属未知、保留、排除 Git**。
- `data/mvp-contract-v0/fixtures.synthetic.json` 与生成器出现秘密形态匹配，正式材料表明它们是 synthetic security fixture；可在数据合同专门提交中纳入，提交说明必须标注“合成测试值，不可作真实凭据”。
- `migration/manifests/warm-input-files.sha256` 出现模式命中且该目录按 handoff 应已删除；不展开内容、不纳入基线，等待统筹裁决。
- 正则扫描只能降低误提交概率，不能证明零秘密。实际暂存后仍需对 **staged blob** 做一次只输出路径的独立扫描，再允许提交。

## 6. 安全建基线步骤

1. 冻结当前 `HEAD` 与工作区清单，确认没有其他 Agent 写入。
2. 先完成设计冻结 `54BB47` 和产品合同同步 `00A9C9`；当前三个待合同 app 文件不进入早期 app baseline。
3. 按白名单分批暂存，每批只允许列出的路径；每批运行 `git diff --cached --name-status` 与 `git diff --cached --stat`，不得运行 `git add .`。
4. 对 staged blob 做敏感模式路径扫描；发现任何未知项立即从暂存移除并保留工作区文件。
5. app 源码基线必须记录 Node/npm/lock hash、正式任务与测试/安全报告指针；本任务不补跑测试。
6. 设计二进制与迁移归档分别决策：final 证据可在冻结任务完成后纳入；中间 PNG 和迁移 tar 不得混入源码基线。
7. 提交前由统筹复核 `git status --short`，确认自动清理尚未执行、未知文件仍存在。
8. 每层单独提交，提交消息写任务/报告编号，保证回滚边界清晰。

## 7. 已验证、未验证与错题自检

### 对抗审查

- 只读对抗审查结果：**P0=0、P1=6、P2=4**。
- 已吸收的 P1：Git 无字节级归属、三文件属于被取代中间方案、两文件仅合同语义相符、迁移时态冲突、目录式暂存风险、Git 排除与磁盘删除分层。
- 已吸收的 P2：精确残留候选、明确保留工具链/SQLite/依赖、敏感扫描解释、tracked 文档不能无差别纳入。`docs/roadmap.md` 等五个文档已列 `DEFER_DOC_RECONCILIATION`。

### 已验证

- HEAD、提交历史、remote、tracked/untracked 状态和五文件索引状态。
- 五文件当前 SHA-256、mtime、正式任务/报告指针和与最新设计的静态关系。
- `.gitignore`/`app/.gitignore` 对 env、依赖、构建、工具链、日志、SQLite 的覆盖。
- 迁移 handoff 所述删除边界与当前文件系统发生冲突；重新出现的 tar hash 与历史收据相符。
- 敏感扫描仅输出路径，未扩散候选值。

### 未验证

- 五文件在 20:20 验收时的逐字节 blob：正式报告未冻结该组 hash，Git 历史也没有 blob。
- 三个 23:01–23:03 文件的具体作者/会话归属：标记为归属未知并保留。
- `54BB47` 的冻结完成状态、最终 SHA 清单和 final 报告：任务仍 claimed。
- 中间设计 PNG 的长期归档介质与 Git LFS 策略。
- 重新出现的迁移四组目录由谁恢复、是否仍有唯一保留价值。
- staged blob 扫描：本任务禁止暂存，因此留给实际建基线任务执行。

### 错题自检

- 没有把文件 mtime 当作作者证明；没有把用户确认的设计 Demo 写成已实现 app。
- 没有把测试/安全 PASS 扩张到验收后修改。
- 没有将归属未知文件列入自动删除。
- 没有输出 `.env`、token、私钥或 SQLite 内容。
- 没有修改 app、Spec/ADR/data/design，没有暂存、提交、重置、回滚、删除或跑全量测试。

TASK_STATE_OK
