---
type: department_report
status: final
date: 2026-08-12
department: 开发部
task_id: TASK-20260812-D8E4A2
decision: pass
external_calls: 3
---

# 临时系统代理 bb0aa 非 force 推送报告

## 结论

冻结双 commit 已通过唯一一次普通 Git smart-HTTP 非 force push 同步到 `origin/codex/first-public-release`。代理只以命令级 `-c http.proxy=http://127.0.0.1:7890` 注入；未写入仓库或用户 Git 配置，未设置 upstream，未使用 force 或 API 写 fallback。

## Push 前门

- HEAD/tree/parent/base：`bb0aa0266b73b7b2f7af38d3f29ac0a07dee772f` / `2fdf2078daf7bb2c6327d19c6c8c81809465abf2` / `2ff1bdba83eadddf01908bfbc884d8efcd0d84d1` / `54e694c13b7369819448a2c3b072cb0fbbc49b7b`。
- `54e…bb0` 恰为两个线性 commit；index 为空；89 runtime clean/blob=HEAD。
- Manifest SHA-256：`6f4642999797aea6d220cc01ec4d9500416a37a5db7de8eaafc573991bff770d`，Git identity 与 roots 匹配。
- 本地代理 listener 存在；持久 Git proxy 配置为空。
- 仓库 `.git/config` 与用户 `.gitconfig` 的 SHA、size、mtime 在 push 前后完全相同。
- 同代理 fresh remote pre：目标 ref=`54e694c…`，main=`ee450639…`。

## 唯一 Push 收据

显式 refspec：

`bb0aa0266b73b7b2f7af38d3f29ac0a07dee772f:refs/heads/codex/first-public-release`

结果：exit `0`，Git porcelain 报告 `54e694c..bb0aa02`，`Done`。Push 次数为 1；重试、force、lease、set-upstream 与 API 写次数均为 0。

## Push 后门

- 同代理 `ls-remote`：目标 ref=`bb0aa0266b73b7b2f7af38d3f29ac0a07dee772f`；main 保持 `ee450639613ffc43e7b860b3237bdde296f0416d`。
- Commit/tree/parent：`bb0aa026…` / `2fdf2078…` / `2ff1bdba…`。
- 本地 index 为空；89 runtime 继续 clean/blob=HEAD。
- Manifest SHA 与 Git identity 零漂移。
- `.git/config` 和 `~/.gitconfig` 零字节、size、mtime 漂移；没有新增持久 proxy 项。

## 未验证边界

本任务只同步代码对象。未执行 build、builder、SSH、M1、DB、prepare、load、cutover 或部署；ignored manifest 不在 GitHub 仓库中。M1 fresh stage 仍需独立受控任务。

TASK_STATE_OK
