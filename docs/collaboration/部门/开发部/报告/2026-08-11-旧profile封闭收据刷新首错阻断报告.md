# 旧 profile 封闭收据刷新首错阻断报告

- 任务：`TASK-20260811-5F3B7D`
- 部门：开发部
- 日期：2026-08-11
- 结论：`BLOCKED`
- 首错：`CLI_INTERNAL_ERROR`
- 机器清单：[manifest.json](../../../../../app/evidence/TASK-20260811-5F3B7D/manifest.json)

## 1. 结果

本任务未能刷新三份旧 profile closed receipt。唯一受控命令链在第一条 M3 命令即返回稳定脱敏错误：

```json
{"event":"cli_failure","status":"rejected","reasonCode":"CLI_INTERNAL_ERROR","externalCalls":0}
```

进程 exit code 为 `1`。命令以 `m3-shadow && public-synthetic` 串联，因此 public-synthetic 命令调用次数为 `0`。按照任务失败路径，开发部没有重试、没有更换入口、没有探测绕过、没有修改代码或放宽 freshness，也没有手写或 touch 收据。

写后只读复核确认：三份旧 receipt 均保持原 SHA、inode、权限和字节数；两个 canonical SQLite 均保持原 SHA、inode、权限和字节数；源码、migration、data、package/config/env 保护 aggregate 与 package-lock 前后零漂移；四个 WAL/SHM sidecar 均不存在；receipt 目录没有残留 `.tmp`、`.candidate` 或 `.lock`。

因此本任务不能完成 acceptance exit，也不能解除 `TASK-20260811-0F65C1` 的 `RECEIPT_STALE` 前置。任务按首错正式 block。

## 2. 写入范围与受控入口

授权写入范围只有：

- `app/.local/receipts/m3-shadow.closed.json`
- `app/.local/receipts/public-synthetic.closed.json`
- `app/.local/receipts/public-synthetic.data.closed.json`

实际入口为既有 ACK 命令实现：项目内 Node `24.18.0` 使用 `--experimental-strip-types` 执行 `app/scripts/profile-closed-receipt.ts`，参数依次计划为 `m3-shadow` 与 `public-synthetic`。环境使用 `env -i`，只传 `HOME`、精确 toolchain `PATH`、`TMPDIR=/tmp`、`LANG=C`、`LC_ALL=C`。

任务没有启动 Web、没有连接网络、没有创建第三数据库真值，也没有运行 0F65C1 的测试、lint、typecheck、build 或 HTTP 链。

## 3. 写前/写后零漂移

### 3.1 Canonical SQLite

| 文件 | 写前 SHA-256 | 写后 SHA-256 | 身份与权限 | sidecar |
| --- | --- | --- | --- | --- |
| `app/.local/f1plus1.sqlite` | `df82598c…19c0` | `df82598c…19c0` | dev `16777230`、ino `4199350`、nlink `1`、`0600`、uid `501`、290816 bytes | WAL/SHM absent |
| `app/.local/f1plus1-public-synthetic.sqlite` | `24536392…041` | `24536392…041` | dev `16777230`、ino `3093547`、nlink `1`、`0600`、uid `501`、507904 bytes | WAL/SHM absent |

两个 DB 的 SHA、inode 和字节数均未改变。由于第一条命令失败，未生成新的 envelope，也没有把“DB 零漂移”外推成 receipt freshness PASS。

### 3.2 三份旧 receipt

| 文件 | 写前/写后 SHA-256 | 身份与权限 | 结果 |
| --- | --- | --- | --- |
| `m3-shadow.closed.json` | `4309961d…bbdc` | ino `4206134`、nlink `1`、`0600`、1414 bytes | 未刷新 |
| `public-synthetic.closed.json` | `2c5ca355…2803` | ino `4206143`、nlink `1`、`0600`、1440 bytes | 未调用、未刷新 |
| `public-synthetic.data.closed.json` | `71286412…acdc` | ino `4206146`、nlink `1`、`0600`、948 bytes | 未调用、未刷新 |

`app/.local/receipts` 继续为真实目录、uid `501`、权限 `0700`。三文件均为普通单链接文件、权限 `0600`。

### 3.3 代码、data 与 package

保护 aggregate 覆盖 `app/src`、`app/scripts`、`app/migrations`、`data` 下全部普通文件，以及 package、TypeScript、Next、ESLint、Vitest、`.env` 和 `.env.example` 配置 allowlist。

| 保护输入 | 写前 | 写后 |
| --- | --- | --- |
| 保护 aggregate | `8eec95a59de912245431501ea59b6c2a3ec9ec8b83e30cb82ebf6804e2421f12` | 相同 |
| `app/package.json` | `b0f58aadebe318cc6b3fd12dddb4d61f2caa2473d17c6eee791d3ac168041893` | 相同 |
| `app/package-lock.json` | `89807e33818cfb851b57da65a5675e413e5593d01d6773c3a71a6cc6ea3d85d3` | 相同 |

## 4. 已验证与未验证

已验证：

- 任务正式 claim 前开发部没有其他 claimed 任务。
- 使用的入口、Node 版本、参数和 clean environment 符合任务限制。
- 首条命令 exit `1`，稳定 reason code 为 `CLI_INTERNAL_ERROR`，`externalCalls=0`。
- 第二条 public-synthetic 命令没有执行。
- 两个 canonical DB 前后 SHA/inode/权限/大小零漂移。
- 三份 receipt 前后 SHA/inode/权限/大小零漂移。
- 保护代码/data/package aggregate 与 lockfile 零漂移。
- WAL/SHM 不存在；receipt 临时文件不存在。

未验证：

- `CLI_INTERNAL_ERROR` 的内部根因；安全 CLI 按合同没有输出路径、stack 或内部细节，本任务禁止额外探针或重试。
- 新 receipt 的 canonical bytes、自 hash、schema/ledger/count/root/artifact revision/validator SHA；没有新 receipt 可复算。
- `validatedAt` 进入 24 小时窗口；三份文件未刷新。
- 进程清单探针因当前 macOS sandbox 的 sysmond 不可用而失败；受控 Node 命令已同步退出，且本任务没有启动 Web 或后台 worker。
- `TASK-20260811-0F65C1` 的完整运行链；本任务明确禁止重跑。

## 5. 恢复条件

后继必须先对受控命令的稳定 `CLI_INTERNAL_ERROR` 做独立、只读或专门授权的最小诊断，确定为何 M3 receipt 生成在写入前 fail closed。诊断不得打印 receipt 敏感载荷、绝对路径或 stack，不得通过手写/touch、放宽 freshness、修改 DB、替换 validator 或绕过 `profile:closed-receipt` 来取得 PASS。

只有既有受控入口成功原子生成三份 receipt，且完整 envelope、权限、canonical bytes、自 hash、DB SHA、schema/ledger/count/root、artifact revision、validator SHA、24 小时 freshness、无 sidecar/临时文件全部复核通过后，才能解除本阻断。

## 6. 错题自检

- 写前保护 aggregate 的第一次只读 shell 尝试使用了 zsh 保留变量名 `path`，在该子 shell 内覆盖了 `PATH`，所以输出被立即作废；在任何 receipt 命令执行前，已改用变量 `candidate_file` 和绝对工具路径重新计算，得到有效写前 aggregate。该失误没有写文件或影响候选。
- 受控命令失败后没有重试，没有把 public-synthetic 单独执行，也没有切换为未授权命令。
- 没有输出 receipt 正文、内部 DB 行、secret、绝对路径或 stack。
- 没有把两个 DB 零漂移写成 receipt 已刷新或 integrity 已通过。
- 没有修改、删除、touch、暂存、提交、重置或清理项目文件。

## 7. 四件套

- 产物：本报告与机器 manifest。
- 已验证：写前/写后 DB、receipt、保护 aggregate、package-lock、权限与 sidecar 零漂移；受控首错和 `externalCalls=0`。
- 未验证：新 envelope/freshness、内部失败原因、0F65C1 运行链、独立安全/测试核收。
- 状态：`BLOCKED`；任务状态工具落账后回传实际状态。由于 acceptance exit 未完成，不能回传 `TASK_STATE_OK`。
