# M4 B 层本地 Web 工程初始化报告

## 结论

B 层 overall 判定：**PARTIAL**。静态 scaffold 已完成；官方 package-lock.json
解析因 registry/DNS 不可达阻断。该阻断属于本任务唯一 B 层失败出口，不能按完整
PASS 结论处理。

TASK-20260802-F8BF72 已完成可交付的静态 B 层部分：app/ 已升级为单包
Next.js App Router 工程，Node/npm/Next 版本栅栏、canonical 目录、安全环境示例、
Next/ESLint/TypeScript 配置和最小页面入口均已落盘。

官方 npm registry 在当前工作机不可达，依赖解析与 package-lock.json 生成构成
本次唯一 B 层阻断。没有使用镜像、unpkg、伪造 integrity 或手写不可信 lockfile。
因此本报告把 lockfile 明确列为 pending，C 层运行收据继续 pending；Node 25 没有被
用作 Node 24 的替代验收证据。

## 范围与依据

- 任务：TASK-20260802-F8BF72，M4/B 层/app 初始化。
- 授权：A 轴 ADR-M4-KICKOFF-001 已 accepted；本任务只开放本地 app/ 初始化。
- 依据：
  - docs/spec.md
  - ADR-M4-KICKOFF-001
- 明确关闭：真实 provider、飞书 Base、Collector、平台采集、AI/媒体调用、真实表单、
  自动发布、部署、付费和外发。

## 官方版本证据

下列版本指针来自官方发行物或官方包页，版本候选不向 latest、canary 或
preview 漂移：

- Node.js v24.18.0 LTS 发布页（https://nodejs.org/en/blog/release/v24.18.0）：确认
  v24.18.0 为 LTS，发布日为 2026-06-23。
- Node.js v24 下载归档（https://nodejs.org/en/download/archive/v24.0.0）：版本表列出
  v24.18.0 随附 npm v11.16.0。
- npm next 16.2.11 包页（https://www.npmjs.com/package/next/v/16.2.11）：确认
  Next stable 16.2.11 存在。
- npm eslint-config-next 版本页（https://www.npmjs.com/package/eslint-config-next?activeTab=versions）：
  确认 16.2.11 版本存在。

本机 CLI 仍按任务要求尝试读取官方 registry：

    npm config get registry
    https://registry.npmjs.org/

    npm view next@16.2.11 version engines peerDependencies dist.tarball dist.integrity --json
    在 20–30 秒限时内无响应，已停止；未把超时当作存在性或 integrity 证据。

收到 registry 恢复通知后，本会话又执行了一次 30 秒 bounded lockfile retry：

    npm install --package-lock-only --ignore-scripts --no-audit --no-fund --fetch-retries=0 --fetch-timeout=20000 --engine-strict=false

当前沙箱仍返回 ENOTFOUND registry.npmjs.org；对 1.1.1.1 的 dig 受到沙箱 socket
限制，curl 也无法解析 registry.npmjs.org。此次重试没有生成 package-lock.json 或
node_modules，唯一 B 层 lockfile/DNS 阻断继续保留。

## 本机工具链与 registry 结果

只读检查结果：

| 检查项 | 结果 | 解释 |
| --- | --- | --- |
| node --version | v25.5.0 | 不满足 Node 24.18.0 栅栏；不用于 C 层验收 |
| npm --version | 11.8.0 | 不满足 npm 11.16.0 栅栏 |
| nvm/fnm/volta | 未发现 | 没有可用的本地 Node 24 版本管理器 |
| npm registry | https://registry.npmjs.org/ | 官方 registry 配置正确 |
| npm cache | 未发现 Next/React 对应可信 packument/tarball | 缓存无法形成完整可追溯 lock |
| lock 解析尝试 | ENOTFOUND registry.npmjs.org | 20–30 秒限时内 DNS 不可达 |

实际 lock 尝试使用了仓库内 npm 命令、--package-lock-only、--ignore-scripts、
--no-audit、--no-fund、--fetch-retries=0 和有限 fetch timeout；命令失败后
没有生成 app/package-lock.json，没有产生 node_modules/，没有执行第三方
lifecycle script。app/.npmrc 继续以 engine-strict=true 和 ignore-scripts=true
fail closed。

## 已交付的 B 层文件

### 版本与依赖合同

- app/package.json：单包、private、ESM、精确 engines 与 packageManager。
- 运行依赖固定为 next@16.2.11、react@19.2.0、react-dom@19.2.0。
- 开发依赖固定为 TypeScript、Node/React 类型、ESLint 和
  eslint-config-next@16.2.11 的具体版本字符串；完整解析等待官方 registry 恢复。
- app/.nvmrc、app/.node-version：均为 24.18.0。
- app/.npmrc：engine-strict=true、ignore-scripts=true、关闭 audit/fund 的
  非交互默认值。
- app/.gitignore：忽略 node_modules、Next 构建输出、本地 SQLite 和真实 .env，
  保留 .env.example。

### 安全环境示例

app/.env.example 只包含 accepted ADR 的 local/loopback、fixture、mock、
manual_only 和三个 REAL_* = false 默认值；没有 token、secret、password、
API key、代理变量或真实 provider 配置。

### canonical 目录与最小 scaffold

- src/app/：layout.tsx、page.tsx、globals.css，另留 (public)、(admin)、api
  路由槽位。
- src/modules/：source-registry、ingestion、normalization、dedupe、
  summary、media、review、publication、shared。
- src/server/：config、db、providers、workers、security。
- src/styles/、src/tests/、migrations/、fixtures/、scripts/。
- next.config.ts、eslint.config.mjs、tsconfig.json、next-env.d.ts。
- scripts/pending.mjs：C 层命令占位，明确打印 pending 并拒绝假装完成；没有业务
  逻辑或外部 I/O。
- app/README.md：已移除“技术栈尚未确定/尚未初始化”的旧地基说明，补充当前
  入口、版本栅栏、目录合同和 lockfile 阻断说明。

## 验证收据

已完成的静态检查：

1. package.json JSON 解析通过；engines、packageManager、Next/React 固定版本和
   canonical 目录断言通过。
2. package.json、.npmrc、版本文件扫描没有 latest/canary，没有
   preinstall/postinstall/其他 lifecycle hook。
3. .env.example 变量名与值扫描通过：loopback、fixture、mock、manual-only、
   REAL_* = false，没有凭证模式和代理注入。
4. app/ 路径与 .gitkeep 槽位检查通过；README 不再宣称 app 未初始化。
5. 追加报告前后执行 git diff --check，并对新增 app 文件执行 trailing-whitespace
   扫描，均通过。

## 唯一阻断与恢复门槛

### B 层阻断：官方 lockfile 解析

恢复官方 registry/DNS 或提供用户批准的合规离线 npm cache 后，在 Node
24.18.0 + npm 11.16.0 下执行：

    cd app
    npm install --package-lock-only --ignore-scripts --no-audit --no-fund
    npm ci --ignore-scripts --no-audit --no-fund

随后必须检查 lockfile 的每个 resolved/integrity 来源、依赖许可证、可选原生包和
lifecycle，确认没有浮动 tag；若官方 registry 仍不可达，保持当前静态 B 层，不改用
第三方镜像或手写 integrity。

### C 层后置门槛

即使 lockfile 恢复，以下仍须独立任务在 Node 24 上验收：npm ci 运行收据、
verify:env、SQLite node:sqlite/WAL/事务/timeout/migration/recovery、mock
adapter/worker、in-process API 合同、lint/typecheck/build/test/security、真实
127.0.0.1 端口验证和四个业务纵向切片。Node 25 只能记录参考事实，不能替代任何
Node 24 通过证据。

## 错误路径与边界自检

- Node 24 不在本机：保留精确版本文件，C 层标记 pending，未伪造运行成功。
- 官方 registry DNS 失败：停止依赖解析，未降级到镜像/unpkg，未写伪造 lock。
- npm 安装潜在 lifecycle：.npmrc 与命令均使用 ignore-scripts=true，未执行
  第三方脚本。
- 外部能力：没有调用真实 provider、Base、平台、网络 API、部署或发布；没有系统级
  安装或提权。
