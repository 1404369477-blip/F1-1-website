# TASK-20260809-3A8C0E｜VS1 P1-01 contract 恢复链断言首错阻断报告

- 日期：2026-08-09
- 部门：开发部
- 结论：`BLOCKED`
- 唯一首错：固定 Node24 clean-room 的首次 `npm run test:contract` 以 `CLI_INTERNAL_ERROR`、`externalCalls=0` 非零退出；按任务失败规则停止，未运行 typecheck，未重跑。

## 1. 范围

本轮只修改 `app/scripts/vs1-contract.ts`，目标是：

1. 为每个 mandatory case 显式断言 `reasonCode`、`exitCode`、`recoveryAction` 与 `externalCalls=0`。
2. 为 `VS1-RETRY-014` 显式断言最多三次、1s/3s fixture-clock delay 和完整 attempt history。
3. 为 `VS1-DEAD-015` 显式断言三次 attempt history 与 `dead_letter +1`。
4. 保留 012、016A-G、approved-chain rollback、replay 与 no-work 断言。
5. 准备输出 25 case 的确定性脱敏 assertion receipt；hash 输入设计为 `JSON.stringify({schemaVersion,cases,externalCalls})` 的 UTF-8 字节。

没有修改 worker、pipeline、fixture registry/seed/manifest、migration、package-lock、公开 UI、Admin、Spec、ADR、data 或 design。

## 2. 唯一执行收据

执行包装：`env -i`，只传 `HOME`、项目 Node24 前置最小 `PATH`、`TMPDIR`、`LANG=C`、`LC_ALL=C`。

执行命令：`npm run test:contract`

结果：exit 1。

```json
{"event":"cli_failure","status":"rejected","reasonCode":"CLI_INTERNAL_ERROR","externalCalls":0}
```

由于目标测试非零退出：

- 未产生 25-case assertion receipt；
- 未运行 typecheck；
- 未运行普通测试、build、check、浏览器或整站回归；
- 未执行第二次 test:contract。

## 3. 首错定位

只读对照冻结 fixture 后确认，失败来自新增 014 断言的 expected 值写错：

- 当前新增断言写为：attempt 2 outcome=`HTTP_503`、delay=`3`。
- 冻结 `VS1-RETRY-014` fixture 实际为：attempt 1=`HTTP_503`，attempt 2=`COLLECTION_TIMEOUT`，attempt 3=`candidate`；pipeline receipt 的终态 outcome 为 `PIPELINE_READY`。
- 因此正确的完整历史应为：

```json
[
  {"attempt":1,"outcome":"HTTP_503","leasePresent":true,"retryDelaySeconds":1},
  {"attempt":2,"outcome":"COLLECTION_TIMEOUT","leasePresent":true,"retryDelaySeconds":3},
  {"attempt":3,"outcome":"PIPELINE_READY","leasePresent":true,"retryDelaySeconds":0}
]
```

该首错属于测试 expected 与冻结 fixture 不一致。当前只读证据没有显示生产 pipeline、fixture 或 accepted v0.2 语义漂移；依照“首个非零即停止、禁止重复运行至通过”，本轮没有修正该行或重跑。

## 4. Hash 复核

### 4.1 目标文件

| 文件 | 修改前 SHA-256 | 当前 SHA-256 |
| --- | --- | --- |
| `app/scripts/vs1-contract.ts` | `7f52c992ffdd3a92c06d3c87aa0babcce83e4fa12c55f3933968e246a0f40297` | `b8f0a2fb441c2012786346fb94e263134ccc9154441bcf6640151844ba027e68` |

### 4.2 保护文件零漂移

| 文件 | SHA-256 |
| --- | --- |
| 冻结设计 HTML | `5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1` |
| `app/scripts/vs1-worker.ts` | `57fcea6ac269daccce8a21072198b4ccc3f0529823a79383a97d0a3af67de814` |
| `app/src/server/vs1/pipeline.ts` | `a74240b8d479cfec2fd0e83bc6146fd05ab6b85e12e7149d4b016dc1b92cf806` |
| `app/src/server/vs1/fixture.ts` | `7c21bf9e3e0c38a166a831613118daf0f3fcf837d08ca6723553e732133326e9` |
| registry | `21347151fbc69de403dd4d7b7aec3f315e2d8de4646f622d8b5377924f610ee1` |
| seed | `4ab8a3bab537c82e43612fa11b81cdacea2043d4027bd09fcf91b04f5677a648` |
| manifest | `7343f8bc76d68b7993b29ed5232e3487621effb3a27518e0f754a5dd07fef39e` |
| `app/package-lock.json` | `89807e33818cfb851b57da65a5675e413e5593d01d6773c3a71a6cc6ea3d85d3` |

VS1 migrations `0001..0006` 依次保持：

1. `dc235534c492b9c763f63e2752bd4d78cc4f05ecf8e3600a3a906c3d0aae029b`
2. `3c96d4be93ebbc3dcc56bbb1780bec7f65c76f9c9ae1a7a391d3895d10f5c748`
3. `f1014f95dea2d556ea632493448b6db98350b89baea55f018a1975c9baec7460`
4. `276e31b466eda82ec5076307f4bab62b37b1ef496146e555edbae5f7a6408696`
5. `f5a8e18f1d437cdca18990d27d15be06c12b08efda771e12f903600bb7bc9a6a`
6. `c27b9c0274605db15ad91462f6752637ae35c54b7632920718229f06dda7c17e`

## 5. 清理与未验证项

- 测试入口通过每个 case 的 `finally` 调用任务专属 cleanup；最终扫描没有 `TASK-20260809-D6114C-*` 临时根残留。
- 外部调用为 0；没有启动网站、真实 provider、AI、Base、Admin、发布或部署能力。
- 未验证：修正后的 25-case assertion receipt、typecheck、数据/安全/测试对新候选的独立门禁。
- 三个 Function ID 状态未改为 complete。

## 6. 错题自检

- 修改前只读检查没有逐项展开 014 冻结 fixture 的第二次 outcome，错误地从 015 的全 `HTTP_503` 历史外推到 014。
- 首次目标测试非零后没有修正 expected、降低断言、跳过 case 或重复执行至通过。
- 没有把 safe CLI 的封闭 `CLI_INTERNAL_ERROR` 输出扩写成未经运行证明的 PASS 收据。
- 没有修改任何保护文件或扩大验证范围。

恢复出口：需由统筹部明确 resume；只把 014 attempt 2 expected 收紧为冻结 fixture 的 `COLLECTION_TIMEOUT`，随后按新授权执行一次目标 `test:contract`，通过后再运行必要 typecheck。当前任务保持 blocked。
