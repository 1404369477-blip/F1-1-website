# M4 VS-1 公开前端 P1 整改实现报告

- 任务：`TASK-20260803-CD4228`
- 部门：开发部
- 日期：2026-08-03
- 当前结论：`PASS（开发自测）`
- 正式状态说明：本报告不修改任务 JSON；仍须原设计部、原测试部独立复验，并由统筹部核收。

## 1. 整改结果

本次只修改公开 UI、直接相关组件、测试和本开发报告；没有修改 Spec、accepted ADR、data、依赖、package/lock、真实外部能力或正式任务状态。

四项 P1 已闭合：

1. 媒体标签改为双主题相同的实色保护层 `#141417` 与白字；浏览器暗色、浅色实得对比度均为 `18.39:1`，高于 `4.5:1`。
2. 分类选中态保留 `aria-pressed`，新增可见 `✓`、`font-weight: 750` 与既有颜色组合；未选项不显示勾选标记。
3. no-more 文案由当前筛选结果生成；浏览器实得 12、3、1、0 条时，信息流、no-more 或空状态数量逐字一致。
4. 详情面包屑返回链接使用 `min-width/min-height: 44px`；390 和 1440 浏览器实得 `58.24 × 44px`。

同一切片补齐：

- `F1PageShell` 上移到 RootLayout，跨首页、详情和段级 404 只保留一个共享壳层；主题通过窄 localStorage 工具与 `useSyncExternalStore` 同步，浏览器在浅色主题下跨详情导航和刷新后均保持 `data-theme=light` 与正确按钮名称。
- `#demo-boundary` 增加 `scroll-margin-top: 80px`；390 的 hash 导航实得 target top `80.06px`、sticky TopBar bottom `57px`，目标未被遮挡。
- 精选区增加自身 H2，三张卡片标题统一为 H3；浏览器可访问树呈现 H1 → Featured H2 → story H3，以及 Feed H2 → story H3。
- 新增组件／输出级回归，公开 UI 测试由 3 项增加到 9 项。

## 2. 修改文件与 SHA-256

| 文件 | SHA-256 |
|---|---|
| `app/src/app/globals.css` | `306327f161907e3a5144341ddf4bb44b903864291adfcb2e0f84dd31f90ea16a` |
| `app/src/app/layout.tsx` | `205dba10a35638c888fd4cc67af9668b041356764b937cc8629a0af23907099f` |
| `app/src/app/page.tsx` | `853cff32988d0344e6e5ec0b566be327148121c4fc934ff8cebdc10b5316104e` |
| `app/src/app/stories/[publicId]/page.tsx` | `0c842483182c1e306aa1d4572064ab06682a95b42fd1ed122fed7359b0386e32` |
| `app/src/app/stories/[publicId]/not-found.tsx` | `57aafe35999e2f4553c8b2193aa7d68192547e09a2e5e7611615ffde3eee82a1` |
| `app/src/components/f1/f1-page-shell.tsx` | `341879012e71603d371e0f548ece9767cd8869cbc4e6eb21ed5f9eddcccb4726` |
| `app/src/components/f1/story-parts.tsx` | `6df4d3b44d2dcd0ef205330be9dcf16360bd50ae46bff4749608470a4e7e3602` |
| `app/src/components/f1/theme-preference.ts` | `35b13341643e9a120db900eccd66c5dee369d5133f2b37a1c856a5ff8e0c24d6` |
| `app/src/features/stories/feed-experience.tsx` | `bb86c1279aa2f0f802d1b4e1b5cf71a07d4f1e9478b555dccfb1d76642f0c862` |
| `app/src/tests/public-ui.test.ts` | `77d7dae86ded7a34ae377d5bbe2ec8d47882fa350eae4d5a016ca63323b21853` |

## 3. 固定 Node24 自动化收据

使用项目内 Node `24.18.0`、npm `11.16.0` 和最小 PATH：

- `npm run test`：3 files、51/51 tests PASS；公开 UI 9/9。
- `npm run lint`：exit 0。
- `npm run typecheck`：exit 0。
- `npm run build`：exit 0；路由仍为 `/`、`/_not-found`、`/api/health`、`/stories/[publicId]`。
- 最终 `npm --silent run check`：exit 0；迁移/59×39 seed/runtime readiness、51/51、lint、typecheck、build、`test:p1` 全部通过；`test:p1` 为 `127.0.0.1:3000 / ready / externalCalls=0`。
- `git diff --check`：exit 0。

## 4. 本地浏览器与 HTTP 收据

- 1440 首页：root/body `clientWidth=scrollWidth=1440`；暗色媒体标签 10 个抽样全部 `18.39:1`；选中分类为 `aria-pressed=true + ✓ + font-weight 750`；标题层级连续；外部 HTTP(S) 属性为 0。
- 1440 详情：root/body 无横向溢出；返回链接 `58.24 × 44px`；Breadcrumb 当前项正确；全局导航没有误标首页当前态；H1=1；浅色媒体标签仍是白字/`#141417`。
- 390 详情：root/body `clientWidth=scrollWidth=390`；返回链接 `58.24 × 44px`；主题保持浅色。
- 390 锚点：target top `80.06px` 大于 TopBar bottom `57px`。
- 主题：切换到浅色后，首页、详情导航、详情刷新三处均为 `data-theme=light`，按钮均为“切换至深色主题”。
- 数量：no-more 组合下实得 `12 → 3 → 1 → 0`；可见 article 和对应文案分别一致，0 条进入明确空状态。
- 404：共享 TopBar 数量为 1，H1 为“这条演示内容不存在或已下架”，全局导航无错误 current，外部 HTTP(S) 属性为 0。
- HTTP：`/` 200、有效详情 200、未知详情 404、`/api/health` 200。
- 浏览器 console error/warn：0。
- 验收结束后：3000、3101 均无 LISTEN。

## 5. 未验证项与边界

- VoiceOver/NVDA/TalkBack 实机、200% 缩放与完整 forced-colors 仍未在本开发自测中执行；它们不由本报告推导为 PASS。
- loading/error/partial/live-refresh、withdrawn/correction 继续等待 SQLite/API 切片；本任务没有伪造后端状态。
- SQLite 公开 API、admin、真实 provider/Base/Collector、平台采集、AI、媒体、发布、部署、R5/R12 与另案 Next 后置启动错误脱敏均不在本任务范围。
- 浏览器按控制边界只验证页面呈现的主题保持；localStorage 的读写/异常降级由 9/9 自动化中的纯函数回归覆盖，没有在浏览器中读取用户存储内容。

## 6. 错题自检

- 首次虽然用 Node24 直接启动 npm CLI，npm 子脚本仍从宿主 PATH 取得 Node26.5.1，造成 22 项 `NODE_VERSION` 预期失败。改用最小 PATH 后 51/51 通过；没有把错误环境的失败改写为代码通过。
- RootLayout 共享壳层可能影响 404 或导航 current；已对有效详情、段级 404、跨路由主题、H1、TopBar 数量和实际 200/404 分别复核。
- no-more 没有通过保留固定 12 绕过测试；浏览器真实交互覆盖 0/1/3/12。
- 没有删除测试、引入依赖、启用外链或修改任务/产品/数据真值。

## 7. 结论

开发自测范围内 `P0=0、P1=0`，四项整改及直接相关回归均有自动化和真实浏览器证据。当前快照可交原设计部与原测试部独立复验；独立复验通过前，本报告不代表统筹核收或前序任务正式放行。
