# TASK-20260813-53677C｜公开站 v0.2 冻结视觉生产部署

## 结论

`status=final`，`decision=PASS`。用户确认的公开站 v0.2 冻结视觉已经提交、非 force 推送、固定 Node 24 构建、内容寻址验签并部署到 M1。生产 `public-beta` 现运行新 release；首页与独立详情统一为原版时间线语言，已移除五分类按钮、灰色原生按钮/卡片外框、面包屑、状态胶囊和 `PUBLIC API` 可见技术标签。真实中文标题、中文摘要、来源图、签名投影 generation 1 均保留。

- 当前公开入口：`[EPHEMERAL-TUNNEL-URL]`
- 设计基准：`design/ui/F1+1-v0.2-全站设计/F1+1-v0.2-final-20260808.html`
- 设计 SHA-256：`5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1`
- Git commit：`52e654915d19ab31802c97a406402896b6a68978`
- Git tree：`7a0e9efd59954e5b9c8494b27ed851d5596c0c14`
- Git parent：`fb0a938fa42fe9d28a8fe675aa963d5ba715aabb`
- 远端分支：`refs/heads/codex/first-public-release=52e654915d19ab31802c97a406402896b6a68978`

## 代码与验证

提交只包含四个公开 UI 文件：

1. `app/src/app/globals.css`
2. `app/src/features/stories/feed-experience.tsx`
3. `app/src/features/stories/story-detail-experience.tsx`
4. `app/src/tests/public-ui.test.ts`

固定 Node `v24.18.0` 下：

- focused Vitest：`1 file / 16 tests PASS`。
- TypeScript `--noEmit`：PASS。
- production Next build：PASS；仅有既存 NFT warning。
- 四文件 `git diff --check`：PASS。
- HEAD、origin、四个运行文件均与 commit 精确一致。

测试覆盖：首页无分类控件；摘要按钮默认透明、零边框；详情采用同一时间线 `tl-item is-open`；无 breadcrumb/status badge/旧卡片体系；真实来源图、中文内容、深浅主题与状态链继续可用。

## release 与 M1 固化

- manifest SHA-256：`fb4d72b1e2b4ffb9311f57b9663e09d919631c792c09d35b4ef9571ffec1da1d`
- runtime files：96
- Next：322 files / 13,536,086 bytes
- Next root：`532e3be54db42c2baa9777bd74711d1bbc86e1465f77a01ae7588062087362bb`
- production packages：52
- dependency root：`2d3d26a867c19d45ebed37b4ca468ff2841a6e6eb70287273b0bae657a8fd0cd`
- content root：`47738844ce519c0fa8679f790622c2d733e3a1cad0f3bb40d654ef3ba1cece2f`
- release root：`5d99dc951ea3312f83f4ab097d185e37eaf820e76f8d817e7e764b7d34f8eaed`
- M1 app root：`[M1-HOME]/F1-1-website/releases/5d99dc951ea3312f83f4ab097d185e37eaf820e76f8d817e7e764b7d34f8eaed/app`

本地 M1-target harness 与 M1 target verifier 均 PASS。部署采用旧 live release 的 APFS clone 加闭集 delta：32 changed、7 deleted、payload 3,737,983 bytes；stage 验签后同盘原子 rename 到上述版本化 release。

## 生产切换与回退

只切换 `com.f1plus1.public-beta`：

- live plist SHA 从 `d79ca6c7d2968bf7900f0b296e0795d45f5f6678f229d7342c3fd1b4ca879907` 变为 `0cf270bc783710e8a90a4dbbcc83ed265252f23a860eef228670b3075fe07295`。
- live plist 仅替换两处旧 app 路径；真实投影 root、验签 key、read mode、Node 与其他环境逐字保留。
- 切换前 live plist 已保存为任务内首选回退：`[M1-HOME]/F1-1-website/.public-v02-deploy-20260813/com.f1plus1.public-beta.rollback.plist`，SHA 为切换前 `d79ca6c7…9907`。
- 上一版 public-real LKG 仍保留：`[M1-HOME]/F1-1-website/.public-real-ui-20260813/com.f1plus1.public-beta.rollback.plist`，SHA `e8108746ef20d657c32fb0d027c53e12ae7acbf7cf71f006dbea1fbdf8445471`。
- 切换脚本已内置异常回退；本次主链无回退触发。

当前 M1 只读回执：

- public-beta running，PID 12228，唯一 listener `127.0.0.1:3000`。
- Admin PID 2754、唯一 `127.0.0.1:3101`，切换前后不变。
- receiver PID 94590、唯一 `127.0.0.1:3102`，切换前后不变。
- active pointer SHA `48d5c857d1c29a2294693f71e7b8cd7a4a1c835476c1ae9341d1246f31efce6e`，generation 1，manifest hash `e612cfd781bc5bbc10c0097bf658cd198303db7f16ba94ce6fcbbb96fdd0c988`，零漂移。

## 真实出口

上线后公开 Quick Tunnel 实测：

- `/`：200。
- `/api/health`：200，`ready / accepted-public-real-snapshot / signed-active-snapshot / Node 24.18.0`。
- `/api/public/feed`：200，1 条真实内容，中文标题、中文摘要、`source_image`。
- 真实详情页面：200。
- 真实详情 API：200，publicId、标题、图片类型与 feed 一致。
- `/api/admin/session`：404。
- `/internal/projections`：404。

## 生产视觉证据

生产 URL 同一候选共生成 12 组真实截图与 DOM 收据：主页/详情 × 深/浅 × 390/1024/1440。

- 路径：`scratch/TASK-20260813-53677C/evidence-production/`
- 清单 SHA-256：`ae009b2a1ef3acbcb2a36bab4a9bb61b2bf4ba2a7f74d8b8d44d251e0787baa4`
- 每组断言：`scrollWidth == innerWidth`；可见越界元素 0；分类控件 0；breadcrumb 0；status badge 0；可见 `PUBLIC API` 0；旧 not-found 容器 0；真实标题与图片均有非零几何。
- 首页六格额外断言：摘要区域 background 为 transparent，border 为 0。
- 390：app 390、timeline 358、左右 16；移动时间簇、图片和底部 dock 均在视口内。
- 1024：app 880，内容时间线约 808，64px 时间列 + 32px 间距成立。
- 1440：app 880 居中，时间线 800，右下低强调工具成立。
- 深浅主题只改变 token，六格几何逐档一致。

同一 commit 在正式部署前还生成了本地 12 格候选证据：`scratch/TASK-20260813-V02-VISUAL/evidence-final2/`；生产证据复核其结构和几何未漂移。

## mistake-check 与未验证

- 首次 focused Vitest 从仓库根执行，Vitest 因 cwd 不正确未发现目标测试；更正为 `app` cwd 后固定 Node 24 运行 16/16 PASS，未掩盖测试失败。
- 首次通过 npm script 调 builder 时子进程解析到系统 Node，被 Node 版本门拒绝且未生成 manifest；改用固定 Node 24 直接执行 builder 后 PASS。
- 浏览器插件对当前 Quick Tunnel 导航出现连接超时；生产视觉证据改用全新临时 profile 的 Chrome headless + CDP，只读访问同一生产 URL。采集脚本前两轮发现并修正“图片未加载就取证”和“主题偏好跨 tab 继承”的验收脚本问题，最终 12 格均从 fresh 页面按预期主题、真实图片非零几何通过。失败轮证据隔离保留，不计入最终收据。
- 详情 HTML 的服务端 RSC payload 仍可能包含路由级旧 `not-found.tsx` 组件文字；最终 hydrated 可见 DOM 的 12 格详情收据确认可见 `PUBLIC API` 与旧 not-found 均为 0，详情 API 200。该不可见 payload 不影响当前 UI，但旧组件源码仍是后续清理项。
- iPhone/iPad 实体 Safari 尚未在本任务内自动化；390 WebKit 几何由冻结设计历史证据覆盖，本轮生产证据使用 Chrome 渲染。需要用户在实体设备刷新后做最后主观确认。
- 当前公网地址是 Cloudflare Quick Tunnel 临时地址，不是固定域名；本次没有更改 quick-tunnel、Tailscale、Admin、receiver、review DB、projection active 或发布数据。

## 独立对抗复审

独立只读复审最终裁决：`PASS / P0=0 / P1=0 / P2=1`。复审重新核对 Git/远端 ref、设计 SHA、M1 manifest 与实际进程命令、live plist、3000/3101/3102、真实 HTTP/API、active generation、rollback 文件和生产 12 格可见 DOM，均与本报告一致。唯一 P2 是详情首次 HTML 的 Next RSC 序列化数据仍携带旧 `not-found.tsx` 的 `PUBLIC API · 404` 文字；当前可见 SSR 壳与客户端水合后的真实详情均使用新版时间线，旧文字在可见 DOM 为 0。后续应同步路由级 not-found 组件，清理序列化残留。

复审另确认：本轮按用户视觉决定移除了分类筛选入口，因此 `PUB-FEED-003` 分类筛选功能不得借本次部署升级为完成；它继续按追踪矩阵原状态处理。
