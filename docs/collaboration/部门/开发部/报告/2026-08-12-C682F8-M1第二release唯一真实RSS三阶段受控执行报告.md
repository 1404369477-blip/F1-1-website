---
task_id: TASK-20260812-C682F8
status: final
decision: pass
department: 开发部
---

# M1 第二 release 唯一真实 RSS 三阶段受控执行报告

## 结果

`baseline → execute → postflight` 三阶段各执行一次且全部 PASS。唯一 wrapper 完成一次真实 RSS 请求，取得 HTTP `200`，从 `50` 条 feed item 选取并新建 `20` 条候选；`20/20` 全部进入 `pending_review`。wrapper 结束后的第一控制动作是唯一 stop，source 回到 disabled、`stop_epoch` 由 `3` 增至 `4`、RSS job 明确 unloaded。20 秒短窗无新 run 或 candidate，公开站数据、页面与三项既有服务零漂移。

## 执行器身份与白名单

- 本地固定执行器 SHA-256：`9067b5a08ead7282ea558c57cfbebcaed6d0ed0b71c9b4d22944fc041c9c38be`
- 全新远端路径：`[M1-HOME]/F1-1-website/.rss-release-TASK-20260812-D460D7/TASK-20260812-9067-controlled-rss-run.mjs`
- `scp`：`1` 次
- 远端 SHA-256：与本地逐字符相等
- 远端身份：mode `0600`、UID `501`、`nlink=1`
- 远端固定 Node24 `--check`：`1` 次，PASS
- 额外远端诊断、grep、pipe、变量包装、inline Node、heredoc：`0`

## baseline 收据

- 执行次数：`1`
- `status=baseline-pass`
- 当前 slot：`1984982`
- 预期缺口 slot：`1984981`
- `gapExpected=true`
- 执行前最新 slot：`1984973`
- 执行前 run 总数：`1`
- 执行前 candidate 总数：`0`
- 执行器合同门：source disabled、`stop_epoch=3`、job unloaded、running run `0`、scheduler gap `0`、slot 余量达标，才返回 `baseline-pass`。
- 公开 DB 基线：SHA-256 `949c78d505e4c032d2495174deaf62d24f9d99b76284ad7ba6fb29a5ac83bb50`，size `737280`，inode `24546198`，mode `0600`，UID `501`，单链接。
- local/public home 与 detail：全部 HTTP `200`。

## execute 唯一 wrapper 与 stop 收据

- execute 执行次数：`1`
- `status=wrapper-and-stop-complete`
- wrapper 尝试：`1`
- wrapper 退出码：`0`
- `externalCalls=1`
- run：`rss-run-1984982`
- wrapper 业务状态：`succeeded`
- reason：`OK`
- HTTP：`200`
- response bytes：`67818`
- response SHA-256：`09754466c27b4a1cdac5af9f6b5817fc716c0db08814d3a6f0dde3e5d3017d7f`
- item / selected / new / updated / duplicate：`50 / 20 / 20 / 0 / 0`
- validator result：`modified`
- stop 尝试：`1`
- stop 退出码：`0`
- `stopWasImmediateNextAction=true`
- stop 收据：`stopped-confirmed`
- 结束安全状态：`source.enabled=false`、`stop_epoch=4`、job `unloaded`、`finalSafety.confirmed=true`。

## postflight 收据

- 执行次数：`1`
- `status=postflight-pass`
- 当前 run：`status=succeeded`、`reason_code=OK`、HTTP `200`，计数与 wrapper 收据精确一致。
- scheduler gap：恪守合同生成 `1` 条 `scheduler_gap/SCHEDULER_GAP/manual_review`，slot `1984981`。
- run 聚合：总数 `3`、running `0`、scheduler gap `1`、latest slot `1984982`。
- candidate 聚合：总数 `20`、`pending_review=20`、`machine_invalid=0`，revision 均为 `1`。
- 人工字段：`editor_title/editor_excerpt/editor_notes/editor_based_on_source_revision` 的已设置计数均为 `0`，`manualFieldsPreserved=true`。
- private DB：`integrity=ok`。
- source：`enabled=0`、`stop_epoch=4`、`last_reason_code=OK`，已有成功时间，job unloaded。
- 短窗：`20` 秒；`zeroNewRunsInWindow=true`、`zeroNewCandidatesInWindow=true`。

## 公开面零漂移

- 公开 DB 最终 SHA/size/inode/mode/UID/link 与 baseline 完全一致。
- 公开 origin 收据 SHA-256：`a472476b3947308e89b613c9c82b0f91c3c72cb9c4cbc9d82301e6573e075b9f`。
- local/public home 与 detail 最终全部 HTTP `200`。
- `public-beta`：running，PID `52089`，与 baseline 一致。
- `quick-tunnel`：running，PID `53446`，与 baseline 一致。
- `receipt-refresh`：当时处于周期性空闲状态，last exit `0`，与 baseline 一致。

## 未验证与边界

- 执行前人工非空字段候选数为 `0`，所以本次证明机器未为新候选写入人工字段；“已有非空人工内容遇真实更新仍保持”没有动态样本。
- 未执行 `resume`、`bootstrap`、900 秒 load、自动发布、Admin 变更或任何清理。
- stage、tar、backup、执行器与 Trash 全部保留，供独立运行审查。

## 错题自检

- 每个phase只执行一次，没有因成功结果再跑证明性轮次。
- execute 内 wrapper 及真实外联各为一次，stop 为紧随 wrapper 的第一控制动作且只执行一次。
- 报告不包含 RSS 正文、candidate ID、响应头、IP 或未脱敏 DB 内容。
- 真实采集成功只放行后续独立运行审查，不在本任务内自动开启 900 秒调度。

TASK_STATE_OK
