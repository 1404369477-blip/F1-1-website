# M4 VS-0 Next 启动错误输出边界整改报告

- 任务：`TASK-20260802-3760F6`
- 部门：开发部
- 日期：2026-08-04
- 当前结论：`PASS（开发自测）`
- 放行边界：仍须测试部与安全部各自独立复验；本报告不 ACK 上游任务，也不放行 R5、R12、VS-0 整体或任何真实外部能力。

## 1. 结果

已关闭安全报告确认的 Next 后置启动错误输出旁路，修改范围只有启动监督器、P1 验收脚本和对应回归测试：

1. `serve.ts` 仍先拒绝所有附加 argv，再执行既有 runtime readiness；通过后由父进程以固定参数启动 Next 子进程，hostname 与 port 仍逐字固定为 `127.0.0.1:3000`。
2. Next 子进程的 stdout/stderr 由父进程接管并持续排空，不直接进入应用输出。端口占用、缺失 `.next`、Next import/listen 或 spawn 后置失败统一回到既有 `runSafeCli`，应用 stdout 为空，stderr 只输出一条四字段 JSON：`event/status/reasonCode/externalCalls`。
3. `SIGINT`/`SIGTERM` 由父进程转发给 Next 子进程；正常主动停止不生成虚假 `cli_failure`。P1 happy-path 验收改为实际发送 `SIGINT`，超时后才使用既有 `SIGKILL` 清理兜底。
4. 新增三个隔离副本中的真实空 argv 负例：`npm --silent run dev` 端口占用、`npm --silent run start` 端口占用、`npm --silent run start` 缺失 production build。三者都在 runtime ready 后进入 Next，再由监督边界收敛为 exact safe JSON；原 Next banner、URL、Error、stack、绝对路径、address 与 port 均未穿透。负例复制只读 M3 冻结依赖和现有 build，不改名、删除或覆盖共享 `app/.next`。

没有修改公开页面、数据合同、Spec、accepted ADR、设计、migration、package/lock 或安全 reason-code allowlist；没有新增依赖、外部请求、真实 provider/Base/Collector/平台/AI/媒体/发布能力。

## 2. 修改文件与冻结 SHA-256

| 文件 | SHA-256 |
| --- | --- |
| `app/scripts/serve.ts` | `5ee435346a1c061eb15a93ac41dd40f61d831db23a0df04652a7c59ecf0a6979` |
| `app/scripts/p1-acceptance.ts` | `bcfd9f8a2f2a92ccea81007debff2b7baf1c443b2174711e025d6d81ec50d29c` |
| `app/src/tests/p1-cli.test.ts` | `6142415116139ca3b2fa8f3ed7fd931189324551a336c9ef67ea7344fbb6f71e` |

以下保护输入复算保持原 SHA，没有被本任务修改：

| 文件 | SHA-256 |
| --- | --- |
| `app/src/server/security/cli.ts` | `1126cca2471da18160b8d870bd4a1d030610324e5b0c1e1ccbf78e4c7ce42e85` |
| `app/src/server/security/log.ts` | `f69347a4d054cb4ce05f92bd333fba4b80b00dfe21108db110da12c7e41e4b00` |
| `app/package.json` | `cb81c6be9db1772537dfc2a53214bf03d7f92f763a701d20c68789d68d1fd452` |
| `app/package-lock.json` | `89807e33818cfb851b57da65a5675e413e5593d01d6773c3a71a6cc6ea3d85d3` |

## 3. 能改变放行判断的验证证据

固定项目 Node `24.18.0`、npm `11.16.0`、最小 PATH 与 `NEXT_TELEMETRY_DISABLED=1`：

- 目标测试：`src/tests/p1-cli.test.ts` 为 `13/13 PASS`。六组 dev/start 附加 argv、四类既有应用 CLI 失败、dev/start 各一组端口占用与 start 缺失 `.next` 三类 Next 后置失败全部通过。
- 三类 Next 真实负例均为 exit 非零、stdout 空、stderr 逐字只有 `{"event":"cli_failure","status":"rejected","reasonCode":"CLI_INTERNAL_ERROR","externalCalls":0}`；对象键顺序和集合均为四字段，扫描无 Next banner、URL、Error、stack、绝对路径、源码行、address 或 port。
- 最终 `npm --silent run check` exit 0：migration/seed/runtime ready；3 files、`54/54` tests；lint、typecheck、build；正常 production start 的 health 为 `ready/local-only/externalCalls=0`。同一最终收据在真实 `SIGINT` 后才输出，并逐字包含 `stopped=true`、`portsClear=[3000,3101]`、`processGroupClear=true`。
- 最终端口检查：`3000`、`3101` 无 LISTEN；`f1plus1-p1-next-*` 与 `p1-next-*` 临时项均已清理；`git diff --check` exit 0。

## 4. 未验证项

- 正常 production start/health/SIGINT 已验证；正常 dev 的 HMR 长时间运行体验没有在本任务另开冗余实机轮次，dev 的附加 argv 前置拒绝仍由三组真实 npm 负例覆盖。
- 父进程可接管的 Next spawn/import/listen/非零退出已闭合；OS 强制终止、Node 自身 fatal crash 或进程无法调度等父进程不可执行的故障不由应用 JSON 合同保证。
- R5 恶意同 UID 竞争和完整 TOCTOU 未验证；R12 OS/系统调用级 no-egress 仍未实现，`externalCalls=0` 是应用 DTO，不构成系统调用证明。
- SQLite 公开读模型/API、admin、真实外部采集/AI/媒体/发布/部署继续关闭，不随本任务改变。

## 5. 错题自检

- 没有把外层 `test:p1` 捕获输出当成用户直接执行 `npm start` 的保护；两个新负例均直接运行真实 `npm --silent run start`。
- 隔离副本首轮遗漏了 seed 所需的项目级冻结数据目录，既有 fixture 路径门禁按预期拒绝；失败轮次没有计入通过证据。最终副本只复制 `m3-base-shadow-import-v0`、`m4-vs0-seed-enrichment-v0`、`mvp-contract-v0` 三个只读依赖，并在 start 端口负例按需复制 build；共享 `app/.next` 全程未移动或删除，失败轮次留下的精确临时目录已清理。
- 首次对抗审查指出共享 `.next` 并发风险、缺失 dev 后置负例及 SIGINT 收据先于清理三个有效问题；三项均按最小范围修正，没有隐藏原审查 FAIL，也没有扩大到其他探针。
- 没有把正常 start 的 happy path 扩张成所有异常已安全；端口占用与缺失 build 均有独立真实子进程负例。
- 没有增加 reason code、放宽 argv、hostname/port、runtime readiness、env/capability 或 no-egress 边界。

## 6. 结论

开发自测范围内为 `P0=0 / P1=0`。原安全 FAIL 的唯一 P1 已形成可运行候选并具备真实负例与完整检查证据，可交测试部和安全部按上述冻结 SHA 独立复验。独立复验通过前，本报告不构成统筹核收或 VS-0 整体放行。
