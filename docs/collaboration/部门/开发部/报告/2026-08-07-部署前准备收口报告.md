# 开发部 · 部署前准备收口报告（TASK-20260807-A55149）

> 部门：开发部（dev · 执行层）· 日期：2026-08-07
> 任务：搭建部署网页前的准备（构建验证与部署配置）
> 任务 JSON：`docs/collaboration/tasks/TASK-20260807-A55149.json`
> 类型：正式收口（实物此前已完成，本报告补正式收口并完成任务状态）

---

## 1. 任务目标

在 M4 本地边界内完成部署前准备：验证生产构建/启动闭环（`npm run build` / `npm run start`）；整理部署配置（环境变量、Next standalone 输出、CSP/缓存/外链跳转/图片代理要点）；产出部署准备文档并列出正式部署前必须由用户确认的门禁；**不进行真实外网部署**。

任务接受出口（`acceptance_exit`）：生产构建验证通过 + 部署准备文档产出 + 未触发真实外网部署 + 开发部提交报告与 `TASK_STATE_OK`。

---

## 2. 产物路径与覆盖点核验结果

**产物**：`app/DEPLOYMENT-PREP.md`（存在，Read 通读确认）。

| 任务要求覆盖点 | 文档章节 | 核验结果 |
|---|---|---|
| 上线前门禁清单 | §1（8 项） | ✅ 存在 |
| 生产构建/启动验证（verify:env / db:migrate / seed:fixtures / next build / npm run start） | §2 | ✅ 存在，含已验证明细与命令序列 |
| 配置要点：环境变量 | §3.1 | ✅ 存在（APP_BIND_HOST / NODE_ENV / REAL_* / 密钥注入） |
| 配置要点：Next standalone | §3.2 | ✅ 存在（可评估 `output: 'standalone'`，需回归 build/start 与公开 HTTP 矩阵） |
| 配置要点：CSP/缓存/外链跳转/图片代理 | §3.2 | ✅ 存在（CSP/nosniff/frame-ancestors/referrer、no-store 缓存协调、外链 scheme 校验、R6 边界内图片代理） |
| R2/R5/R12/R13 复核 | §4 | ✅ 存在（R2 origin/CORS、R5 umask/权限/realpath、R12 no-egress、R13 审计脱敏） |
| 静态预览部署说明 | §5 | ✅ 存在（设计草稿纯静态可独立托管，无后端依赖） |
| 检查清单 | §6 | ✅ 存在（9 项） |

**脚本定义核验**（只读，未运行）：`app/package.json` 已确认存在 `verify:env`（scripts/verify-env.ts）、`db:migrate`（scripts/db-migrate.ts）、`seed:fixtures`（scripts/seed-fixtures.ts）、`build`（next build）、`start`（node scripts/serve.ts start）等命令定义。未运行任何会触发外部网络/生产/启动服务的命令。

---

## 3. 构建链验证事实（引用 `docs/progress.md` 2026-08-07 收据）

`docs/progress.md` 2026-08-07 条目已记录并支撑以下已验证事实（Node 24.18.0 工具链 + 干净环境）：

- ✅ `verify:env` → `fixture/mock/manual_only`、`externalCalls: 0`
- ✅ `db:migrate` → SQLite 3.53.1、WAL、userVersion 3、busy_timeout 250、synchronous FULL
- ✅ `seed:fixtures` → `public-synthetic` / `mvp-local-v0.4` / `public-demo-12-v0.4`，12 条完整图，`syntheticOnly`、`externalCalls: 0`、`writesToBase: false`
- ✅ `next build` → Next 16.2.11（Turbopack）编译成功、TypeScript 通过、静态页 3/3；路由 `/`、`/api/health`、`/api/public/feed`、`/api/public/stories/[publicId]`、`/stories/[publicId]`
- 生产启动 `npm run start`：此前本机已验证 no-store 全绿；`next dev` 因 Next 16 注入 `NODE_OPTIONS` 与 R1 冲突，不用于公开 API 演示

**关键发现（ENV_FORBIDDEN）**：进程环境含任何禁止变量（代理类 `HTTPS_PROXY/HTTP_PROXY/ALL_PROXY/NO_PROXY`，令牌/密钥/凭证类如 `ANTHROPIC_AUTH_TOKEN`、`BRAVE_API_KEY`、`API_KEY` 等）会被 `verify:env` 以 `ENV_FORBIDDEN` 拒绝——R1 fail-closed 正确表现。**部署容器/进程必须以最小化、干净的环境变量启动**，仅注入 `app/DEPLOYMENT-PREP.md` §3.1 清单变量。

---

## 4. 正式部署未触发

本任务全程保持 M4 本地边界：未进行真实外网部署、未启动生产服务、未调用付费/外网 API、未修改 `app/` 代码。真实上线的运营主体/部署地域/数据区域/平台条款/容量等授权仍留待用户确认（见下节）。

---

## 5. 待用户确认的 8 项门禁清单（正式部署前的硬门槛）

按 `app/DEPLOYMENT-PREP.md` §1 逐项列出，均未确认、维持 M4 本地（fixture/mock、loopback、manual_only 发布）：

1. **运营主体**——部署方是谁（个人/组织），负责对外合规与责任
2. **部署地域**——服务器所在地（影响数据区域、跨境与访问合规）
3. **数据区域**——数据存储位置、是否跨境
4. **平台条款**——内容来源（Motorsport.com 等）与托管平台的条款遵守
5. **容量/预算**——实例规模、带宽、存储、成本与伸缩策略
6. **AI 跨境/模型**——若启用真实总结模型，数据出境与模型训练退出确认
7. **生产存储**——从本地 SQLite 到生产数据库（Postgres 等）的独立决策与验证
8. **多实例/网络文件系统**——生产多副本、文件存储一致性尚未验证

---

## 6. 错题自检

已检查 `docs/collaboration/错题集.md`（当前无条目），本任务未命中任何已沉淀错题。收口过程遵守：不把建议写成授权、不替审核层做最终质量/安全/发布放行结论、真实上线门禁统一由统筹部请用户逐项确认。

---

## 7. 结论

- 实物与构建链验证收据齐备，`app/DEPLOYMENT-PREP.md` 覆盖任务全部接受要求。
- 本任务为开发部 `complete` 收口，`TASK_STATE_OK` 由 `agent_team_task.py` 返回。
- 真实外网部署未触发；上线前 8 项门禁待用户逐项确认（由统筹部办理）。
