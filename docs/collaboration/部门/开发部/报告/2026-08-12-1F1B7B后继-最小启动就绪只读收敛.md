# 1F1B7B 后继｜最小启动就绪只读收敛

## 当前冻结状态

- Admin deployment-v3 manifest：`f4d581400b6ac3ae039c4e1d1d9db26061f66ae0014ff1439762ddd6b12423e8`，`prepared-disabled`，bind `127.0.0.1:3101`。
- Public deployment-v2 manifest：`3a4824e27aaa0658fe64b4b9f55a6bc437e0a969fa3a7d631e5c8729f4ddff3d`，receiver bind `127.0.0.1:3102`，read mode `public-real-snapshot`。
- review DB existing-only 身份：uid501、mode0600、nlink1、dev `16777233`、ino `24570709`；3101/3102 当前无 listener。
- Tailscale Serve/Grant 与 public cutover 保持关闭。

## 下一次单事务精确顺序

1. 前置门：只读复算两 manifest SHA、review DB dev/ino/uid/nlink、3101/3102 absent；固定 d186 app root 与 Node24。
2. Receiver：把 Public 私有根的 `com.f1plus1.public-projection.plist` 原子复制到 `~/Library/LaunchAgents/`，`launchctl bootstrap gui/501`，只读确认 3102 loopback listener；失败则 bootout receiver 并恢复 LaunchAgent 旧/缺失状态。
3. Admin：把 Admin 私有根的 `com.f1plus1.admin-service.plist` 原子复制到 `~/Library/LaunchAgents/`，bootstrap；运行时 `openExistingSafeDatabase` 只接受同一 dev/ino 的 user_version 1/2/3，按 0002/0003 原位迁至 3，随后绑定 3101。迁移后必须确认 DB dev/ino 不变、user_version=3、WAL/synchronous=FULL/foreign_keys=ON；失败先 bootout Admin，保留 DB/WAL/审计事实，停止事务。
4. Loopback 健康门：确认 3102 仅 loopback；确认 3101 仅 loopback且安全中间件对缺失 Tailscale headers fail closed。不得用公网或 Serve 验收替代。
5. Sender：Admin runtime 本身已经每 60 秒单 worker sender tick；不要再常驻启动第二 sender。只在 receiver与Admin健康后允许一次 `scripts/projection-sender.ts --manifest <Admin manifest> --once` 作为显式首轮；随后由 Admin 内置 sender 接管。失败停止后续、保留 outbox/receipt，不切 public。
6. 提交门：receiver/Admin job均 loaded且3102/3101健康、DB同inode v3、sender receipt确定；保持 Tailscale Serve/Grant 未配置，保持 public live/read mode未切换。

## 精确执行入口

- Receiver：d186 `scripts/public-projection-runtime.ts --manifest '[M1-HOME]/Library/Application Support/F1Plus1/Public/projection-deployment.json'`（由 prepared plist 固化）。
- Admin：d186 `scripts/admin-service.ts --manifest '[M1-HOME]/Library/Application Support/F1Plus1/Admin/deployment.json'`（由 prepared plist 固化）。
- 首轮 sender：固定 Node24 + d186 `scripts/projection-sender.ts --manifest '[M1-HOME]/Library/Application Support/F1Plus1/Admin/deployment.json' --once`。

## 唯一真正阻断输入

- 用户需确认唯一 iOS peer 是拟授权 iPhone，并授权管理面最小 Grant；该输入只阻断后续 Tailscale Serve/外部设备验收，不阻断纯 loopback 启动事务。
- 真正执行 bootstrap 与 DB 原位迁移仍需新的用户授权，因为会改变运行态和数据库 schema。
