# 开发部 · 交接班文档

> 角色 ID:`dev` ·最近更新:2026-08-09
> 这是本部门的**语义交接**(给接班的人看),不是任务状态真值或流水账。任务所有权、状态和产物只认 `../../tasks/TASK-*.json`;若两者冲突,以任务 JSON 为准并修正本文件。
> 铁律:从这里删掉的档案级事实,必须先用 `../../scripts/agent_team_log.py append` 追加到本周日志末尾,绝不直接丢;普通过程不记。

## 当前任务补充说明

> 只记录任务 JSON 不适合承载的恢复信息。没有时写“无”;干活时不刷收件箱。

- 当前任务:`TASK-20260809-8FAD30` 已交付公开阅读闭环第一波，报告为 `报告/2026-08-09-公开阅读功能闭环第一波实现报告.md`；任务状态以 TASK JSON 为准，等待统筹核收。
- 当前实现:`PUB-DETAIL-001/004/005` 的成功、404、503、同对象重试与返回信息流已在最终 production 候选闭环；`UI-TOP-001` 鼠标/键盘回顶为 y=0 且焦点回主区；`UI-ACCORDION-001` 连续互斥、整条居中、外点关闭闭环；非法主题 cookie 与 cookie-only 刷新/重开闭环，存储不可用时客户端沿用已白名单化的 SSR cookie 主题。390 Escape 焦点当前候选精确回原主图。
- 当前边界:`UI-TOP-001/UI-ACCORDION-001` 的 reduced-motion 实机、`UI-THEME-002` 的真实 Storage 抛错实机与本轮 1024 视觉复验继续为 P1 successor；P1-03 多图、真实 provider/采集/外部 I/O/admin/发布/部署继续关闭。

### 2026-08-08 Git 基线历史补充（仍有效）

- `TASK-20260808-9EE352` 已完成只读溯源与安全 Git 基线清单，报告为 `报告/2026-08-08-前端验收后改动溯源与安全Git基线报告.md`，执行清单为 `报告/2026-08-08-TASK-20260808-9EE352-Git基线白名单.md`。
- 当时工作区为 11 个 tracked 修改 + 765 个 untracked 文件，不能使用 `git add .`；五个目标前端文件均无 Git blob 基线。此后 33533E 按已 ACK 最终设计/产品合同正式吸收五文件，但仍未执行 Git 暂存或提交。
- 迁移冲突:`migration/CURRENT-HANDOFF.md` 写明已删除的 `bundles/`、`manifests/`、`portable-assets/`、`scripts/` 当时重新出现；与正式迁移证据重叠，保持原样等待统筹裁决。

### 2026-08-07 历史补充（仅供恢复）

- 已做到:`TASK-20260807-A55149`(部署前准备)实物已完成——`app/DEPLOYMENT-PREP.md` + Node24 干净环境构建链全绿,但任务未正式收口(仍 `queued`)。
- 关键中间结论:运行环境含代理/密钥/令牌类禁止变量会被 `verify:env` 以 `ENV_FORBIDDEN` 拒绝(R1 fail-closed 正确表现),部署容器必须以最小化干净环境启动。
- 相关产出路径:`app/DEPLOYMENT-PREP.md`

## 已定、不再回退的决策

- v0.2 时间线设计已由用户确认方向;落地必须保留 M4 安全门禁(无外联、REAL_* fail-closed、`externalCalls=0`),不新增后端实体,不扩 DTO。
- 公开 feed/detail DTO 与 SQLite 投影不变;v0.2 只改前端视觉与交互层。

## 下一步

- 等统筹部核收 `TASK-20260809-8FAD30`；按报告 §8 拆 reduced-motion、Storage 抛错与 1024 实机 successor。后续真实多图、异常存储/触发元素卸载故障注入或辅助技术实机复验必须使用独立任务，不得在当前 DTO 上伪造媒体。
- Git 基线仍须另开独立任务按 9EE352 白名单逐文件处理；不得使用 `git add .`、`git add -u` 或目录通配符。
- `TASK-20260804-3AF992` 已由协议工具取代；`TASK-20260804-C25855` 作为替代链审计锚点继续 blocked。不得手改旧任务 JSON。

### 2026-08-07 历史下一步（已过期，不作为当前指令）

- 新开发部会话接班后**先收口 A55149**:补正式收口报告(指向 `app/DEPLOYMENT-PREP.md` + 构建链验证收据)并 `complete`,再通知统筹核收。这是并行波 1 的一部分,不依赖产品合同。
- 然后 claim `TASK-20260807-6CD2FB`(将 v0.2 时间线设计落地到公开信息流前端),**前置条件:产品部 `EA00A8` 合同就绪**。合同未就绪前可先做代码侧准备(不改正式实现),但不得绕过门禁。
- 既有阻断任务 `3AF992`(no-store)、`C25855`(安全启动收据)待补正式收据后关闭或标 superseded。

## 已知坑 / 未决问题

- 浏览器端 fetch 会被 CORS 拦截,真实抓取必须走服务端采集链路,网页只读。
- Next 16 dev(Turbopack)会注入 `NODE_OPTIONS`,与 R1 fail-closed 冲突导致 dev 模式公开 API 返回 `ENV_FORBIDDEN`;演示走 `npm run start`。
- 不得把 dev 模式/探针写成正式选型。

## 关键文件指针

- 当前任务真值:`../../tasks/TASK-20260809-8FAD30.json`
- 当前报告:`报告/2026-08-09-公开阅读功能闭环第一波实现报告.md`
- Git 基线历史报告:`报告/2026-08-08-前端验收后改动溯源与安全Git基线报告.md`
- Git 基线历史清单:`报告/2026-08-08-TASK-20260808-9EE352-Git基线白名单.md`
- 设计冻结/产品合同:`../../tasks/TASK-20260808-54BB47.json`、`../../tasks/TASK-20260808-00A9C9.json`
- 历史任务真值:`../../tasks/TASK-20260807-A55149.json`、`../../tasks/TASK-20260807-6CD2FB.json`
- 部署准备:`../../../../app/DEPLOYMENT-PREP.md`
- 落地依赖的产品合同:`../../tasks/TASK-20260807-EA00A8.json`
