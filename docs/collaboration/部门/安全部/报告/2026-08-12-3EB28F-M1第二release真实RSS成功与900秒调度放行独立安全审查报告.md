---
type: audit_report
department: 安全部
target: "TASK-20260812-3EB28F / C682F8 M1 one-shot runtime evidence"
status: final
date: 2026-08-12
related_task: TASK-20260812-3EB28F
decision: pass
severity_count: { P0: 0, P1: 0, P2: 1 }
tags: [RSS-REAL-001, M1, HTTP-200, stop-fence, pending-review, public-zero-drift, scheduler-gate]
summary: "PASS。独立SSH只读回读确认同一第二release运行闭包、Node、deployment/plist与执行器身份；唯一成功run为HTTP 200、externalCalls=1、50/20/20、response SHA匹配，20/20新候选均为pending_review且机器无效/人工字段计数均为0；唯一scheduler_gap身份与三条run分布匹配；source当前disabled、stop_epoch=4、RSS job unloaded，独立20秒短窗run/candidate零新增；公开DB身份、四页面HTTP 200及三项服务状态与C682F8基线一致。P0=0/P1=0/P2=1；P2仅为既有非空人工字段遇真实更新的动态保护仍Unknown。允许下一独立任务在硬前置复核后启用900秒调度，本任务未enable/load/collector。"
---

# M1 第二 release 真实 RSS 成功与 900 秒调度放行独立安全审查报告

## 1. 结论

**PASS；P0=0，P1=0，P2=1。**

同一候选的四项运行门均已独立成立：

1. 唯一真实RSS运行成功且收据闭合：`externalCalls=1`、HTTP `200`、`item/selected/new=50/20/20`；
2. wrapper后停采闭合：source `enabled=0`、`stop_epoch=4`、RSS LaunchAgent明确unloaded；
3. 候选合同闭合：`20/20 pending_review`、`machine_invalid=0`、新候选人工字段均为空；
4. 公开面闭合：公开DB身份无漂移、local/public home/detail均为HTTP `200`、三项既有服务状态与基线一致。

因此，**允许统筹部创建下一独立任务，在第8节硬前置全部实时满足后启用900秒调度**。本审查没有执行enable、resume、bootstrap、load、wrapper、collector、清理或文件/DB修改；唯一仓库写入是本报告。

## 2. 审查对象与方法边界

审查输入为已ACK的`TASK-20260812-C682F8`及其开发报告，审查合同为`TASK-20260812-3EB28F`。本轮通过既有SSH alias对M1执行白名单式只读回读：文件SHA/stat、固定`rss-control status`、`sqlite3 -readonly`、`plutil`、`launchctl print`、公开DB SHA/stat及四个既有公开页面GET。

没有发起RSS请求，没有执行候选wrapper或collector，没有加载RSS LaunchAgent，没有改变source、DB、plist、release、日志、收据或公开数据，没有清理任何stage、backup、执行器或收据。

## 3. release、运行时与部署身份

### 3.1 固定身份

| 对象 | M1只读实值 | 结论 |
|---|---|---|
| release manifest SHA-256 | `34c1032cc6adca96990cf35b092c2d61b940a2bda8fdd53a0c8ccfbb6e7c8d95` | MATCH |
| manifest `contentRootSha256` | `2aae04864325f9db436b72267b578df681efdc51f79f81d8348a5490b5e8c153` | MATCH |
| manifest `releaseSha256` | `6d402a5d0b81fcf2d20e9720e3d0eef5f6e2ea63bb939ca8c7921df85159eb02` | MATCH |
| deployment manifest SHA-256 | `1f681429aa56a49e1b0bfb3ea6405db07ffacd0aa8e5b339dbf1909522cfad50` | MATCH |
| RSS plist SHA-256 | `60cd3b0d96b2b7bdf652ffb190288197db2685de4ece6fa2e7daae12c0ad787e` | MATCH |
| Node二进制SHA-256 | `ee6fb0e015284d83a91e8ec5213f43a157f8a392b58555301682892ba928c04a` | MATCH |
| Node版本 | `v24.18.0` | MATCH |
| transport SHA-256 | `a55b76fa8899173341671a3c8e13d67c47e6f74d8fd6e0ff68a29e1abc134028` | MATCH |
| 三阶段执行器SHA-256 | `9067b5a08ead7282ea558c57cfbebcaed6d0ed0b71c9b4d22944fc041c9c38be` | MATCH |

固定`rss-control status`以外部manifest SHA锚点重新验签正式运行闭包、递归生产依赖、Node及deployment/plist绑定后，返回`status=observed`、`externalCalls=0`、source disabled/epoch4、LaunchAgent unloaded。

### 3.2 权限与链接身份

- release manifest、deployment manifest、plist、执行器、private DB及collector stdout/stderr日志均为UID `501`、单硬链接、mode `0600`；
- 固定Node为UID `501`、单硬链接、mode `0755`；
- collector两份日志当前均为`0` bytes；
- 未发现目标文件为symlink或存在组/其他用户写权限。

## 4. private DB、run与scheduler gap

独立以只读SQLite连接回读，`PRAGMA integrity_check=ok`。固定产品status入口的schema门通过，source唯一行当前为：

`motorsport-f1-news | enabled=0 | stop_epoch=4 | last_reason_code=OK | last_attempt_at非空 | last_success_at非空`

全部run共三条，且均已终态：

| run类别 | slot | status / reason / next_action | HTTP与计数 |
|---|---:|---|---|
| 旧诊断run | `1984973` | `failed / NETWORK_FAILURE / next_slot` | HTTP为空，所有计数为0 |
| 唯一gap | `1984981` | `scheduler_gap / SCHEDULER_GAP / manual_review` | 所有计数为0 |
| C682F8成功run | `1984982` | `succeeded / OK / next_slot` | HTTP `200`；`50/20/20/0/0` |

C682F8成功run的其余固定字段为：

- `run_id=rss-run-1984982`；
- `validator_result=modified`、`validator_capability=unknown`；
- response bytes `67818`；
- response SHA-256 `09754466c27b4a1cdac5af9f6b5817fc716c0db08814d3a6f0dde3e5d3017d7f`；
- `item/selected/new/updated/duplicate=50/20/20/0/0`；
- `finished_at`非空，running run为0。

唯一scheduler gap为`rss-gap-1984981`，身份、slot、reason、next action和零计数均与后继slot合同一致；没有第二条scheduler gap。

## 5. 候选与人工字段

候选聚合只读结果：

| 字段 | 结果 |
|---|---:|
| candidate总数 | `20` |
| `review_status=pending_review` | `20` |
| machine-invalid | `0` |
| `editor_title`非空 | `0` |
| `editor_excerpt`非空 | `0` |
| `editor_notes`非空 | `0` |
| `editor_based_on_source_revision`非空 | `0` |
| revision范围 | `1..1` |

这组证据证明本次20条新候选没有被机器写入人工字段，且全部进入人工审核队列。它没有提供“执行前已经有非空人工字段、同一外部条目发生真实更新”的动态样本；该边界单列为P2，不阻断本次调度门。

## 6. 收据、停采与当前短窗

### 6.1 七件任务收据

七件收据均为UID `501`、mode `0600`、单硬链接。独立实算SHA-256如下：

| 收据 | SHA-256 |
|---|---|
| baseline | `132c05534892073e2c6337116d05c98e28adce56e6e43d3a4e930e38beb9da82` |
| enable | `890ea7af251dd7a5a20ba502021abb7c82d0093be1ad9dcf4898425d46484bfc` |
| combined run-stop | `1af27aa55bbe75fc1b4a4c7b73a3c60d37a18a40276f3b685bc49bc016ed6e00` |
| postflight | `4ee5b1feb0f91fdc26efabb73dd3c625599d3e85a440753ff82174684a05fd08` |
| wrapper | `f107037e9fd61237a36458eae0fc04ca8dfb3d825efa141880445ea860a6e875` |
| stop process | `d5b99e11671445b9e2da01653b9b26131d6ada6ca3e9dea50851aa980866995d` |
| stop | `0f7c9cf5c6f11e0249dd841ff9fadadd6ed1fd9859c2b3c60f082dd157f92628` |

wrapper收据为`succeeded/OK`、`externalCalls=1`、HTTP `200`，run/slot/response SHA与DB逐字段一致。stop收据为`stopped-confirmed`、`externalCalls=0`、source disabled/epoch4、LaunchAgent unloaded。combined最终安全状态为`confirmed=true`、`controlStopHealthy=true`、无emergency路径。

### 6.2 独立当前20秒短窗

本审查在同一只读SSH会话内取两次private DB快照，中间等待20秒：

`snapshot A = run 3 / max slot 1984982 / candidate 20 / enabled 0 / epoch 4 / OK`

`snapshot B = run 3 / max slot 1984982 / candidate 20 / enabled 0 / epoch 4 / OK`

两次结果逐字段相等。随后`launchctl print gui/501/com.f1plus1.rss-collector`返回service not found，独立确认job仍为unloaded。短窗内没有新增run、候选或source状态变化。

## 7. 公开面零漂移

### 7.1 公开DB

当前公开DB实值：

- SHA-256：`949c78d505e4c032d2495174deaf62d24f9d99b76284ad7ba6fb29a5ac83bb50`；
- size：`737280`；inode：`24546198`；
- UID `501`、mode `0600`、单硬链接。

以上与C682F8 baseline和postflight逐字段一致。

### 7.2 页面与既有服务

- local home：HTTP `200`；local固定detail：HTTP `200`；
- public home：HTTP `200`；public固定detail：HTTP `200`；
- 公网origin只记录脱敏SHA-256：`a472476b3947308e89b613c9c82b0f91c3c72cb9c4cbc9d82301e6573e075b9f`，与C682F8收据一致；
- `com.f1plus1.public-beta`：running，PID `52089`；
- `com.f1plus1.quick-tunnel`：running，PID `53446`，当前last exit仍为既有`1`；
- `com.f1plus1.receipt-refresh`：已加载、周期空闲、last exit `0`。

三项服务的状态/PID/last-exit语义与C682F8 baseline/postflight一致，公开面未发现漂移。

## 8. 900秒调度放行与硬前置

本报告放行的是**下一独立调度任务**，不授权在本任务内补做操作。下一任务只有同时满足以下条件才可enable/load：

1. 操作前实时重验release manifest=`34c1032c…c8d95`、deployment=`1f681429…ad50`、plist=`60cd3b0d…787e`、Node及正式运行闭包仍MATCH；
2. private DB schema/integrity仍通过，source仍为disabled/epoch4，RSS job仍unloaded，当前没有running run；
3. 只使用已验收的固定control入口启用调度，不手工运行wrapper/collector，不删除或覆盖C682F8七件收据；
4. enable/load后立即回读source/job/日志/首个调度收据；任何身份漂移、重复slot、无法确认stop围栏或公开面漂移，立即按已验收control stop停采并阻断后续；
5. 900秒运行结果、持续调度行为和后续真实更新人工字段保护均由下一任务单独留证和裁定，不由本次单次成功外推。

## 9. Finding与Unknown

### P0

无。

### P1

无。

### P2-01：已有非空人工字段的真实更新保护缺少动态样本

本次20条均为首次创建且revision=1，人工字段初始为空。静态候选此前已有保护审查，但本轮M1运行数据无法动态证明已有非空人工内容在真实RSS更新时保持不变。状态：**Unknown / P2，非本次900秒启用阻断项**。后续一旦自然出现同一external item更新，应只读核对机器字段/revision变化与四个人工字段保持；禁止为制造样本修改正式candidate。

## 10. 未验证与自审

### 未验证

- 900秒LaunchAgent尚未enable/load，本轮没有观察连续调度周期；
- 已有非空人工字段遇真实RSS更新的动态保持；
- RSS源未来可用性、未来响应内容与远端网络稳定性。

### 自审

- 没有把开发报告作为唯一证据；关键SHA、DB、receipt、source/job、短窗及公开面均独立回读；
- 没有读取或写入RSS正文、响应头、candidate ID、IP或未脱敏业务内容；
- 没有把新候选人工字段为空扩大为既有非空人工字段动态保护已证实；
- 没有把单次HTTP 200扩大为未来900秒调度必然成功；
- 没有执行collector/wrapper、enable/resume/bootstrap/load、DB/文件修改、清理或外部RSS请求。

TASK_STATE_OK
