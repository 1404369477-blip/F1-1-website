---
type: audit_report
department: 安全部
status: final
date: 2026-08-04
task_id: TASK-20260804-A1A095
related_task: TASK-20260804-A1A095
domain_stage: SQLite启动能力P1安全复验
decision: pass
severity_count: { P0: 0, P1: 0, P2: 0 }
target: "TASK-20260804-A01DF7 + TASK-20260802-3760F6 final candidate"
tags: [M4, SQLite, Next, security-review]
summary: "PASS：运行时数据库覆盖符号已不可达；两个真实CLI负例各执行一次，分别安全拒绝为DB_PATH与CLI_INTERNAL_ERROR，未写随机库、未泄露Next诊断，清理后无数据库、sidecar、.next或端口残留。"
---

# 真实 CLI 数据库覆盖与 Next 安全出口聚焦复验

## 结论

- SQLite：`PASS / P0=0 / P1=0 / P2=0`。
- Next missing-build 安全出口：`PASS / P0=0 / P1=0 / P2=0`。
- 整体：`PASS`。

本结论只覆盖 `TASK-20260804-A01DF7` 的单点整改及 `TASK-20260802-3760F6` 的 missing-build 安全出口，不扩展为 R5、R12、Repository/API、真实外部能力或部署放行。

## 静态审查

- 五个整改文件 SHA-256 与开发报告逐项一致：
  - `config/env.ts`：`7158d2d4233d24326a08abf80ec247c07527627f987a0a7c4370f388e5909877`
  - `runtime-config.ts`：`c2d2d209f6e2851066aec7e7ff1f21b67414c5389685e527ae14abd024e3be92`
  - `db/profile.ts`：`a0cd5842b8eebfb212ff169c643fcadad370277416f5400fc4847ff4f9c751b9`
  - `p1-cli.test.ts`：`c935d176f7e7a2e2e2fc699099cdd2ed3c21cf43f6a0649d81cf888b6cec8fb7`
  - `runtime-profile-boundary.test.ts`：`d0f637bf613b9588fd835fcbd4fa01aa2dbcb2a65f8dfbfa827d2963bd519ec2`
- `AppConfig`、`loadAppConfig()`、`loadRuntimeConfig()` 与 profile 边界均不再包含 `allowTestDatabasePath` 或 `testDatabasePathOverride`；`NODE_ENV`、`APP_ENV` 和环境值不能启用随机数据库路径。
- `loadAppConfig()` 对 `m3-shadow` 与 `public-synthetic` 分别逐字绑定 canonical SQLite 路径；随机 `F1_DB_PATH` 在 fixture/数据库打开前以 `DB_PATH` 拒绝。
- `serve.ts` 仍在 Next 子进程前执行 argv gate 与 runtime readiness；子进程参数仍固定 `127.0.0.1:3000`，stdio 继续由父进程 pipe 并排空，SIGINT/SIGTERM 边界未放宽。
- `serve.ts`、`p1-acceptance.ts`、`security/cli.ts`、`security/log.ts`、`package.json`、lockfile 与 `TASK-20260802-3760F6` 冻结 SHA 一致；`TASK-20260804-253A43` 中除本次明确整改的三个实现文件外，其余 migration、seed、database、public graph、source 及目标测试 SHA 均与原报告一致。
- 四个包外信任根仍逐字为：`3b296868…e554`、`1f771949…af1`、`34ecfa83…b81a`、`058be83…60c`。

## 两个真实 CLI 负例

两个负例均使用项目固定 Node `24.18.0` / npm `11.16.0`，各执行一次。

1. `NODE_ENV=test` + 随机 `F1_DB_PATH=.local/security-a1a095-random.sqlite` 执行 `npm --silent run start`：exit `1`，合并应用输出逐字只有：

```json
{"event":"cli_failure","status":"rejected","reasonCode":"DB_PATH","externalCalls":0}
```

随机数据库及 `-wal`、`-shm`、`-journal` 均未产生，3000/3101 未监听。

2. 以 canonical `m3-shadow` 完成 v3 migration 和 59 条 disabled seed，确认 `.next` 不存在后执行 `npm --silent run start`：exit `1`，合并应用输出逐字只有：

```json
{"event":"cli_failure","status":"rejected","reasonCode":"CLI_INTERNAL_ERROR","externalCalls":0}
```

输出未出现 Next banner、URL、`Error`、stack、源码/绝对路径、address 或 port。

## 清理、未验证与错题自检

- 已精确删除本轮 canonical M3 数据库及 `-wal`、`-shm`、`-journal`，随机数据库目标同样不存在；`.next` 不存在，3000/3101 无监听。
- 按任务的高性价比边界未 build、未运行 full check 或测试套件、未复制 `node_modules`、未修改正式代码。
- 没有把 migration/seed 准备命令计作负例 PASS；没有继承开发部或测试部的行为结论；没有把本次窄范围 PASS 外推到未实现系统能力。

TASK_STATE_OK
