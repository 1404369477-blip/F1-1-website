---
type: audit_report
department: 安全部
status: final
date: 2026-08-02
related_task: TASK-20260802-4CE4F0
domain_stage: M4-B-independent-security-review
execution_mode: local_read_only
decision: pass
target: app/package.json; app/package-lock.json; app/.node-version; app/.nvmrc; app/.npmrc; app/.env.example; app/.gitignore; app/scripts/pending.mjs; app/README.md; TASK-20260802-F8BF72; TASK-20260802-D2724D
tags: [M4, B层, package-lock, integrity, lifecycle, defaults, read-only]
summary: "B 层静态安全复验 PASS：P0=0、P1=0；package/lock 精确版本、官方 resolved、完整 integrity、env/npmrc/gitignore/pending 拒绝边界均通过。sharp 与 unrs-resolver 的 hasInstallScript 及 64 个 optional/native 条目仅作锁元数据，未发现 node_modules 或生命周期执行证据；Node24/npm ci/runtime 留 C 层 pending。"
---

# TASK-20260802-4CE4F0：M4 B 层依赖锁与安全默认地基复验

## 1. 唯一结论与范围

本任务给出唯一判定：**PASS**。

| 计数 | 结果 | 说明 |
|---|---:|---|
| P0 | 0 | 未发现官方源、完整性、精确版本、默认安全边界或敏感信息泄露阻断项。 |
| P1 | 0 | lifecycle/native 风险已从 lock 元数据与实际执行事实分层；未发现执行迹象。 |
| 总判定 | **PASS** | 满足任务要求的 B 层静态安全出口。 |

本轮只读核对已 ACK 的 TASK-20260802-F8BF72、TASK-20260802-D2724D、当前 app 静态地基及其报告。没有修改 app、package、lock、配置或上游任务；没有联网、安装依赖、执行 npm ci、运行任意项目脚本、触发 lifecycle、创建 node_modules 或访问真实 provider/Base/平台/外部资源。

本 PASS 只覆盖 B 层 package/lock 与安全默认地基。Node 24.18.0 实机运行、npm 11.16.0 npm ci、license/runtime、SQLite、构建、测试、业务切片和安全运行收据继续属于 C 层，保持 pending/unverified。

## 2. 上游收据与历史边界

| 收据 | 当前事实 | 证据 |
|---|---|---|
| TASK-20260802-F8BF72 | execution_state=acknowledged；原始静态初始化报告保留其因官方 registry/DNS 阻断形成的 PARTIAL 历史 | docs/collaboration/tasks/TASK-20260802-F8BF72.json:28,41-55；开发部初始化报告:3-16 |
| TASK-20260802-D2724D | execution_state=acknowledged；后续 lockfile 恢复报告给出 B 层 PASS（P0=0，P1=0） | docs/collaboration/tasks/TASK-20260802-D2724D.json:25,38-55；开发部 lockfile 报告:3-12 |
| 本任务 | 已正式 claim，仅复验，不覆写上游任务 JSON 或报告 | docs/collaboration/tasks/TASK-20260802-4CE4F0.json |

F8BF72 的 PARTIAL 是当时 DNS/lockfile 未恢复的历史事实；D2724D 的后续 lockfile 恢复是新的静态收据。本报告保持两者的时间顺序。

## 3. package/lock 精确版本与官方来源

### 3.1 根合同与直接依赖

| 检查 | 结果 | 证据 |
|---|---:|---|
| package-lock lockfileVersion | 3 | app/package-lock.json:1-6 |
| 根 name/version | f1-plus-1-app / 0.0.0，与 package.json 一致 | app/package.json:1-4；app/package-lock.json:7-10 |
| 根 engines | Node 24.18.0、npm 11.16.0，与 package.json 一致 | app/package.json:7-11；app/package-lock.json:24-27 |
| packageManager | npm@11.16.0 | app/package.json:11 |
| runtime dependencies | next 16.2.11、react 19.2.0、react-dom 19.2.0 | app/package.json:26-29；lock 根及直接条目 |
| devDependencies | 6 项精确版本，与 lock 根及直接条目一致 | app/package.json:31-37；本轮内存断言 |
| 根 requires | true | app/package-lock.json:4-6 |

独立 Python 标准库断言确认：package 与 lock 根依赖键集合、精确字符串、直接 node_modules/<name>.version 均完全一致；没有 latest、canary、preview、git/file/workspace 或其他浮动直接版本。

### 3.2 resolved 与 integrity

当前 lock 共 400 个 package 条目，除根条目外 399 个：

- resolved：399/399 均以 https://registry.npmjs.org/ 开头；没有非官方域、非 TLS HTTP、git、file、workspace、github/gitlab/bitbucket 解析。
- integrity：399/399 均存在且为 sha512 格式。
- lock 元数据中没有 lifecycle script body；只出现 npm package metadata 字段。
- 当前文件 SHA-256：app/package.json = a611082420a6e8e77c0040af5caf6ce8237687730d6a07b6df55502c38ba8670；app/package-lock.json = de0eb658c3e407b17d6c94466f4766a343997b7e11d88e60c52d2dccd8544b83。

官方源可追溯性来自 lock 中每个 resolved URL 及 D2724D 的生成收据；本任务未重新访问 registry，符合不得联网的范围。

## 4. hasInstallScript、optional 与实际执行边界

lock 中有 2 个 hasInstallScript=true 条目：

| package | version | 当前结论 |
|---|---:|---|
| sharp | 0.34.5 | 仅为 lock package metadata；未推断为已执行。 |
| unrs-resolver | 1.12.2 | 仅为 lock package metadata；未推断为已执行。 |

另有 64 个 optional=true 条目，主要为 Next SWC、sharp/libvips、unrs-resolver 平台 binding、@emnapi/@napi-rs 等跨平台 native/WASM 包。它们说明未来安装或运行时需要 C 层隔离验证，当前不构成已执行事实。

本轮和 D2724D 收据均明确没有执行 npm ci、安装、preinstall、install、postinstall 或 prepare；app/node_modules 不存在，.next、out、build、.local 也不存在；没有 app 内 npm/lifecycle 日志。生成 lock 的上游上下文使用 package-lock-only 与 ignore-scripts，本任务不重跑该命令。因此本项判为 P1=0。

## 5. npm、环境与敏感信息门禁

app/.npmrc 仅包含 engine-strict=true、ignore-scripts=true、audit=false、fund=false。精确 Node/npm 栅栏不会静默放宽，第三方生命周期默认不执行。

app/.env.example 的安全默认是：APP_BIND_HOST=127.0.0.1、APP_PUBLIC_ORIGIN=http://127.0.0.1:3000；SOURCE_CONFIG_PROVIDER=fixture、ADAPTER_MODE=mock、SUMMARY_MODE=fixture、MEDIA_MODE=fixture；PUBLISH_MODE=manual_only、ADMIN_ACCESS_MODE=local_dev_only；REAL_FEISHU_IO=false、REAL_EXTERNAL_IO=false、REAL_FORM_SUBMIT=false；DB 使用 .local/f1plus1.sqlite，fixture 使用本地 M3 shadow batch。

本轮解析全部 16 个环境键，没有发现 token、secret、password、API key、credential、private key 或 proxy 变量；没有秘密样式字面量；没有非 loopback URL。app/.env、.env.local 等真实环境文件不存在。

app/.gitignore 忽略 node_modules/、.local/、.env、.env.*，保留 .env.example，并忽略构建输出。app/scripts/pending.mjs 只读取 npm lifecycle 名称，打印 C 层仍 pending 并设置 process.exitCode = 1；本轮只读源码，没有执行该脚本。静态扫描未发现 fetch、axios、node-fetch、undici、child_process、exec、spawn 或秘密字面量。

## 6. B 层验收矩阵

| 检查项 | 结果 | 依据 |
|---|---:|---|
| 精确 Node/npm/Next/React 版本 | PASS | package、lock 根与直接条目一致 |
| 官方源与完整 integrity | PASS | 399/399 official HTTPS resolved，399/399 sha512 |
| 浮动、git、file、workspace、非官方源 | PASS | resolved/spec 字段定向扫描无命中 |
| hasInstallScript/optional 分层 | PASS | 2 个脚本元数据、64 个 optional；无 node_modules/执行证据 |
| npmrc/env/gitignore | PASS | engine-strict、ignore-scripts、loopback/fixture/mock/manual-only/REAL=false、敏感文件保护 |
| pending 脚本拒绝 | PASS | 源码显式非零退出；未将占位命令当作实现收据 |
| B 层 P0/P1 | **0/0** | 本轮唯一总判定 PASS |

## 7. C 层未验证项、停止条件与错题自检

Node 24.18.0 实机运行、npm 11.16.0 npm ci、依赖许可证、安装后真实树、native binary 加载、lifecycle 隔离、运行时供应链、verify:env、SQLite/WAL/CAS、worker、HTTP/CSRF、lint/typecheck/build/test/security/deny-all、本地端口、业务切片及所有真实 provider/Base/平台/AI/媒体/发布/部署能力均留在 C 层 pending。当前 Node25/npm11.8 不替代精确版本证据。

若后续发现任一 resolved 非官方、integrity 缺失、版本漂移、秘密或真实外联默认值、node_modules/lifecycle 执行证据，或要求联网、安装、运行脚本、修改 app/lock/config，应立即停止并另建授权任务。

错题自检：保留 F8BF72 的历史 PARTIAL；没有把 hasInstallScript/optional 当成已执行；没有用 Node25/npm11.8 冒充 Node24/npm11.16；没有运行 npm ci 或项目脚本；没有把 package-lock-only 收据扩展成 live runtime/license/native 安全通过；没有把 fixture/mock/manual-only 与 REAL=false 写成真实外部能力。

## 8. 收口

本报告 status=final、decision=pass，B 层 P0=0/P1=0；已验证、未验证、停止条件和错题自检均已列明。C 层运行、依赖安装、业务实现和真实外部能力保持关闭。

TASK_STATE_OK
