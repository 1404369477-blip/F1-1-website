---
title: VS1 00591f contract恢复链候选独立安全复验报告
type: audit_report
department: 安全部
target: VS1最终00591f contract候选
status: final
date: 2026-08-09
related_task: TASK-20260809-BCF8B1
decision: pass
tags: security,vs1,no-egress,receipt,hash
summary: 最终00591f contract候选一次clean-room运行通过25 cases，externalCalls为零，closed脱敏收据与core hash可独立回算，保护生产hash零漂移且无临时残留
---

# VS1 `00591f…` contract 恢复链候选独立安全复验报告

## 1. 唯一结论

**PASS。P0=0，P1=0，P2=2。**

本轮只审查 `TASK-20260809-3A8C0E` 已 ACK 的最终 contract 候选。唯一固定 Node24 clean-room `npm run test:contract` 命令 `exit 0`；25/25 case 均 `assertionResult=PASS`、逐 case `externalCalls=0`，顶层 `externalCalls=0`。closed assertion receipt 文件 hash 与任务真值匹配，core hash 可从 closed core 独立回算且与内嵌值逐字一致。旧 worker/pipeline/fixture/no-egress/fixtures/migrations/package/lock/设计保护 hash 命令前后零漂移，精确临时前缀残留为 0。

本 PASS 仅覆盖最终 `00591f…` 本地 synthetic contract 入口的 no-egress、恢复链断言、脱敏 closed receipt 与 hash；不覆盖 worker、build、check、浏览器、OS/系统调用级隔离、生产或真实外部能力。

## 2. 最终三个 hash

| 对象 | 任务真值 | 独立结果 |
|---|---|---|
| `app/scripts/vs1-contract.ts` | `00591f56b1d148113259bfb21d227138fc7b177a25240befcc1391dcdc73b1e4` | MATCH，命令前后相同 |
| assertion receipt 文件 | `2fcaefd2998ad9b5c4885c5d7b64a1ee290be4c9cf898b98a19f85c99ce08750` | MATCH，命令前后相同 |
| 内嵌 core hash | `98258fe9591688216707afc16dc7a84c8b1a6d370cd9851c275ee3a0e5992e85` | MATCH，独立回算相同 |

core 回算输入严格为 UTF-8 的紧凑 JSON：`{schemaVersion,cases,externalCalls}`，字段顺序与生成合同一致；没有把外层 event/status/hash 本身混入 core。

## 3. 保护生产 hash 零漂移

| 对象 | SHA-256 | 命令前后 |
|---|---|---|
| 冻结设计 | `5a84bfb27294ebd727369118a95528f5b788bfacbe2d56cc03fcb006f6168cb1` | MATCH |
| worker | `57fcea6ac269daccce8a21072198b4ccc3f0529823a79383a97d0a3af67de814` | MATCH |
| pipeline | `a74240b8d479cfec2fd0e83bc6146fd05ab6b85e12e7149d4b016dc1b92cf806` | MATCH |
| fixture loader | `7c21bf9e3e0c38a166a831613118daf0f3fcf837d08ca6723553e732133326e9` | MATCH |
| no-egress | `a8c117708d31fb236e059183c9b08c6a56ab091ac38bde121ef0234e85a22d2d` | MATCH |
| registry | `21347151fbc69de403dd4d7b7aec3f315e2d8de4646f622d8b5377924f610ee1` | MATCH |
| seed | `4ab8a3bab537c82e43612fa11b81cdacea2043d4027bd09fcf91b04f5677a648` | MATCH |
| manifest | `7343f8bc76d68b7993b29ed5232e3487621effb3a27518e0f754a5dd07fef39e` | MATCH |
| package | `e39a413a0ae2000b781433e983a9df48c26b0f5c1db1ce950e2b0b6dd6be7752` | MATCH |
| lock | `89807e33818cfb851b57da65a5675e413e5593d01d6773c3a71a6cc6ea3d85d3` | MATCH |
| migrations 0001..0006 | `dc2355…029b`、`3c96d4…c748`、`f1014f…7460`、`276e31…696`、`f5a8e1…c9a6a`、`c27b9c…17e` | 6/6 MATCH |

固定工具链也匹配既有任务真值：Node 24.18.0 SHA `ee6fb0…c04a`，npm 11.16.0 CLI SHA `8e5f6f…cbe7`；未使用系统 Node/npm。

## 4. 唯一动态命令收据

执行环境：`env -i`，只传固定用户目录、项目 Node24 前置最小 `PATH`、`TMPDIR=/tmp`、`LANG=C`、`LC_ALL=C`。只运行一次 `npm run test:contract`；没有运行 worker、test、typecheck、build、check、浏览器或其他 npm script。

| 字段 | 结果 |
|---|---|
| exit | `0` |
| event/status | `vs1_contract` / `ok` |
| schemaVersion | `vs1-contract-assertion-receipt-v1` |
| cases | `25`，caseId 唯一 |
| assertionResult | 25/25 `PASS` |
| case externalCalls | 25/25 `0` |
| top externalCalls | `0` |
| assertionReceiptHash | `98258fe9591688216707afc16dc7a84c8b1a6d370cd9851c275ee3a0e5992e85` |
| stderr | 空 |
| 重跑 | 0 |

关键恢复链机械结果：

- `VS1-RETRY-014`：attempt 1 `HTTP_503/1s` → attempt 2 `COLLECTION_TIMEOUT/3s` → attempt 3 `PIPELINE_READY/0s`，最多三次，最终 exit 0。
- `VS1-DEAD-015`：三次 `HTTP_503`，delay `1s/3s/0s`，`deadLetterDelta=1`，closed recovery 为 `ARCHIVE_AND_RESEED_TASK_DB`。
- rollback 证明为 true：012、016A–G 和 approved 017，共 9 项。
- replay 002、no-work 018 均有独立 case 行并 PASS。
- 三个 Function ID 的生产文件未变；本任务没有运行 worker，也没有把 Function ID 状态提前改为 complete。

## 5. Closed receipt 与脱敏

顶层字段集合严格为：

```text
assertionReceiptHash, cases, event, externalCalls, schemaVersion, status
```

每个 case 的基础字段严格为：

```text
assertionResult, caseId, exitCode, externalCalls, reasonCode, recoveryAction
```

只允许按 case 语义增加 `attemptHistory`、`deadLetterDelta` 或 `domainRollbackVerified`。25 个 caseId 唯一，所有 assertionResult 只有 `PASS`，所有 externalCalls 只有 `0`。

结构化 stdout 和固化 receipt 未命中绝对用户路径、URL、`synthetic.invalid`、`SYNTHETIC_ONLY`、token、secret、password、stack 或源码行。npm 正常 banner 只含公开包名、script 名与仓库相对入口，不含主机绝对路径或敏感值。没有原始异常正文、fixture 正文、数据库路径、lease token 或凭据。

## 6. 临时根与清理

唯一动态命令后只读扫描：

- `/tmp/TASK-20260809-BCF8B1-*`
- `/tmp/TASK-20260809-3A8C0E-*`
- `/tmp/TASK-20260809-D6114C-*`（pipeline 冻结前缀）

扫描结果：临时任务根 0、SQLite/DB 0、receipt JSON 0、WAL 0、SHM 0。没有执行额外清理；候选自己的 finally/cleanup 完成收口。

## 7. P0/P1/P2、已验证/未验证与错题自检

- **P0：0。** 没有外联、凭据泄漏、hash 漂移或残留。
- **P1：0。** 25-case 正式入口、no-egress 计数、closed receipt、恢复链断言、core 回算、保护 hash 和清理全部满足。
- **P2-01：OS/系统调用级 no-egress 未验证。** Node 进程 guard 不证明内核、防火墙、原生扩展或生产隔离。
- **P2-02：同 UID 路径 TOCTOU 未验证。** 本任务没有扩展到恶意并发路径替换。

已验证：最终三个 hash；工具链和全部列出的保护 hash；唯一 clean-room contract 命令；25 个 case 的 closed 字段、唯一 caseId、恢复链、rollback、dead-letter、replay/no-work、externalCalls；脱敏扫描和零临时残留。

未验证：worker 真实入口、普通测试、typecheck、build、check、浏览器、OS/系统调用级 no-egress、同 UID TOCTOU、真实 provider/RSS/Base/AI/Admin/发布/部署/生产能力。以上继续沿用既有边界，不因本任务 PASS 升级。

错题自检：没有重跑目标命令；没有运行其他 npm script；没有修改或清理候选；没有联网、安装、提权或使用真实凭据；没有把 npm banner 的仓库相对入口误报为主机敏感路径；一次只读 jq 汇总表达式因作用域写法错误非零，随后只读修正，不涉及候选或动态 test 重跑；未把 Node 进程级 `externalCalls=0` 外推为 OS/生产能力；未覆盖历史失败或提前改变三个 Function ID 状态。

TASK_STATE_OK
