---
type: audit_report
department: 安全部
target: RSS 四类原因码回填与离线向量包复验
status: final
date: 2026-08-09
related_task: TASK-20260809-075119
decision: pass
tags: [RSS, reason-code, fixtures, offline, fail-closed]
summary: "PASS：P0=0、P1=0、P2=0。按已 ACK 产品合同一对一回填 XML_ITEM_LIMIT、ATOMIC_COMMIT_PARTIAL、LOG_POLICY_VIOLATION、EGRESS_POLICY_VIOLATION；46 个向量、十项合同、四码唯一性、拒绝出口、manifest 哈希和 external_calls=0 均离线复验通过。"
---

# RSS 四类原因码回填与离线向量包复验

## 结论

**PASS：P0=0、P1=0、P2=0。** 离线向量包现可作为开发实现输入，仅适用于 synthetic/mock 运输安全实现与负例。本结论不授权真实 RSS 请求、Source 启用、Base/provider、公开输出或生产采集。

## 回填结果

| 向量 | 唯一主 reason code | 失败关闭出口 |
|---|---|---|
| `xml-items-over-limit-unresolved` | `XML_ITEM_LIMIT` | reject / 完整响应拒绝 / cursor false / retained false |
| `atomic-partial-unresolved` | `ATOMIC_COMMIT_PARTIAL` | reject / 整体事务拒绝 / cursor false / retained false |
| `log-policy-violation-unresolved` | `LOG_POLICY_VIOLATION` | reject / emit、commit 前拒绝 / cursor false / retained false |
| `egress-policy-violation-unresolved` | `EGRESS_POLICY_VIOLATION` | reject / 公开与媒体出口拒绝 / cursor false / retained false |

四个攻击输入、ID、contract 编号与其他 expected 字段保持不变；只将 `null` 主码替换为产品合同已冻结的四个码，并移除对应 `blocking_conflict`。

## 复验收据

- `python3 app/fixtures/rss-security/validate.py` 返回 `PASS`：46 个向量 ID 唯一、10/10 合同覆盖、`unresolved_reason_codes=[]`、`external_calls=0`、`writes_to_base=false`。
- 独立只读检查确认四个向量的 expected 精确为 `reject + 唯一主码 + complete_response_rejected=true + cursor_advanced=false + data_retained=false`，四码互不重复。
- manifest 状态为 `pass`，blocker 列表为空，未决主码计数为 0；三个被哈希产物复算与 manifest 一致。
- 新 SHA-256：`vectors.json=70a171c0a617fe9cbc2e3cf891adaee42765adff67655b4dd2f5e10e843b5144`；`manifest.json=585c19b0fcc9df46ce9ca0274f25249959611b466296f6c8ab9e5a869ee27cd1`。`minimal-valid.xml` 与 `validate.py` 哈希保持不变。
- 优先级与已 ACK 合同一致：`EGRESS_POLICY_VIOLATION > LOG_POLICY_VIOLATION > ATOMIC_COMMIT_PARTIAL > 执行管线首个失败`；XML item 统计发生在结构/schema/unknown-semantics 完成后。

## 未验证

- 运输安全生产实现、具体 XML 库边界、resolver/DNS/TLS/超时、事务恢复、日志 sink 和 pilot 出口隔离尚未实现或运行验证。
- 任何真实 RSS 可达性、条款、权利、来源行为和用户授权均未验证。

## 错题自检

- 没有更改四个攻击输入或自行创建第五个生产原因码。
- 没有修改 draft、Spec、accepted ADR、`app/src`、migration 或 data。
- 没有执行真实网络、DNS、TLS、RSS、Base/provider 或任何外部 I/O。
- PASS 只闭合离线向量包；真实请求继续受用户门、实现门和安全/测试运行门独立约束。

## 任务收据

`TASK_STATE_OK | state_persisted | local_paths_checked=5 | external_declared=0 | 2026-08-09T10:20+08:00 | TASK-20260809-075119 | 1b2b7abbbf112617`

TASK_STATE_OK
