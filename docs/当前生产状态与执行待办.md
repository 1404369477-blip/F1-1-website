# F1+1 当前生产状态与执行待办

> 状态截止：**2026-09-24 21:51（Asia/Shanghai）**。本文件是当前运行事实和下一步的唯一摘要；协议看 `docs/spec.md`，执行材料看 [handoff.md](handoff.md)，任务状态看正式 TASK JSON。历史已移至[归档索引](archive/2026-09-24-project-handoff/README.md)，不要默认全量加载历史。

## 现在能做什么、还缺什么

| 链路 | 已有实证 | 当前缺口 |
|---|---|---|
| 公开站 / RSS 自动审核与发布 | 真实中文信息流已上线；9月23日新 The Race 内容已自然完成审核、发布到 Pages 列表与详情；407/408限定恢复已结束 | 尚不能称持续稳定。21:00、21:30采集槽三源均报 `SQLITE_FAILURE`，外调0次；未在本次归档时核查根因或最新公开代次 |
| 日常备份 | 仍为 M1 快照＋M5 独立读取/证明的旧路径；最新 `last-register` 记录 SUCCESS，RP为 **9月24日21:14:57.920**，同包应用恢复成功 | 21:51观察时该点已超过15分钟；当前快照被新遗留锁阻断，`STALE_LOCK`；连续RPO未通过 |
| M1 直接 iCloud | r4运行包已暂存；历史包2.39GB真实上传、驱逐、下载、完整hash核对及独审通过 | 同包应用恢复、正式trust后继和调度切换、新鲜REGISTER、持续RPO、M5离线运行均未完成 |
| X 信源 | M1已登录Chrome＋Playwright/GLM路线已读到F1、McLarenF1、ChrisMedlandF1真实帖子；维护UI视觉已确认；候选代码、安装/迁移准备已有局部独审 | 最新已验生产导入为0；0017、真实维护认证、正式准入和自然发布未完成。此次归档未开库重算导入数 |
| M1空间与收尾 | 9月24日已清理618文件，约15.77GB，109项必要文件复核一致；旧锁已正规隔离 | 21:51可用105GiB，仅为新观察，额外空间变化原因未调查。自动回收后继仍未部署，后续验证须逐阶段清理 |

## 最新只读观察及第一优先级

归档时只做读取，未部署、停启服务、清锁或写数据库。原始回件：[21:51观察](archive/2026-09-24-project-handoff/evidence/current-observation-r2/stdout.json)。这些是逐文件采样，未进行新一轮签名/数据库登记验收。

- 新 `backups/snap/run.lock` 内容为 PID **689**、开始于 **21:31:00.597**；读取时PID已不存在，锁SHA `109137ee1d9c12e5d131e6f8a209589ba1a0e6496ebffca047ae0dbc9d385fad`。与早上已处理的PID41530不同。**不能重放早上的固定恢复脚本。** 下一步先刷新该锁的inode、父目录、进程/调度和现场证据，再按原正式隔离入口准备精确恢复；同时查异常退出原因，避免只反复解锁。
- 最新已登记包为 `1790255697920_f6e3393f4a7b7efa`。记录中的应用恢复耗时134965ms，Admin/Public返回200、未授权401、子进程退出；真实通行密钥恢复未验。历史 `writerReady=true` 字段不能替代按当前时间核验恢复点年龄。
- RSS最新日志：20:45 Motorsport新增10、Sky新增4，The Race连接超时；21:00、21:30三源 `SQLITE_FAILURE`。仅凭错误码无法判断数据库损坏或确定其与备份门的因果。
- Admin部署摘要和逻辑schema16仍匹配既有RSS cache版本，production trust仍r3。公开站最新HTTP/浏览器状态和X实时入库数本轮未重验。

## 下一步执行顺序

1. **先恢复当前备份故障并核RSS错误。** 使用当前证据准备新的精确遗留锁恢复；形成新鲜合法恢复点，并观察后继自然周期。保留RPO缺口及故障收据，不重放历史入口或放宽时限。
2. **完成现存 r4 readback 的隔离应用恢复。** 只读现有2.39GB下载副本；不重做上传/驱逐/下载，不重建已清理sealed。新入口须包括成功、失败、超时后的进程及大文件收尾，审查后执行。
3. **完成正式 M1→iCloud 后继。** 复用已暂存r4，完成同密钥、保留非空index的trust迁移与调度安装；验证新鲜REGISTER及持续≤15分钟RPO后，验证M5离线运行，再退出M5旧observer。
4. **完成 X 正式接入。** 将安装/0017准备绑定最终provider和当前完整数据库，完成维护入口真实认证、正式安装/0017/恢复运行，再用三源新捕获签名准入并自然发布。通过后再扩展27源。
5. **收尾验收与临时文件回收。** 核公开列表/详情和后台真实出口；更新任务及当前文档。全量功能矩阵其他缺口按原归属保留，不把当前链路局部通过当整站完成。

上述主线已获用户授权。M1负责采集和日常运行，M5是随身电脑；maintenance-design-r2已确认，已有浏览器连接授权无需重复询问。范围依据：[9月22日实施合同](reviews/2026-09-22-M1-iCloud备份与X正式接入实施合同.md)。当前无新增待用户决策，真实登录/设备交互若再次需要操作时才报告具体阻点。

## 已完成且应复用的成果

- RSS 407/408受控恢复及普通审核/发布已有实际成功，**不要再发历史POST**。新TheRace内容650→Pages651列表/详情、Public652签名链已有证据；代次差不等于跨端原子同步。
- GitHub截图中的push已成功：此前两次部署被过时head检查拒绝，一次Pages HTTP500；后续成功部署已核对。Node20警告不是已确认根因；本轮未重查GitHub最新状态。[历史故障结论](../scratch/github-pages-run-audit-20260923-r1/REPORT.md)。
- r4历史字节往返于00:24完成，81.939秒；两文件完整hash一致，Apple4355初次失败完整保留。[独立实机审查](../scratch/pro-analysis-20260923/canary-review/byte-actual-review.json)。该历史包禁止REGISTER为新鲜恢复点。
- 08:53、08:57两批精确清理和原PID41530锁隔离已独审；原调度恢复后09:00新快照成功。21:51出现的是后来的新锁。[清理实际独审](../scratch/m1-space-cleanup-20260924/review/actual-review.json)。
- 外部GPT分析已完整读取，采纳聚焦端到端、减少重复复验的意见；其8月10日仓库基线不能覆盖当前实况。暂不扩展SSR、换数据库、全站样式或新管理框架。

## 验收缺口与执行边界

- 首次canary入口12例本地测试及独验通过；resume新增7例组合测试仍因fixture的ctime漂移未整体通过，不写成PASS。真实一次接续和字节结果的独审是另一个范围。
- X安装r4及材料接口本地审查通过，仍缺最终provider与现场计划；0017外层材料还绑定旧provider，不能直接执行。
- 不降低RPO≤15分钟、RTO≤4小时、认证/加密/来源规则；不把合成通行密钥检查当真实用户验收；不回退旧数据库覆盖当前数据。
- 已执行的stage、activate、rollback、byte/resume、清理/恢复及已撤销X r2入口都不能机械重放；详见[接续禁令](handoff.md#不能重放的操作)。
- 有效备份保留集和小型审计证据保持。大文件规则统一见[临时大文件收尾](agent-guide.md#临时大文件收尾)。

## 正式任务与证据入口

主任务 [TASK-20260913-D1BAB4](collaboration/tasks/TASK-20260913-D1BAB4.json) 仍为 **claimed / user_confirmed / revision 13**，不能因归档标记完成。旧任务和人员状态见[当前工作分工](collaboration/当前工作分工与交接.md)。工作区归档前有265项未提交变更；本轮不混合提交业务代码，也不清理他人修改。

- [接续操作、精确路径和材料索引](handoff.md)
- [近期里程碑](progress.md)
- [原文归档、校验清单与历史证据快照](archive/2026-09-24-project-handoff/README.md)
- [全功能追踪矩阵](spec/F1+1-初版全功能追踪矩阵-v0.1.md)：用于范围核对，当前归档不改动其历史状态。

后续只更新本文件的当前摘要；详细时间线追加到进度或对应证据，避免在四个入口重复堆叠完整历史。
